# Session session_0151JrVYzJ2bdCShaXhFAjLo

*(Première session du système de suivi — cf. `docs/systeme-de-suivi.md` pour le format et la
portée rétroactive décidée avec l'utilisateur : démarre à partir de la création du système, sans
reconstruire les tâches déjà terminées avant son existence dans cette même session.
**Mise à jour du 2026-09-19T20:35Z** : ce fichier n'avait pas été retouché depuis 16h45 — trouvaille
réelle de `findOpenTasks()` fraîchement construit, qui montrait encore ARGUS/HARMONIA "en cours"
alors que terminés depuis des heures, et rien du travail effectué depuis. Statuts corrigés et
tâches manquantes ajoutées ci-dessous.)*

**Découpage du 2026-09-22** : les tâches #117 à #200 vivent dans [`session_0151JrVYzJ2bdCShaXhFAjLo-partie-1.md`](session_0151JrVYzJ2bdCShaXhFAjLo-partie-1.md) et les tâches #201 à #299 dans [`session_0151JrVYzJ2bdCShaXhFAjLo-partie-2.md`](session_0151JrVYzJ2bdCShaXhFAjLo-partie-2.md) — aucune ligne perdue ni résumée, seulement déplacée (ce fichier pesait ~109 295 tokens, le plus lourd du dépôt). Ce fichier-ci reste la tranche VIVANTE : toute nouvelle tâche s'écrit ici.


## Tâches

| N° | Horodatage | Sujet | Sous-sujet | Sensibilité | Description | Statut |
|---|---|---|---|---|---|---|
| 300 | 2026-09-22T04:45Z | Simulation / Analyse (full_sim17) | Analyse qualitative complète de full_sim17 avec citations exactes — loveRealized enfin déclenché, nouvelle trouvaille de pensées mot pour mot identiques | important | Suite de la priorité déjà actée à la ligne #298 (« Analyser la simulation d'abord, CASSANDRA ensuite »). Lecture complète du transcript déjà faite avant compaction ; ce tour a vérifié chaque citation exacte contre le fichier réel (grep) et croisé avec le journal JSON brut (`full_sim17_messages.json`) pour comprendre le MÉCANISME derrière chaque observation, pas seulement le texte affiché. Trois trouvailles : (1) **Bonne nouvelle confirmée** : `life.loveRealized` s'est déclenché pour la première fois depuis le correctif du chantier 3 (2026-09-21) — Noé au round 80 (`attraction` 60→76), Lia au round 87 (60→76). Nuance réelle trouvée en lisant le code exact (`app/api/lia/route.ts:1597`) : le mécanisme suppose que le `thought` du modèle au tour exact du franchissement porte naturellement sur l'attirance — vérifié faux dans les deux occurrences (Noé : « Il croit nous faire une faveur en nous laissant gérer notre temps. », transcript l.1151-1154 ; Lia : « Il s'emballe un peu vite [...] », l.1276-1279 — aucun des deux sur le thème amoureux). Le vrai contenu "doute amoureux" n'apparaît que plus tard via le pool de secours. (2) **Nouvelle trouvaille, root-cause tracée (Article 19)** : ce pool de secours (`groundPrivateThought()`, `lib/dialogue.ts:146-149`) est structurellement IDENTIQUE entre Lia et Noé (seul `${peer}` diffère) — violation de l'Article 0/11 (voix étanches) pour ce cas de repli, aggravée par une sélection indexée sur le cycle qui fait souvent tomber les deux personnages sur le même slot au même moment. Deux paires identiques confirmées par citation exacte (l.1544/2299 et l.2159/2339). (3) Confirmé (root-cause déjà connue) que la répétition thématique des rounds ~91-180 vient de mon propre script de test (boucle `p2-tick`/`p2-wait`, 5 messages d'attente fixes réutilisés jusqu'à 25 fois), pas d'un bug du jeu. `docs/simulations/correctifs-a-revalider.md` mis à jour (loveRealized passé de 0/2 à 1/2 avec la nuance, nouvelle ligne pour la trouvaille des pensées identiques) ; `docs/simulations/index.md` complété. Comparaison rapide avec d'autres correctifs actifs (accord de genre) : aucune occurrence trouvée dans une vérification ciblée, pas une confirmation exhaustive. Rien corrigé dans le code ce soir — conformément à l'étape 7 de l'Article 18, les questions de calibrage sont posées à l'utilisateur avant tout correctif | terminée — fidèle (analyse ; correctifs en attente des réponses de calibrage) |

| 301 | 2026-09-22T05:15Z | Charte / Outillage de travail (EL-PROFESSOR, KPI) | Écart de process réel trouvé et corrigé : le rapport KPI de full_sim17 et sa note EL-PROFESSOR n'avaient jamais été livrés | important | Question directe de l'utilisateur : « tu ne m'as pas livré de rapport à part le transcript depuis la fin de la simulation [...] normalement, tu dois me faire livrer les rapports, les lire, les inclure à ton analyse ? fiabilise stp ». Vérifié (Article 19) : le rapport KPI avait bien tourné (`.kpi-report-latest.html` existait) mais j'avais appelé `kpi-report.mjs` directement avec un label générique `manuel-2026-09-21T02:30` au lieu de passer par `node scripts/le-regisseur.mjs kpi full_sim17` — jamais archivé en `.txt`, jamais indexé dans `docs/referentiel/kpi-index.md`, jamais livré. Reconstitué et corrigé : `.txt` régénéré depuis le cache HTML, ligne d'index écrite (avec la précision honnête du mauvais label, jamais corrigé rétroactivement dans le CSV), fichiers livrés. Plus grave : EL-PROFESSOR n'avait carrément JAMAIS tourné pour full_sim17 — confondu avec ma propre analyse libre déjà livrée. Rapport complet écrit (`docs/el-professor/full_sim17.md` + `.html`, note globale 67/100) et livré. Puis demande explicite de l'utilisateur : « quel outil gère le process de la simulation ? il faut faire des corrections pour que ça ne se reproduise plus ». Investigation (Article 19) : `scripts/le-regisseur.mjs::postSimulationChecklist()` énumère déjà les bonnes étapes dans le bon ordre (étape 4bis : « faire lire par EL-PROFESSOR avant toute analyse ») — l'outil est correct, je ne l'ai simplement jamais consulté après full_sim17. Root-cause plus profonde trouvée : `findMissingNotes()`/`findOrphanNotes()` (el-professor.mjs) existaient déjà et sont déjà testés dans check-house.mjs, mais n'étaient jamais appelés nulle part dans le réseau réel — câblés maintenant dans le vrai crochet post-commit (`scripts/hooks/check-last-commit.mjs`), sur le même patron warn-only que ARGUS/HARMONIA/CLEAN-DIRTY-OLD/CLONE-HUNTER : tout futur commit qui archive une simulation sans sa note EL-PROFESSOR correspondante sera désormais signalé le jour même, jamais des tours plus tard. `check-house.mjs` vert (155 tests), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |
| 302 | 2026-09-22T05:30Z | Charte / Refonte graphique (html-report.mjs) | Généralisation des règles couleurs/zoom à TOUS les rapports HTML + garde-fou mécanique Doc-Report | normal | Deux demandes explicites de l'utilisateur, généralisant une correction faite plus tôt ce soir pour le seul transcript : « tous les rapports html doivent être aux couleurs de la charte (règle) même si celle-ci évolue. et tous les rapports html doivent s'ouvrir avec zoom 150% (doc-report) ». `--lia`/`--noe` et `body{zoom:1.5}` déplacés directement dans `THEME_CSS` (`scripts/html-report.mjs`, partagé par tous les rapports), retirant le traitement spécial devenu redondant dans `renderTranscriptHtml()`. Nouvelle fonction `checkHtmlReportTheme()` (`scripts/doc-report.mjs`) compare mécaniquement les couleurs réelles de `app/globals.css` à celles de `html-report.mjs` et vérifie la présence du zoom — câblée dans `main()` de Doc-Report, jamais un simple rappel textuel. Règles documentées dans `docs/regles-de-travail.md` §7 et `docs/referentiel/regles-des-graphismes.md` (garantie que la future charte graphique de la refonte devra maintenir cette synchronisation). 6 nouvelles assertions `check-house.mjs` (2 sur le zoom généralisé au dossier, 4 sur `checkHtmlReportTheme()` dont 2 vérifiées en direct contre les vrais fichiers). `check-house.mjs` vert (155 tests), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |
| 303 | 2026-09-22T06:15Z | Charte / Relation Lia-Noé (Correctif 2, loveRealized) | Correctif 2 (tâche #188) : le beat loveRealized n'exige plus que le thought tombe pile sur le tour exact du franchissement — plus deux bugs de fragilité de test trouvés en stabilisant la suite | important | Suite directe de full_sim17 (ligne #300) : la nuance trouvée en lisant `app/api/lia/route.ts:1597` (le thought du tour exact du franchissement ne portait sur l'attirance dans aucune des deux occurrences observées) déjà calibrée avec l'utilisateur plus tôt dans la session — « attendre un moment où la pensée convient vraiment, jamais forcer un fallback, jamais garantir le beat ». Nouvelle fonction exportée `isRelationalThought()` (`lib/dialogue.ts`), extraite du filtre déjà utilisé en interne par `groundPrivateThought()` (Article 3 : jamais une seconde règle divergente sur la même détection). Le gate de `route.ts` ne regarde plus `agent.emotions.attraction<=75` (le tour EXACT du franchissement) mais seulement si l'attirance EST actuellement au-dessus de 75 et si le thought de CE tour est relationnel — le jeu attend patiemment, sur autant de tours qu'il faut, jamais un contenu forcé (Article 9). Bug trouvé en écrivant le test (même famille que la trouvaille CLONE-HUNTER du même soir, cf. `docs/referentiel/points-fragiles.md`) : `\bNoé\b` ne matchait JAMAIS en JS non-Unicode (« é » n'est pas un caractère de mot pour `\b` sans le drapeau `u`) — corrigé avec des lookarounds Unicode (`(?<![\p{L}\p{N}_])...(?![\p{L}\p{N}_])`). Deux bugs de FRAGILITÉ DE TEST (jamais de production) trouvés et corrigés en stabilisant la suite complète après ce changement (Article 5, jamais dépendre d'un état accumulé silencieusement par d'autres tests) : (1) le test "chantier 3" (solitary decrease, tout en fin de fichier) ne remettait jamais `story.apartTurns` (top-level, jamais sous `life`) à zéro — un tour de plus ajouté au test du doute amoureux privé plus haut dans le fichier suffisait à faire dépasser silencieusement le seuil de 2 qui bloque `canSeparate()` (`lib/turn.ts`) ; (2) même test ne vidait jamais l'historique de conversation partagé — `nextSpeaker()` (lib/dialogue.ts) lit le dernier message de la table `conversations`, commune à tous les tests du fichier, pour décider qui est "meneur" ce tour ; or seul le "second" (jamais le "meneur") voit sa propre pièce respectée via `stayAlone` (route.ts, la pièce du "meneur" est TOUJOURS `turnPlan.room`, qui lit lui-même cet historique via `agreedDestination()`/`warmthChain()`/`investigativeCue()`) — un tour de plus ailleurs dans le fichier faisait basculer silencieusement `nextSpeaker()` et fusionnait Lia et Noé dans la même pièce malgré `stayAlone`, cassant un test à des centaines de lignes de distance sans lien apparent. Diagnostiqué par instrumentation temporaire (`console.error` gardés derrière `process.env.DEBUG_LOVE`, tous retirés avant commit) puis corrigé en réinitialisant explicitement `apartTurns` et en vidant `conversations`/`dialogue_fingerprints`/`world_requests` au début du test, exactement comme le fait déjà le test voisin du doute amoureux privé. `lib/reference.ts` Version 219. `check-house.mjs` vert (155 tests), `tsc --noEmit` sans nouvelle erreur (seule l'erreur préexistante et non liée de `vite.config.ts` subsiste) | terminée — fidèle |

| 304 | 2026-09-22T06:30Z | Charte / Outillage de travail (CLAUDE.md, CASSANDRA-RH, mode nocturne) | Passe manuelle d'allègement de CLAUDE.md (tâche #171) + formalisation du « mode nocturne autonome » + Ronde CIRCLE-TASKS partielle | normal | Suite du message où l'utilisateur, avant d'aller dormir, demande de continuer à enchaîner les tâches ouvertes sans s'arrêter (sauf pour la refonte graphique), en respectant les mêmes règles de calibrage que d'habitude. (1) Tâche #171 : `listDatedNarrativeMarkers()` relancé sur CLAUDE.md, 3 nouveaux candidats trouvés (memory-audit, find-booster, CLONE-HUNTER). Les deux premiers déplacés vers `docs/referentiel/claude-md-asides-historique.md` (zéro marqueur normatif détecté par `extractNormativeMarkers()`) ; le 3e délibérément laissé en place (contient une vraie preuve d'application de l'Article 19, le garde-fou « en cas de doute, ne pas couper » s'applique). (2) Nouvelle section `docs/regles-de-travail.md` §1bis « Mode nocturne autonome » : formalise, pour les prochaines fois, que ce mode change seulement le RYTHME (enchaîner sans attendre de retour), jamais l'AUTORITÉ de décision (l'utilisateur reste seul décideur final, Article 16/14 intégralement en vigueur) — protocole d'entrée (état des lieux 3 zooms, lecture des rapports, plan d'exécution), conduite pendant (Rondes CIRCLE-TASKS ponctuelles, Smart Conso API/SMART-CONSO-TOKEN avant toute action coûteuse, jamais bloquer sèchement sur une question de calibrage — préparer des suggestions et passer à autre chose) et sortie (compte rendu clair, docs/suivi à jour). (3) Note ajoutée à `docs/cassandra-rh-conception.md` sur le PS de l'utilisateur (« cassandra pourrait decider si certains scripts meritent de passer membres certifiés ») : rapprochée de la mission « Promotion de poste » déjà actée, mais ambiguïté de terminologie avec le badge mécanique existant explicitement signalée comme à lever avec l'utilisateur, jamais tranchée seule. (4) Ronde CIRCLE-TASKS partielle réellement exécutée (pas seulement affichée) : Smart Conso API scan (rien à signaler), SMART-CONSO-TOKEN scan (rien à signaler), rapport KPI lancé et archivé (`manuel-2026-09-22-ronde`, indexé dans `kpi-index.md`). `check-house.mjs` vert (155 tests), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 305 | 2026-09-21T04:35Z | Outillage de travail (check-tasks-details, tâche #185) | Construit checkChantierFileFreshness() (vérification mécanique de fraîcheur des fichiers préliminaires de chantier) + trouvé et corrigé un vrai faux positif en le lançant en direct | normal | Suite du mode nocturne autonome (tâche déjà identifiée dans le suivi comme ouverte). Implémenté `CHANTIER_PRELIMINARY_FILES` (registre CASSANDRA-RH/refonte graphique) et `checkChantierFileFreshness()` dans `scripts/check-tasks-details.mjs`, réutilisant `lastTouchDays()` de CLEAN-DIRTY-OLD (Article 3, jamais un second calcul de fraîcheur) — compare la tâche de suivi la plus récente sur un chantier connu à la dernière modification git réelle de son fichier dédié, avec une tolérance d'une journée pour un commit groupé. Câblé dans `buildReport()` (bloc + meta) et dans `main()`. 7 cas de test ajoutés à `check-house.mjs`. **Faux positif réel trouvé en lançant l'outil en direct contre le vrai dépôt** (dogfooding, jamais supposé fonctionner sans vérification) : 2 chantiers signalés à tort « en retard » — root-cause tracée à un décalage entre la date narrative « aujourd'hui » donnée en tête de session (utilisée pour horodater les lignes de suivi) et l'horloge système réelle utilisée par git (constaté en direct : système à `2026-09-21 04h35 UTC` pendant qu'une ligne de suivi fraîchement écrite portait `2026-09-22T06:30Z`, un écart de ~26h) — sans protection, cet écart produisait une soustraction négative et donc un « retard » entièrement fabriqué. Corrigé en clampant l'âge d'une tâche de suivi à 0 minimum, jamais en dessous ; 1 nouveau cas de test dédié à cette régression exacte ; re-vérifié en direct après correctif (0 faux positif). `docs/referentiel/check-tasks-details.md` mis à jour avec le détail complet de la trouvaille. `lib/reference.ts` Version 220. `check-house.mjs` vert (156 tests), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 306 | 2026-09-21T04:50Z | Outillage de travail (find-booster, tâche #180) | 5e motif d'extraction find-booster (commentaires denses non accolade-préfixés) — route.ts passe de 10 à 125 entrées navigables | normal | Suite du mode nocturne autonome. Construit `extractCommentedStatementIndex()` (`scripts/find-booster.mjs`) : un long commentaire (2+ lignes) directement suivi de code réel, sans exiger l'accolade top-level seule qu'`extractBlockIndex()` réclame — le style réel du cœur dense de `app/api/lia/route.ts`, jusqu'ici invisible pour find-booster (seulement 10 fonctions nommées trouvées, confirmé en direct via `git stash`). Bug de décalage d'une ligne trouvé et corrigé en écrivant le test de dédoublonnage : `extractBlockIndex()` rapporte la ligne de l'accolade ouvrante, jamais celle du commentaire qui vit juste en dessous — sans le `+1` correctif, le dédoublonnage contre `extractBlockIndex()` échouait silencieusement (comparait des lignes qui ne correspondaient jamais). Câblé dans `buildIndexFromSource()`. Vérifié en direct contre le vrai dépôt après correctif : `route.ts` 10→125 entrées, `check-house.mjs` ~91→297. `docs/referentiel/find-booster.md` et `CLAUDE.md` mis à jour (4→5 motifs, blueprint générique inclus). `lib/reference.ts` Version 221. `check-house.mjs` vert (157 tests, 1 nouveau bloc), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |
| 307 | 2026-09-21T09:11Z | Méthode de travail / Suivi (rattrapage) | Ligne manquante comblée : commit `99580ad6` (brouillon WIP `scripts/cassandra-rh.mjs`) | normal | Rattrapage a posteriori, trouvé par la veille hebdomadaire automatique du réseau d'outils (`docs/regles-de-travail.md`, section « Veille hebdomadaire automatique du réseau ») : `check-suivi-fidelity.mjs` a signalé le commit réel `99580ad6` (2026-09-21T03:12:04Z, entre les lignes #298 et #299 ci-dessus) comme n'ayant jamais reçu de ligne de suivi. Vérifié avant de combler (jamais à l'aveugle) : le commit ajoute `scripts/cassandra-rh.mjs` (254 lignes), un noyau CASSANDRA-RH écrit selon le round de calibrage déjà consigné dans `docs/cassandra-rh-conception.md`, syntaxe vérifiée (`node --check`) mais délibérément jamais exécuté ni testé ni câblé nulle part — mis en pause pour prioriser l'analyse de full_sim17 (ligne #300), commité tel quel uniquement pour ne pas perdre le travail dans un environnement de session éphémère. La ligne #297 (round de calibrage CASSANDRA-RH, antérieure à ce commit dans l'ordre réel) affirmait à tort « rien construit côté code ce soir » — devenu inexact dès ce commit suivant, non corrigé rétroactivement ici (Article 13 : seuls les documents d'état ACTUEL sont corrigés, jamais l'historique déjà écrit). Reste couvert par la tâche #184 (toujours `pending`, noyau non fini/non câblé) | terminée — fidèle (rattrapage administratif, aucun nouveau travail de code) |
| 308 | 2026-09-21T09:11Z | Méthode de travail / Suivi (rattrapage) | Ligne manquante comblée : commit `5bab0995` (correction du choix d'outil, `docs/regles-de-travail.md`) | normal | Même origine que la ligne #307 ci-dessus (veille hebdomadaire). `check-suivi-fidelity.mjs` a signalé le commit réel `5bab0995` (2026-09-21T03:23:00Z, entre les lignes #299 et #300 ci-dessus) comme n'ayant jamais reçu de ligne de suivi. Vérifié avant de combler : le commit modifie `docs/regles-de-travail.md` (16 insertions/5 suppressions) suite à une remarque directe de l'utilisateur — check-tasks-details.mjs (gratuit, a déjà lu `docs/suivi/`) est le bon outil par défaut pour vérifier la fraîcheur d'un fichier préliminaire de chantier, pas THE-DEEP-READER (coûteux, réservé au cas plus rare où une idée n'aurait même jamais atteint `docs/suivi/`) | terminée — fidèle (rattrapage administratif, aucun nouveau travail de code) |
| 309 | 2026-09-21T09:40Z | Outillage de travail (ALWAYS-NEW-CODE, tâche #170) | Premier vrai passage d'ALWAYS-NEW-CODE (zone Fatigue) — 2 trouvailles confirmées, aucun correctif appliqué | normal | Suite du mode nocturne autonome, sur une zone identifiée par la veille hebdomadaire comme jamais examinée malgré 3 signalements successifs des Rondes CIRCLE-TASKS. Consulté SMART-CONSO-TOKEN (`full_repo_scope`, verdict "avertissement souple", confirmé) avant le raisonnement. Zone "Fatigue" choisie par `recommendZone()` (rotation, seule zone jamais examinée). Survol : la mécanique traverse 7 fichiers (`lib/simulation.ts`, `lib/daynight.ts`, `lib/life.ts`, `lib/drama.ts`, `app/api/lia/route.ts`, `lib/lia.ts`, `docs/referentiel/`) — vérifié explicitement (garde-fou `trottoirGranted`, Article 19) que ce découpage suit le même patron déjà appliqué partout ailleurs dans le projet, jamais de la dette à corriger. Zoom, 2 trouvailles CONFIRMÉES (jamais de simple signal faible) : (1) le seuil de sommeil `fatigue>=68` (`lib/simulation.ts:96`) est retapé en toutes lettres dans le prompt de `lib/lia.ts` ("FATIGUE ET REGISTRE"), sans constante partagée — contrairement au patron déjà établi par `lib/daynight.ts` (`DAY_ROUNDS`, `MIDNIGHT_OFFSET`, prédicats nommés) précisément pour éviter ce risque de divergence si le seuil est un jour rééquilibré ; (2) la détection de l'aube (`cyclePosition(round)===0&&round>0`) est tapée deux fois à l'identique dans `app/api/lia/route.ts` (lignes ~1364 et ~1539), alors que `lib/daynight.ts` exporte déjà `isNight()`/`isMidnight()` pour ce même besoin — aucun `isDawn()` équivalent n'a jamais été créé. Aucune des deux n'est un bug actif aujourd'hui (les copies sont synchronisées), seulement un risque structurel de divergence future. Conformément à l'Article 23 (jamais une application automatique), aucun changement de code n'a été fait : rapport complet archivé dans `docs/always-new-code/2026-09-21-fatigue.md` (livré en fichier joint), `docs/always-new-code/index.md` et `docs/referentiel/points-fragiles.md` mis à jour, 3 questions de calibrage préparées pour le réveil de l'utilisateur (2 sur les trouvailles, 1 sur le format du rapport lui-même). `lib/reference.ts` Version 222. `check-house.mjs` vert (157 tests, aucun changement de code), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |
| 310 | 2026-09-21T10:05Z | Méthode de travail / Suivi (mode nocturne autonome) | Recalibrage du mode nocturne autonome : dernier geste systématique = état des lieux projet entier affiché à l'écran | normal | Demande explicite de l'utilisateur à son réveil : « je voudrais par ex que la derniere tache soit un etat des lieux des taches en cours affiché à l'écran que je me repere (comme pour toi quand tu commences) ». 3 questions de calibrage posées (Article 16) avant modification : (1) niveau de zoom → « Projet entier » (pas seulement en cours) ; (2) forme → « Les deux » (résumé rédigé + tableau brut de l'outil) ; (3) remplace ou s'ajoute au récap narratif déjà existant → « En plus, à la toute fin ». `docs/regles-de-travail.md` §1bis mis à jour : nouveau paragraphe « Dernier geste, systématique, avant de rendre la main » — `node scripts/check-tasks-details.mjs projet_entier` toujours affiché directement dans la conversation (jamais seulement en fichier joint) en fin de session nocturne, résumé compact d'abord puis détail complet, après (jamais à la place de) le compte rendu narratif habituel. Aucun changement de code | terminée — fidèle |
| 311 | 2026-09-21T11:20Z | Nouvel outil / Outillage de travail (THE-GHOST) | Construit THE-GHOST, orchestrateur du mode nocturne autonome (rituels d'entrée/sortie + rythme de session) | important | Demande explicite de l'utilisateur : « créé un petit agent script "the-ghost" qui gere le mode autonome [...] quand je vais dormir ou quand je te laisse travailler seul [...] vois comment on peut ameliroer ce script ». Reprioritisation immédiate en cours de route : « on finit les outils en cours de construction, cassandra en dernier » — liste confirmée avec l'utilisateur (the-ghost → objectifs/résultats → registre canonique des outils → CASSANDRA-RH). Investigation Article 19 avant construction : vérifié qu'aucun outil existant (LE-COORDINATEUR, CIRCLE-TASKS) ne gère déjà le cycle de vie du mode nocturne lui-même. 3 questions de calibrage posées, puis 2 clarifications directes de l'utilisateur en cours de route qui ont fixé la frontière exacte : « the host [the-ghost] ne gere que les elements propres au mode autonome, tandis que check-details a des fonctions plus transverses, comme evamluer la pertinence des taches » — check-tasks-details.mjs reste seul propriétaire du jugement de pertinence (`recommendNextTasks()`, jamais réimplémenté) ; the-ghost ne possède que ce qui est propre à la nuit elle-même. Investigation menée en direct pour éviter tout doublon de journal (réponse « à voir » de l'utilisateur) : `tool-usage.mjs::recordToolUsage()` existe mais n'a aucun appelant réel en production (jamais alimenté) ; `circle-tasks.mjs::loadLastRun()` EST réellement alimenté (`recordCircleTasksRun()` appelé par l'agent après chaque vraie Ronde) — réutilisé tel quel pour le signal « depuis quand aucune Ronde n'a tourné », jamais un second calcul. Seule donnée réellement absente ailleurs : l'heure de début de LA session nocturne en cours — un unique état minimal (`.the-ghost-session.json`, gitignored) la retient. `scripts/the-ghost.mjs` : `enterAutonomousMode()` (ouvre la session + déclenche les 3 zooms de check-tasks-details via `sh()`), `recordTaskChained()` (compteur de progression), `checkPacing()` (rythme, avec repli honnête sur la durée du mode si aucune Ronde n'a jamais été journalisée — cas de démarrage à froid), `exitAutonomousMode()` (réutilise le pipeline complet de check-tasks-details au zoom projet_entier calibré la veille, referme la session). Statut décidé pragmatiquement (réponse de l'utilisateur : « on fait au plus rentable », CASSANDRA pas encore là pour arbitrer ce genre de question) : utilitaire mince sans blueprint, comme LE-COORDINATEUR/CIRCLE-TASKS, documenté directement dans `docs/regles-de-travail.md` §7ter, avec renvois ajoutés dans §1bis (entrée/rythme/sortie). Dogfooding en direct réussi (start → pacing → chained → end, fermeture propre de session confirmée). 8 nouveaux cas de test ajoutés à `check-house.mjs` avec sauvegarde/restauration du vrai fichier local (même discipline que `tool-usage.mjs`). `.gitignore` mis à jour. `check-house.mjs` vert (158 tests), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |
| 312 | 2026-09-21T11:45Z | Nouvel outil / Outillage de travail (find-brain) | Surnom « find-deep-booster » pour route-booster + construit find-brain, cerveau unifié des deux outils de recherche | important | Demande explicite de l'utilisateur : « renomme (surnom) le script "route-booster" par "find-deep-booster" + je veux un cerveau intelligent "find-brain" qui englobe les 2 scripts find-booster et find-deep-booster pour plus d'efficacité dans les recherches. toi tu dois etre pluggé en priorité à ce cerveau qui te rappelle d'utiliser ces 2 outils le plus souvent possible ». 3 questions de calibrage posées (Article 16) : (1) portée du renommage → « surnom seulement, même patron que MEMENTO » (fichier technique route-booster.mjs inchangé) ; (2) mécanisme → « script séparé » (scripts/find-brain.mjs, jamais une fonction ajoutée à l'un des deux existants) ; (3) force du rappel → « les deux » (renforcer le rappel automatique existant ET ajouter une obligation écrite dans la charte). Construit `scripts/find-brain.mjs` : `recommendFindDeepBooster()` (symétrique à recommendFindBooster() déjà existant côté find-booster, absent jusqu'ici pour route-booster — un fichier mérite un vrai zoom de découpage seulement s'il est à la fois long ET riche en points de coupe candidats), `flagFindDeepBoosterCandidates()` (même forme que flagFindBoosterCandidates() de doc-report.mjs, balaie les mêmes REGISTRIES réels), `recommendFindBrain()` (jugement unifié JAMAIS exclusif — un même fichier peut mériter les deux recherches à la fois, vérifié en direct : app/api/lia/route.ts recommande les deux, scripts/lib-shell.mjs aucun des deux). Rappel renforcé sur les deux volets demandés : (1) le rappel post-commit existant (`scripts/hooks/check-last-commit.mjs`) est rejoint par un second message symétrique pour find-deep-booster ; (2) une obligation écrite ajoutée dans `docs/regles-de-travail.md` §7ter (même statut que l'obligation d'usage de SMART-CONSO-TOKEN — aucun mécanisme technique ne peut intercepter un Read avant qu'il n'ait lieu, la seule vraie garantie est une règle écrite), accompagnée d'un rappel plus général demandé explicitement par l'utilisateur au même endroit de la charte sur la consultation systématique du catalogue LE-COORDINATEUR avant toute nouvelle tâche, pas seulement pour la recherche dans le code. PRESTATIONS (le-coordinateur.mjs) mis à jour : nouvelle entrée « Pack Cerveau de recherche ». Dogfooding en direct réussi contre le vrai dépôt. `lib/reference.ts` Version 224. `check-house.mjs` vert (159 tests, 1 nouveau bloc), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |
| 313 | 2026-09-21T12:00Z | Méthode de travail / Outillage de travail (find-brain, Article 13) | Audit demandé par l'utilisateur : écart réel trouvé et corrigé dans CLAUDE.md (référence stale à find-booster seul, jamais mise à jour vers find-brain) | important | Question directe de l'utilisateur : « les rappels pour l'utilisation des outils et particulierement des outils via find brain sont bien implementés dans le code pour que tu ameliores ton reflexe d'utiliser les outils ? ». Vérifié en direct (jamais supposé) : le rappel post-commit fonctionne bien (déjà confirmé au commit précédent, les deux messages find-booster/find-deep-booster s'affichent). Mais un vrai écart trouvé par `grep` : CLAUDE.md contenait déjà, avant ce soir, une « Obligation écrite d'usage réel » pour find-booster — mais elle pointait encore vers `recommendFindBooster()` seul et ne mentionnait ni find-brain ni le surnom find-deep-booster, tous deux créés dans ce même échange. Or CLAUDE.md est le SEUL document relu à CHAQUE message de la session (cf. la leçon déjà tirée pour l'Article 22 dans `docs/regles-de-travail.md` : une règle transversale invisible dans le texte qu'on exécute reste invisible en pratique, même connue en théorie) — un rappel qui vit uniquement dans `docs/regles-de-travail.md` §7ter (lu une fois, pas à chaque message) était donc structurellement plus fragile. Corrigé (Article 13) : la section find-booster de CLAUDE.md pointe désormais vers `node scripts/find-brain.mjs <fichier>`, mentionne le surnom find-deep-booster de route-booster.mjs et la raison de préférer find-brain (jugement unifié). `lib/reference.ts` Version 225. `check-house.mjs` vert (159 tests, aucun changement de code), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |
| 314 | 2026-09-21T12:15Z | Conception / Nouvel outil (CASSANDRA-RH, Stagiaires) | Correction de §8ter : le Stagiaire n'est plus un agent séparé, reconçu en script mécanique — 3 Stagiaires proposés | important | Correction explicite de l'utilisateur : « pour les tagiaires je ne veux pas d'agent séparé. ecris quelque part que j'utilise le terme "agent" de facon abusive regulierement [...] propose moi une nouvelle orga [...] des stagiaires qui aident vraiment : catalogue, dossier KPI et RECRTUTEMENT ». Vérifié avant de corriger (Article 19) : la règle « Agent veut souvent dire script » existait déjà dans `docs/regles-de-travail.md` (2026-09-20) — non appliquée au moment de concevoir §8ter, addendum ajouté citant ce cas réel comme exemple concret. `docs/cassandra-rh-conception.md` §8ter réécrit : le Stagiaire n'est plus « un vrai second agent séparé » (coût fixe ~37k tokens, personnage fixe, consultation Smart Conso API/SMART-CONSO-TOKEN) mais une fonction/script mécanique, même nature que les autres Membres — jamais de jugement ni de prose rédigés par le Stagiaire, toujours l'agent qui pilote sous la voix de CASSANDRA/LE-COORDINATEUR. 3 Stagiaires proposés à l'utilisateur, validation en attente : Stagiaire Catalogue (sert LE-COORDINATEUR, assemble usage réel + fraîcheur par entrée PRESTATIONS), Stagiaire Dossier KPI (pré-agrège les chiffres de kpi-report.mjs en blocs narrables), Stagiaire Recrutement (sert CASSANDRA, assemble les signaux ARGUS/HARMONIA/AXA-CHECK/CLEAN-DIRTY-OLD/tool-usage.mjs en fiche de comparaison). Rien construit ce soir (CASSANDRA-RH reste en dernier dans l'ordre confirmé). `lib/reference.ts` Version 226. `check-house.mjs` vert (159 tests, aucun changement de code), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |
| 315 | 2026-09-21T13:30Z | Nouvel outil / Reclarification organigramme (tool-brain, Membre certifié) | Construit tool-brain (rappel centralisé + KPI + auto-diagnostic) ; reclarifié le badge « Membre certifié » en 2 catégories après une confusion réelle trouvée par l'utilisateur | important | Demande explicite : « cree un petit outil "tool-brain" [...] rappel centralisé [...] rapport txt à chaque ronde [...] il a son KPI (tres important) [...] est-ce qu'on en fait un membre certifié avec poste de travail ? ». En route vers un poste complet type Agent, l'utilisateur a signalé une confusion : « les agents que tu cites [LE-COORDINATEUR, CIRCLE-TASKS, route-booster] [...] ce sont bien des membres certifiés avec badge [...] refaisons une passe [...] j'attends tes 10 questions ». Vérifié avant de répondre (Article 19) : ce que j'avais dit reflétait fidèlement le texte alors écrit (ces 3 outils étaient bien « Utilitaire nommé », sans badge) — la règle elle-même a changé en calibrant. Round de questions (2 fenêtres) : critère retenu — « connaissance propre au projet à documenter à part » (oui/non) sépare désormais 2 sous-catégories de membres certifiés, toutes deux badgées 🎖️ : Sages/Gardiens (connaissance propre, inchangé) et « Membre certifié (classique) » (nouveau — LE-COORDINATEUR, CIRCLE-TASKS, route-booster, tool-brain). Construit `scripts/tool-brain.mjs` : `adviseToolBrain()` (généralise find-brain à tout PRESTATIONS), `formatToolBrainReminder()` (remplace les 3 blocs post-commit find-booster/find-deep-booster/menu nu par une bannière unique dans check-last-commit.mjs), `buildToolBrainUsageReport()`/`diagnoseToolBrainSelf()`/`formatToolBrainReport()` (KPI + auto-diagnostic borné à son propre périmètre, item CIRCLE-TASKS `tool-brain-report` + CLI `rapport`). Code modifié : `checkAgentOnboarding()` gagne le paramètre `ownKnowledge` (dispense instanciation/registre/blueprint pour un membre classique), `CERTIFIABLE_STATUTS`/`CLASSIQUE_STATUT` (le-coordinateur.mjs) remplacent le filtre `statut==="Agent"` dans `checkAllAgentBadges()`/`badgeWarningsForOutils()`. Table maîtresse et `docs/referentiel/organisation-agence.md` (Axe A) mis à jour dans le même commit — plan d'action explicite pour que l'écart doc/règle ne se reproduise plus. Surnom CLAUDE.MD.SPY → CHARTER-SPY (périmètre élargi à regles-de-travail.md, demande explicite, même patron que MEMENTO). Bug réel trouvé en lançant `node scripts/tool-brain.mjs rapport` pour de vrai contre ce dépôt : `knownToolSlugsFromPrestations()` slugifiait le texte entier d'un `outils` avec parenthèse, produisant un faux doublon jamais utilisé par `recordToolUsage()` — corrigé avec le découpage `primaryName` déjà établi ailleurs. `lib/reference.ts` Version 227. `check-house.mjs` vert (161 tests, 3 nouveaux blocs), `tsc --noEmit` sans nouvelle erreur. Reste ouvert, signalé explicitement : promotion éventuelle de find-brain/CHARTER-SPY/tool-usage.mjs/Doc-Report/gabarit HTML vers « classique » (question distincte, pas encore posée) ; schéma d'organisation demandé par l'utilisateur pour alimenter le futur projet CASSANDRA (pas encore livré) | terminée — fidèle |

| 316 | 2026-09-21T13:45Z | Livrable / CASSANDRA-RH (organigramme visuel) | Livré un Artifact HTML consolidant l'organigramme complet de l'Agence Codex, intégré à une vue jeu/agence | normal | Demande explicite de l'utilisateur : « montre moi un schema de l'organisation de l'agence, integré au projet global [...] servons nous de cette tache pour alimenter le projet "CASSANDRA" [...] c'est cassandra qui s'occupe de ce genre de questions normalement ». Publié via l'outil Artifact (page HTML autonome, thème sombre dédié, aucune librairie) : vue projet globale (Le Jeu vs L'Agence Codex), légende des 5 catégories du badge (Gardien sacré/Sage/Membre certifié classique/Utilitaire nommé/Infrastructure), Direction, 5 Gardiens sacrés, Agents Spéciaux, les 6 suites de Membres avec la reclassification `tool-brain`/CIRCLE-TASKS/route-booster de la tâche #315, Utilitaires nommés/Infrastructure, et le rappel des 3 catégories hors agence. `docs/cassandra-rh-conception.md` §10 ajouté : documente ce livrable comme première mission concrète que CASSANDRA-RH héritera une fois construite (reconstruction mécanique attendue depuis AGENT_CATEGORIES/la table maîtresse, jamais une transcription manuelle comme ce soir). Aucun changement de code. `check-house.mjs` vert (161 tests) | terminée — fidèle |

| 317 | 2026-09-21T14:15Z | Nouveau document / Conception (organisation-globale-projet, LE-GRAND-ARCHITECTE) | Créé le document mère organisation-globale-projet.md + la conception de LE-GRAND-ARCHITECTE ; corrigé 4 points sur le schéma déjà livré | important | Demande explicite : « je veux un document mere pour l'organisation du projet [...] cree ce document general : organisation-globale-projet [...] il y a un agent script derriere "organisation globale" [...] cet agent s'appelle le-grand-architecte [...] il a des liens privilégiés avec cassandra, avec tool-brain ». Créé `docs/referentiel/organisation-globale-projet.md` (document mère : les deux mondes Le Jeu/L'Agence Codex, table des documents d'organisation par pôle avec responsable actuel/cible, décision explicite de ne pas créer de sous-documents supplémentaires — organisation-agence.md couvre déjà correctement le pôle Agence, Article 19) et `docs/le-grand-architecte-conception.md` (même patron que cassandra-rh-conception.md : rôle, frontière vérifiée avec CASSANDRA-RH/ALWAYS-NEW-CODE/HARMONIA/THE-KING/Doc-Report, liens privilégiés CASSANDRA-RH + tool-brain, statut cible Agent/Sage, construction non commencée — ajoutée à la file, cassandra-rh reste avant elle). Quatre corrections directes demandées sur le schéma Artifact déjà livré : (1) route-booster → surnom d'affichage « find-deep-booster » partout (table maîtresse, organisation-agence.md, CLAUDE.md, et surtout PRESTATIONS le-coordinateur.mjs — `outils:["route-booster"]` → `["find-deep-booster"]`, sans quoi `findToolsMissingFromMenu()` l'aurait signalé manquant) ; (2) « gabarit HTML » → surnom « doc-HTML » ; (3) vérifié en direct par grep des vrais imports (Article 19) que Doc-Report n'importe JAMAIS doc-HTML — il l'audite de l'extérieur (lecture de source), alors que ~10 autres scripts l'importent directement : les deux sont des pairs « Utilitaire nommé », jamais une hiérarchie — corrigé dans regles-de-travail.md et CLAUDE.md ; (4) Utilitaire nommé et Infrastructure, fusionnés par erreur en une seule section dans le premier schéma, scindés en 2 sous-catégories de lecture nommées « Utilitaires nommés » et « La Plomberie » (paragraphe dédié ajouté dans regles-de-travail.md, artifact republié en Version 2). `lib/reference.ts` Version 228. `check-house.mjs` vert (161 tests), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 318 | 2026-09-21T14:30Z | Calibrage / Conception (LE-GRAND-ARCHITECTE, organisation-globale-projet) | Calibrage des 4 questions de la tâche #317 + renommage La Plomberie→Équipe Infrastructure + règle d'économie tokens | important | Réponses de l'utilisateur : (1) déclenchement 2 niveaux léger/lourd, s'inspirer d'ALWAYS-NEW-CODE — le-grand-architecte-conception.md §5 mis à jour ; (2) « essaie de voir s'il n'y a pas un pole [...] pour alleger le travail general » — candidat trouvé et ajouté à organisation-globale-projet.md (cartographie des blueprints, dupliquée entre CLAUDE.md ~40 sections et regles-de-travail.md §7ter, non exécutée, procédure formalisée à suivre séparément) ; (3) ordre de construction confirmé après CASSANDRA-RH ; (4) « je veux que la plomberie [...] soit bien identifié [...] on renomme [...] par Equipe Infrastructure mais pas besoin d'editer à nouveau le doc et de me l'envoyer [...] regles à fixer pour economies token » — renommé dans regles-de-travail.md (artifact local mis à jour sans republication, conformément à la demande), et nouvelle règle d'économie ajoutée à docs/regles-de-travail.md §7 (Livrables) : une correction cosmétique mineure sur un Artifact déjà livré s'applique sans republier ni renarrer systématiquement. `lib/reference.ts` Version 229. `check-house.mjs` vert (161 tests, aucun changement de code) | terminée — fidèle |
| 319 | 2026-09-21T15:00Z | Nouvel outil / Correctif (objectifs-vs-resultats #287, registre canonique #290, bug tool-brain) | Construit objectifs-vs-resultats.mjs (Membre standalone) ; findGardiensMissingFromSource() (registre canonique) ; corrigé un crash ENOENT réel de recommendFindBooster/recommendFindDeepBooster trouvé en dogfooding | important | Demande : « il n'ya pas d'autre taches en cours ? enchaine sur les taches en cours à finaliser. ensuite : on enchaine sur 'objectifs vs resultats', 'registre des outils' [...] ». Audit rapide confirmé : aucune tâche cachée au-delà de #287/#290 (les autres lignes ouvertes du suivi sont des décisions de conception CASSANDRA-RH, pas du travail séparé) ; le point-fix CLONE-HUNTER→HYPER-SCAN-CHECKPOINT signalé comme "en attente" par un résumé antérieur s'est révélé déjà corrigé et committé (commit d399518, promotion CLONE-HUNTER). En consultant tool-brain AVANT de créer un nouveau fichier (habitude que l'utilisateur demandait de vérifier), crash réel trouvé : `recommendFindBooster()`/`recommendFindDeepBooster()` plantaient sur ENOENT pour un fichier pas encore créé — corrigé avec un retour honnête `{notFound:true, worthwhile:false}`, propagé à `recommendFindBrain()`/tool-brain CLI. Tâche #290 : root cause confirmée par investigation (aucune liste unique de "quel outil DOIT être appelé où") ; plutôt qu'un nouveau registre, `findGardiensMissingFromSource()` (doc-report.mjs) réutilise AGENT_CATEGORIES (lib-shell.mjs) + REGISTRIES pour vérifier live que hyper-scan-checkpoint.mjs appelle bien chaque Gardien sacré du code — écart connexe trouvé et corrigé : la `family` de CLONE-HUNTER dans REGISTRIES était restée "Qualité du code" depuis avant sa promotion. Tâche #287 : construit `scripts/objectifs-vs-resultats.mjs`, nouveau Membre standalone (jamais un sous-agent de CASSANDRA-RH, jamais une extension du KPI, per la conception déjà validée) — registre hand-maintained (`docs/objectifs-vs-resultats/registre.md`), résultat réel lu via `loadToolUsageHistory()` de tool-brain.mjs (jamais un second calcul), statuts atteint/en dessous/dépassé/pas de données, période à-venir/en-cours/clos distincte. Wiré dans AGENT_CATEGORIES (Suite Suivi-Conso), REGISTRIES, PRESTATIONS (Pack Objectifs), table maîtresse, et exclu de CIRCLE-TASKS (registre consulté à la demande, pas une routine à habitude comme tool-brain). Blueprint + instanciation + registre créés le même soir. Premier objectif enregistré : tool-brain, 5 sollicitations/7 jours. `lib/reference.ts` Version 230. `check-house.mjs` vert (162 tests, 3 nouveaux/étendus blocs), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |
| 320 | 2026-09-21T15:20Z | Charte / Outillage de travail (find-brain avant grep, réflexe 3 axes) | Renforcé l'obligation find-brain avant grep + ajouté le principe général "un réflexe outil avant chaque commande, sur 3 axes" | important | Demande explicite : « toujours passer par find brain avant de faire une recherche [...] avant chaque commande, tu devrais regarder si l'utilisation d'un outil ne peut pas t'aider à repondre plus rapidement/efficacement/completement ». CLAUDE.md renforcé : l'obligation couvre désormais toute recherche dans un fichier existant, jamais seulement une lecture intégrale/un grep répété — preuve honnête citée (find-booster lancé sans tool-brain plus tôt le même soir). Nouveau principe ajouté à `docs/regles-de-travail.md` §7ter. `lib/reference.ts` Version 231. **Note de fidélité (Article 13)** : ce commit (81455f59) a été fait sans mettre à jour docs/suivi/ dans le même commit, signalé par le crochet post-commit — corrigé ici, dans le commit suivant, conformément à sa propre consigne. `check-house.mjs` vert (162 tests, aucun changement de code), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |
| 321 | 2026-09-21T15:45Z | Nouvel outil / Correctif majeur (noyau CASSANDRA-RH fiabilisé, bug checkAllAgentBadges) | Fiabilisé et testé le brouillon existant de CASSANDRA-RH (découvert non testé, tâche #184) ; câblé dans tout le réseau d'outils ; trouvé et corrigé un bug réel de longue date dans checkAllAgentBadges() qui empêchait TOUT Agent au nom composé de recevoir son badge automatique | critique | Demande : « pour cassandra, rien a été fait ? [...] go pour le codage de cassandra [...] on suit notre plan [...] enchaine tout en bloc ». Découverte clé (Article 19, avant tout code) : `scripts/cassandra-rh.mjs` existait déjà (commit "WIP : brouillon non testé", jamais annoncé à l'utilisateur comme fait), 254 lignes, noyau complet (effectif, KPI, badge, outils à retirer, squelette de recrutement) — jamais testé ni câblé. Corrigé plutôt que réécrit : (1) bug réel confirmé en testant `teamRoster()` en direct — slugifiait le texte ENTIER de la colonne Outil (parenthèses incluses), laissant CLONE-HUNTER/memory-audit/find-booster/objectifs-vs-resultats "catégorie non répertoriée" ; corrigé avec le découpage `primaryName` déjà établi ailleurs, `slugifyAgentName()` réutilisée (suppression du doublon local `slugify()`) ; (2) `computeBadgeResults()` ajoutée pour appeler réellement `checkAgentOnboarding()` par membre (le brouillon avait un badge hardcodé à zéro) ; (3) `main()` refactorée en `collectRealCassandraData()` + sous-commande `rapport` (HTML complet) vs signal léger. 24 nouvelles assertions dans check-house.mjs, dont une vérification live contre la vraie table maîtresse (zéro Agent réel non catégorisé). Câblage complet : AGENT_CATEGORIES (déjà présent), REGISTRIES, PRESTATIONS (Pack Direction RH), table maîtresse §7ter, CIRCLE_ITEMS (cassandra-rh-signal), blueprint + instanciation + registre créés, `docs/cassandra-rh-conception.md` marqué archivé (récit fondateur conservé, plus la source de vérité opérationnelle). Idem pour objectifs-vs-resultats, dont la section CLAUDE.md "blueprint exportable" manquait aussi (gap identique trouvé en vérifiant le badge de CASSANDRA-RH). **Trouvaille majeure en testant le badge réel des deux nouveaux outils** : `checkAllAgentBadges()` passait `row.tool` BRUT (avec parenthèses) à `checkAgentOnboarding()`, produisant un slug garbage pour TOUT Agent dont la cellule Outil porte une précision entre parenthèses — bug présent depuis la création de `checkAllAgentBadges()` le 2026-09-22, jamais détecté car les tests existants n'appelaient cette fonction qu'avec des noms déjà propres. Corrigé avec le même découpage `primaryName`. Vérifié en direct : 7 certifications réelles se sont déclenchées d'un coup au premier passage post-fix (CIRCLE-TASKS, memory-audit, find-deep-booster, tool-brain, find-booster, objectifs-vs-resultats, CASSANDRA-RH) — la preuve vivante que ce mécanisme n'avait jamais fonctionné correctement pour ces outils depuis sa création. `lib/reference.ts` Version 232. `check-house.mjs` vert (163 tests), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 322 | 2026-09-21T16:05Z | Nouvel outil / Correctif (CASSANDRA-RH « trous d'équipe », second bug de badge) | Ajouté computeCoverageGaps()/runAxaCheckCoverage() (couverture de test réelle par membre) au rapport complet ; trouvé et corrigé un second bug identique au bug badge du soir, cette fois dans computeBadgeResults() lui-même | important | Demande explicite avec question de contradiction posée : « CASSANDRA doit voir les trous dans l'organisation » — deux lectures possibles (équipe vs structure documentaire, ce second sens étant déjà réservé à un futur outil séparé). Tranché par l'utilisateur : trous dans l'équipe, et « élargir maintenant » l'accès aux rapports existants. `computeCoverageGaps()` (pure) + `runAxaCheckCoverage()` (relance une instrumentation V8, même mécanique qu'axa-check.mjs::main()) ajoutés — réservés au rapport complet (`node scripts/cassandra-rh.mjs rapport`), jamais au signal léger. Vérifié en direct : le rapport réel signale bien THE-SCREENER/THE-KING/INES-official/memory-audit/find-booster/objectifs-vs-resultats/CASSANDRA-RH elle-même comme jamais scannés, et HARMONIA à 25% de couverture. En testant, second bug trouvé : `computeBadgeResults()` (cassandra-rh.mjs) avait exactement le même défaut que `checkAllAgentBadges()` corrigé plus tôt dans la soirée — passait `member.tool` brut (parenthèses incluses) à `checkAgentOnboarding()` au lieu du `primaryName` déjà calculé par `teamRoster()`. Corrigé (teamRoster() expose désormais `primaryName` sur chaque membre du roster, réutilisé partout). `lib/reference.ts` Version 233. `check-house.mjs` vert (163 tests, assertions étendues dont une vérification live sur 3 vrais Agents à nom composé), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 323 | 2026-09-21T16:25Z | Charte / Outillage de travail (fiabilisation tool-brain, écart de reporting badge, désambiguïsation, surnom R/O-Guardian) | Réponses groupées à 4 demandes distinctes de l'utilisateur : hiérarchie tool-brain clarifiée en CLAUDE.md, écart de process corrigé (badges jamais recopiés dans la conversation), phrase de désambiguïsation check-tasks-details/LE-PLANIFICATEUR ajoutée, surnom R/O-Guardian adopté en parenthèse | important | Demandes explicites : « je veux une organisation bien claire [...] tool-brain doit t'aider systematiquement [...] je consultes et j'utilises plus les outils » ; « je n'ai pas vu defiler les messages de badge dans la conversation [...] fiabilise » ; « je veux bien une phrase pour l'ambiguité entre check-details et le plannificteur » ; « pour le nom de objectifs vs resultats : R/O-guardian qu'en penses-tu ? ». (1) CLAUDE.md réécrit : un seul point d'entrée obligatoire (tool-brain), schéma en arbre explicite, interdiction de contourner directement find-brain/find-booster/find-deep-booster. (2) Écart réel trouvé (Article 15) : les 7 certifications de badge déclenchées Version 232/233 n'existaient que dans la sortie terminal du crochet post-commit, jamais recopiées dans un message à l'utilisateur — nouvelle règle explicite écrite dans regles-de-travail.md (tout bloc `🎖️ CERTIFICATION` recopié verbatim au prochain message, jamais résumé). (3) Phrase ajoutée à regles-de-travail.md : check-tasks-details.mjs lit LE-PLANIFICATEUR en lecture seule et construit un rapport par-dessus, jamais un second endroit qui écrit la mémoire des tâches. (4) « R/O-Guardian » adopté comme surnom d'objectifs-vs-resultats, ajouté strictement en PARENTHÈSE (jamais en nom principal de la cellule table maîtresse) — vérifié avant application que ce Membre est un vrai Agent avec registre/blueprint/AGENT_CATEGORIES propres accrochés à son slug, contrairement à route-booster/CLAUDE.MD.SPY qui n'avaient aucune dépendance de ce type, rendant leur renommage en tête sans risque là où celui-ci en aurait un. Erreur autocorrigée en cours d'édition : une première tentative avait mis le surnom en position PRINCIPALE de la cellule, ce qui aurait cassé `slugifyAgentName()` pour ce Membre — revenue en arrière avant toute vérification. `lib/reference.ts` Version 234. `check-house.mjs` vert (163 tests, aucun changement de code testable), `tsc --noEmit` sans nouvelle erreur (seule l'erreur préexistante et non liée de `vite.config.ts` subsiste) | terminée — fidèle |

| 324 | 2026-09-21T16:45Z | Correctif (3e cas du bug primaryName) / Nouvel outil (objectifs-vs-resultats élargi à un signal KPI) | Corrigé un 3e cas du bug de slugification des noms d'Agent avec parenthèses dans check-last-commit.mjs ; ajouté une 3e source "kpi:<colonne>" à objectifs-vs-resultats.mjs pour couvrir les objectifs de simulation, avec déplacement de parseKpiHistoryCsv() vers kpi-report.mjs | important | Trouvé en vérifiant à fond la fiabilisation de tool-brain (Article 19) : `gardienOverrides` dans `check-last-commit.mjs` slugifiait `row.tool` brut, désactivant silencieusement l'override axaCoveragePct/reciprocalWiring pour find-booster/CLONE-HUNTER/CASSANDRA-RH — corrigé avec le même découpage `primaryName`. Puis, suite aux réponses de calibrage de l'utilisateur (« on élargit maintenant » pour les objectifs de simulation, « le découpage que vous proposiez » pour le workflow de fixation, Phase 1 pour la suite de CASSANDRA-RH) : ajouté la source `kpi:<colonne>` à `objectifs-vs-resultats.mjs` — lit `kpi-historique.csv` via `parseKpiHistoryCsv()`. Cette fonction vivait dans `cassandra-rh.mjs` (dupliquerait un second parseur si copiée) — déplacée vers `kpi-report.mjs` (son propriétaire naturel, KPI_HISTORY_COLUMNS/KPI_HISTORY_PATH), ré-exportée depuis cassandra-rh.mjs pour préserver le point d'import déjà utilisé par check-house.mjs. Retient le run le plus récent de la période demandée (jamais une moyenne). `docs/referentiel/objectifs-vs-resultats.md` mis à jour (3 sources). `lib/reference.ts` Version 235. `check-house.mjs` vert (163 tests, assertions étendues), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 325 | 2026-09-21T17:05Z | Nouvel outil (CASSANDRA-RH Phase 1 — nouveaux visages) | Construit detectNewArrivals()/narrateNewArrivals() : CASSANDRA narre chaque nouveau membre jamais vu (complet ou non) en tête de son rapport complet, persisté dans un nouveau journal local | important | Suite du roadmap CASSANDRA-RH présenté à l'utilisateur — Phase 1 choisie via question de calibrage. Répond directement à « c'est typiquement le role de cassandra de verifier que chaque nouveau membre est intégré selon le process, avec remise de badge à la fin et message ici dans la conversation ». `detectNewArrivals()`/`narrateNewArrivals()` (scripts/cassandra-rh.mjs) : compare le roster réel à `.cassandra-rh-known-members.json` (nouveau journal local, gitignored, ajouté à Doc-Report LOCAL_JOURNALS), narre chaque membre jamais vu — distinct du badge mécanique (présence vs complétude, les deux cohabitent). Bloc placé en tête du rapport complet, jamais dans le signal léger. Règle de relais symétrique à celle des badges ajoutée dans regles-de-travail.md. `docs/cassandra-rh-conception.md` §5 et `docs/referentiel/cassandra-rh.md` mis à jour. Vérifié en direct : premier passage réel a accueilli les 21 membres actuels, second passage silencieux (persistance confirmée). `lib/reference.ts` Version 236. `check-house.mjs` vert (163 tests, assertions étendues), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 326 | 2026-09-21T17:25Z | Correctif (gabarit de certification fiabilisé) / Écarts doc trouvés (organisation-agence.md) | Ajouté description + combinaisons typiques au bloc de certification (parseToolsTable/toolCompanions), corrigé 2 mentions périmées « CASSANDRA-RH à construire » | important | Demande explicite : « fiabilise le gabarit du bloc certification : même format assuré à chaque fois : j'ai besoin d'une courte description, de savoir si tout est bien pluggé, de savoir aussi avec quels outils cet outil est susceptible de se plugger. Circle tasks n'avait jamais été certifié ? ». Réponse factuelle vérifiée : si, CIRCLE-TASKS a été certifié le 2026-09-21T15:28:44Z (déjà montré dans la conversation). Ajouté `parseToolsTable()` extraction de la colonne « Ce qu'il détecte/régule » par nom, nouvelle fonction `toolCompanions()` (dérive les combinaisons du catalogue PRESTATIONS déjà existant, jamais une donnée nouvelle) ; `checkAgentOnboarding()`/`formatBadgeCeremonyAnnouncement()` exposent un gabarit fixe à 6 lignes garanties. Vérifié en direct sur CLONE-HUNTER/CIRCLE-TASKS/tool-brain/AXA-CHECK. Corrigé au passage (Article 13, HARMONIA-style, trouvé en relisant organisation-agence.md face au code réel) : 2 mentions « CASSANDRA-RH, à construire » restées périmées depuis la construction du noyau. `lib/reference.ts` Version 237. `check-house.mjs` vert (163 tests, assertions étendues), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 327 | 2026-09-21T17:50Z | Correctif majeur (compteur d'usage réel des outils, câblage complet) | Ajouté recordCliUsage()/origine "cli_direct", câblé dans le main() de 25 scripts réels | critique | Réponse choisie explicitement : « tous les outils d'un coup ». Suite de l'audit honnête (tool-brain rapport montrait 0 sollicitation partout malgré de vrais usages ce soir). Ajouté `USAGE_ORIGINS` 4e valeur "cli_direct" (tool-usage.mjs) et `recordCliUsage()` (best-effort, jamais bloquant). Câblé dans le main()/point d'entrée réel de : check-argus, check-harmonia, axa-check, clean-dirty-old, clone-hunter, always-new-code, hyper-scan-checkpoint, check-level-target, the-king, ines-official, smart-conso-api, smart-conso-token, kpi-report, el-professor, the-screener-capture, le-regisseur, check-tasks-details, le-coordinateur, find-booster, objectifs-vs-resultats, cassandra-rh, circle-tasks, tool-brain, route-booster, find-brain, check-gemini-quota (25 scripts). Non câblés, à raison : the-final-judge.mjs/the-deep-reader.mjs/memento.mjs/gemini-key-health.mjs/api-providers.mjs n'ont aucun point d'entrée CLI propre (fonctions pures importées ailleurs). Vérifié en direct : un vrai lancement de tool-brain/circle-tasks/objectifs-vs-resultats produit désormais un vrai événement dans `.tool-usage-history.json`. `lib/reference.ts` Version 238. `check-house.mjs` vert (163 tests, assertions étendues), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 328 | 2026-09-21T18:10Z | Réponses point par point (6 demandes distinctes) / Correctif (règle report-reading évolutive, dérive de description CLONE-HUNTER) | Renforcé la règle de lecture de rapport → action de suivi (docs/regles-de-travail.md) ; corrigé la fiche PRESTATIONS de CLONE-HUNTER (v2 manquante) ; conclusions livrées sur CLONE-HUNTER/simulations et CIRCLE-TASKS/CLAUDE.md | important | Message à 6 points traité un par un (Article 16, complément du 2026-09-17). (1) « vas-tu bien lire tous les rapports [...] une règle évolutive » : paragraphe ajouté sous la règle déjà existante, portée définie par appartenance à `CIRCLE_ITEMS` (jamais une liste figée). (2) « clone-hunter [...] detecte les repetions dans les simu ? » : investigué (clone-hunter.md/blueprint, aucune mention), conclusion — domaine déjà couvert par le système anti-écho runtime (lib/dialogue.ts, Article 11) + EL-PROFESSOR, pas d'extension nécessaire. (3) « toutes les descriptions [...] harmonisées » : spot-check CASSANDRA-RH/tool-brain (cohérentes), dérive réelle trouvée et corrigée sur CLONE-HUNTER (fiche PRESTATIONS ne citait que la v1, la table maîtresse cite déjà v1+v2 depuis le 2026-09-22) — audit exhaustif des ~25 fiches restant à proposer séparément, pas fait ce soir. (4) « nettoyage CLAUDE.md dans circle ? » : confirmé déjà présent (item lié à CHARTER-SPY/SMART-CONSO-TOKEN, thème Qualité du code). Points 5 (gabarit certification, fusion vs écrasement) et 6 (3 nouvelles idées CASSANDRA « OK GO ») traités séparément dans le même tour (question de calibrage pour le 5, implémentation pour le 6). `lib/reference.ts` Version 239. `check-house.mjs` vert (163 tests, assertion Version 239), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 329 | 2026-09-21T18:30Z | Nouvel outil (CASSANDRA-RH — 3 signaux « OK GO ») | `toolsToReconsider()` enrichi de 3 croisements optionnels (objectifs-vs-resultats, SMART-CONSO-TOKEN, Doc-Report) | important | Point 6 du message à 6 demandes, réponse « OK GO » de l'utilisateur sur 3 idées neuves proposées par l'agent. (1) Un objectif « en dessous » (objectifs-vs-resultats.mjs) renforce un « jamais sollicité » déjà présent. (2) Nouvelle fonction `tokenInvestmentVerdict()` (cassandra-rh.mjs) relit le dernier verdict SMART-CONSO-TOKEN déjà enregistré dont le contexte mentionne le slug de l'outil — correspondance textuelle honnête, jamais une reclassification. (3) Un registre Doc-Report jamais committé signale que personne ne consulte jamais la sortie de l'outil. Câblé dans `collectRealCassandraData()`, vérifié en direct (`node scripts/cassandra-rh.mjs rapport` sans erreur). `docs/referentiel/cassandra-rh.md` mis à jour. `lib/reference.ts` Version 240. `check-house.mjs` vert (164 tests, 1 nouveau bloc), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 330 | 2026-09-21T19:15Z | Nouvel Article (Article 24, évolutivité) / Correctifs (4 dérives réelles trouvées et corrigées) / Extension règle report-reading aux simulations | Audit d'évolutivité complet demandé explicitement : « Inspection complète maintenant » ; extension explicite de la règle de lecture de rapport aux rapports de simulation | critique | Suite du point 2 du message à 6 demandes, poussé jusqu'au bout après calibrage (portée code/outils seulement, corriger les 2 premiers trouvés maintenant, puis audit complet des 17 outils restants sur demande explicite « Inspection complète maintenant »). Nouvel Article 24 (CLAUDE.md) : toute construction qui reflète l'état d'un autre système doit soit le lire dynamiquement, soit être accompagnée d'un garde-fou mécanique — jamais une copie à la main sans vérification, portée strictement au code/outillage (jamais le contenu narratif, déjà couvert par les Articles 10/17). 4 dérives réelles trouvées et corrigées, chacune avec un nouveau garde-fou mécanique testé et câblé : THEMES (always-new-code.mjs) et SENSITIVE_NODES (check-level-target.mjs) recopiaient la carte HARMONIA sans vérification (`findThemesDivergingFromHarmonia()`/`findSensitiveNodesDivergingFromHarmonia()`) ; AGENT_SCRIPT_FILES (axa-check.mjs) n'avait qu'une borne basse figée en test — 6 Agents réels invisibles depuis leur construction (the-king, ines-official, memory-audit, find-booster, objectifs-vs-resultats, cassandra-rh) trouvés et ajoutés (`findScriptsMissingFromAgentFiles()`, câblé dans `runNetworkCheck()`) ; LOCAL_JOURNALS (doc-report.mjs) — 2 journaux locaux réels jamais déclarés trouvés et ajoutés (.memento-history.json, .the-ghost-session.json ; `findUndeclaredLocalJournals()`). Par ailleurs, demande explicite reçue en cours de route : « étends ce principe aux rapports issus des simulations » — la règle de lecture intégrale → action de suivi (docs/regles-de-travail.md) étendue explicitement aux livrables de l'Article 18 (transcript, dossier, KPI, EL-PROFESSOR, THE-SCREENER, LE-RÉGISSEUR), jamais seulement les rapports CIRCLE_ITEMS. `docs/referentiel/always-new-code.md`/`check-level-target.md`/`axa-check.md` mis à jour. `lib/reference.ts` Version 241. `check-house.mjs` vert (167 tests, 4 nouveaux blocs + 1 borne de test mise à jour), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 331 | 2026-09-21T19:35Z | Correctif (2 fichiers préliminaires manquants créés) | `docs/concordance-evolutivite-conception.md` et `docs/agence-exportable-conception.md` créés, ajoutés au registre et à `CHANTIER_PRELIMINARY_FILES` | important | Demande explicite : « assure toi que toutes les idees formulées pour des projets d'outil ou autres ont bien été consignées dans un fichier preliminaire comme le veut la regle ». Deux idées de gros chantier formulées ce soir (outil concordance/évolutivité — potentiel 6e Gardien sacré — et agence exportable comme gabarit générique) n'avaient pas encore leur fichier dédié malgré la règle du 2026-09-22 (`docs/regles-de-travail.md`). Créés sur le modèle de `docs/cassandra-rh-conception.md`, ajoutés au registre chantier→fichier ET à `CHANTIER_PRELIMINARY_FILES` (`check-tasks-details.mjs`) pour que le garde-fou mécanique `checkChantierFileFreshness()` les couvre aussi. `lib/reference.ts` Version 242. `check-house.mjs` vert (167 tests), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 332 | 2026-09-21T19:55Z | Nouvel outil (signal CIRCLE-TASKS chantier-preliminaire-signal) | Extension de checkChantierFileFreshness()/loadAllTaskRows() câblée dans la Ronde périodique, nouveau thème « Suivi des chantiers » | important | Demande explicite : « je veux un petit outil qui verifie lors de la ronde que les idees formulées ont bien été renseignées dans un fichier prelimnaire [...] certainement une extension d'un outil existant ». Questions de calibrage posées et répondues : signal automatique à chaque Ronde (pas un item à cocher), sur écart réel propose explicitement de mettre à jour le fichier, câblé sur le registre + check-tasks-details + docs/suivi (déjà la plomberie existante de checkChantierFileFreshness(), rien à dupliquer). Nouveau thème « Suivi des chantiers » créé car « Suivi & référentiels » était déjà à sa limite réelle de 4 items par question. Refactor associé pour éviter un cycle d'import : `daysSince()` déplacée de circle-tasks.mjs vers lib-shell.mjs (re-exportée telle quelle depuis circle-tasks.mjs), même raison que walkDocsPaths() déjà extraite là. Vérifié en direct (`node scripts/circle-tasks.mjs` sans erreur, aucun écart trouvé). `docs/regles-de-travail.md` (table maîtresse CIRCLE-TASKS) mis à jour. `lib/reference.ts` Version 243. `check-house.mjs` vert (167 tests, bornes de comptage + 1 nouvelle assertion live), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 333 | 2026-09-21T20:40Z | Nouvel outil (généralisation « idées à trancher » — signal CIRCLE-TASKS + règle temps réel) | `idee-a-trancher-signal` (CIRCLE-TASKS), `detectPendingIdeaCandidates()`/`loadIdeaDecisions()`/`findIdeasNeedingDecision()` (check-tasks-details.mjs), `docs/idees-a-trancher.md`, nouvelle règle permanente dans docs/regles-de-travail.md | important | Suite directe de la tâche #332 (message combiné de l'utilisateur : « en + [...] me demander systematiquement, pour chaque idee developpée [...] si je souhaite la creation d'un fichier preliminaire [...] ou abandonnée, ou entre-deux [...] l'alerte remontera alors une deuxieme fois lors de la prochaine ronde »). Généralise le patron « gros chantier → fichier préliminaire » à TOUTE idée nouvelle. Deux mécanismes distincts, clarifiés par l'utilisateur dans le même message : réflexe PRINCIPAL en temps réel (dès qu'une idée est proposée en conversation, question à 3 voies immédiate — documenté comme règle permanente dans docs/regles-de-travail.md) et signal CIRCLE-TASKS `idee-a-trancher-signal` comme FILET DE SÉCURITÉ mécanique (« l'avantage de la ronde, c'est que c'est mecanique, donc impossible à zapper »). Balayage rétrospectif manuel de tout l'historique (Article 19, jamais un scan textuel jugé peu fiable pour ce cas) : 3 sous-idées génuinement ouvertes trouvées, toutes déjà couvertes par `docs/cassandra-rh-conception.md` (Catalogue #297, 3 Stagiaires #226, « Promotion de poste » — entre-deux, question encore due). Trois bugs réels trouvés en testant en direct AVANT/pendant tout câblage (Article 3/19) : (1) un plancher de DATE aurait remonté 40+ tâches « Nouvel outil »/« Conception » du jour même comme fausses alertes — corrigé avec un plancher au NUMÉRO de tâche (`sinceTaskNumber=332`) ; (2) `loadIdeaDecisions()` lisait la décision à un index de cellule fixe, valide seulement pour l'un des deux tableaux du registre (largeurs différentes) — corrigé pour détecter la cellule décision PAR VALEUR, plus un bug connexe sur l'extraction de numéro de tâche (`\d+` nu confondait un numéro avec un chiffre de référence de section) corrigé avec `#(\d+)` strict ; (3) troisième bug trouvé au tout premier passage réel contre ce dépôt (le crochet pre-commit a bloqué le commit lui-même) : cette même tâche de suivi (#333, construite et close dans le même tour) s'est retrouvée signalée à tort comme « idée en attente » — corrigé en excluant les lignes `statusKey==="terminee"` (une tâche déjà terminée documente un travail déjà livré). `docs/referentiel/check-tasks-details.md` mis à jour. `lib/reference.ts` Version 244. `check-house.mjs` vert (168 tests, 1 nouveau bloc + bornes de comptage mises à jour), `tsc --noEmit` sans nouvelle erreur (seule l'erreur préexistante non liée de vite.config.ts subsiste) | terminée — fidèle |

| 334 | 2026-09-21T21:15Z | Nouvel outil / Correctif majeur (ALWAYS-NEW-CODE promu sixième Gardien sacré du code) | Couche légère seulement (recommendZone()/addendaSignal()/churnSignal()) câblée dans le crochet post-commit, badge, CIRCLE-TASKS ; organisation-agence.md §3 corrigé | important | Demande explicite (message combiné avec la tâche #333) : « On devrait avoir une version legere de always new code qui tourne à chaque commit : always new est un gardien à mon sens [...] fais ce changement tout de suite [...] integration complete comme pour les autres gardeins sacrés : verifier tous les branchements ». Trouvaille réelle en relisant organisation-agence.md §3 avant d'agir (Article 19) : le critère d'exclusion des Gardiens nommait ALWAYS-NEW-CODE par son NOM GLOBAL comme exemple d'outil exclu — trop large, corrigé pour cibler le LIVRABLE qui exige un raisonnement payant (le vrai zoom profond), jamais l'outil entier quand une couche légère gratuite existe séparément (précédent qui ne s'étend PAS à THE-FINAL-JUDGE, sans aucune couche gratuite). Câblage complet suivant le Répertoire des 7 fonctionnements partagés des Gardiens : nouveau bloc post-commit (scripts/hooks/check-last-commit.mjs) calculant le signal sur le seul fichier principal de la zone recommandée (coût minime) ; AGENT_CATEGORIES (lib-shell.mjs) mis à jour ; checkAgentOnboarding() gagne alwaysNewCodeFlagged → koParts, "OK 100%" exige désormais les 6 Gardiens ; item CIRCLE-TASKS always-new-code-signal retiré (redondant, même précédent que clone-hunter-run), CIRCLE_EXCLUDED_REGISTRIES documente la raison ; HYPER-SCAN-CHECKPOINT déjà câblé de longue date, aucun changement nécessaire. CLAUDE.md Article 20/23 et docs/referentiel/always-new-code.md mis à jour. Vérifié en direct : `node scripts/hooks/check-last-commit.mjs` tourne sans erreur, aucun signal fabriqué. `lib/reference.ts` Version 245. `check-house.mjs` vert (168 tests, 1 nouveau bloc de test 6e Gardien), `tsc --noEmit` sans nouvelle erreur (seule l'erreur préexistante non liée de vite.config.ts subsiste) | terminée — fidèle |

| 335 | 2026-09-21T21:50Z | Nouvel outil / Conception (protocole CIRCLE-TASKS AUTO/PRIME/GOAT + périodicité) | recommendCircleSelectionWithPeriodicity()/costlyItemDueStatus()/primeAddableItems() (circle-tasks.mjs) ; idée niveau 1/2/3 de l'agence exportable consignée dans docs/agence-exportable-conception.md §5 | important | Message combiné de l'utilisateur : (1) idée « agence exportable en niveaux 1/2/3, exécution sans réflexion » — réponse : oui pour la RÈGLE générique dès maintenant, non pour écrire le contenu détaillé des 25 outils aujourd'hui (travail spéculatif sur un futur projet pas encore connu) — consignée dans le fichier de conception existant (§5 nouvelle section, écrasée à chaque reprise de ce sous-sujet précis, jamais un empilement chronologique — discipline actée pour les fichiers de conception des gros chantiers uniquement, jamais docs/suivi qui reste append-only) ; (2) demande « il doit y avoir 3 niveaux [...] AUTO/PRIME/GOAT » pour la fenêtre de lancement de la Ronde — AUTO lance la sélection recommandée directement, PRIME l'étend avec des tâches gratuites en plus (primeAddableItems(), jamais les 2 items costly — réservés à GOAT avec renvoi explicite), GOAT choisit tout manuellement + le niveau de profondeur PAR TÂCHE (calibrage explicite) ; (3) demande de construire dès maintenant la vraie logique de périodicité (recommendCircleSelectionWithPeriodicity()/costlyItemDueStatus(), seuil 30 jours, lu depuis les vrais registres THE-FINAL-JUDGE/THE-DEEP-READER) — un item costly dû rejoint exceptionnellement AUTO avec une alerte explicite et un substitut gratuit documenté (COSTLY_SUBSTITUTES) plutôt qu'une pure suppression. docs/regles-de-travail.md (section CIRCLE-TASKS) réécrit pour ce protocole à 3 modes. `lib/reference.ts` Version 246. `check-house.mjs` vert (168 tests, 1 nouveau bloc), `tsc --noEmit` sans nouvelle erreur (seule l'erreur préexistante non liée de vite.config.ts subsiste) | terminée — fidèle |
| 336 | 2026-09-21T22:15Z | Exécution réelle / Suivi & référentiels (première Ronde CIRCLE-TASKS en mode AUTO, 18 items) | Nouvelle fiche profil-utilisateur (2026-09-22-0115.md) ; nouveau scan Global SMART-CONSO-TOKEN archivé (docs/smart-conso-token/scans/scan-2026-09-21-circle.md) ; nouvelle édition du catalogue LE-COORDINATEUR ; récapitulatif de Ronde livré en fichier joint | important | Réponse finale de l'utilisateur au protocole calibré en tâche #335 : « AUTO (Recommandé) ». Les 18 items recommandés par `recommendCircleSelectionWithPeriodicity()` ont été exécutés un par un (profil, référentiel THE-KING, KPI, CLEAN-DIRTY-OLD, câblage HTML, poids CLAUDE.md, correctifs/points fragiles, tâche ouverte la plus ancienne, fraîcheur chantier-préliminaire, idée-à-trancher, scan Smart Conso API, scan Global SMART-CONSO-TOKEN, rapport tool-brain, fraîcheur INES-official, signal CASSANDRA-RH, garde-fou profil-utilisateur, synthèse réseau LE-COORDINATEUR, catalogue LE-COORDINATEUR), aucun report à la prochaine Ronde. Aucun item costly dû (les deux registres THE-FINAL-JUDGE/THE-DEEP-READER datent du 2026-09-20, sous le seuil de 30 jours) — le mécanisme de périodicité construit en tâche #335 reste donc silencieux ce soir, comme attendu. Trouvailles réelles à signaler à l'utilisateur (jamais corrigées seul, chaque item ne juge jamais) : (1) le signal de tâche ouverte la plus ancienne du suivi remonte la conception CASSANDRA-RH (2026-09-20T09:40Z) alors que le noyau a depuis été construit et clos (tâche #184) — probablement une clôture manquée à vérifier ; (2) le scan Global SMART-CONSO-TOKEN (130 documents désormais, 71 le 2026-09-20) confirme CLAUDE.md toujours seul en action_requise (29 606 tokens, +2,8% depuis le dernier scan) et signale `docs/regles-de-travail.md` en forte croissance (52 228 tokens, informatif seulement — jamais toujours-chargé). Récapitulatif complet (`buildCircleRunSummaryText()`) livré en fichier joint, jamais collé en clair (préférence actée le 2026-09-18). `lib/reference.ts` Version 247. `check-house.mjs` vert (168 tests, assertion de version mise à jour), `tsc --noEmit` sans nouvelle erreur (seule l'erreur préexistante non liée de vite.config.ts subsiste) | terminée — fidèle |
| 337 | 2026-09-21T22:40Z | Correctif / Suivi & référentiels (rapports post-Ronde + fiabilisation CIRCLE-TASKS) | KPI archivé et indexé (`kpi-rapports/manuel-2026-09-21T18-52.txt`, `kpi-index.md`) ; nouvelle sous-commande `node scripts/circle-tasks.mjs record-run` ; ligne de suivi CASSANDRA-RH clarifiée | important | Demande explicite de l'utilisateur, en 3 points traités point par point : (1) « tous rapports qui doivent etre produits suite à circle » — trouvé un vrai oubli de procédure (Article 13/18) : le rapport KPI lancé pendant l'item `kpi` de la Ronde (tâche #336) avait bien tourné et sa ligne existait déjà dans `kpi-historique.csv`, mais son `.txt` n'avait jamais été archivé ni indexé (les étapes 2/3 de la procédure documentée dans `kpi-index.md` avaient été sautées en plein enchaînement des 18 items) — corrigé en réutilisant la sortie déjà capturée dans la conversation, sans relancer le script (qui aurait créé une seconde ligne divergente pour le même run). (2) « tu fiabilises circle suite à cette ronde » — trouvé la vraie cause du message stale « 32 commits sans Ronde » affiché juste après avoir exécuté la Ronde : `recordCircleTasksRun()` existait déjà en code (mémoire `.circle-tasks-last-run.json`) mais n'avait AUCUN chemin d'appel simple — seul un `node -e` improvisé pouvait l'invoquer, et l'agent qui pilotait a justement oublié de le faire. Corrigé par une vraie sous-commande CLI (`record-run`, réutilise `git rev-list --count HEAD`, même commande que le crochet post-commit — jamais un second calcul divergent), documentée dans `docs/regles-de-travail.md` comme étape de clôture OBLIGATOIRE de toute Ronde, testée (vérification structurelle du branchement dans `check-house.mjs`, jamais un appel réel en test qui écraserait le vrai fichier local), et exécutée rétroactivement pour la Ronde de ce soir. (3) « les correctifs prevus par les resultats de circle » — la tâche de suivi la plus ancienne (ligne #—/2026-09-20T09:40Z, conception CASSANDRA-RH) restait figée « en cours » sans jamais mentionner que son noyau a depuis été construit (tâche #184) — mise à jour avec un état honnête plutôt que close à tort (plusieurs de ses points restent réellement ouverts, développement complet à suivre). `lib/reference.ts` Version 248. `check-house.mjs` vert (168 tests, 3 nouvelles assertions ajoutées au bloc CIRCLE-TASKS existant plutôt qu'un nouveau bloc séparé), `tsc --noEmit` sans nouvelle erreur (seule l'erreur préexistante non liée de vite.config.ts subsiste) | terminée — fidèle |
| 338 | 2026-09-21T23:10Z | Conception / Nouvel outil (changement-de-modele-IA, CIRCLE-TASKS) | Idée consignée dans `docs/changement-de-modele-ia-conception.md`, 20 questions de calibrage posées et répondues intégralement, rien codé (décision explicite) | important | Demande explicite de l'utilisateur (point 4 du message combiné) : un protocole en 3 questions au lancement de CIRCLE-TASKS pour permettre de faire tourner un autre modèle/agent IA (ex. Opus) pendant que Claude exécute la Ronde, puis revenir — avec « pose 20 questions de calibrage pour ce programme, à mettre en to do, fichier ». Les 20 questions ont couvert : portée (CIRCLE-TASKS seulement, simulation/audits coûteux notés comme extension possible plus tard), sens exact de « changer de modèle » (le modèle Claude de cette conversation, jamais un outil externe), qui bascule techniquement (l'utilisateur, jamais l'agent), le déroulement précis des deux branches avant/après (une seule pause après tous les scans côté « avant » ; rapports écrits tout de suite sans attendre côté « après », rappel final systématique portant la mention « dernier rappel »), la gestion d'un « non » au dernier rappel (continuer sans insister), la vérification technique du modèle réellement actif (utilisée pour repérer un désaccord, jamais pour sauter la confirmation manuelle), l'absence de mémorisation d'une Ronde à l'autre, le comportement par défaut sans réponse ou en mode nocturne autonome (process habituel, jamais de blocage), la journalisation par Ronde, les 3 modes concernés (AUTO/PRIME/GOAT, sans exception), et le statut de construction (décrire et mettre de côté). Précision majeure obtenue à la toute dernière question : l'objectif réel n'est pas seulement de « libérer l'accès au modèle habituel » mais de permettre des **phases de switch bornées et sécurisées, aux repères définis par l'agent qui pilote** (jamais intempestives) — reformulé dans le document de conception. Rappel demandé pour la prochaine Ronde : conversationnel seulement, jamais câblé en dur dans le code (une première proposition de rappel mécanique a été explicitement écartée par l'utilisateur). `CHANTIER_PRELIMINARY_FILES` (check-tasks-details.mjs) mis à jour avec cette nouvelle entrée. `lib/reference.ts` Version 249. `check-house.mjs` vert (168 tests), `tsc --noEmit` sans nouvelle erreur (seule l'erreur préexistante non liée de vite.config.ts subsiste) | terminée — fidèle |
| 339 | 2026-09-21T23:25Z | Correctif / Outillage de travail (checkChantierFileFreshness — paradoxe temporel du crochet pre-commit) | `isStagedForCommit()` ajoutée à check-tasks-details.mjs, tests enrichis (cas 4bis), `lib/reference.ts` Version 250 | important | Bug réel trouvé en committant la tâche #338 : le tout premier commit de `docs/changement-de-modele-ia-conception.md` (introduit dans le MÊME commit que sa tâche de suivi #338, exactement la discipline demandée par Article 13) s'est vu bloqué par le crochet pre-commit — `checkChantierFileFreshness()` le signalait comme « introuvable ou jamais commité ». Root-cause tracée (Article 3/19) : `lastTouchDays()` interroge `git log`, qui ne voit encore AUCUN commit touchant un fichier tant que le commit en cours (celui que le crochet pre-commit est justement en train de valider) n'a pas réellement eu lieu — un vrai paradoxe temporel structurel, jamais un oubli réel, qui aurait bloqué le premier commit de N'IMPORTE QUEL futur fichier préliminaire de chantier introduit selon la discipline recommandée (fichier + tâche de suivi dans le même commit). Corrigé en ajoutant `isStagedForCommit()` (vérifie `git diff --cached --name-only`) comme filet de repli quand `lastTouch()` renvoie undefined : un fichier déjà STAGÉ pour le commit en cours n'est plus jamais confondu avec un fichier réellement jamais écrit. Tests existants (cas 4/5) mis à jour pour injecter `isStaged: () => false` explicitement (déterminisme, jamais un appel au vrai git dans un test unitaire) et nouveau cas 4bis couvrant le cas stagé. `check-house.mjs` vert (168 tests), `tsc --noEmit` sans nouvelle erreur (seule l'erreur préexistante non liée de vite.config.ts subsiste) | terminée — fidèle |
| 340 | 2026-09-21T23:45Z | Correctif majeur / Nouvel outil (audit complet des rapports produits par CIRCLE-TASKS, préparation circle-process-guardian) | `producesReport` sur CIRCLE_ITEMS, `REPORT_ICON` dans formatCircleMenu()/buildCircleRunSummaryText(), `findOrphanReportFiles()`/`REPORT_PER_RUN_REGISTRIES` (doc-report.mjs) câblé dans runNetworkCheck(), 6 fichiers ARGUS orphelins indexés rétroactivement, `docs/circle-process-detail.txt` créé | important | Question directe de l'utilisateur, traitée point par point : (1) « on est ok que les 18 outils sollicités ont tous produit un rapport ? » — vérifié un par un contre le vrai code : seuls 4 des 18 exécutés ce soir (8 des 23 items existants au total) produisent réellement un artefact archivé (profil, kpi, smart-conso-token-scan, coordinateur-catalogue, + dream-team-photo/the-screener/the-final-judge/the-deep-reader non exécutés ce soir) — les autres sont des signaux console par conception, confirmé en relisant le commentaire de code de chacun. (2) « ça correspond à deux catégories ou à une erreur ? » — deux catégories réelles, documentées explicitement dans le code de chaque item, jamais une erreur. (3) « le rapport doit-il être auto pour les membres certifiés ? » — non, choix déjà tranché ailleurs (INES-official/CASSANDRA-RH : signal léger en Ronde, rapport complet seulement sur demande explicite, pour des raisons de coût/consentement). (4) « icône rapport partout où c'est nécessaire » — ajoutée dans la fenêtre à cocher (`formatCircleMenu()`) et le récapitulatif de fin de Ronde (`buildCircleRunSummaryText()`), lue honnêtement depuis le nouveau champ `producesReport` de CIRCLE_ITEMS (jamais devinée) ; rollout au catalogue LE-COORDINATEUR et à organisation-agence.md/regles-de-travail.md §7ter explicitement reporté (PRESTATIONS combine souvent plusieurs outils aux statuts différents, un rollout précipité risquerait des icônes fausses). (5) Trouvaille au-delà des 18 items, suite à la relance directe de l'utilisateur (« en plus des 18 outils, il y a des rapports qui doivent etre produits mecaniquement ») : ARGUS écrit un vrai fichier de scan à CHAQUE commit (pas seulement pendant une Ronde), mais rien ne garantissait que son index suive — 10 fichiers réels existaient, seulement 4 indexés, 6 orphelins (dont celui produit par la Ronde de ce soir elle-même) — les 6 ont été lus et indexés rétroactivement. Corrigé durablement par `findOrphanReportFiles()` (doc-report.mjs), avec un périmètre volontairement CURATÉ (`REPORT_PER_RUN_REGISTRIES = ["argus"]`, jamais tout REGISTRIES par défaut — deux vraies exceptions trouvées en testant en direct : `kpi` a son index à un chemin frère, `hyper-scan-checkpoint` ne loggue délibérément que les passages complets), câblé comme nouvelle ligne dans `runNetworkCheck()` (12e ligne, item `network-check-run` de chaque Ronde). (6) « toi tu lis combien de rapports après l'exécution de circles, et ta synthèse cite-t-elle tous les outils ? » — réponse honnête donnée à l'utilisateur en conversation : les 18 sorties console ont bien été lues en direct pendant l'exécution, mais les 4 rapports archivés n'ont jamais été relus séparément depuis le disque après écriture (rédigés directement depuis les données déjà en contexte) ; le récapitulatif livré cite bien les 18 items par leur nom avec un résultat en une ligne chacun, mais ce n'est pas une synthèse approfondie du contenu de chaque rapport — nuance assumée, jamais cachée. (7) Demande de renommer le futur outil « circle-process-guardian » et de produire d'abord un fichier texte détaillant tout le processus CIRCLE-TASKS actuel (23 items, 6 Gardiens automatiques, intégration future du changement de modèle IA) pour bien le concevoir — fait (`docs/circle-process-detail.txt`, généré en relisant le vrai code, jamais recopié de mémoire) ; construction de l'outil lui-même explicitement différée à après lecture de ce fichier par l'utilisateur. `lib/reference.ts` Version 251. `check-house.mjs` vert (168 tests, nouvelles assertions sur findOrphanReportFiles/REPORT_ICON), `tsc --noEmit` sans nouvelle erreur (seule l'erreur préexistante non liée de vite.config.ts subsiste) | terminée — fidèle |
| 341 | 2026-09-22T00:40Z | Correctif majeur (règle générale : tous les items de la Ronde CIRCLE-TASKS produisent un rapport texte minimum) + réinversion du récapitulatif de fin de Ronde en HTML | `recordCircleItemReport()`/`CIRCLE_REPORT_FOLDERS` (circle-tasks.mjs, générique, 2 régimes selon qu'un index.md curaté existe déjà), 9 nouvelles entrées `doc-report.mjs::REGISTRIES` (html-wiring-check, claude-md-weight, suivi-open-tasks, chantier-preliminaire, idee-a-trancher, tool-brain, network-check, relecture-referentiel, relecture-correctifs), `producesReport:true` sur les 23 items de CIRCLE_ITEMS (0 exception jugée justifiable), cassandra-rh-signal promu rapport complet à chaque Ronde, network-check-run gagne un vrai rapport txt, `shouldSnapshotPhilosophy()` (the-king.mjs) + snapshot texte de docs/philosophie-et-politique.md sur changement réel, `docs/profil-utilisateur/profil-actuel.txt` toujours à jour, `buildCircleRunSummaryHtml()` (nouvelle fonction canonique) + nouveau type de bloc `"highlight"` dans html-report.mjs, retrait du doublon `checkHtmlWiring()` de circle-tasks.mjs (recâblé sur `auditHtmlDecisions()` de doc-report.mjs), `buildDeepReaderReportHtml()` ajoutée à the-deep-reader.mjs (écart réel trouvé par ce recâblage : jamais câblé sur html-report.mjs depuis sa création) | important | Suite directe de la lecture par l'utilisateur de `docs/circle-process-detail.txt` (tâche #340), traitée point par point avec des questions de calibrage à chaque décision de conception réelle (Article 16) : (1) sur html-wiring-check, l'utilisateur a pointé qu'un outil s'occupait déjà de la décision HTML/texte — vérifié (Article 19) : `doc-report.mjs::checkHtmlWiring()/auditHtmlDecisions()` existait déjà et était strictement supérieur à la version locale de circle-tasks.mjs (3 scripts codés en dur, jamais mise à jour) — doublon retiré, ce qui a lui-même révélé un second écart réel (the-deep-reader.mjs jamais câblé sur html-report.mjs malgré son statut "delivery_html"), corrigé le même soir. (2) cassandra-rh-signal passe en rapport complet à chaque Ronde sur demande explicite, aucune tension Article 22 (calcul local, zéro appel API). (3) network-check-run confirmé comme les « branchements entre les outils » (les lignes de synthèse de runNetworkCheck()), gagne un rapport txt archivé. (4) Correction générale demandée : « tous les outils qui interviennent lors de la ronde DOIVENT produire un rapport txt au minimum » — vérifié un par un (15 items concernés), aucune exception jugée réellement justifiable ; 4 questions de calibrage posées (relectures manuelles incluses ou non — oui ; rangement en dossier commun ou par outil — par outil, choisi ; tout de suite ou progressif — tout de suite, avant CASSANDRA-RH) puis exécutées en conséquence. (5) Étape 5 (récapitulatif de fin de Ronde) : passage de texte à HTML explicitement demandé, ce qui inversait une règle déjà actée par écrit le 2026-09-21 plus tôt le même soir — Article 14 appliqué à la lettre : tension signalée, PUIS double confirmation explicite obtenue avant d'exécuter (deux fenêtres de question séparées) ; nouveau bloc HTML `"highlight"` ajouté à html-report.mjs pour porter l'analyse mise en évidence, jamais confondu visuellement avec le bloc `"note"` existant (mise en garde discrète). (6) Extension demandée en cours de route : THE-KING édite en plus une snapshot texte à jour de la philosophie (déclenchée sur changement réel du contenu, jamais une cadence fixe en Ronde — proposition de l'agent après que l'utilisateur a explicitement délégué le choix : « je ne sais pas quelle est la meilleure solution, propose ») et le profil utilisateur gagne un fichier "toujours à jour" séparé de son historique daté. (7) Vérification croisée demandée sur le tableau de bord : confirmé que `docs/referentiel/kpi-rapports/*.txt` contient déjà les 6 familles complètes avec N/A honnête — aucun doublon nécessaire, l'item `kpi` couvre déjà cette demande. (8) Tableau de poids fourni sur demande (estimation par octets/tokens à partir de fichiers réels déjà mesurés sur le disque, jamais un chiffre inventé) : ~6 000-8 000 tokens supplémentaires par Ronde complète, négligeable face aux ~29 000 tokens déjà fixes de CLAUDE.md. (9) `docs/circle-process-detail.txt` mis à jour (Partie 6 ajoutée) avec toutes les décisions ci-dessus, et l'idée « circle-process-guardian » officiellement enregistrée dans `CHANTIER_PRELIMINARY_FILES` (check-tasks-details.mjs) pour que sa fraîcheur soit désormais vérifiée mécaniquement, sur demande explicite de l'utilisateur (« tous ces elements doivent bien etre consignés dans les idees process guardian »). `lib/reference.ts` Version 252. `check-house.mjs` vert (nouvelles assertions sur recordCircleItemReport, buildCircleRunSummaryHtml, le bloc highlight, l'audit HTML en direct — a trouvé et fait corriger le vrai écart the-deep-reader.mjs en cours de route), `tsc --noEmit` sans nouvelle erreur (seule l'erreur préexistante non liée de vite.config.ts subsiste). Reste ouvert : construction de circle-process-guardian lui-même (toujours différée), puis retour à CASSANDRA-RH (développement complet des fonctionnalités restantes). | terminée — fidèle |
| 342 | 2026-09-22T01:15Z | Correctif (snapshot texte datée pour CLAUDE.md + retrofit THE-KING, historique local jamais un fichier écrasé) | `shouldSnapshotText()` (ex-`shouldSnapshotPhilosophy()`, déplacée dans `lib-shell.mjs` pour un second appelant réel), `recordSnapshotIfChanged()` (circle-tasks.mjs, nouvelle snapshot datée seulement sur vrai changement), `claude-md-weight-signal` en bénéficie désormais (`docs/claude-md-weight/snapshot-*.txt`), correction de `the-king-signal` (overwrite unique corrigé en historique daté avant tout premier usage réel) | important | Demande explicite de l'utilisateur : « je veux aussi une copie de claude.md dans un fichier txt à chaque ronde avec un historique local, quel outil s'en charge ? » — réponse : claude-md-weight-signal (déjà existant), étendu ce soir. En construisant cette extension, trouvaille rétroactive : la snapshot THE-KING ajoutée plus tôt ce même soir (tâche #341) écrasait à tort un seul fichier (`philosophie-et-politique-derniere-version.txt`) au lieu de garder l'historique des instantanés déjà explicitement demandé au moment de sa conception (« il y a un interet à garder une trace des instantanés ») — erreur de conception de l'agent, jamais appliquée en production (aucune Ronde réelle n'avait encore tourné depuis sa création), corrigée avant tout premier usage réel plutôt que migrée. `lib/reference.ts` Version 253. `check-house.mjs` vert (nouveaux tests shouldSnapshotText/recordSnapshotIfChanged), `tsc --noEmit` sans nouvelle erreur. Documenté après coup (Article 13 : ce commit n'avait pas reçu sa ligne de suivi dans le même commit que le code, écart corrigé dès détection par `findCommitsMissingSuiviUpdate()`). | terminée — fidèle |
| 343 | 2026-09-22T01:45Z | Charte / process (séquence stricte Étape 5 CIRCLE-TASKS + questions forcées en fin d'analyse) | `docs/circle-process-detail.txt` (Partie 7 ajoutée, point 5ter en Partie 5), `docs/regles-de-travail.md` (nouveau paragraphe dédié) | important | Demande explicite de l'utilisateur, à écrire noir sur blanc et prévoir pour circle-process-guardian : (1) ordre non négociable pour l'Étape 5 — livrer tous les rapports txt dans la conversation, PUIS SEULEMENT les lire, PUIS construire l'analyse, PUIS livrer le récapitulatif HTML, PUIS poser une série de 5 à 10 questions à choix forcé proportionnellement au nombre de points trouvés — raison explicite donnée par l'utilisateur : il doit pouvoir lire les rapports avant l'analyse de l'agent pour proposer une lecture différente ; (2) but assumé des questions forcées, écrit noir sur blanc par l'utilisateur lui-même : « le but inavoué : me forcer à remarquer, attirer mon attention sur les problemes principaux, meme si je n'ouvre aucun rapport » — chaque problème mérite une action, même minimale. Documentation pure, aucun code à modifier (la règle porte sur le comportement de l'agent pilotant une future Ronde, pas sur une fonction existante). `lib/reference.ts` Version 254. `check-house.mjs` vert (aucun changement de code, seulement la version bump testée). | terminée — fidèle |
| 344 | 2026-09-22T02:10Z | Vérification (passage HARMONIA à raisonnement, thème relation Lia/Noé) | `docs/harmonia/index.md` (nouvelle ligne), `lib/reference.ts` Version 255 | important | Demande explicite : « lance HARMONIA sur la relation Lia/Noé aussi ». Consulté SMART-CONSO-TOKEN avant lancement (`full_repo_scope`, verdict avertissement_souple — ciblé sur les fichiers du thème plutôt qu'un vrai balayage de tout le dépôt). Relu relationship.ts, life.ts, dialogue.ts, world.ts, turn.ts, lia.ts : aucune friction trouvée — les deux points de vigilance historiques de ce thème (pools de pensées partagés entre Lia/Noé, dossier/attirance mal isolés) étaient déjà corrigés avant ce soir (tâches #187/#188), et `wordFrequency` volontairement partagé (Article 11) a été vérifié comme un choix voulu plutôt qu'un bug. Trouvaille positive : `lia.ts` porte une garde explicite écrite dans le prompt lui-même contre l'accès aux pensées privées de l'autre, une seconde couche de protection au-dessus de la séparation architecturale des deux appels Gemini (Article 8). Limite honnête assumée et écrite dans le registre : passage ciblé sur les fichiers du thème, pas un audit ligne à ligne de app/api/lia/route.ts ni de simulation.ts. Résultat enregistré dans l'historique SMART-CONSO-TOKEN comme investissement réel. `check-house.mjs` vert (version bump testée, aucun changement de code). | terminée — fidèle |
| 345 | 2026-09-22T02:35Z | Nouvel outil (construction de circle-process-guardian) | `scripts/circle-process-guardian.mjs` (nouveau, `hasFreshReportFile()`/`verifyRondeProcess()`), `docs/circle-process-detail.txt` (Partie 8 ajoutée, outil marqué construit), `docs/regles-de-travail.md` (paragraphe dédié), `lib/reference.ts` Version 256 | important | Suite directe de la demande combinée de la tâche #343 (« avant cassandra, je crois que je veux creer cet agent [...] »). Point d'entrée unique `verifyRondeProcess({...})`, jamais un second calcul divergent : réutilise `findOrphanReportFiles()` (doc-report.mjs) et `findRegistriesMissingFromCircle()` (circle-tasks.mjs) telles quelles. Couvre mécaniquement les 8 points de la Partie 5 + le point 9 de la Partie 7 de `docs/circle-process-detail.txt` : fichier de rapport frais daté du jour (`hasFreshReportFile()`, même motif circle-signal-*/snapshot-* déjà utilisé ailleurs), écart de commits depuis le dernier `record-run()` (trouvaille réelle en testant en direct : 9 commits d'écart avec la dernière Ronde enregistrée), registres orphelins/manquants dans CIRCLE_ITEMS. Portée honnête assumée dès la conception : les faits de conversation (question AUTO/PRIME/GOAT posée, items réellement cochés vs exécutés, séquence stricte de l'Étape 5 respectée, nombre de questions forcées posées) ne sont jamais déductibles du code seul — l'agent qui pilote doit les fournir explicitement, leur absence étant elle-même signalée comme un écart plutôt que supposée conforme. Reste ouvert, signalé dans les deux docs : le branchement réel de `verifyRondeProcess()` dans le déroulement effectif d'une Ronde n'est pas encore automatique, l'agent doit l'invoquer lui-même en fin de Ronde. `check-house.mjs` vert (nouveau bloc de tests dédié, ~20 assertions couvrant chacun des 9 points plus un scénario entièrement propre), `tsc --noEmit` sans nouvelle erreur (seule l'erreur préexistante non liée de vite.config.ts subsiste). Suite prévue par l'utilisateur (« on enchaine tout de suite ») : protocole changement-de-modele-IA (3 questions), puis relancer une Ronde. | terminée — fidèle |
| 346 | 2026-09-22T02:55Z | Nouvel outil (construction du protocole changement-de-modele-IA) | `docs/regles-de-travail.md` (nouveau paragraphe, protocole en 3 questions avant AUTO/PRIME/GOAT), `docs/changement-de-modele-ia-conception.md` (statut mis à jour), `executedByModel` (nouveau paramètre optionnel de `buildCircleRunSummaryText()`/`buildCircleRunSummaryHtml()`, circle-tasks.mjs), `lib/reference.ts` Version 257 | important | Suite directe du plan de l'utilisateur (« on enchaine tout de suite [...] mettre en place process changement de modele et relancer une ronde »), reprenant intégralement la conception à 20 questions déjà calibrée (tâche #338). Le protocole lui-même (Q1 avant même AUTO/PRIME/GOAT dans les 3 modes, jamais mémorisée d'une Ronde à l'autre, sautée en mode nocturne/autonome, absence de réponse = non ; Q2/Q3 selon la réponse, pause unique après tous les scans ou rappel final « dernier rappel » ; vérification technique via `get_session` en appui, jamais en remplacement de la confirmation manuelle) est un comportement conversationnel de l'agent, documenté noir sur blanc plutôt que codé — cohérent avec la nature déjà purement conversationnelle du garde-fou AUTO/PRIME/GOAT lui-même (`formatCircleMenu()` affiche un menu, mais c'est bien l'agent qui pose la question via l'outil de questions). Seule partie réellement codée : la journalisation demandée par la conception (« chaque Ronde doit garder une trace écrite de quel modèle/IA l'a réellement exécutée ») — `executedByModel`, paramètre optionnel ajouté aux deux fonctions de récapitulatif, silence total si non fourni (comportement inchangé), une simple note ajoutée au récapitulatif si fourni. Limite honnête assumée et écrite dans les deux docs : aucun garde-fou mécanique ne peut vérifier que Q1/Q2/Q3 ont bien été posées en conversation — circle-process-guardian ne couvre pas ce protocole aujourd'hui. `check-house.mjs` vert (4 nouvelles assertions sur executedByModel, silence par défaut vérifié pour les deux variantes texte/HTML), `tsc --noEmit` sans nouvelle erreur (seule l'erreur préexistante non liée de vite.config.ts subsiste). Suite prévue par l'utilisateur : relancer une Ronde CIRCLE-TASKS réelle. | terminée — fidèle |
| 347 | 2026-09-22T03:20Z | Correctif / Nouvel outil (discipline d'exécution étendue à HYPER-SCAN-CHECKPOINT, double lecture Smart Conso, correction d'un malentendu sur le point 3) | `verifyHyperScanProcess()` (circle-process-guardian.mjs), `filterIndexRowsByVersion()` (smart-conso-token.mjs) + nouvelle ligne dans `runNetworkCheck()` (le-coordinateur.mjs), `docs/referentiel/hyper-scan-checkpoint.md` et `docs/referentiel/smart-conso-token.md` mis à jour, commentaire permanent ajouté dans circle-process-guardian.mjs sur la double communication, `verifyDoubleCommunication()` (comparaison mécanique réelle une fois les deux textes fournis), `lib/reference.ts` Version 258 | important | Réponse à un message combiné traité point par point : (1) confirmé, sans changement de code, que `recordSnapshotIfChanged()` fonctionne déjà exactement comme demandé (« une sauvegarde à chaque fois que ca change, rien de plus ») ; (2) expliqué en conversation le rôle d'HYPER-SCAN-CHECKPOINT (vérification exceptionnelle à raisonnement, jamais automatique, KPI = taux de trouvailles réelles) vs LE-COORDINATEUR (zéro raisonnement, agrège/affiche ce que les autres outils disent déjà) et son intégration à l'agence (Agent, Suite Audit lourd) ; (3) **malentendu réel trouvé et corrigé le même soir** : le point 3 du message de l'utilisateur (« l'outil process.circle doit aussi l'avoir en tete, on parle de discipline d'execution ») a d'abord été compris à tort comme visant HYPER-SCAN-CHECKPOINT (sujet du point 2 juste avant) — l'utilisateur a clarifié qu'il visait en réalité la « double communication » (alerte console + rapport), déjà garantie par construction (`recordCircleItemReport()` écrit exactement le texte annoncé à l'oral) pour le contenu des items, ET une vérification plus large que l'agent a bien reçu/compris chaque message et agit en conséquence — ajouté comme commentaire permanent dans circle-process-guardian.mjs PLUS `verifyDoubleCommunication()` (comparaison mécanique réelle dès que les deux textes annoncé/enregistré sont fournis, jamais qu'un rappel sans prise). Question de calibrage posée explicitement sur le travail déjà fait par erreur sur HYPER-SCAN-CHECKPOINT (garder ou retirer) : réponse de l'utilisateur, garder — reste comme extension séparée et valide ; (5) `filterIndexRowsByVersion()` (nouvelle fonction générique) + nouvelle ligne dans `runNetworkCheck()` permettent à SMART-CONSO-TOKEN de piocher des données dans le registre HYPER-SCAN-CHECKPOINT (filtré à ses seules lignes "complète"), réutilisant `findJudgeSpawnsWithoutConsultation()` sans duplication. `check-house.mjs` vert (nouveau bloc de tests verifyHyperScanProcess + verifyDoubleCommunication, tests filterIndexRowsByVersion, comptage runNetworkCheck 12→13 lignes mis à jour), `tsc --noEmit` sans nouvelle erreur (seule l'erreur préexistante non liée de vite.config.ts subsiste). Points 4 (« OK ») et 6-8 du message traités séparément (voir tâches suivantes). | terminée — fidèle |
| 348 | 2026-09-22T03:45Z | Vérification / Conception (utilité des outils dans CLAUDE.md vs la Ronde) | `docs/claude-md-tool-listing-conception.md` (nouveau, fichier préliminaire), `CHANTIER_PRELIMINARY_FILES` (check-tasks-details.mjs) mis à jour, `lib/reference.ts` Version 259 | important | Demande explicite de l'utilisateur : « mettre les outils dans claude.md ne sert à rien la plupart du temps [...] peux tu verifier la pertinence des elements presents dans claude.md [...] est-ce qu'un outil peut t'aider à faire ce travail, regarde ». Trouvaille réelle : `CLAUDE_MD_INCLUDE_EXCLUDE` (CHARTER-SPY, scripts/smart-conso-token.mjs) portait déjà exactement le test recherché (« la retirer ferait-elle faire une erreur à l'agent ? »), jamais appliqué sous cet angle précis auparavant. Verdict : la RÈGLE comportementale (quand consulter quoi) doit rester dans CLAUDE.md, rien d'autre ne force sa relecture à chaque tour ; le MÉCANISME NARRATIF détaillé d'un outil déjà entièrement codé échoue ce test — mesuré en direct : la section Smart Breaker pèse ~2820 tokens estimés (~9,5% du poids total actuel de CLAUDE.md), largement une justification narrative déjà censée vivre ailleurs par l'Article 13 lui-même. Aucune modification de CLAUDE.md exécutée ce soir (strictement une analyse), consignée sur demande explicite (« mets ca sur la to do avec fichier stp ») dans un nouveau fichier préliminaire, enregistré dans `CHANTIER_PRELIMINARY_FILES` pour que `checkChantierFileFreshness()` le couvre désormais. `check-house.mjs` vert (aucun changement de code testable, uniquement la version bump et l'entrée du registre), `tsc --noEmit` sans nouvelle erreur. | terminée — fidèle |
| 349 | 2026-09-22T04:15Z | Nouvel outil / Correctif majeur (HYPER-SCAN-CHECKPOINT modernisé + intégré à CIRCLE-TASKS, circle-process-guardian gagne un double rôle aide/vigilance) | `hyper-scan-checkpoint-light` (nouvel item CIRCLE_ITEMS, circle-tasks.mjs), `mechanicalCircleFindings()` + 9 nouveaux registres suivis (hyper-scan-checkpoint.mjs), `findCircleItemsMapDrift()`/`findStaleItemCountReferences()` (circle-process-guardian.mjs, câblées dans `main()`), `docs/argus/index.md` (fichier orphelin indexé), `lib/reference.ts` Version 260 | important | Trois demandes explicites traitées dans l'ordre : (1) « vois comment tu peux ameliorer hyper-scan [...] le mettre en phase avec le paysage actuel des outils [...] plus efficace, plus performant » — `hyper-scan-checkpoint.mjs` agrège désormais `verifyRondeProcess({})` de circle-process-guardian (filtré à ses 3 signaux mécaniques via `mechanicalCircleFindings()`) et 9 registres jusque-là jamais suivis, créés depuis sa conception le 2026-09-19 (SMART-CONSO-TOKEN, THE-FINAL-JUDGE, THE-DEEP-READER, THE-KING, INES-official, memory-audit, find-booster, objectifs-vs-resultats, CASSANDRA-RH) ; volontairement pas de second appel à `runNetworkCheck()` pour ne jamais relancer `check-house.mjs`/ARGUS/HARMONIA une deuxième fois. Un premier lancement live pendant cette modernisation a trouvé un vrai fichier ARGUS orphelin (`scan-2026-09-21-22-13.txt`) jamais indexé — corrigé le même soir (`docs/argus/index.md`). (2) « vois comment integrer hyper-scan dans circle sinon ca n'a pas trop de sens que circle.process verifie hyper-scan, je veux aussi rentabiliser notre erreur de comprehension » — nouvel item `hyper-scan-checkpoint-light` dans CIRCLE_ITEMS (thème Audit lourd, jamais `costly` car sa couche mécanique est réellement gratuite, mais `periodicityTracked: true` réutilisant `costlyItemDueStatus()` sans le classer à tort comme coût financier ; addable en PRIME, sélectionnable en GOAT, jamais recommandé par défaut) — donne enfin un contexte concret à `verifyHyperScanProcess()`, hors sol jusque-là. (3) « je veux que circle.process t'aide quand tu mets à jour circle [...] 2 niveaux » — `findCircleItemsMapDrift()` (signal confirmé : une entrée orpheline dans NOT_RECOMMENDED_BY_DEFAULT/COSTLY_SUBSTITUTES/CIRCLE_REPORT_FOLDERS pointant un id retiré, ou un item costly/periodicityTracked sans substitut déclaré) et `findStaleItemCountReferences()` (un compte figé qui ne correspond plus au vrai `CIRCLE_ITEMS.length`) câblées dans `main()` — niveau AIDE si consultées avant de committer, niveau VIGILANCE sinon. Preuve vivante trouvée le soir même de leur construction : l'ajout de `hyper-scan-checkpoint-light` avait laissé deux assertions `check-house.mjs` et un commentaire `regles-de-travail.md` bloqués sur l'ancien compte (23 au lieu de 24), attrapé en direct par `findStaleItemCountReferences()` lui-même — corrigé, ainsi que le test de regroupement par thème et la liste PRIME-addable, tous deux affectés par le nouvel item. `check-house.mjs` vert (nouveau bloc de tests pour les deux fonctions, fixtures composées à l'exécution pour ne jamais polluer le vrai scan du fichier), `tsc --noEmit` sans nouvelle erreur (seule l'erreur préexistante non liée de vite.config.ts subsiste). Suite immédiate demandée par l'utilisateur : lancer la Ronde CIRCLE-TASKS. | terminée — fidèle |
| 350 | 2026-09-22T02:50Z | Méthode de travail / Ronde CIRCLE-TASKS | Deuxième Ronde en mode AUTO exécutée de bout en bout (18 items), sous Opus 5 | important | Ronde complète : 18 items AUTO, zéro plantage, 13 écarts réels remontés dont 9 inédits. Séquence stricte de l'Étape 5 respectée telle que l'utilisateur l'a écrite — livraison des 18 rapports txt EN PREMIER, puis lecture, puis analyse, puis récap HTML, puis 8 questions à choix forcé (une action décidée par problème). Tous les rapports écrits via `recordCircleItemReport()` (13 items à dossier) ou dans leur propre registre (les 9 qui s'auto-rapportent : profil, kpi, smart-conso-token-scan, cassandra-rh-signal, coordinateur-catalogue...). Snapshot CLAUDE.md écrite (le fichier avait changé). Modèle d'exécution consigné dans le récap HTML via `executedByModel`. | terminée |
| 351 | 2026-09-22T02:55Z | Outillage de travail / ARGUS | Auto-indexation des scans ARGUS, corrigée à la source (Article 3/24) | important | Écart structurel trouvé par la Ronde : `check-argus.mjs` écrivait bien son rapport mais n'ajoutait JAMAIS la ligne d'index correspondante, alors que TROIS chemins distincts l'appellent (crochet post-commit, `runNetworkCheck()`, HYPER-SCAN-CHECKPOINT) — cinq orphelins rattrapés à la main en deux jours, dont un pendant cette Ronde même. Corrigé à la source plutôt que chez chaque appelant : nouvelle `indexArgusScan()`, idempotente, insère en tête de tableau, ligne volontairement factuelle (décompte mécanique, jamais un jugement, à écraser par un vrai constat si le scan est réellement lu). 7 assertions dédiées dans check-house.mjs, dont les deux chemins d'échec doux (index absent, index sans tableau). | terminée |
| 352 | 2026-09-22T03:00Z | Outillage de travail / CIRCLE-TASKS | Garde-fou sur les fichiers promis par la documentation mais jamais créés (Article 24) | important | Trou réel : `docs/profil-utilisateur/profil-actuel.txt` n'avait JAMAIS été créé, alors que DEUX documents promettaient noir sur blanc qu'il serait réécrit à chaque Ronde (l'`execute` de l'item `profil` et `docs/regles-de-travail.md` §9, demande explicite du 2026-09-21). Découvert par hasard en tentant de l'écraser, jamais par un contrôle — exactement la promesse sans mécanisme que l'Article 24 interdit. Fichier créé, et nouveau `CIRCLE_PROMISED_FILES`/`findPromisedFilesMissing()` câblé dans la sortie de `circle-tasks.mjs`. Liste volontairement courte et curatée (chaque entrée nomme la promesse qu'elle garde), nature manuelle écrite noir sur blanc comme l'Article 24 l'exige. 5 assertions, dont une vérification en direct contre le vrai disque. | terminée |
| 353 | 2026-09-22T03:05Z | Outillage de travail / Tableau de bord | KPI : distinguer « hors de portée ici » de « cassé » | normal | Décision explicite de l'utilisateur pendant la Ronde. Problème : quatre des six familles (performance, qualité de sortie, cohérence logique, rejouabilité) ne peuvent structurellement pas être mesurées sans serveur de jeu avec vrai trafic — jamais le cas pendant une Ronde. La couverture affichait donc 33 % et levait une alerte à CHAQUE passage, pour une raison qui n'est pas un défaut ; une alerte toujours rouge est une alerte qu'on cesse de lire. `dashboardCoverageScore()` accepte désormais des entrées `{famille, score, outOfReach}` et ne note que les familles atteignables, les autres étant nommées à part. Rétrocompatible à dessein (un simple tableau de scores se comporte exactement comme avant). 4 assertions, dont le cas « rien d'atteignable » → 100 % honnête plutôt que 0 % accusateur. | terminée |
| 354 | 2026-09-22T03:10Z | Méthode de travail / Système de suivi | Découpage du fichier de session en trois tranches (109 295 → 26 829 tokens sur la tranche vivante) | important | Trouvaille du scan SMART-CONSO-TOKEN de la Ronde : ce fichier était devenu le document le PLUS LOURD du dépôt (~109 295 tokens, 469 lignes, 233 tokens/ligne contre ~21 pour regles-de-travail.md), sans aucun plafond ni rotation prévus par le système de suivi. Choix explicite de l'utilisateur : découper par tranches, jamais résumer — aucune ligne perdue ni condensée. `tool-brain`/`find-booster` consultés d'abord comme la charte l'exige : verdict honnête « pas utile ici » (2 entrées seulement dans 109k tokens, le fichier étant un seul tableau géant) — le bon diagnostic, ce n'était pas un index qu'il fallait. Partie 1 (#117-200, 36 811 tokens), partie 2 (#201-299, 46 234), tranche vivante (#300+, 26 829). Total vérifié : 84+99+50 = 233 lignes, zéro doublon, `categorizeAllSessions()` relit bien 283 tâches comme avant. Leçon consignée dans la partie 2 : viser le POIDS réel, jamais un nombre de tâches égal (le premier découpage à #201 laissait encore 72 800 tokens côté vivant). | terminée |
| 355 | 2026-09-22T03:15Z | Suivi / Carnet de correctifs | Écart doc/code corrigé : un correctif déjà appliqué était encore noté « jamais corrigé » | normal | L'analyse de la Ronde a d'abord conclu, sur la foi du carnet, que la pensée d'attirance identique mot pour mot entre Lia et Noé n'était pas corrigée — l'utilisateur a validé de la corriger ce soir. Relecture du code AVANT de le toucher (Article 19) : `groundPrivateThought()` porte déjà deux pools `LIA_OPTIONS`/`NOE_OPTIONS` réellement distincts, le genre grammatical correct dans chaque pool, et un décalage `+actor` pour que les deux ne tombent jamais sur le même index — correctif appliqué à la tâche #187. C'est le CARNET qui était périmé, jamais le code. Ligne corrigée (elle reste à 0/2 : aucune simulation ne l'a encore observée). Exactement l'écart doc/code que l'Article 13 interdit, et la démonstration que l'ordre « comprendre d'abord, coder ensuite » évite une correction inutile sur du code déjà sain. | terminée |
| 356 | 2026-09-22T03:30Z | Outillage de travail / Simulation Article 18 | Le pilote de simulation intégrale n'avait JAMAIS été committé — construit et versionné | important | Trou structurel trouvé en voulant lancer la simulation demandée : l'étape 1 du protocole dit « lancer LE script de simulation intégrale », mais aucun script de ce nom n'existait dans le dépôt — il était réécrit à la volée dans un dossier temporaire à chaque fois, puis perdu avec la session. Dix-sept simulations archivées, zéro pilote conservé : exactement ce que l'Article 24 interdit, et la raison pour laquelle deux simulations successives n'ont jamais été strictement comparables (le scénario de phase 2 était retapé de mémoire). `scripts/run-simulation.mjs` créé et committé : reset, phase 1 autonome jusqu'à la révélation (throttle serveur de 20 s RESPECTÉ, jamais contourné — un rythme artificiel donnerait un transcript non représentatif), phase 2 en 20 messages scénarisés FIXES couvrant chaque palier de la charte (ordre autoritaire, mépris, menace, insultes répétées, humour noir, désescalade, bienveillance soutenue, négociation, discorde Lia/Noé), 3 tirages de bonus répartis. Deux bugs corrigés au smoke test : l'epoch doit se lire sur /api/world avant le reset (le 409 ne le renvoie pas), et le 423 final de mark_dossier_seen est une réponse légitime, jamais à réessayer. | terminée |
| 357 | 2026-09-22T03:45Z | Outillage de travail / check-tasks-details | Rapport de Ronde en quatre parties (chiffres vérifiés, état du projet, œil critique, trois zooms) | important | Demande explicite de l'utilisateur, calibrée par quatre questions avant tout code (parties retenues, zooms, format, questions forcées). Construit : `suiviFigures()` (chiffres recalculés, incohérences signalées plutôt que lissées), `projectStanding()` (état par chantier, réutilise CHANTIER_PRELIMINARY_FILES plutôt qu'une seconde liste), `criticalEye()` (uniquement des constats chiffrés), `buildRondeTextReport()` (les quatre parties, les trois zooms dans un seul fichier), la sous-commande `ronde` (txt archivé + HTML de présentation, mêmes données), et l'item CIRCLE_ITEMS `check-tasks-report`. SIX bugs trouvés : quatre en lançant pour de vrai (statusKey et non categorie, thème via splitSujet, plancher de numérotation à #117 et non 1 — 116 faux trous —, âge négatif affiché « il y a -1 j »), deux par les tests eux-mêmes (un constat sans chiffre, corrigé dans le constat jamais en affaiblissant le test ; un projet vide signalant « le numéro 0 manque »). Contrainte d'interface attrapée par le test : thème « Suivi des chantiers » et non « Suivi & référentiels », déjà à la limite de 4 options par question. Première trouvaille réelle : 4 tâches CASSANDRA-RH immobiles depuis 20 rapports, et 93 % des tâches tracées portant sur l'outillage contre 7 % sur le jeu. | terminée |
| 358 | 2026-09-22T03:50Z | Outillage de travail / circle-process-guardian | CIRCLE_ITEMS_CHANGELOG : le gardien devient aussi l'historien de la Ronde | normal | Demande explicite de l'utilisateur dans le même message que #357 : « que process.circle soit là pour consigner ce changement ». Le gardien ne faisait que VÉRIFIER la discipline d'exécution ; il consigne désormais l'histoire de CIRCLE_ITEMS elle-même (quel item, quand, pourquoi). Placé chez lui plutôt que dans circle-tasks.mjs parce qu'il est déjà celui qui compare CIRCLE_ITEMS à ses tables satellites — circle-tasks.mjs reste la définition, jamais son propre historien. Nature manuelle assumée et écrite noir sur blanc (le « pourquoi » n'est déductible d'aucun diff), assortie du garde-fou mécanique que l'Article 24 exige : `findItemsMissingFromChangelog()`, avec `KNOWN_ITEMS_BEFORE_CHANGELOG` figé sur les 24 items antérieurs — une liste qui ne grandit jamais, aucun faux historique reconstruit après coup. | terminée |
| 359 | 2026-09-22T04:10Z | Nouvel outil / ecotoken-claude.md | Construire l'outil de réduction du coût permanent de CLAUDE.md, membre complet avec blueprint | important | Demande explicite de l'utilisateur, calibrée par quatre questions (niveau d'action, première cible, fermeté du garde-fou, statut) puis élargie en cours de route par trois messages : l'outil doit agir « dans tout contexte, pas seulement sur le probleme des blueprints », être branché à SMART-CONSO-TOKEN, et produire un rapport dans la Ronde. Nom corrigé par l'utilisateur de « claude.md-ecotoken » à « ecotoken-claude.md » pour éviter la confusion avec CLAUDE.md. CINQ stratégies génériques détectées mécaniquement (catalogue, extraction, apartés, doublons, déjà-mécanisé), jamais une liste de sections codée en dur (Article 24). Mesure centrale nouvelle : le RENDEMENT (citations réelles ÷ lignes) — l'Article 3 vaut 40,7, l'Article 19 tombe à 0,7. Plan mesuré : ~12 600 tokens récupérables, 41 % de la charte. Trois bugs trouvés par ses propres tests : une proposition à gain NÉGATIF (l'en-tête du catalogue coûtait plus cher que les sections remplacées), un double comptage de ~2 200 tokens inencaissables, et un faux positif où une liste à puces comptait pour un paragraphe géant. | terminée |
| 360 | 2026-09-22T04:15Z | Nouvel outil / ecotoken-claude.md | Stratégie « déjà mécanisé » : les règles qu'un crochet applique déjà tout seul | important | Angle ouvert par une question de l'utilisateur : « les scripts qui sont presents dans claude.md, qui ne s'executent une fois à l'ouverture, comme des coups d'eppee dans l'eau ». VÉRIFICATION DE LA COMPRÉHENSION (demandée explicitement) : CLAUDE.md ne contient AUCUN script exécutable — un seul bloc de code sur tout le fichier (un schéma) et quatre commandes citées en exemple, rien ne s'exécute à l'ouverture. Mais l'intuition vise juste sous un autre nom : une RÈGLE relue à chaque message alors qu'un crochet l'applique déjà sans l'agent. `findAutomatedTools()` lit les crochets eux-mêmes (16 outils réellement automatisés, jamais une liste recopiée) et `findAlreadyMechanised()` croise avec les passages de la charte contenant un verbe de déclenchement manuel. 7 passages, ~2 510 tokens (3 après déduplication avec les autres stratégies). Précision écrite noir sur blanc dans le code : « déjà mécanisé » ne veut JAMAIS dire « inutile » — la règle reste vraie, seule sa procédure détaillée devient superflue. | terminée |
| 361 | 2026-09-22T04:20Z | Outillage de travail / CIRCLE-TASKS | Harmonisation du sujet CLAUDE.md dans la Ronde : un seul item, jamais deux | normal | Demande explicite : « je crois qu'il y a deja un rapport sur claude.md dans circle. vois comment tu peux tout harmoniser sur ce sujet. » Il y en avait bien un (`claude-md-weight-signal`). Plutôt qu'un 26e item disant la même chose en mieux, CET item lance désormais ecotoken-claude.md — il n'a rien perdu (il relit toujours SMART-CONSO-TOKEN) et gagne le rendement, le plan chiffré, le budget et la mémoire des passages. Registre `docs/ecotoken-claude-md/` déclaré dans CIRCLE_EXCLUDED_REGISTRIES avec sa raison. Modification consignée dans CIRCLE_ITEMS_CHANGELOG, dont le garde-fou a été étendu pour distinguer un ajout d'une modification. | terminée |
| 362 | 2026-09-22T04:40Z | Outillage de travail / Gardiens sacrés | Réveil conditionnel des 6 Gardiens : ils ne tournent que si le commit a touché ce qu'ils surveillent | important | Question de l'utilisateur, posée en deux temps et avec une exigence explicite de vérifier sa compréhension avant réponse : « et le fait que les scripts sont dans les crochets, ca n'est pas un coup d'epee dans l'eau non plus ? [...] je veux que la suite des gardiens s'executent sur le code pour le verifier. ou quand tu codes, toi. mais pas comme ca en plein milieu de rien. » DISTINCTION ACTÉE, qui gouverne toute la conception : ce qui GARANTIT le code = check-house.mjs + tsc, dans le pre-commit, BLOQUANTS, jamais conditionnés ; ce qui RENFORCE = les 6 Gardiens, dans le post-commit, qui n'ont jamais rien bloqué (tous en `|| true`). Seul le second étage est conditionné — conditionner le premier créerait un trou de surveillance. MESURE RÉELLE qui a motivé le changement : sur les 20 derniers commits, 2 ne touchaient aucun fichier de code, et les 18 autres ne touchaient `lib/` QUE par `lib/reference.ts` — un fichier de DONNÉES (le référentiel affiché en jeu, incrémenté à chaque commit) qui faisait passer tout commit pour un changement de moteur. C'est la cause racine : `NOT_REALLY_CODE` l'exclut. `GARDIEN_DOMAINS` déclare ce que chaque Gardien surveille vraiment, lu dans leur code (Article 19). Deux chemins de prudence : un Gardien inconnu tourne toujours, et si git ne dit pas ce qui a changé, tout tourne — ne pas savoir n'autorise jamais à se taire. | terminée |
| 363 | 2026-09-22T04:45Z | Méthode de travail / Honnêteté d'analyse | Correction d'une généralisation abusive de ma part sur les Gardiens | normal | J'avais annoncé « 20 coups d'épée dans l'eau » pour les six Gardiens. Vérification faite avant de coder : c'est vrai pour ARGUS seul (il ne regarde que `lib/life.ts` et les marqueurs TODO, et `life.ts` n'a pas bougé), mais FAUX pour AXA-CHECK (qui couvre `scripts/` via AGENT_SCRIPT_FILES/collectScriptCoverage) et CLONE-HUNTER (dont les racines sont lib/scripts/app/components) — or `scripts/` a changé à presque chaque commit, donc ces deux-là travaillaient réellement. J'avais généralisé à partir d'un seul cas. Correction annoncée à l'utilisateur avant toute autre réponse, et épinglée par une assertion dédiée dans check-house.mjs pour qu'elle ne se reperde pas. Second écart du même tour, corrigé aussi : mes étiquettes de gardiens dans le crochet étaient décalées d'un bloc (ARGUS et HARMONIA partagent le même try, ce que je n'avais pas vu), ce qui aurait conditionné le mauvais gardien à chaque fois. | terminée |
| 364 | 2026-09-22T05:10Z | Outillage de travail / ecotoken | Badge Membre certifié : 3 écarts fermés | normal | Ligne dans la table maîtresse `docs/regles-de-travail.md` §7ter, bullet `docs/referentiel/ecotoken.md` dans la section « Référentiel technique » de CLAUDE.md, et déclaration dans CLAUDE.md. Le 3e écart demandait d'ajouter une 23e section « blueprint exportable » — exactement la famille qu'ecotoken propose de condenser. Résolu dans le sens cohérent : les 22 sections sont devenues un tableau, ecotoken y a sa ligne, et `checkAgentOnboarding()` accepte désormais les DEUX formes (section dédiée OU ligne de catalogue citant un `docs/*.md`) — sans quoi les 22 outils auraient perdu leur badge d'un coup. Vérifié : aucune régression sur les 20 outils testés. Badge obtenu. | terminée |
| 365 | 2026-09-22T05:25Z | Outillage de travail / ecotoken | 4 portées de scan, vocabulaire importé jamais réinventé | normal | `global` (tout le paysage surveillé) / `partiel` (un dossier réel) / `zoome` (un fichier) / `focus` (une section). `SCOPE_LEVELS` est la MÊME liaison que celle de smart-conso-token.mjs, reprise de THE-FINAL-JUDGE — un test compare les deux par identité. `recommendScope()` choisit la portée selon ce qui a réellement changé. CLI `scan <portée> [cible]`. Mesures réelles : CLAUDE.md 15 807 tk, docs/referentiel/ 18 644 tk sur 37 documents. | terminée |
| 366 | 2026-09-22T05:35Z | Outillage de travail / ecotoken | Déclenchement : déclaré vs réellement câblé | normal | `TRIGGERS` déclare le contrat, `auditTriggers()` lit les vrais fichiers et vérifie chaque câblage — un commentaire promettant « branché dans le crochet » n'est jamais une preuve (Article 24). 4 déclencheurs : post-commit conditionnel, Ronde CIRCLE-TASKS, passage réseau LE-COORDINATEUR, sur demande. Preuve d'utilité : à son premier lancement il a trouvé le 4e non câblé (ecotoken absent de la table maîtresse), corrigé dans la foulée. 4/4 aujourd'hui. | terminée |
| 367 | 2026-09-22T05:50Z | Outillage de travail / ecotoken | Quatre défauts du premier catalogue, corrigés à la racine | normal | Le tout premier tableau produit sur CLAUDE.md avait : memory-audit absent (son suffixe continuait après une virgule), find-booster pointant vers la mauvaise fiche (le premier `docs/referentiel/` cité, pas la sienne), l'Outil de résilience API affiché « — » alors que son blueprint existe sans le mot « blueprint » dans son nom, et des cellules coupées en plein mot. Corrigés dans le générateur (`findHeadingFamilies`, `meilleurChemin`/`affiniteChemin`, `couperProprement`, `roleUtile`), jamais retouchés à la main dans la sortie. Gain passé de 5 521 à 6 109 tk. | terminée |
| 368 | 2026-09-22T06:05Z | Outillage de travail / ecotoken | Sûreté structurelle : l'outil ne peut pas abîmer ce qu'il analyse | sensible | `assertSafeWriteTarget()` refuse toute écriture hors de `docs/ecotoken/`, et un test vérifie les DEUX moitiés : que la fonction refuse CLAUDE.md, et qu'aucun `writeFileSync` du fichier ne la contourne. La garantie devient une propriété de structure au lieu d'une promesse de bonne conduite. Limite dite franchement et jamais masquée : ces contrôles vérifient qu'une règle est encore ÉCRITE, jamais qu'elle a le même SENS. | terminée |
| 369 | 2026-09-22T06:20Z | Outillage de travail / ecotoken | Criticité : 6 niveaux mesurés, comportement adapté | sensible | Deux niveaux catégoriels (`maitre`, `tuyauterie`) atteints par un seul fait, jamais par cumul, puis critique/sensible/ordinaire/périphérique par score. La TUYAUTERIE était le vrai trou : un crochet git n'a presque aucun « jamais/toujours » et aurait été classé périphérique, alors que le casser casse tout. `detectMasterFile()` reconnaît le fichier maître à ce qui le REND maître, jamais à son nom — condition pour que le blueprint fonctionne ailleurs ; testé dans les 3 cas (déclaré, déduit, aucun). La criticité FILTRE réellement : sur CLAUDE.md, 2 propositions sur 4 sortent des applicables, listées à part plutôt que masquées. | terminée |
| 370 | 2026-09-22T06:35Z | Outillage de travail / ecotoken | Contrôle de perte, né d'une perte réelle | sensible | Le premier catalogue appliqué à CLAUDE.md a fait disparaître six outils entiers (LE-COORDINATEUR, CIRCLE-TASKS, doc-HTML, tool-usage, Doc-Report, find-deep-booster) et l'obligation d'usage de tool-brain — des paragraphes de RÈGLE logés en fin de sections qui n'étaient que des renvois. Vu seulement en comparant les chemins cités à la main. D'où `findLostReferences()` (avec pertes déclarées attendues, sinon le garde-fou crierait au loup à chaque réduction réussie) et `verifyProtectiveSubstance()` (Articles par intitulé exact, phrases socles, phrases normatives triées). Contenu perdu restauré à la main dans un bloc dédié. | terminée |
| 371 | 2026-09-22T06:50Z | Outillage de travail / Harmonie des échelles | Ponts entre les 4 échelles de criticité du projet | sensible | Constat d'abord : CHECK-LEVEL-TARGET (effort), SENSITIVE_NODES (nœud du moteur), CHARTER-SPY (gravité d'une règle) et ecotoken (criticité d'un fichier) ne mesurent pas le même objet — les fondre aurait détruit de l'information. Trois ponts réels à la place : criticité → niveau de vérification minimal (vocabulaire de CHECK-LEVEL-TARGET, jamais un 5e inventé) ; nœud sensible LU au lieu d'être rejugé ; socles DÉRIVÉS des 6 Articles que CHARTER-SPY classe déjà non négociables. Plus `findCriticalityDisagreements()`, qui a trouvé 4 désaccords réels dès son premier lancement (lib/simulation.ts, story.ts, turn.ts, daynight.ts) : HARMONIA avait raison, ecotoken était aveugle aux nœuds du moteur. | terminée |
| 372 | 2026-09-22T07:00Z | Outillage de travail / ecotoken | Bug réel : clés absolues dans loadRepoFiles | sensible | Les clés étaient des chemins ABSOLUS : toute recherche par clé rendait silencieusement « rien trouvé » — un faux négatif parfait dans un outil de mesure, qui faisait passer `scripts/check-house.mjs` pour « sensible » au lieu de tuyauterie. Les crochets git, sans extension, étaient de plus exclus par le filtre — précisément la tuyauterie à reconnaître. Corrigé, pinné par test. | terminée |
| 373 | 2026-09-22T07:10Z | Outillage de travail / CIRCLE-TASKS | Renommage claude-md-weight-signal → ecotoken-scan | normal | Le nom ne portait plus le périmètre réel (ecotoken surveille 3 documents, pas la seule charte). Plutôt que de réécrire KNOWN_ITEMS_BEFORE_CHANGELOG — c'est-à-dire falsifier la liste figée de ce qui existait avant le registre — un troisième type de changement a été ajouté au garde-fou : le RENOMMAGE, accepté seulement s'il nomme un ancien id réellement connu. Un id inventé se déclarant renommé reste signalé (vérifié par test). Registres consolidés : `docs/claude-md-weight/` → `docs/ecotoken/ronde/`, un seul sujet un seul registre. | terminée |
| 374 | 2026-09-22T07:25Z | Outillage de travail / ecotoken | Fiabilisation de la détection : 3 défauts trouvés en auditant | sensible | (1) Une MENTION n'est pas un LANCEMENT : le crochet pre-commit parle de post-commit et de kpi-report.mjs dans ses commentaires, les deux étaient annoncés « lancés par le crochet bloquant ». Seules les lignes exécutables comptent désormais. (2) Tout ce qui vit dans scripts/hooks/ n'est pas un crochet — git n'exécute que les fichiers sans extension ; install.mjs reste de la tuyauterie mais avec la bonne raison. (3) Les candidats au titre de fichier maître étaient cherchés via loadRepoFiles(), qui ne descend pas à la racine : la déduction ne marchait que grâce à la liste de noms connus, et un projet avec une charte nommée RULES.md n'aurait rien trouvé. Vérifié sur un faux projet créé pour l'occasion : RULES.md détecté, README.md volumineux écarté. Plus deux durcissements : un profil périmé n'est plus cru aveuglément, et l'ambiguïté entre deux candidats est dite au lieu d'être tranchée en silence. | terminée |
| 375 | 2026-09-22T07:45Z | Outillage de travail / ecotoken | Passe ARGUS sur l'outil lui-même : 6 trous de logique trouvés et corrigés | sensible | Honnêteté d'abord : la couche mécanique d'ARGUS ne regarde QUE lib/life.ts et les marqueurs TODO — elle n'a jamais examiné ecotoken, donc « ARGUS n'a rien trouvé » aurait été trompeur. Vraie passe faite à la main. (1) Performance : chaque document analysé relisait tout le dépôt pour sa criticité — 37 relectures sur un scan de docs/referentiel/, 1 320 ms ; le dépôt est lu une fois, 365 ms (3,6×). (2) Article 24 : un document profilé sans budget pouvait grossir sans le moindre avertissement — garde-fou findProfiledDocumentsWithoutBudget(), qui trouve parametres.md. (3) L'ironie : verifyProtectiveSubstance() était déclarée OBLIGATOIRE sur le fichier maître mais rien ne la rendait exécutable (il fallait avoir gardé une copie) — une obligation écrite sans mécanisme, exactement ce que l'outil reproche aux autres ; une copie de référence est désormais déposée à chaque scan. (4) Un focus sur une section inexistante rendait « 0 gain », indiscernable d'une vraie section sans gain — il le dit maintenant. (5) ecotokenShouldRun et recommendScope répondaient à la même question avec deux implémentations parallèles (Article 3) — une seule source. (6) La copie de référence aurait dupliqué la charte entière dans le dépôt à chaque changement — journal local, déclaré dans .gitignore. | terminée |
| 376 | 2026-09-22T07:55Z | Outillage de travail / Gardiens | Passe des Gardiens sur ecotoken.mjs | normal | CLONE-HUNTER : zéro duplication. CLEAN-DIRTY-OLD : rien (fichier neuf). AXA-CHECK : 55/67 fonctions couvertes (82 %) et 12 jamais exécutées par un test, dont articleYield (le RENDEMENT, mesure phare de l'outil) et ecotokenShouldRun (le réveil conditionnel). Tests ajoutés pour les 8 réellement testables ; restent hors test celles qui ne peuvent pas l'être honnêtement (main, lecture de git, lecteur injecté par défaut). Une erreur de ma part au passage, du même type que d'habitude : j'ai écrit le test contre un champ `citations` qui n'existe pas — le vrai nom est `referencesCroisees` ; c'est le test qui avait tort, pas l'outil. | terminée |

## Notes

- Les tâches terminées avant la création de ce système (chantier 2 du tableau de bord, full_sim10,
  règles de suivi première version, etc.) ne sont pas reconstruites ici — portée rétroactive
  tranchée explicitement avec l'utilisateur (cf. `docs/systeme-de-suivi.md`). Elles restent
  consultables dans l'historique de conversation et dans les documents qu'elles ont produits
  (`docs/referentiel/tableau-de-bord.md`, `docs/referentiel/kpi-index.md`, etc.).
- Les horodatages approximatifs (`~17h`, `~18h`, etc.) reflètent l'absence d'horodatage exact
  enregistré au moment de la clôture initiale de ces tâches, avant que ce fichier ne soit remis à
  jour le 2026-09-19T20:35Z — jamais reconstruits avec une fausse précision.
