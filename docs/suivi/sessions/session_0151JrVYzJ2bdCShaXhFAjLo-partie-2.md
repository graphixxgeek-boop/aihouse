# Session session_0151JrVYzJ2bdCShaXhFAjLo — partie 2 (tâches #201 à #299)

*(Deuxième tranche, découpée le 2026-09-22 dans le même mouvement que la partie 1 — cf. son
préambule pour la raison complète. Un premier découpage à #201 s'était révélé déséquilibré (la
tranche vivante pesait encore ~72 800 tokens contre ~36 800 pour la partie 1) : les tâches récentes
portent des descriptions bien plus longues que les anciennes, donc un découpage à numéros égaux ne
donne pas des tranches de poids égal. Constat à retenir pour le prochain découpage : viser le POIDS
réel, jamais un nombre de tâches. Aucune ligne perdue ni condensée ici non plus.)*

## Tâches

| N° | Horodatage | Sujet | Sous-sujet | Sensibilité | Description | Statut |
|---|---|---|---|---|---|---|
| 201 | 2026-09-20T18:20Z | Charte / Outillage de travail | checkAgentOnboarding() construit — la "séance d'accueil" mécanique de LE-COORDINATEUR | important | Réponses de calibrage : script automatique (recommandé), déclenchement sur demande explicite uniquement (recommandé, jamais dans le crochet post-commit). Implémenté dans `le-coordinateur.mjs` : `slugifyAgentName()` + `checkAgentOnboarding(nomAgent, options)` — vérifie table maîtresse, menu PRESTATIONS, instanciation, registre (`docs/<slug>/` par défaut, `registryPathPrefix` pour une déviation réelle déclarée), blueprint (sauf `cousinOf` déclaré). Précision honnête trouvée en calibrant : la définition du statut Agent (juste écrite) ne collait pas à THE-DEEP-READER (pas de blueprint, par construction) — corrigée dans `docs/regles-de-travail.md` : le minimum réel est instanciation+registre, le blueprint est la norme mais pas obligatoire si `cousinOf` déclaré. Calibré en vrai : check-tasks-details → complet ; THE-DEEP-READER → complet une fois ses deux déviations réelles déclarées (cousinOf THE-FINAL-JUDGE, registryPathPrefix docs/suivi/relectures-lourdes/). 131/131 tests, `lib/reference.ts` Version 156 | terminée — fidèle |
| 202 | 2026-09-20T18:30Z | Charte / Outillage de travail | Règle d'intégration à deux volets documentée (CASSANDRA-RH + checkAgentOnboarding) + précision honnête sur la détection de doublons de fonctions | important | Demande explicite de l'utilisateur : « je veux que lorsqu'un nouvel employé est identifié, cassandra le consigne et le coordinateur fait son accueil opérationnel [...] tu me suis dans l'image ? ». Confirmé et documenté dans `docs/regles-de-travail.md` : un nouvel Agent n'est intégré que lorsque CASSANDRA-RH l'a consigné (volet explicitement marqué "en attente", CASSANDRA-RH pas construite) ET que `checkAgentOnboarding()` rapporte complet. Jamais un troisième script d'orchestration (anti-duplication). Deuxième point du même message : « le coordinateur est aussi capable d'identifier des doublons de fonctions [...] mais normalement ce point est déjà vérifié en amont » — précision honnête apportée : ce n'est PAS mécaniquement vérifié aujourd'hui (ni ARGUS, ni HARMONIA, ni CLEAN-DIRTY-OLD ne couvrent la duplication de fonctions), seule la discipline manuelle de l'Article 19 protège contre ça actuellement — ne jamais laisser croire à une couverture mécanique qui n'existe pas | terminée — fidèle |
| 203 | 2026-09-20T18:35Z | Conception / Nouvel outil (CASSANDRA-RH) | Précision : la "fiche" de vocation d'un Agent, c'est instanciation + blueprint (si présent), jamais le blueprint seul | normal | Demande explicite de l'utilisateur : « pour chaque agent [...] il y a une fiche qui répertorie sa vocation, ses fonctionnalités [...] c'est le blueprint c'est ça ? ». Correction honnête : le blueprint décrit le principe générique, l'instanciation les fonctionnalités concrètes de ce projet — certains Agents (THE-DEEP-READER) n'ont pas de blueprint du tout, donc s'en tenir au seul blueprint laisserait CASSANDRA-RH sans rien à dire sur eux. Note ajoutée au round de calibrage de CASSANDRA-RH (tâche #134, pas encore construite) : elle doit toujours lire l'instanciation (toujours présente) et le blueprint s'il existe, jamais l'un sans l'autre | terminée — fidèle |
| 204 | 2026-09-20T18:50Z | Charte / Outillage de travail | checkAgentOnboarding() enrichi (CLAUDE.md/suivi/rappels) + disambiguïsation LE-COORDINATEUR/LE-PLANIFICATEUR | important | Reprise après compactage de contexte. Demande explicite : « fiabilise ce process, enrichis-le [...] en quoi ce process garantit un vrai bénéfice ? » — `checkAgentOnboarding()` étendu (paramètres `claudeMdText`/`suiviText`, déjà en place ; champ `rappels` non bloquant toujours renvoyé) ; section "en quoi ce process garantit un vrai bénéfice, pas une formalité" ajoutée à `docs/regles-de-travail.md`, avec la limite honnête assumée (vérifie la PRÉSENCE des raccordements, jamais leur QUALITÉ). Deuxième demande explicite dans le même message : « il n'y a pas de confusion entre le coordinateur et le planificateur ? [...] mets une règle pour vérifier ». Vérification faite (`grep -rn "LE-PLANIFICATEUR" CLAUDE.md docs/ scripts/`) : aucune confusion réelle trouvée dans la documentation existante. Règle de disambiguïsation ajoutée à `docs/regles-de-travail.md` (définitions claires, mnémotechnique, commande de re-vérification) | terminée — fidèle |
| 205 | 2026-09-20T19:00Z | Conception / Nouvel outil (badge) | Champ badge 🎖️ ajouté à checkAgentOnboarding() + colonne dans la table maîtresse | important | Prompt intempestif (règle #186 appliquée). Demande explicite : « la remise de son badge [...] c'est cassandra qui supervise ces opérations, le coordinateur vérifie que tous les membres de l'équipe ont bien leur badge ». Trois questions de calibrage posées (emplacement, nature recalculée vs persistée, qui délivre en attendant CASSANDRA-RH) — les trois réponses "recommandé" choisies : colonne dans le tableau existant, recalculé à chaque appel (jamais persisté), LE-COORDINATEUR délivre de fait dès aujourd'hui. Implémenté : `checkAgentOnboarding()` retourne désormais `badge` ("🎖️ Membre certifié"/"⚠️ Pas encore certifié", pur résumé lisible de `complet`, jamais un fait stocké) ; colonne "🎖️ Badge" ajoutée à la table maîtresse de `docs/regles-de-travail.md`, remplie en relançant réellement le check contre les 14 Agents du projet — ce qui a immédiatement trouvé 3 écarts réels, jamais fabriqués : (1) faux positif PRESTATIONS pour Smart Conso API/CHECK-LEVEL-TARGET (outils de régulation interne, jamais "menu-worthy") — corrigé en réutilisant l'exemption `isMenuWorthy()` déjà en place pour `findToolsMissingFromMenu()` ; (2) `docs/the-screener/index.md` absent (le dossier existait, créé lazily par le mécanisme de capture, mais son registre n'avait jamais été créé) — corrigé ; (3) SMART-CONSO-TOKEN sans sa propre section "## ... — blueprint exportable" dans CLAUDE.md (texte existant en gras dans l'Article 22, jamais un vrai titre H2) — corrigée par une nouvelle section dédiée juste après celle de Smart Conso API. Frontière de portée précisée à la demande de l'utilisateur : le badge ne concerne QUE les lignes Statut=Agent (outillage meta), jamais Lia/Noé (produit du jeu) | terminée — fidèle |
| 206 | 2026-09-20T19:15Z | Conception / Nouvel outil (badge) | Alerte badge câblée dans suggestPrestationsForTask() (badgeWarnings) | important | Prompt intempestif (règle #186 appliquée). Demande explicite : « si un membre de l'équipe est sollicité alors qu'il n'a pas de badge, une alerte doit nous être remontée ». Question de calibrage posée sur la portée (aucun passage obligé unique n'existe aujourd'hui pour tous les appels) — réponse "recommandé" choisie : alerte au seul point de passage déjà construit (`suggestPrestationsForTask()`), jamais un nouveau dispatcher central. Implémenté : `badgeWarningsForOutils()` (nouvelle fonction privée) + champ `badgeWarnings` ajouté au retour de `suggestPrestationsForTask()` (optionnel, rétrocompatible — vide par défaut sans `onboardingContext`) ; `parseToolsTable()` enrichi pour lire la colonne "Statut" par nom d'en-tête (nécessaire pour ne vérifier le badge que sur une ligne Statut=Agent, jamais un Utilitaire nommé/Infrastructure). `lib/reference.ts` Version 157, `check-house.mjs` vert (aucune régression, nouveaux blocs pour parseToolsTable().statut, le badge, l'exemption PRESTATIONS et badgeWarnings), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |
| 207 | 2026-09-20T19:40Z | Charte / Outillage de travail | Ronde CIRCLE-TASKS sur mesure (21 commits sans passage) | normal | Ronde calibrée à la main (fonction de sélection "recommandé" toujours en file, tâche #155) plutôt que le menu complet : 4 items ciblés à partir des signaux de fraîcheur réels (les autres — KPI, scans Smart Conso API/SMART-CONSO-TOKEN, CLEAN-DIRTY-OLD, tâche ouverte la plus ancienne — étaient déjà frais). (1) Profil utilisateur : le signal "undefined jour(s)" investigué et confirmé NON BOGUÉ — c'est le garde-fou anti-fabrication déjà voulu (`daysSince()` refuse un compte négatif), déclenché par un simple décalage d'horloge entre la dernière fiche (14:20Z) et l'horloge du conteneur (13:57Z) ; nouvelle fiche réelle ajoutée (`docs/profil-utilisateur/observations/2026-09-20-1930.md`) couvrant le segment badge/intégration. (2) Zone ALWAYS-NEW-CODE négligée signalée ("Fatigue") — le vrai zoom profond reste hors scope de cette ronde légère (raisonnement à part, Article 23). (3) Câblage HTML des rapports : gap confirmé (el-professor.mjs, the-final-judge.mjs, the-screener-capture.mjs ne citent jamais html-report.mjs) — laissé à la tâche #144 déjà ouverte plutôt qu'une implémentation précipitée de trois rapports sous pression de temps. (4) Poids CLAUDE.md : 27314 tokens (niveau "élevé"), hausse cohérente avec l'ajout légitime de la section SMART-CONSO-TOKEN manquante (#206) — pas une dérive, la passe d'allègement (tâche #181) reste en pause pour une reprise dédiée | terminée — fidèle |
| 208 | 2026-09-20T20:00Z | Conception / Nouvel outil (badge) | Câblage réel du badge dans check-tasks-details.mjs + 2 bugs réels trouvés et corrigés | important | Question directe de l'utilisateur : « est-ce que le check de badge pour les agents est fait systématiquement à chaque de leur utilisation ? [...] estce que ce systeme ne nuit pas à la performance ? à la conso ? » — investigation révèle un ANGLE MORT RÉEL : `check-tasks-details.mjs` était l'UNIQUE appelant réel de `suggestPrestationsForTask()` en production (grep confirmé), et ne passait jamais `onboardingContext` — le badge n'était donc jamais vérifié nulle part en pratique, malgré son chokepoint déjà construit lors de la session précédente (#206). Corrigé : `suggestToolsForOpenTasks()` et `buildReport()` acceptent désormais `onboardingContext` (rétrocompatible, `null` par défaut) ; nouvelle fonction `buildRealOnboardingContext()` construit ce contexte à partir de fichiers déjà sur disque (CLAUDE.md, docs/regles-de-travail.md, arborescence docs/, sessions docs/suivi/) — coût réel nul (aucun appel API, ce script entier est du Node.js pur) ; `main()` la construit et la passe. **Deuxième bug réel trouvé en testant** : `buildRealOnboardingContext()` produisait des chemins tronqués ("ocs/check-tasks-details" au lieu de "docs/...") à cause d'un `slice(root.length + 1)` qui ne tenait pas compte du séparateur déjà présent dans `ROOT` — corrigé, plus `agentOverrides` ajouté pour la déviation connue de THE-DEEP-READER (sans quoi il ressortait à tort "sans badge"). Vérifié en conditions réelles : le rapport signale désormais correctement THE-DEEP-READER (vraiment non câblé) et plus aucun faux positif sur check-tasks-details lui-même. Réponses aux autres questions de l'utilisateur : le badge n'alourdit jamais le code du jeu (vit entièrement dans `scripts/`, jamais `lib/lia.ts`/`route.ts`) ; zéro appel API, coût CPU négligeable (recherches texte sur des fichiers déjà en mémoire) | terminée — fidèle |
| 209 | 2026-09-20T20:10Z | Méthode de travail / Suivi | Manquement réel reconnu : la fenêtre de calibrage de check-tasks-details n'avait jamais été déclenchée pour une précédente demande d'état des lieux | important | Signalé directement par l'utilisateur : « je t'ai demandé un état des lieux des tâches en cours et ça n'a pas déclenché la fenêtre de questions [...] comme prévu par un agent ? ». Confirmé : la demande antérieure « fais-moi le point sur les tâches en cours » avait été traitée en récupérant les données en interne (`check-tasks-details.mjs`/`check-suivi-fidelity.mjs`) sans jamais poser la fenêtre de calibrage dédiée (zoom × forme) ni livrer le rapport — un vrai écart au protocole propre de cet Agent. Corrigé immédiatement (« ensuite reprends les tâches en cours ») : fenêtre posée (zoom "en cours" / forme "arborescence détaillée", tous deux recommandés choisis), rapport généré et livré en fichier joint | terminée — fidèle |
| 210 | 2026-09-20T20:15Z | Conception / Nouvel outil (CLAUDE.MD.SPY) | CLAUDE.MD.SPY finalisé (reste-à-faire de la tâche #199) | important | Reprise explicite ("enchaîne sur toutes les tâches en cours sans t'arrêter"). Généré `docs/referentiel/claude-md-regles.md` (24 lignes, table réelle contre le vrai CLAUDE.md, 0 redondance forte à seuil strict) via `buildClaudeMdRuleTable()`/`renderClaudeMdRuleTable()` ; nouvelle section dédiée ajoutée à `docs/referentiel/smart-conso-token.md` (commande de régénération documentée) ; item CIRCLE-TASKS `claude-md-weight-signal` enrichi d'un signal de redondance (réutilise `findRedundantRulePairs()`, jamais un second calcul) | terminée — fidèle |
| 211 | 2026-09-20T20:20Z | Méthode de travail / Suivi | Clôture de 3 tâches "en cours" obsolètes trouvées en reprenant la liste | normal | En reprenant systématiquement toutes les tâches marquées "en cours" dans docs/suivi/, 3 rows stales trouvées et closes avec justification vérifiée : blocage quota Gemini (dépassé par full_sim16, ligne #141) ; premier passage THE-DEEP-READER (rapport + réconciliation déjà complets dans `docs/suivi/relectures-lourdes/2026-09-20-nuit-autonome.md`, jamais mis à jour après coup) ; CLAUDE.MD.SPY (cf. #210 ci-dessus) | terminée — fidèle |
| 212 | 2026-09-20T20:30Z | Charte / Outillage de travail | Retour utilisateur sur le premier rapport check-tasks-details livré : 2 vrais bugs trouvés et corrigés | important | Demande explicite : « analyse le rapport fourni, trouve des erreurs éventuelles et corrige [...] la distinction être fait/en cours/à faire n'est pas assez claire, pas assez visible ». Lecture complète du rapport HTML faite pour la première fois (jusque-là seuls les compteurs terminal avaient été lus). Bug 1 : `appendSnapshot()` comptait des instantanés identiques consécutifs (générés à 90 secondes d'écart pendant le débogage du badge) comme des observations réellement séparées, gonflant artificiellement le signal de stagnation ("16 tâches identiques depuis 3 rapports" alors que 2 des 3 dataient de quelques secondes) — corrigé par dé-doublonnage (signature n+statusKey). Bug 2 (retour direct) : le statut de chaque tâche était noyé en texte simple sans repère visuel — corrigé par une icône (🔄/📋/✅/❓) dans le libellé + une classe CSS colorée (`tree-status-*`) dans `html-report.mjs`, appliquée aux deux formats (liste et arborescence). Nouveau rapport régénéré et livré. `lib/reference.ts` Version 159, check-house.mjs vert (nouveaux tests pour le dé-doublonnage et le rendu coloré), tsc propre | terminée — fidèle |
| 213 | 2026-09-20T20:45Z | Charte / Outillage de travail | Reprise de la tâche #181 (allègement CLAUDE.md), section 3 : 2 asides datées trouvées manuellement dans le bloc adjacent à l'Article 19 | important | Priorité explicite de l'utilisateur : « on a pas fini le claude.md je veux finir claude.md en priorité » (après avoir dismissé les questions CASSANDRA-RH round 2, remises en file, tâche #134 toujours ouverte). Utilisé `docs/referentiel/claude-md-regles.md` (CLAUDE.MD.SPY) pour identifier le plus gros candidat : Article 19 ressortait à 267 lignes, un artefact mécanique du découpage par "## " — en réalité deux blocs distincts (Smart Breaker, opérationnel et dense, laissé intact conformément au garde-fou non négociable ; puis un bloc de compléments à l'Article 16 mal placé dans le document). `listDatedNarrativeMarkers()` renvoyait 0 sur tout le fichier — investigué et expliqué : 2 asides datées réelles ("Double lecture en parallèle", "Questions de calibrage après « voici mes commentaires »") échappaient au regex pour une raison inédite (phrase terminée par un point avant l'astérisque de fermeture), documentée dans `claude-md-asides-historique.md` comme limite distincte de celle déjà connue (parenthèses imbriquées). Genèse déplacée, règles laissées 100% verbatim. `diffNormativeMarkers()` : 0/258 perdu. Poids : 27314 → 27246 tokens. `lib/reference.ts` Version 160, check-house.mjs vert, tsc propre | terminée — fidèle |
| 214 | 2026-09-20T21:00Z | Charte / Outillage de travail | Reprise de la tâche #181, section 4 : premier vrai resserrement de FORMULATION (pas seulement d'asides) | important | Question de calibrage posée après avoir constaté que le gisement facile (asides datées) était épuisé — réponse "recommandé" choisie : continuer maintenant avec un resserrement prudent. Cible : les 5 compléments verbeux de l'Article 16, mal placés après l'Article 19 dans le document (type de question, format de fenêtre, clarté non-développeur, une question = une idée, traçabilité) — reformulés en prose plus dense, listes à puces converties en clauses. Chaque règle distincte vérifiée présente une par une avant de valider. `diffNormativeMarkers()` : 3 faux positifs identifiés et confirmés inoffensifs (listes reformulées en prose, jamais reconnues identiques par le comparateur de phrases — limite connue) ; 1 vrai risque trouvé et corrigé en cours de route (clause "le but est d'éviter toute erreur de compréhension" restaurée après un premier essai qui l'avait coupée par excès de zèle). Poids : 27314 → 27023 tokens (~291 tokens, le plus gros gain de cette 4e passe à ce jour). `lib/reference.ts` Version 161, check-house.mjs vert, tsc propre | terminée — fidèle |
| 215 | 2026-09-20T21:15Z | Charte / Outillage de travail | Reprise de la tâche #181, section 5 : 3 asides datées supplémentaires trouvées dans le Protocole de simulation complète (Article 18) | important | Réponse "recommandé" au calibrage : continuer sur l'Article 18 (87 lignes, le prochain plus gros bloc verbeux). 3 asides datées manuelles trouvées aux étapes 3bis/4/4bis, même limite de regex déjà rencontrée en section 3 (`).* ` au lieu de `)*`). Genèse déplacée, règles laissées 100% verbatim — y compris le contenu opérationnel dense (Smart Breaker, KPI, cadence EL-PROFESSOR), jamais touché. `diffNormativeMarkers()` a signalé 5 changements : 3 déjà connus (bullets→prose de la section 4), 2 nouveaux dus à des phrases fusionnées sans point entre la règle et l'aside retirée — les deux vérifiés manuellement, substance intégralement présente. Poids : 27314 → 26798 tokens (~516 tokens, le meilleur gain de toute cette 4e passe). `lib/reference.ts` Version 162, check-house.mjs vert, tsc propre | terminée — fidèle |
| 216 | 2026-09-20T22:00Z | Conception / Nouvel outil (check-tasks-details) | recommendNextTasks() : ordre recommandé des prochaines tâches, cinq critères dont une corroboration ARGUS/HARMONIA/AXA-CHECK/CLEAN-DIRTY-OLD ajoutée en cours de route, affinés en calcul progressif | important | Demande explicite détaillée de l'utilisateur (protocole complet : check-tasks recommande l'ordre des 4 prochaines tâches en fin de rapport, l'agent lit le rapport en entier puis évalue la pertinence réelle, une fenêtre de questions propose l'ordre — jamais posée avant confirmation manuelle que l'utilisateur a lui-même lu le rapport). Implémenté `recommendNextTasks()` (4 critères calibrés par 2 questions : sensibilité déclarée, stagnation, ancienneté, priorité explicite) et câblé dans `buildReport()`. Deux règles de comportement documentées dans `docs/regles-de-travail.md` (section « Moi (l'agent) → check-tasks-details ») suite à deux messages explicites de l'utilisateur : (1) toute question type « où en est-on ? »/« état des tâches » déclenche systématiquement régénération + livraison en fichier séparé + lecture intégrale du rapport avant de répondre, jamais un résumé de mémoire ; (2) confirmation manuelle de lecture exigée avant d'ouvrir la fenêtre de questions sur l'ordre. Question explicite de l'utilisateur sur la vraie compréhension de l'outil (« comment bien cabler cet outil avec toi ? ») a mené à un 5e critère ajouté le jour même : corroboration par les registres ARGUS/HARMONIA/AXA-CHECK/CLEAN-DIRTY-OLD (`loadRegistryFindings()`/`parseRegistryTable()`/`corroborateWithRegistries()`, réutilise le tokenizer de `suggestPrestationsForTask()`, `significantWords` exporté de `le-coordinateur.mjs`). Puis demande explicite d'affiner l'échelle pour mieux comparer les tâches (« l'echelle dvrait s'affiner [...] permettre une meilleure comparaisone netre les taches ») — calibrée par 2 questions : 3 critères passés d'un forfait fixe à un calcul progressif (stagnation via un vrai streak `consecutiveOpenStreak()` plutôt qu'un booléen, corroboration selon le nombre de vigies + force du rapprochement, priorité explicite à 2 paliers « absolue »/« explicite ») ; décision explicite de NE PAS toucher aux 4 cases de sensibilité elles-mêmes (centaines de tâches historiques déjà écrites, doctrine « portée non rétroactive » déjà actée dans `docs/systeme-de-suivi.md`). Documenté aussi, en comblant un angle mort trouvé au passage (Article 13) : le badge/`buildRealOnboardingContext()` du 2026-09-20 n'avait jamais été ajouté à `docs/referentiel/check-tasks-details.md`. `lib/reference.ts` Version 163, `check-house.mjs` vert (nouveaux tests : streak progressif, parseRegistryTable/loadRegistryFindings/corroborateWithRegistries, les 5 critères de recommendNextTasks() séparément puis combinés, limite, exclusion des tâches fermées/sans signal, wiring dans buildReport()), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 217 | 2026-09-20T22:15Z | Conception / Nouvel outil (check-tasks-details) | Premier usage réel de recommendNextTasks() : bug "#undefined" corrigé, faux positif CASSANDRA-RH capturé par la lecture obligatoire | important | Suite à la demande explicite « reflechis avec tasks detail à quelle est la meilleure suite possible ». Premier rapport projet_entier/arborescence généré et lu en entier (protocole nouvellement documenté, tâche #216). Deux trouvailles réelles : (1) bug de rendu — une tâche sans N° réel affichait "#undefined" dans le bloc recommandé au lieu du "—" déjà utilisé partout ailleurs dans le rapport, corrigé par cohérence (`r.n ?? "—"`) + test de régression ajouté ; (2) trouvaille plus significative, jamais corrigée dans le code (limite honnête déjà assumée : un rapprochement mécanique n'est jamais une certitude) — la note de conception CASSANDRA-RH ressortait recommandée en position n°1 à cause d'un faux positif du critère « priorité explicite » : son propre texte contient littéralement « Priorité explicite de l'utilisateur » mais en parlant de PRIORISER le travail CLAUDE.md ET DE REPORTER CASSANDRA-RH — l'exact inverse de ce que le score y a lu. Confirme concrètement l'utilité du protocole de lecture obligatoire avant toute réponse (docs/regles-de-travail.md) : un chiffre seul aurait fait recommander à tort l'exact opposé de l'intention réelle de l'utilisateur, déjà connue par ailleurs (« épuiser toutes les tâches ouvertes avant de passer à cassandra »). `lib/reference.ts` Version 164, check-house.mjs vert, tsc propre, registre `docs/check-tasks-details/index.md` nettoyé du rapport buggé jamais livré | terminée — fidèle |

| 218 | 2026-09-20T22:30Z | Charte / Outillage de travail | Vérification des clôtures avant de construire l'ordre des tâches ouvertes + nouvelle tâche mise en file (AXA-CHECK) | important | Suite à « reflechis avec tasks detail à quelle est la meilleure suite [...] on epuise toutes les taches ouvertes avant de passer à cassandra ». Deux décisions actées par calibrage explicite avant l'ordre lui-même : (1) le faux positif CASSANDRA-RH (tâche #217) reste volontairement non corrigé dans le code — confirmé par l'utilisateur (« laisser tel quel »), cohérent avec la doctrine ARGUS/HARMONIA (un rapprochement mécanique reste toujours à vérifier, jamais une certitude automatique). (2) Nouvelle tâche mise en file, proposée par l'utilisateur : étendre AXA-CHECK aux scripts internes de travail (aujourd'hui limité à `lib/*.ts`+`route.ts`, jamais `scripts/*.mjs`) — calibrée à seulement les ~15 outils à badge (pas tous les scripts), en simple information affichée à côté du badge, jamais une condition qui pourrait le faire perdre. Vérification demandée puis effectuée sur 3 tâches soupçonnées déjà satisfaites par le travail de ce soir : #157 reste OUVERTE (explicitement mise en queue par l'utilisateur lui-même, à traiter seulement après la clôture complète du chantier #181, une vraie dépendance jamais vue par le calcul mécanique de recommendNextTasks()) ; #176 CLÔTURÉE (la règle demandée — « solliciter réellement les outils, le dire explicitement » — est bien écrite dans `docs/regles-de-travail.md` et activement démontrée toute la soirée, vérifié en relisant la section correspondante) ; #181 reste OUVERTE mais avec 4 de ses 6 sous-points réellement faits ce soir (recherche web SMART-CONSO-TOKEN, plusieurs passes d'allègement CLAUDE.md) — seuls les points 4 (redemander à SMART-CONSO-TOKEN d'autres pistes de réduction après coup) et 6 (lui demander comment organiser durablement le respect de cette contrainte) restent réellement à faire | terminée — fidèle |

| 219 | 2026-09-20T22:45Z | Conception / Nouvel outil (CASSANDRA-RH) | Précision capturée sur l'extension AXA-CHECK ↔ badge ↔ futur rôle de CASSANDRA-RH — rien construit, juste actée | normal | Demande explicite de l'utilisateur, en 5 questions rapides : (1) le badge ne doit jamais afficher une couverture AXA "absente" — un scan AXA-CHECK initial doit faire partie de l'onboarding (`checkAgentOnboarding()`) de tout outil devenant éligible au badge, garantissant qu'il a toujours au moins un statut réel dès qu'il a son badge. (2) Un outil fraîchement intégré affiche donc logiquement "en cours" (couverture partielle probable), jamais "OK" d'emblée. (3) Une fois CASSANDRA-RH construit (chantier explicitement différé), il devra surveiller une régression de couverture AXA d'un outil (OK → en cours), même logique que la régression de statut déjà détectée par `compareSnapshots()` dans check-tasks-details, appliquée cette fois à la couverture plutôt qu'au statut d'une tâche. (4) Il devra aussi vérifier qu'aucun outil à badge ne reste jamais "absente" (jamais scanné). Capturé ici, à la suite de la note de conception CASSANDRA-RH déjà existante (2026-09-20T09:40Z) et de la tâche AXA-CHECK-extension mise en file (#218) — aucune des deux n'est construite ce soir, cette précision les enrichit seulement pour ne rien perdre avant leur tour dans l'ordre des tâches | terminée — fidèle |

| 220 | 2026-09-20T23:00Z | Correctif de code / Charte | Corrige la répétition sévère de la fenêtre d'attente phase 2 (full_sim16) | important | Reprise de l'ordre convenu des tâches ouvertes (item TaskCreate #142, jamais sa propre ligne docs/suivi jusqu'ici — capturé seulement en prose dans la ligne #164 du 2026-09-20T12:05Z). Root-cause investiguée (Article 19) en relisant directement `docs/simulations/full_sim16_transcript.txt` (l.657-769) et `docs/el-professor/full_sim16.md` (thème 3, 6/20) : ~25 tours consécutifs ressassaient « rien de ce que fait l'observateur ne changera notre nature de code/lignes », un thème réel jamais couvert par les 4 motifs existants de `THEME_MOTIFS` (`lib/dialogue.ts`) — les 4 premiers ont fonctionné comme prévu, c'était un vrai 5e thème manquant, pas un bug des précédents. Corrigé : 5e motif ajouté, regex calibrée et vérifiée directement contre 6 vraies lignes du transcript (zéro faux positif testé contre des lignes légitimes sans rapport, ex. "les lignes du code défilent sur l'écran"). Documenté dans `docs/referentiel/principes.md` (5.2quater) et `docs/simulations/correctifs-a-revalider.md` (0/2, à revalider sur les 2 prochaines simulations propres). `lib/reference.ts` Version 167, `check-house.mjs` vert (6 lignes réelles du transcript + 2 contrôles négatifs), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 221 | 2026-09-20T23:15Z | Correctif de code / Charte | Vérification de la règle du jeu pour l'absence de dossier retourné (full_sim16) — deux décisions actées, rien à coder ce soir | important | Reprise de l'ordre convenu (item TaskCreate #143). Analyse : `dossierGateEligible` (`app/api/lia/route.ts`) partage son patron « aucun besoin urgent chez les deux » (`!priority(needs)`) avec plusieurs autres beats relationnels (`eligibleBeat`, `personalLead`, `affectionOpportunity`) — rien ne suggère qu'elle soit cassée spécifiquement pour le dossier ; le vrai facteur limitant de full_sim16 est le budget fixe de 25 tours du script de simulation (scratchpad, jamais committé), qu'un vrai joueur n'a jamais. Deux questions de calibrage posées, deux décisions actées : (1) laisser la règle du jeu telle quelle, aucun code touché — confirmé un choix légitime plutôt qu'un bug caché. (2) allonger le budget de tours de cette fenêtre lors de la PROCHAINE préparation d'un script de simulation — décision qui ne peut pas être committée maintenant (le script n'existe pas encore, réécrit à chaque simulation), donc capturée ici pour ne pas la perdre : la prochaine fois qu'un script de simulation est préparé (Article 18, étape 1), la fenêtre d'ouverture du piège `dossierTraps` doit recevoir un budget de tours plus généreux que 25 | terminée — fidèle |

| 222 | 2026-09-20T23:30Z | Conception / Nouvel outil (CASSANDRA-RH) | Organigramme complet de l'équipe capturé (Direction/Équipe noyau/Membres/VIP) — rien construit, à mettre en file | important | Demande explicite de l'utilisateur (« aide-moi à imaginer tout le système, pose-moi des questions... tâche à mettre à la file »), en prolongement direct de la précision #219 sur AXA-CHECK/badge. Question de fond posée par l'utilisateur lui-même : « est-ce que tout ça est bien utile ? je veux pas trop faire l'enfant dans le code ». Avis donné : ce n'est pas un nouveau mécanisme mais l'organisation d'une asymétrie qui existe déjà (ARGUS/HARMONIA/AXA-CHECK/CLEAN-DIRTY-OLD produisent déjà des verdicts jamais agrégés en un seul "c'est bon pour ce membre"), directement raccordable au principe déjà acté de CASSANDRA-RH (« traduit les verdicts existants, n'en recalcule aucun »). Organigramme calibré en 4 rondes de questions : (1) **Direction (CODIR)** = CASSANDRA-RH + LE-COORDINATEUR (aucun des deux ne vérifie le code lui-même — l'un route le travail, l'autre évalue les postes) ; (2) **Équipe noyau** = exactement les 4 outils déjà "toujours déployés" de l'Article 20 (ARGUS/HARMONIA/AXA-CHECK/CLEAN-DIRTY-OLD) — jamais un groupe inventé, un nom donné à une frontière déjà existante dans la charte ; (3) **Membre de l'équipe** = tous les autres outils, statut Agent/Utilitaire nommé/Infrastructure inchangé ; (4) **VIP** = l'utilisateur et l'agent (Claude), explicitement hors du tableau, jamais évalués, jamais une ligne du badge. Décisions structurelles : le rôle (Direction/Noyau/Membre) est une case spéciale À L'INTÉRIEUR du statut Agent existant, jamais un 4e axe séparé qui casserait tout ce qui lit déjà le statut aujourd'hui. Contrôle croisé confirmé : l'équipe noyau se vérifie aussi ELLE-MÊME entre ses 4 membres (personne au-dessus de la vérification). Les deux directeurs (CASSANDRA-RH, LE-COORDINATEUR) passent par les MÊMES scans mécaniques que tout le monde (couverture AXA-CHECK, trous ARGUS, frictions HARMONIA) — seule leur ÉVALUATION RH de pertinence de poste (jugement de CASSANDRA-RH) échappe structurellement à eux-mêmes, jamais les scans mécaniques objectifs. Agrégation choisie : vit DANS le badge existant, enrichi (pas un nouveau mécanisme séparé, pas encore le cœur de CASSANDRA-RH). **Rien construit ce soir** — capturé pour rejoindre la file des tâches déjà en attente (extension AXA-CHECK #218, CASSANDRA-RH lui-même #134/#147/#171/#172/#179), à la suite de la note de conception CASSANDRA-RH déjà existante (2026-09-20T09:40Z) et de sa précision #219 | terminée — fidèle |

| 223 | 2026-09-20T23:45Z | Charte / Outillage de travail | Câblage html-report.mjs dans el-professor.mjs, the-final-judge.mjs, the-screener-capture.mjs (tâche #144) | important | Reprise de l'ordre convenu. Investigation (Article 19) : les 3 scripts n'ont pas la même forme — `el-professor.mjs`/`the-screener-capture.mjs` ont chacun un vrai `main()` (wiring direct, même patron que `kpi-report.mjs` : `buildElProfessorCoverageHtml()` reprend missing/orphans déjà calculés, `buildScreenerCaptureHtml()` affiche la capture réelle via un bloc "image" ou l'échec honnêtement) ; `the-final-judge.mjs` n'a AUCUN main() — le vrai rapport est écrit en prose par l'agent séparé, jamais un calcul de ce script — `buildFinalJudgeReportHtml()` est donc le point d'intégration réel (appelé à la main par l'agent orchestrateur au moment de livrer un rapport), avec un bloc "code" plutôt que "paragraph" pour préserver les sauts de ligne du texte libre. Vérifié live contre les 3 vrais fichiers sur disque (`checkHtmlWiring()` appliqué au vrai contenu, pas seulement à une fixture synthétique). `buildScreenerCaptureHtml()` vérifié manuellement hors check-house.mjs (le fichier importe `playwright`, jamais importé dans la suite principale par choix déjà existant — pas une régression introduite ce soir). `lib/reference.ts` Version 168, `check-house.mjs` vert, `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 224 | 2026-09-20T23:50Z | Conception / Nouvel outil (badge) | Message final du badge + échelle de couverture à 3 niveaux affinée — rien construit, à mettre en file | normal | Suite directe de la capture #222. Message d'annonce proposé et validé dans le principe : « 🎖️ [Nom] obtient son badge — intégration complète vérifiée (blueprint, instanciation, registre, mention CLAUDE.md, présence PRESTATIONS). Couverture de code : [niveau]. » — toujours deux phrases séparées, le badge restant sur l'intégration (jamais conditionné par la couverture, déjà décidé). Échelle de couverture affinée de 2 à 3 niveaux (proposition de l'utilisateur, corrigée avant validation) : « en cours » (jamais scanné/très faible) / « partiel » (couverture réelle incomplète) / « OK 100% ». Correction actée : « OK 100% » ne doit JAMAIS inclure une promesse de « zéro bug » — AXA-CHECK ne peut mécaniquement prouver qu'une ligne EXÉCUTÉE est une ligne JUSTE (limite déjà documentée), affirmer zéro bug serait la sur-promesse que ce projet refuse partout ailleurs (ARGUS/ALWAYS-NEW-CODE). Décision finale de l'utilisateur : aller plus loin que la couverture seule — « OK 100% » exigera EN PLUS zéro trouvaille ouverte d'ARGUS/HARMONIA sur cet outil, un verdict combinant plusieurs membres de l'équipe noyau (cohérent avec l'organigramme #222), pas seulement AXA-CHECK isolément. Rien construit ce soir | terminée — fidèle |

| 225 | 2026-09-21T00:00Z | Charte / Outillage de travail | Ronde CIRCLE-TASKS (19 commits sans passage) | normal | Ronde demandée explicitement par l'utilisateur avant d'attaquer le chantier 3. Menu complet généré ; items sélectionnés selon leur fraîcheur réelle, même discipline que la ronde #207 (« sur mesure », tâche #195). (1) Profil utilisateur : nouvelle fiche ajoutée (`docs/profil-utilisateur/observations/2026-09-20-2350.md`, 4 signaux nouveaux), couvrant le segment recommendNextTasks()/bugs full_sim16/organigramme CASSANDRA-RH. Le signal « undefined jour(s) » réapparu, revérifié et reconfirmé NON BOGUÉ (même cause déjà diagnostiquée en #207 : horloge du conteneur en retard sur l'horodatage perçu de session, `daysSince()` refuse honnêtement un compte négatif). (2) Écart réel trouvé et corrigé en vérifiant les carnets (Article 13) : `docs/referentiel/points-fragiles.md` gardait une entrée « `checkHtmlWiring()`/tâche #144 repose sur une prémisse fausse » — devenue fausse elle-même depuis la clôture réelle de #144 ce soir (ligne #223) : `el-professor.mjs`/`the-screener-capture.mjs` calculent bien de vraies données mécaniques (contrairement à ce que l'entrée affirmait), seul `the-final-judge.mjs` n'a effectivement aucun `main()` — entrée retirée. (3) Zone ALWAYS-NEW-CODE la plus négligée toujours "Fatigue" (inchangé depuis #177/#207, le vrai zoom reste hors scope d'une ronde légère). (4) HTML wiring : déjà résolu par #144 ce soir, confirmé vert. (5) CLAUDE.md/KPI/Smart Conso API/SMART-CONSO-TOKEN/tâche la plus ancienne : tous déjà frais, rien à faire. Items non retenus cette fois : relecture exhaustive du référentiel (coûteux, pas de signal de staleness réel), photo de la dream team (récréatif, pas prioritaire), THE-SCREENER (pas de serveur de session en cours), THE-FINAL-JUDGE/THE-DEEP-READER (coûteux, jamais par défaut) | terminée — fidèle |
| 226 | 2026-09-21T00:20Z | Charte / Outillage de travail | Deux vrais manquements trouvés par l'utilisateur sur la Ronde #225 — garde-fou en 2 temps + rapport de fin de Ronde | important | Question directe de l'utilisateur : « pour la dernière ronde, tu ne m'as pas du tout consulté dans le choix des outils [...] volontaire de ta part ? » — vérifié : ce n'était pas un choix assumé mais un vrai bug de process, la Ronde #225 a été exécutée sans jamais ouvrir la fenêtre à cocher pourtant déjà obligatoire (« jamais un tout-en-un silencieux », `docs/regles-de-travail.md`), une dérive du précédent « Ronde sur mesure » (tâche #195/#207) qui a fini par remplacer la confirmation elle-même plutôt que de rester une proposition. Deuxième question directe, même échange : « je n'ai pas eu de rapport à la fin de la ronde, c'est voulu ? » — vérifié également non voulu : `circle-tasks.mjs::main()` n'a jamais affiché que le menu AVANT exécution, alors que `docs/regles-de-travail.md` citait déjà CIRCLE-TASKS parmi les rapports rendus par html-report.mjs (même écart Article 13 que #223, jamais câblé). Quatre questions de calibrage posées et actées : (1) garde-fou en 2 temps limité à CIRCLE-TASKS uniquement, jamais généralisé aux autres listes "recommandées" du projet ; (2) items déjà "recommandé" réaffichés normalement (étiquette conservée) dans la fenêtre de paramétrage fin, à re-cocher comme les autres — limite technique actée : aucune case ne peut être pré-cochée dans l'outil de questions utilisé ici ; (3) format "partiel" du badge (encore non construit, cf. #224) précisé : plusieurs vérifications KO listées ensemble séparées par une virgule (« partiel (KO ARGUS, KO AXA-CHECK) ») plutôt qu'une seule citée ; (4) le détail AXA-CHECK affichera le vrai pourcentage mesuré (« KO AXA-CHECK 62% »), jamais un KO nu comme pour ARGUS/HARMONIA qui n'ont pas de pourcentage. Implémenté ce soir : `buildCircleRunSummaryHtml()` (`scripts/circle-tasks.mjs`), même patron que `buildFinalJudgeReportHtml()` (pas de main() qui connaît la sélection réelle — l'agent qui pilote l'appelle lui-même une fois la Ronde terminée), index léger calibré (item exécuté/résultat en une phrase/lien vers la sortie déjà produite, jamais son contenu dupliqué) ; le garde-fou en 2 temps documenté dans `docs/regles-de-travail.md` (nouvelle sous-section dédiée, à appliquer dès la prochaine Ronde — comportemental, aucun nouveau code requis pour cette partie) ; la précision "partiel (KO X, KO Y)"/pourcentage AXA-CHECK ajoutée à la capture de conception déjà existante (#224), toujours rien construit pour le badge lui-même. `lib/reference.ts` Version 169, `check-house.mjs` vert (nouveaux tests : rapport vide honnête, rendu réel avec ordre préservé et sans "undefined" qui fuit), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |
| 227 | 2026-09-21T01:10Z | Correctif de code / Charte | Chantier 3 : diagnostic et correction de l'anomalie loveRealized (arc relationnel Lia/Noé) | important | Reprise de l'ordre convenu (item TaskCreate #108), investigué SANS nouvelle simulation comme demandé par CLAUDE.md. Investigation : `lib/relationship.ts::attractionAfterTurn()` et la boucle solitaire de `app/api/lia/route.ts` lues en entier (Article 19) ; recherche exhaustive dans les 14 transcripts archivés de toute trace du doute amoureux privé (`loveRealized`, introduit le 2026-09-18) — zéro occurrence dans les 8 simulations produites depuis cette date, y compris full_sim16 (229 tours). Trouvé au passage : les 6 simulations plus anciennes contiennent l'ANCIEN mécanisme scripté qu'il a remplacé (`life.loveNoticed`, deux lignes fixes "Punaise… je crois que je tombe amoureuse"/"Je crois que je tombe amoureux", déjà documenté comme retiré dans un commentaire de route.ts) — confirme que le mécanisme actuel n'a, lui, jamais fonctionné une seule fois. Root-cause identifiée par calcul (pas de simulation) : un tour solo (pièces séparées, `!together`) efface TOUTE variation d'attirance proposée par le modèle avant même le système de crédit de 28 % — double frein cumulatif. Trois questions de calibrage posées et actées : (1) rendre le seuil de 75% réellement atteignable en session normale (pas un trésor caché rare) ; (2) assouplir en priorité la règle "ensemble ou rien ne pousse" plutôt que le taux de crédit ; (3) garder l'écart existant Noé/Lia (Noé légèrement plus rapide), ne pas symétriser. Implémenté (`app/api/lia/route.ts`, boucle solitaire) : une HAUSSE solo proposée par le modèle survit désormais à moitié (arrondie), avant de repasser par le même crédit de 28% déjà en place pour les tours ensemble ; une BAISSE solo reste entièrement effacée, comportement inchangé. Bug de méthodologie de test trouvé et corrigé en écrivant le test dédié (Article 5) : `coordinateRooms()` (lib/turn.ts) réunit automatiquement le "suiveur" dans la pièce du "meneur" dès que `story.round>=8`, sauf `stayAlone:true` côté suiveur — sans ce champ, un scénario de test à pièces séparées redevenait "ensemble" avant même d'atteindre la boucle solitaire. `docs/referentiel/parametres.md`, `docs/simulations/correctifs-a-revalider.md` (nouvelle entrée, 0/2) et le Plan d'origine de CLAUDE.md (point 3) mis à jour. `lib/reference.ts` Version 170, `check-house.mjs` vert (nouveau test en toute fin de fichier — délibérément isolé du reste de la suite car il consomme 2 vrais tours et doit restaurer intégralement l'état partagé pour ne pas décaler les tests suivants), `tsc --noEmit` sans nouvelle erreur. Reste ouvert : à revalider via une prochaine simulation réelle (le calage précis de l'arc par rapport à la révélation, lui, reste toujours à confirmer en jouant) | terminée — fidèle |
| 228 | 2026-09-21T01:15Z | Charte / Outillage de travail | SMART-CONSO-TOKEN : précision capturée sur le mode proactif (tâche #136) — rien construit | normal | Question directe de l'utilisateur sur le chantier 3 : « je suppose que c'est concernant les economies de token ? donc suivi-conso aurait pu te delivrer le meme conseil proactivement on est ok ? à ajouter à la to do ». Clarifié : le choix de ne pas lancer de simulation pour le chantier 3 n'était pas une économie de tokens de mon initiative mais une consigne déjà écrite dans CLAUDE.md pour ce chantier précis — donc pas un vrai gap ce coup-ci. Mais la généralisation est actée et capturée comme précision de la tâche déjà en file #136 (rendre SMART-CONSO-TOKEN proactif) : avant de recommander/lancer une simulation coûteuse, vérifier d'abord si les données déjà archivées répondent à la question. Rien construit ce soir | terminée — fidèle |
| 229 | 2026-09-21T01:40Z | Charte / Outillage de travail | CIRCLE-TASKS : garde-fou de fraîcheur du catalogue (registres oubliés) | important | Question directe de l'utilisateur : « est-ce que la ronde a bien dans son catalogue tous les outils pertinents ? incluant tous les nouveaux outils/scripts ? verifie tout ca stp et fiabilise ». Vérifié : LE-COORDINATEUR a déjà `findToolsMissingFromMenu()` garantissant que PRESTATIONS reste à jour à chaque nouvel outil coûteux — rien d'équivalent n'existait pour `CIRCLE_ITEMS`, un vrai trou confirmé (pas une supposition). Contrairement à PRESTATIONS (un seul critère mécanique, `isMenuWorthy()`), aucun prédicat unique ne décide "doit rejoindre la Ronde" — chaque inclusion/exclusion actuelle (ARGUS/HARMONIA/AXA-CHECK/CLEAN-DIRTY-OLD déjà relancés à chaque commit, EL-PROFESSOR déjà obligatoire à chaque simulation, etc.) est une vraie décision documentée séparément, jamais une règle recalculable. Implémenté `findRegistriesMissingFromCircle()` + `CIRCLE_EXCLUDED_REGISTRIES` (`scripts/circle-tasks.mjs`) : compare chaque registre réel sur disque (`docs/<slug>/index.md`) à CIRCLE_ITEMS ET à la liste d'exclusions (chaque exclusion porte sa propre raison écrite, jamais un silence), comparaison bidirectionnelle par id (un registre plus court ou plus long que l'id de son item doit matcher dans les deux sens — bug réel trouvé en testant : "profil-utilisateur" vs l'item "profil" ne matchait qu'un seul sens). Refactor associé pour éviter un cycle d'import (check-tasks-details.mjs importe déjà `daysSince` de circle-tasks.mjs) : `walkDocsPaths()` extraite de check-tasks-details.mjs vers `lib-shell.mjs` (même règle de mutualisation §7ter que `sh()`), les deux fichiers l'important désormais depuis là. Câblé dans `main()` : alerte ⚠️🔴 si un registre orphelin est trouvé — vérifié en direct contre l'état réel du dépôt, zéro trouvaille aujourd'hui. `lib/reference.ts` Version 171, `check-house.mjs` vert (nouveaux tests : correspondance bidirectionnelle courte/longue, exclusion documentée, registre neuf détecté par son nom exact, faux positif évité sur un fichier non-index, vérification live sans fixture), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |
| 230 | 2026-09-21T01:45Z | Conception / Nouvel outil (Doc-Report + fréquence d'usage) | Doc-Report (index global des rapports), partage HTML/texte, compteur d'utilisation des outils — capture de conception, rien construit | normal | Demande explicite détaillée de l'utilisateur (plusieurs points traités un par un) : (1) HTML vs texte : proposé un partage (KPI/EL-PROFESSOR/THE-SCREENER/THE-FINAL-JUDGE/THE-DEEP-READER/check-tasks-details/dream-team-photo méritent le HTML ; scans Smart Conso API/SMART-CONSO-TOKEN et registres ARGUS/HARMONIA/AXA-CHECK/CLEAN-DIRTY-OLD/ALWAYS-NEW-CODE/CLAUDE.MD.SPY restent en texte) — 3 cas d'hésitation soumis (transcripts de simulation, récap de fin de Ronde CIRCLE-TASKS, scans Smart Conso). Réponse de l'utilisateur : seuls les transcripts de simulation méritent le HTML (coloration par personnage) ; les deux autres restent texte. (2) Existence d'un index global des rapports : vérifié, non — 16 registres séparés existent (`docs/*/index.md`), aucun ne les relie entre eux. (3) Rôle de Doc-Report, redéfini après une première proposition rejetée par l'utilisateur (« à chaque fois qu'un nouveau membre est créé... je dois avoir une boîte de question qui me demande si le rapport doit être HTML ou texte, même chose pour un rapport neuf sur un outil existant ») : Doc-Report devient le GARDIEN de la décision (index global + champ HTML/texte enregistré par rapport), jamais celui qui la prend — la question se pose au moment de la création (nouvel outil → 6e type de trou dans `checkAgentOnboarding()` ; rapport neuf sur un outil existant → discipline comportementale, pas mécaniquement détectable). Confirmé par l'utilisateur comme correspondant exactement à son intention. (4) Déclenchement : intégré à la Ronde CIRCLE-TASKS (confirmé "recommandé"). (5) Compteur d'utilisation des outils (transmis à CASSANDRA-RH pour juger si un agent a toujours sa place) : vérifié, rien n'existe aujourd'hui — construction neuve, pas une extension. Source de la donnée confirmée par l'utilisateur : journal auto-déclaré par l'agent (même principe honnête que l'historique SMART-CONSO-TOKEN, jamais vérifiable mécaniquement). Rejoint la file des chantiers déjà en attente (CASSANDRA-RH, extension AXA-CHECK aux scripts) — rien construit ce soir | terminée — fidèle |
| 231 | 2026-09-21T02:00Z | Charte / Outillage de travail | Ronde CIRCLE-TASKS (23 commits sans passage) — première application réelle du garde-fou en 2 temps | normal | Première Ronde depuis la mise en place du garde-fou en 2 temps (#226) : question posée avant toute exécution (« voulez-vous paramétrer la Ronde ? »), réponse de l'utilisateur « NON, lancer avec les paramètres recommandés ». Sélection recommandée basée sur les signaux de fraîcheur réels de `buildCircleReport()` : (1) Profil utilisateur — nouvelle fiche ajoutée (`docs/profil-utilisateur/observations/2026-09-21-0150.md`, 4 signaux nouveaux, 2 confirmés), signal "undefined jour(s)" revérifié et reconfirmé non bogué (même décalage d'horloge conteneur déjà diagnostiqué en #207/#225). (2) Écart réel trouvé et corrigé en vérifiant docs/suivi (Article 13) : la ligne d'origine du chantier 3 (« Investiguer l'arc relationnel Lia/Noé avant la refonte graphique », 2026-09-19T20:35Z) était restée "ouverte" malgré sa clôture réelle via #227 — corrigée en "terminée — fidèle". (3) Carnets de correctifs/points-fragiles relus : aucun correctif n'a pu graduer (aucune nouvelle simulation cette nuit), aucune entrée obsolète trouvée. (4) Tâche ouverte la plus ancienne recalculée après (2) : passe de 2026-09-19 (chantier 3, maintenant clôturé) à 2026-09-20T09:47Z (proposition de score cible CASSANDRA-RH). (5) KPI/zone ALWAYS-NEW-CODE/Smart Conso API/SMART-CONSO-TOKEN/HTML wiring/CLAUDE.md : tous déjà frais ou sans action requise. Récapitulatif de fin de Ronde livré via `buildCircleRunSummaryHtml()` (première vraie utilisation depuis sa construction en #226) | terminée — fidèle |

| 232 | 2026-09-21T02:20Z | Charte / Outillage de travail | CIRCLE-TASKS : 2 nouveaux items « Passages réels (smoke run) » + recherche d'autres candidats | normal | Suite directe de #230 : confirmation explicite « Oui, les deux (recommandé) » pour ajouter `check-profil-utilisateur.mjs` (jamais lancé une seule fois pour de vrai avant ce soir — confirmé fonctionnel : « OK — 7 fiche(s) sur disque, toutes référencées dans l'index, aucun lien mort. ») et `runNetworkCheck()` de LE-COORDINATEUR (déjà exclu du crochet post-commit par choix documenté, mais ayant déjà attrapé un vrai bug caché cette session — `summarizeTokenHistory is not defined`, #140/#148 — qu'aucun test unitaire n'avait détecté) comme nouveaux items CIRCLE_ITEMS. Nouveau thème créé, « Passages réels (smoke run) », plutôt que de forcer l'un des deux dans "Suivi & référentiels" ou "Qualité du code" (déjà pleins à 4 items chacun, la vraie limite d'options d'une fenêtre à cocher). `docs/regles-de-travail.md` corrigé au passage (Article 13) : le paragraphe décrivant les thèmes était resté à "4 thèmes fixes" en oubliant complètement "Qualité du code", un écart déjà présent avant ce soir. Recherche demandée explicitement par l'utilisateur (« d'autres petits scripts tout mignons à intégrer aux rondes régulières ? cherche à nouveau [...] parmi tous les derniers scripts écrits récemment ») : passage en revue de tout `scripts/` (32 fichiers) — aucun autre candidat trouvé équivalent (tout le reste est soit déjà câblé à chaque commit/simulation, soit coûte de vrais appels Gemini, soit un outil à la demande par nature, soit un module utilitaire sans contenu à exécuter). Deux idées mises en file pour plus tard, à la demande explicite de l'utilisateur (« à mettre de côté pour plus tard ») — pas de conception ni de construction ce soir : (a) un processus produisant une « version de référence » propre et fiabilisée de tout le code — probablement déjà largement couvert par la combinaison ALWAYS-NEW-CODE/HARMONIA/AXA-CHECK/CLEAN-DIRTY-OLD (déjà agrégée par `runNetworkCheck()`), sauf la vraie divergence : produire un ARTEFACT réécrit plutôt qu'un diagnostic, ce qui serait un auto-refactor risqué à calibrer très prudemment ; (b) un outil dédié à la gestion de la mémoire des personnages (Lia/Noé) — pertinence reconnue a priori, utile en particulier pour l'Article 2/4 (cohérence historique/jauges/enquête), possiblement un « instantané + vérificateur de cohérence » de ce qu'un personnage retient réellement en session, jamais évalué plus loin que cette remarque ce soir. `CIRCLE_ITEMS.length` 15→17, `THEME_ORDER` 5→6 thèmes, `lib/reference.ts` Version 172, `check-house.mjs` vert (nouveaux tests : les 2 items sont bien gratuits et dans le bon thème, jamais bundlés dans "Audit lourd"), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 233 | 2026-09-21T02:35Z | Charte / Outillage de travail | Écart trouvé et corrigé : AXA-CHECK/CLEAN-DIRTY-OLD n'avaient jamais de vrai passage post-commit | important | Question directe de l'utilisateur : « pour les rondes, est-ce qu'il y a bien les scans harmonia et argus en priorité ? ainsi que les autres membres de l'équipe noyau ? ». Vérifié en lisant le code (jamais supposé) : `scripts/hooks/check-last-commit.mjs` (post-commit) appelle bien réellement `findDeadLifeFields`/`findTodoMarkers`/`checkLinks` (ARGUS/HARMONIA) contre le vrai code à chaque commit, mais n'appelle JAMAIS AXA-CHECK ni CLEAN-DIRTY-OLD — leur seule couverture est `check-house.mjs` (crochet pre-commit) qui ne teste QUE leur logique pure sur des fixtures synthétiques (confirmé par grep : `collectCoverage`/`robustnessScore` testés uniquement avec des dossiers de couverture fictifs, `relativeStaleness` avec des `lastTouchDays` inventés). Tension directe avec l'Article 20 (« pour AXA-CHECK et CLEAN-DIRTY-OLD [...] c'est la totalité de l'outil qui tourne ainsi »). Question de calibrage posée (fenêtre à choix) : corriger le texte de la charte, ou câbler un vrai passage. Réponse de l'utilisateur : « Ajouter un vrai passage au crochet de chaque commit ». Implémenté sans jamais relancer check-house.mjs une seconde fois (règle anti-doublon déjà documentée en tête de le-coordinateur.mjs pour ce exact cas) : `scripts/hooks/pre-commit` fixe désormais `NODE_V8_COVERAGE` sur un dossier prédictible et gitignoré (`.sites-runtime/axa-check-postcommit-cov`, recréé à chaque commit) pendant le lancement de check-house.mjs déjà obligatoire ; `scripts/hooks/check-last-commit.mjs` lit ce même dossier (`collectCoverage`+`robustnessScore`, importés d'`axa-check.mjs`) pour un vrai score global, puis le nettoie ; ajoute aussi un vrai calcul CLEAN-DIRTY-OLD (`lastTouchDays`+`relativeStaleness`, importés de `clean-dirty-old.mjs`, un seul `git log -1` par fichier de `LIB_MAP`, coût minime). Vérifié manuellement avant d'écrire le code : `NODE_V8_COVERAGE=... node scripts/check-house.mjs` reste vert et produit une vraie couverture exploitable (99.3% mesuré sur les 22 fichiers de LIB_MAP, ~6s de plus). `docs/referentiel/axa-check.md` et `docs/referentiel/clean-dirty-old.md` mis à jour (section Déclenchement, écart documenté). `lib/reference.ts` Version 173, `check-house.mjs` vert, `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |
| 234 | 2026-09-21T02:40Z | Charte / Outillage de travail | Correction directe de l'utilisateur : le récap de fin de Ronde CIRCLE-TASKS doit être en texte, pas en HTML | important | Message direct : « doc-report : le rapport de circle devrait etre en txt et non html ». Vérifié : `buildCircleRunSummaryHtml()` (construite en #226, 2026-09-20) rendait bien du HTML via `renderHtmlReport()` — un vrai écart avec la décision explicite du partage HTML/texte des rapports du projet (#230, même session : seuls les transcripts de simulation méritent le HTML, le récap CIRCLE-TASKS reste texte), jamais revisitée contre cette décision une fois prise (Article 13). Corrigé : fonction renommée `buildCircleRunSummaryText()`, rend un texte brut (même table markdown que `formatCircleMenu()`), fichier `.circle-tasks-run-summary-latest.txt` (au lieu de `.html`, `.gitignore` mis à jour), import `renderHtmlReport` retiré de `circle-tasks.mjs` (plus utilisé dans ce fichier). `docs/regles-de-travail.md` corrigé au même endroit. `lib/reference.ts` Version 174, `check-house.mjs` vert (nouveau test : le rendu ne contient plus aucune balise HTML), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 235 | 2026-09-21T02:55Z | Charte / Outillage de travail | SMART-CONSO-TOKEN : compareChantiers() (#135), rappel proactif ARCHIVE_FIRST_REMINDER (#136), câblage réel de findJudgeSpawnsWithoutConsultation() (#137) | important | Demande explicite : « lance les tâches smart conso token en attendant que je regarde les taches à faire ». Article 19 respecté : tout le fichier `scripts/smart-conso-token.mjs` (972 lignes) relu en entier avant de toucher quoi que ce soit. (1) `compareChantiers()`/`formatChantierComparison()` ajoutées : comparent des chantiers déjà estimés côte à côte, jamais un second calcul de coût, jamais un choix d'ordre de traitement — même discipline "informe, jamais ne décide" que `classifyConsumption()`. (2) `ARCHIVE_FIRST_REMINDER` : généralisation actée de la précision du chantier 3 (#228) — `assess()` rappelle désormais systématiquement, sur tout verdict `avertissement_souple`/`seuil_dur`, de vérifier d'abord les données déjà archivées avant de relancer quoi que ce soit de coûteux. (3) Audit réel de #137 : `checkToolConnections()` déjà fiable (vérifié en direct/bloquant en pre-commit) ; Smart Conso API a déjà son équivalent câblé (`findUnconfirmedBursts()` dans `runNetworkCheck()`) ; mais `findJudgeSpawnsWithoutConsultation()` (SMART-CONSO-TOKEN, autorité sur THE-FINAL-JUDGE/THE-DEEP-READER), entièrement écrite et testée par fixtures, n'était JAMAIS appelée en production — même angle mort que les deux items smoke-run corrigés plus tôt ce soir. Corrigé : deux nouvelles lignes réelles dans `runNetworkCheck()` (le-coordinateur.mjs), une par registre (`docs/the-final-judge/index.md`, `docs/suivi/relectures-lourdes/index.md`), donc réellement exercées à chaque Ronde CIRCLE-TASKS qui coche `network-check-run`. #138 (accompagnement temps réel d'une tâche longue) explicitement PAS implémenté — jugé trop ouvert (quel signal, quelle forme, quelle fréquence) pour être deviné sans calibrage explicite (Article 16), laissé en file. `docs/referentiel/smart-conso-token.md` mis à jour (3 nouvelles sections + section "Autorité réelle" corrigée). `lib/reference.ts` Version 175, `check-house.mjs` vert (nouveaux tests : compareChantiers()/formatChantierComparison(), ARCHIVE_FIRST_REMINDER présent/absent selon le verdict, runNetworkCheck() à 10 lignes désormais, vérifié en direct contre les vrais registres), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 236 | 2026-09-21T03:10Z | Correctif de code / Outillage de travail | THE-DEEP-READER/THE-FINAL-JUDGE : factorisation de la duplication (#152) | normal | Article 19 respecté : les deux fichiers relus en entier avant de toucher quoi que ce soit. `extractPersonaBlock()` confirmée strictement identique dans les deux fichiers ; `detectGenericReport()` partage la logique "sections manquantes"/"rapport trop court" mais garde un signal propre à chaque personnage. Créé `scripts/judge-persona-shared.mjs` (extractPersonaBlock, missingSectionsSignal, tooShortSignal) — les deux fichiers appelants importent et ré-exportent, comportement observable inchangé (vérifié : tous les tests déjà existants restent verts sans modification). `lib/reference.ts` Version 176, nouveau test dédié au module partagé, `check-house.mjs` vert, `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |
| 237 | 2026-09-21T03:15Z | Nouvel outil / Charte | CIRCLE-TASKS : sélection recommandée par défaut, codée (#155) | normal | Encode explicitement la pratique réelle des Rondes #225/#231 : `recommendCircleSelection()`/`NOT_RECOMMENDED_BY_DEFAULT` (scripts/circle-tasks.mjs) excluent, chacun avec sa raison écrite, la relecture exhaustive du référentiel, l'item récréatif, l'item conditionnel THE-SCREENER, et les deux items costly déjà exclus — tout le reste recommandé par défaut. `docs/regles-de-travail.md` mis à jour (garde-fou en 2 temps). `lib/reference.ts` Version 177, nouveaux tests, `check-house.mjs` vert, `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |
| 238 | 2026-09-21T03:45Z | Nouvel outil / Charte | LE-COORDINATEUR : catalogue d'offres nommé et historisé, accroché à CIRCLE-TASKS dès le départ (#154) | important | Séquence dense de demandes/corrections dans la même soirée, traitées point par point. (1) Construction initiale : PRESTATIONS gagne un champ `nom` par offre (14 "Pack X") ; `renderNamedCatalog()`/`recordCatalog()` (historisation anti-doublon dans docs/le-coordinateur-catalogue/, sous-commande `node scripts/le-coordinateur.mjs catalogue`). (2) Demande explicite : « le catalogue du coordinateur doit être accroché à circle, dès le départ, comme la mise à jour du profil psycho » — nouvel item CIRCLE_ITEMS `coordinateur-catalogue` (thème "Passages réels (smoke run)"). (3) Précision explicite sur le format : « en format html si c'est un nouveau catalogue jamais produit, en txt si c'est un catalogue qui n'a pas changé » — `buildCatalogDelivery()` réutilise directement le booléen `written` de `recordCatalog()`. (4) Question directe de l'utilisateur : « on est d'accord que le coordinateur a pris le soin de réfléchir aux différentes combinaisons... ? » — vérifié honnêtement : NON, seules 2/14 lignes combinaient réellement plusieurs outils ; écart réel trouvé au passage : `runNetworkCheck()` (6 outils combinés, déjà exposé dans la Ronde via `network-check-run`) n'avait AUCUNE ligne PRESTATIONS — corrigé (« Pack Panorama » ajouté). Revue combinatoire rendue SYSTÉMATIQUE (demande explicite : « je veux que ce soit le coordinateur qui réalise systématiquement cette passe ») dans l'`execute` de l'item CIRCLE-TASKS, jamais une heuristique mécanique inventée dans le code (LE-COORDINATEUR ne raisonne jamais lui-même, §7ter). (5) Vrai bug trouvé en conditions réelles (pas en test) : le nom de fichier `YYYY-MM-DD.md` a fait collisionner deux vraies versions tombées sur la même date calendaire (décalage d'horloge du conteneur, déjà documenté ailleurs) — la première version (15 offres) a été silencieusement écrasée par la seconde (16 offres) tout en laissant une ligne d'index périmée. Corrigé à la racine (Article 3) : précision à la minute (`YYYY-MM-DD-HH-MM.md`), même convention que les scans ARGUS ; ancien dossier buggé supprimé et régénéré proprement. (6) Nouvelle règle générale ajoutée à docs/regles-de-travail.md §7 (demande explicite : « quand un rapport est produit, tu dois le lire entièrement aussi ») : tout rapport produit doit être relu en entier par l'agent avant de répondre — pour un rapport récurrent comme ce catalogue, un commentaire court et réel dans la conversation à chaque mise à jour (démontré en direct après la régénération : 16 prestations, avis sur Pack Sentinelle/Pack Panorama/packs coûteux rares). `lib/reference.ts` Version 178, tests dédiés (renderNamedCatalog/recordCatalog/buildCatalogDelivery, y compris le cas de collision same-day corrigé), `check-house.mjs` vert, `tsc --noEmit` sans nouvelle erreur, vérifié en conditions réelles (pas seulement fixtures) via `node scripts/le-coordinateur.mjs catalogue` | terminée — fidèle |

| 239 | 2026-09-21T04:10Z | Nouvel outil / Charte | LE-COORDINATEUR : catalogue — séparation coût API/tokens, description par offre, réordonnancement (#154 suite) | normal | Suite directe de la tâche #238 dans la même soirée, deux demandes explicites successives. (1) « on est ok qu'il y a à chaque fois un prix en token et api ? [...] une courte description de la prestation » : vérification honnête plutôt qu'un accord réflexe — plusieurs entrées n'avaient qu'une seule dimension de coût (Pack Provocation sans coût token, Pack Ronde sans mention explicite "0 appel API"). PRESTATIONS restructurée : `cout` (Gemini/API seul), nouveau `tokensEstimes` (Claude seul, même séparation déjà validée pour CIRCLE_ITEMS), nouveau `description` (ce que délivre l'offre, distinct de `demande`). Deuxième revue combinatoire plus profonde ("TOUTES CELLES qui peuvent être utiles") : 2 combinaisons réelles encore absentes trouvées et ajoutées — "Pack Éclaireur" (AXA-CHECK + CLEAN-DIRTY-OLD + nœuds sensibles HARMONIA, contrôle ciblé sur un seul fichier) et "Pack Décollage" (Smart Conso API + carnet de correctifs, contrôle pré-simulation, jamais combinés avant). `renderNamedCatalog()` étendue (2 nouvelles colonnes, tiret honnête sur fixture ancienne). (2) « en premier, sont présentés les outils seuls, avant les combinaisons » : PRESTATIONS réordonnée, 13 offres à un seul outil puis 5 combinaisons par nombre croissant d'outils — vérifié qu'aucun test ne dépendait de l'ordre avant de réordonner. `docs/le-coordinateur-catalogue/` régénéré en conditions réelles (nouvelle version historisée à la minute, l'ancienne conservée intacte). En parallèle, question de l'utilisateur sur l'allègement de CLAUDE.md : découvert que CLAUDE.MD.SPY (`buildClaudeMdRuleTable()`/`renderClaudeMdRuleTable()`, scripts/smart-conso-token.mjs) existait déjà, construit lors d'une session antérieure mais jamais exécuté pour de vrai contre le fichier réel — lancé, résultat livré à l'utilisateur avec mise en garde sur un artefact de mesure (Article 19 attaché à tort 251 lignes du bloc Gemini non numéroté). `lib/reference.ts` Version 179, tests `renderNamedCatalog()` étendus, `check-house.mjs` vert (135 assertions), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 240 | 2026-09-21T04:25Z | Correctif de code / Outillage de travail | LE-COORDINATEUR : le tableau HTML du catalogue n'affichait pas les colonnes Description/Coût tokens (#154 suite) | normal | Trouvé en vérifiant honnêtement une question de l'utilisateur (« ne sont sauvegardées que les versions HTML puisque les fichiers txt ne comportent aucune nouveauté ? ») plutôt qu'en répondant sans relire le code (Article 19). Réponse à la question elle-même : non, l'archivage disque (recordCatalog(), toujours un .md, écrit seulement si le contenu a changé) et le choix HTML/texte de livraison (buildCatalogDelivery(), même booléen `written`) sont deux mécanismes séparés — rien n'est perdu quand rien n'a changé. Mais en relisant buildCatalogDelivery() pour répondre précisément, trouvé que son tableau HTML utilisait encore les 4 anciennes colonnes, jamais mis à jour après que renderNamedCatalog() a gagné Description/Coût tokens (Version 179) — la voie de livraison la plus importante (contenu réellement changé) aurait donc perdu ces deux colonnes. Corrigé : mêmes 6 colonnes des deux côtés, jamais une structure divergente. Nouveau test dédié, `lib/reference.ts` Version 180, `check-house.mjs` vert (135 assertions), `tsc --noEmit` sans nouvelle erreur, vérifié en conditions réelles (`node scripts/le-coordinateur.mjs catalogue` confirme qu'aucune donnée réelle n'a changé) | terminée — fidèle |

| 241 | 2026-09-21T05:10Z | Charte / Nouvel outil | Allègement ciblé de CLAUDE.md : Articles 18/20/21/22/23 + nouvel outil LE-RÉGISSEUR (#162) | important | Demande explicite : « redonne moi la liste des principes de claude.md et je te dis s'il y en a qu'on peut basuler dans les regles de travail ». CLAUDE.MD.SPY (déjà construit, jamais exécuté pour de vrai) lancé contre le fichier réel — confirmé Articles 20/21/22/23 comme dupliqués avec la fiche technique dédiée de chaque outil déjà existant (argus.md, harmonia.md, axa-check.md, clean-dirty-old.md, hyper-scan-checkpoint.md, always-new-code.md, docs/regles-de-travail.md §7ter) : réduits à un aiguillage court. Article 18 (protocole de simulation, 79 lignes) : correction explicite de l'utilisateur en cours de route (« je parle d'un script au statut membre de l'équipe », pas l'outil Agent à ~37 000 tokens fixes) — protocole intégral déplacé vers docs/regles-de-travail.md §6bis (numérotation préservée), nouvel outil LE-RÉGISSEUR (scripts/le-regisseur.mjs) construit après calibrage en 3 questions (nom, périmètre d'automatisation, doc dédiée) : orchestre mécaniquement l'archivage transcript/dossier/résumé + le rapport KPI, jamais les deux index de jugement (docs/simulations/index.md, docs/referentiel/kpi-index.md — frontière trouvée en lisant leur propre règle avant de coder, Article 19). Nouveau principe général ajouté à smart-conso-token.md (une règle servie par un outil fiable devient un stub). Question de vérification de l'utilisateur sur Articles 4/5/7/8 : analysés en détail et laissés INTACTS, chacun justifié individuellement (contenu créatif, réflexe à l'écriture, version informelle d'un outil rare, protection explicite d'une architecture contre un outil). Vérifié avec diffNormativeMarkers() (40 fragments signalés, tous confirmés déplacés, aucun perdu). Gain honnête mesuré : ~3592 tokens estimés/tour (~13%), le plus gros à ce jour. `lib/reference.ts` Version 181, `check-house.mjs` vert (136 assertions), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 242 | 2026-09-21T05:20Z | Charte / Documentation | Nouveau principe d'arbitrage capturé dans philosophie-et-politique.md (§2.9) | normal | Demande explicite de l'utilisateur après la passe d'allègement de CLAUDE.md : « retiens cette experience avec moi pour les prochaines tensions ». Nouvelle section 2.9 « Un outil fiable allège une règle, mais ne la remplace jamais par association » (étiquette [Synthèse]) : généralise le test appliqué ce soir (comparer le texte de la règle phrase par phrase à ce que l'outil couvre réellement, jamais juger sur la seule présence du nom de l'outil) — avec l'exemple concret des 5 Articles réduits (18/20/21/22/23) contre les 4 conservés intacts (4/5/7/8), et la Portée générale réutilisable sur un futur projet | terminée — fidèle |

| 243 | 2026-09-21T05:35Z | Correctif de code / Outillage de travail | recordAction() : reductionPct jamais réellement câblé, computeAdoptionKpi() jamais alimenté depuis sa création (#162 suite) | normal | Demande explicite : « alimente aussi l'outil dédié avec le travail qu'on vient de faire » puis « alimente aussi l'outil qui sert à faire ce travail sur claude.md le spy ». En voulant nourrir computeAdoptionKpi() avec le travail de ce soir, trouvé que recordAction() ne faisait jamais passer reductionPct depuis ses options — le commentaire du code prétendait déjà que c'était le cas, jamais vérifié. Corrigé (scripts/smart-conso-token.mjs), nouveau test avec backup/restore du vrai fichier local. Travail de ce soir réellement enregistré dans .smart-conso-token-history.json (local, jamais committé) : un scan CLAUDE.MD.SPY réel (zoomé, 23461 tokens) et une proposition_appliquee (13% de réduction) — computeAdoptionKpi() rapporte désormais un vrai chiffre au lieu d'une absence permanente. lib/reference.ts Version 182, check-house.mjs vert (137 assertions), tsc --noEmit sans nouvelle erreur | terminée — fidèle |

| 244 | 2026-09-21T06:00Z | Conception / Nouvel outil (INES-official, MEMENTO) | Rattrapage : deux noms d'outils capturés en conversation jamais écrits nulle part de durable | important | Écart réel trouvé en vérifiant les outils encore non concrétisés (question directe de l'utilisateur) : INES-official et MEMENTO, nommés lors d'une session antérieure, n'existaient dans AUCUN document du projet (zéro trace dans docs/suivi/, CLAUDE.md ou ailleurs) — risque réel de perte totale si la conversation avait été compactée ou interrompue avant ce rattrapage (exactement le risque que §2.8 de docs/philosophie-et-politique.md décrit). Capturé ici pour ne rien perdre, rien construit ce soir. **INES-official** : outil "membre de l'équipe" imaginé comme une secrétaire qui fournit le code du projet entier, propre, nettoyé, en une version de référence — rôle encore à trancher (placement d'équipe probable : "Membre de l'équipe" occasionnel comme HYPER-SCAN-CHECKPOINT/THE-FINAL-JUDGE/ALWAYS-NEW-CODE, jamais "Équipe noyau" qui reste réservée aux 4 outils toujours déployés de l'Article 20), nécessite un répertoire dédié pour stocker les versions de référence datées, un blueprint propre, et un "rapport" atypique : le code source lui-même en fichier texte plutôt qu'un rapport de synthèse. Piste de conception avancée par l'agent, jamais validée par l'utilisateur : plutôt qu'une réécriture réelle du code (risquée, coûteuse), un MVP sûr consisterait à aplatir le dépôt en un seul fichier texte consolidé, annoté avec les signaux déjà calculés par ARGUS/HARMONIA/AXA-CHECK/CLEAN-DIRTY-OLD (fragilité, staleness) — pourrait aussi réduire le coût futur d'un THE-FINAL-JUDGE en lui donnant un instantané pré-construit plutôt qu'une exploration à froid. **MEMENTO** : outil de gestion de la mémoire des personnages (Lia/Noé) — nom seul acté, aucune conception de rôle/mécanisme encore engagée | terminée — fidèle |

| 245 | 2026-09-21T07:10Z | Conception / Nouvel outil (organigramme, CASSANDRA-RH) | Correction de l'organigramme (#222) : catégorie "Personnages" (Lia/Noé) manquante, cause directe d'une vraie erreur | important | Trouvé en construisant MEMENTO : j'ai proposé (à tort) que le verdict de MEMENTO — pensé pour la mémoire narrative de Lia/Noé — devienne une condition du badge des MEMBRES DE L'ÉQUIPE (scripts comme ARGUS/HARMONIA), une confusion de catégorie que l'utilisateur a immédiatement relevée. Root-cause : l'organigramme capturé en #222 (Direction/Équipe noyau/Membre de l'équipe/VIP) ne mentionne nulle part Lia et Noé, alors qu'une note de conception antérieure (2026-09-20T09:40Z) les incluait explicitement comme "mascottes" à noter par CASSANDRA-RH — la case restait vide, exactement l'ambiguïté qui a permis l'erreur. Corrigé en ajoutant une 5e catégorie explicite, structurellement distincte des 4 autres : **Personnages** (Lia, Noé) — entités narratives propulsées par Gemini, jamais évaluées avec les concepts de "membre de l'équipe" (aucun badge, aucun blueprint, aucune couverture AXA-CHECK — rien de tout ça n'a de sens pour un personnage). CASSANDRA-RH les évaluera un jour selon des critères complètement différents (fidélité au personnage, cohérence narrative — jamais une couverture de test). Règle durable actée : **MEMENTO cible exclusivement la catégorie Personnages, jamais Membre de l'équipe** — aucun outil de mémoire/cohérence ne doit jamais s'appliquer aux scripts de travail, qui n'ont pas de mémoire narrative comparable. Rien construit ce soir côté code, correction de conception uniquement | terminée — fidèle |

| 246 | 2026-09-21T07:40Z | Nouvel outil / Correctif de code | AXA-CHECK étendu aux scripts, badge 3 niveaux corrigé (format exact + 4e membre), garde-fou Personnages (#163/#164/#245) | important | AXA-CHECK : AGENT_SCRIPT_FILES/collectScriptCoverage()/scriptRobustnessScore() (scripts/axa-check.mjs) mesurent la couverture réelle des ~14 outils à badge (noms de fichiers réels non uniformes, jamais déduits d'un slug). Badge : implémenté avec le format exact retrouvé sur relecture explicite demandée par l'utilisateur (tâche #226, jamais deviné) — chaque KO nommé séparément, virgule si plusieurs, pourcentage entre parenthèses uniquement pour AXA-CHECK. Écart trouvé en répondant à une question directe de l'utilisateur : "OK 100%" n'incluait que 3 des 4 membres de l'équipe noyau (CLEAN-DIRTY-OLD manquant) — corrigé pour exiger les 4 ensemble. Date de dernière vérification ajoutée au message. Erreur de conception réelle trouvée et corrigée : proposé (à tort) que MEMENTO (mémoire de Lia/Noé) devienne une condition du badge des scripts — root-cause : l'organigramme (#222) ne mentionnait jamais Lia/Noé, laissant la case vide. Nouvelle catégorie "Personnages" actée + garde-fou mécanique PERSONNAGES/assertNotAPersonnage() (scripts/lib-shell.mjs, partagé pour MEMENTO) qui refuse explicitement tout appel de checkAgentOnboarding() sur Lia/Noé. `lib/reference.ts` Version 183, `check-house.mjs` vert (139 assertions), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 247 | 2026-09-21T08:10Z | Nouvel outil / Membre de l'équipe | Compteur d'utilisation des outils construit (tâche #166) | normal | Nouveau module `scripts/tool-usage.mjs` — journal auto-déclaré (même honnêteté que SMART-CONSO-TOKEN : aucune preuve mécanique externe qu'un outil a été sollicité) dans `.tool-usage-history.json` (local, jamais committé, cumul PERMANENT jamais remis à zéro, décision explicite de l'utilisateur). `recordToolUsage(toolSlug, origin, now, foundSomething)` capture l'ORIGINE (spontane/demande/automatique_post_commit) et un verdict optionnel de trouvaille ; `toolUsageStats()` calcule un vrai taux de trouvaille (discipline anti-vanity-metric déjà éprouvée pour ALWAYS-NEW-CODE/THE-DEEP-READER) ; `toolsNeverUsed()` compare la liste réelle des outils connus aux événements enregistrés, pensé pour nourrir la tâche #165 (Doc-Report) et la future CASSANDRA-RH. Testé contre le vrai fichier local avec sauvegarde/restauration complète. `lib/reference.ts` Version 184, `check-house.mjs` vert, `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 248 | 2026-09-21T08:20Z | Méthode de travail / Suivi | Correction : ligne #192 restée "ouverte" à tort, catalogue déjà livré | normal | Repéré en lisant le rapport check-tasks-details en_cours (Article 7 — épreuve de la relecture périodique) : la ligne #192 (mise en file du catalogue LE-COORDINATEUR) était encore listée "ouverte" alors que la tâche #154 qu'elle décrit est terminée depuis plusieurs sessions (renderNamedCatalog(), docs/le-coordinateur-catalogue/, garde-fou de fraîcheur #160). Corrigé directement sur la ligne #192 plutôt que par une note séparée qui aurait laissé l'écart en place | terminée — fidèle |

| 249 | 2026-09-21T08:35Z | Conception / Nouvel outil (KPI-hunter) | Idée proposée par l'utilisateur, jugée redondante avec l'existant — pas construite | normal | Demande explicite (« si pertinent ») : un agent "KPI-hunter" dont le rôle serait de récolter des KPI à toutes les occasions possibles pour alimenter le tableau de bord. Évalué avant construction (l'utilisateur a lui-même laissé le jugement de pertinence à l'agent) : ce rôle est déjà largement couvert par l'infrastructure existante — `kpi-report.mjs` agrège déjà toutes les familles à la demande, LE-RÉGISSEUR (#162) le lance et l'archive automatiquement après chaque simulation (Article 18 étape 4), CIRCLE-TASKS l'inclut déjà dans sa Ronde périodique, et chaque outil individuel (SMART-CONSO-TOKEN, tool-usage.mjs, etc.) s'auto-alimente déjà à l'usage plutôt que via une collecte externe. Un agent séparé dont le seul rôle serait de "provoquer" plus de collecte dupliquerait ces trois points d'entrée sans ajouter de mécanisme nouveau — recommandation : ne pas construire de nouvel agent, mais renforcer directement la tâche #145 déjà ouverte (audit périodique complet du système tableau de bord/KPI), qui couvre exactement le même besoin (vérifier que toutes les familles existent et sont à jour) sans complexifier davantage l'organigramme. Idée capturée ici pour ne rien perdre (§2.8 philosophie-et-politique.md), jamais implémentée | terminée — fidèle |

| 250 | 2026-09-21T09:15Z | Nouvel outil / Membre de l'équipe | Doc-Report construit (tâche #165) | normal | Nouveau module `scripts/doc-report.mjs`, statut "sans blueprint" (LE-COORDINATEUR/CIRCLE-TASKS/html-report.mjs). Table REGISTRIES (18 registres réels) porte famille + décision HTML/texte déjà actée (#230). checkHtmlWiring() vérifie mécaniquement cette décision contre le vrai code (import de html-report.mjs), jamais deviné — trouve dès son premier lancement 2 écarts réels (THE-DEEP-READER, Simulations jamais câblés malgré la décision). registryAge() réutilise lastTouchDays() de CLEAN-DIRTY-OLD ; buildDocReportIndex() croise avec toolsNeverUsed() de tool-usage.mjs (#166). findRegistriesMissingDecision() = 6e type de gap de checkAgentOnboarding() (paramètre hasDocReportDecision, undefined par défaut). Documenté dans CLAUDE.md (2 nouveaux paragraphes : tool-usage.mjs + Doc-Report) et docs/regles-de-travail.md §7ter (2 nouvelles lignes de table). Nouvelle entrée "Pack Registre" dans PRESTATIONS. `lib/reference.ts` Version 185, `check-house.mjs` vert, `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 251 | 2026-09-21T09:40Z | Conception / Nouvel outil (CASSANDRA-RH) | Décision : CASSANDRA-RH reprend le flambeau complet du KPI (dédoublonnage avec l'existant) + 2 premières missions actées | important | Demande explicite de l'utilisateur, en plusieurs messages : « ce sera elle qui fera veritabelement entrer en scene les kpi, ca fait partie de son role : elle challenge [...] il faut peut etre mutualiser, dedoublonner certaines taches de cassandra avec les kpi [...] j'aimerais bien que ce soit elle qui reprenne le flambeau et soit responsable de toute la partie kpi. » Vient enrichir directement le design déjà capturé à la ligne #—/2026-09-20T09:40Z : CASSANDRA-RH devient responsable de TOUTE la partie KPI (organiser, fiabiliser), pas seulement d'un signal RH en plus — implique de revoir, au moment de sa construction réelle, la frontière avec kpi-report.mjs/le tableau de bord existant plutôt que d'empiler une seconde couche (même principe que docs/philosophie-et-politique.md §2.9 : un outil fiable existant s'absorbe, jamais dupliqué à côté). **Deux premières missions actées, dans cet ordre** : 1/ faire entrer réellement les KPI dans le travail quotidien, organiser ce point, le fiabiliser (rejoint directement la tâche #145 déjà ouverte). 2/ RECRUTEMENT — une compétence de recherche web ciblée pour trouver des scripts/extensions/skills utiles (cf. la section sur les skills déjà évoquée précédemment, à retrouver avant la construction réelle), un processus en 3 temps (CV provisoire → entretien préliminaire qui restreint la liste → proposition d'une ou deux embauches pertinentes sur demande de l'utilisateur, jamais spontané). CASSANDRA-RH produit aussi, régulièrement dans la Ronde périodique, un rapport RH chiffré (effectif, organigramme à jour, besoins de recrutement, nouveaux candidats à étudier). **Exigence explicite de rigueur à l'intégration** : bien harmoniser avec le processus déjà prévu d'intégration d'un nouvel employé et le système de badge existant — utiliser les autres outils du réseau si besoin, précision et exactitude requises. **Prise de fonction complète actée** : à son arrivée, CASSANDRA-RH crée les fiches de chaque membre, l'organigramme, les documents prévus, met en place le processus badge et vérifie que tout le monde a bien son badge — une prise de fonction totale, pas une mise en route partielle. Rien construit ce soir (CASSANDRA-RH reste après les 7 tâches en cours, confirmé une nouvelle fois par l'utilisateur : « mais onfinit deja tout le reste avant cassandra »), capturé ici pour ne rien perdre avant le round de calibrage #134 | terminée — fidèle |

| 252 | 2026-09-21T10:20Z | Nouvel outil / Agent complet | THE-KING construit (tâche #167) | important | Nouvel Agent complet (blueprint `docs/the-king-blueprint.md`, instanciation `docs/referentiel/the-king.md`, registre `docs/the-king/`), `scripts/the-king.mjs`. 6 catégories de déclenchement (classifyDecisionTriggers/reminderFor), parseur dédié du document de philosophie (extractPrincipleUnits, distinct d'extractRuleUnits de CLAUDE.MD.SPY), digest chronologique daté (buildEvolutionDigest), détection de tension possible entre deux principes (findPossibleTensions, enrichissement "oui maintenant" — vocabulaire partagé ET polarité divergente, zéro tension trouvée sur le document réel), fraîcheur (philosophyFreshnessDays, réutilise lastTouchDays). Câblé dans CIRCLE-TASKS dès la création (item the-king-signal) — le garde-fou de fraîcheur de la Ronde l'exigeait. Entrée PRESTATIONS "Pack Régence", entrée Doc-Report REGISTRIES. `lib/reference.ts` Version 186, `check-house.mjs` vert, `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 253 | 2026-09-21T10:35Z | Méthode de travail / Suivi | Renforcement de la règle de sollicitation réelle des outils : mentionner systématiquement le coût (gratuit) | normal | Demande explicite de l'utilisateur : « quand je te fais une demande qui peut faire intervenir un outil, fais intervenir l'outil [...] le cout est gratuit, et mentionne le systematiquement ». Vient renforcer une règle déjà existante (docs/regles-de-travail.md, section « Recevoir une sortie d'outil n'est pas la même chose que la traiter », complément du 2026-09-20 déjà en place) : ajouté que la mention doit toujours nommer explicitement le coût réel (« gratuit — 0 appel API »), jamais seulement le nom de l'outil sollicité. Questions posées en même temps par l'utilisateur, réponses données dans la conversation (jamais construites, une réponse honnête sur la pratique réelle) : (1) l'agent bénéficie réellement de l'organisation des rapports/index (lecture directe de docs/suivi/, docs/referentiel/, Doc-Report ce soir) ; (2) la recherche spontanée dans ces documents reste réelle mais pas systématique à 100% — reconnu honnêtement, à corriger par cette même règle renforcée ; (3) pour une recherche demandée par l'utilisateur, l'outil dédié (grep, check-tasks-details, Doc-Report, etc.) doit désormais être systématiquement sollicité plutôt que répondu de mémoire | terminée — fidèle |

| 254 | 2026-09-21T11:10Z | Nouvel outil / Agent complet | INES-official construit (tâche #168) | important | Nouvel Agent complet (blueprint `docs/ines-official-blueprint.md`, instanciation `docs/referentiel/ines-official.md`, registre `docs/ines-official/`), `scripts/ines-official.mjs`. MVP restreint (aplatir + annoter, jamais réécrire — décision explicite). 2 périmètres choisis à chaque édition (code / code_et_docs). annotateFile() réutilise lastTouchDays() de CLEAN-DIRTY-OLD, limite honnête assumée sur ARGUS/HARMONIA (scans texte libre non indexés par fichier). 2 enrichissements "oui maintenant" : table des matières numérotée, datage/versionnage (nextEditionVersion/buildIndexRow). Économie de dépôt : le corps volumineux reste local (gitignored, jamais un historique), seule la métadonnée légère rejoint l'index committé — même raisonnement que le journal JSON des simulations. Câblé dans CIRCLE-TASKS dès la création (item ines-official-signal, déclenchement périodique jamais réservé à la demande). Entrée PRESTATIONS "Pack Secrétariat", entrée Doc-Report REGISTRIES. `lib/reference.ts` Version 187, `check-house.mjs` vert, `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 255 | 2026-09-21T11:35Z | Correctif de code / Enrichissement | Doc-Report étend son inventaire aux journaux locaux + INES-official gagne un rapport de synthèse avec crochet CASSANDRA-RH | important | Deux demandes explicites de l'utilisateur en cours de session. (1) « l'outil doc-report est aussi capable de les organiser + index ou c'est un outil jumeau qui doit le faire ? » — répondu et implémenté : MÊME outil, LOCAL_JOURNALS/auditLocalJournals()/findJournalsMissingFromGitignore() dans scripts/doc-report.mjs, vérifié vert contre le vrai .gitignore. (2) « inclure le rapport de Ines [...] elle fait ses commentaires [...] donne des chiffres intéressants [...] elle reprend aussi les données fournies par cassandra sur les KPI » — évalué la cohérence (strictement descriptif, jamais un jugement qui empiéterait sur ARGUS/HARMONIA/AXA-CHECK/CLEAN-DIRTY-OLD), implémenté buildEditionSummary()/renderEditionSummary() dans scripts/ines-official.mjs avec un crochet kpiFromCassandra explicite (null tant que CASSANDRA-RH n'existe pas, jamais fabriqué, cf. décision #251). `lib/reference.ts` Version 188, `check-house.mjs` vert, `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 256 | 2026-09-21T12:00Z | Conception / Nouvel outil (Doc-Report, compaction des journaux) | Idée proposée : compaction progressive des journaux locaux (brut → résumé → résumé de résumés) | important | Demande explicite de l'utilisateur : « pour la problematique de memoire locale : trouve un systeme qui permet d'archiver progressivement [...] au bout d'un certain temps, l'element du journal [...] est résumé [...] par lot [...] resumé synthese des résumés [...] le role de resumer est assumé par le script doc-report ». Analysé avant d'implémenter (Article 19) : vérifié en direct que les journaux locaux ne sont PAS tous homogènes — `.smart-conso-token-history.json` (8 entrées) et `.smart-conso-session.json` (18 entrées) sont de vraies listes qui grossissent, `.le-coordinateur-last-run.json`/`.circle-tasks-last-run.json` ne sont que des instantanés (jamais une liste) et n'ont donc aucun besoin de compaction. Recommandation donnée à l'utilisateur : Doc-Report SIGNALE (rôle de gardien déjà établi), mais l'exécution du résumé reste chez l'outil PROPRIÉTAIRE de chaque journal (seul à connaître son propre format), via une fonction de compaction partagée générique — jamais Doc-Report qui devrait connaître le format de tout le monde. Rien construit ce soir : l'utilisateur a confirmé finir MEMENTO (tâche #169) d'abord (« on finit toutes les taches en cours, yes ») ; seuils et périmètre exact à calibrer par questions une fois MEMENTO terminé | terminée — fidèle |

| 257 | 2026-09-21T12:45Z | Conception / Nouvel outil (MEMENTO, Smart Conso API) | Idée notée : Smart Conso API pourrait être éclairée par le poids réel mesuré par MEMENTO | normal | Demande explicite de l'utilisateur (« on est d'accord que le suivi conso beneficie des donnes MEMENTO ? ») — réponse donnée et confirmée à noter (« note le bien ! ») : SMART-CONSO-TOKEN ne doit JAMAIS consommer les données MEMENTO (hors de son domaine par écrit — jamais le texte envoyé à Gemini pour Lia/Noé, territoire Article 8/0). Smart Conso API non plus aujourd'hui (même exclusion écrite : « jamais la taille d'un texte »). MAIS une vraie connexion légitime existe, pas encore câblée : Smart Conso API sait QUAND les appels ont lieu mais pas COMBIEN chacun pèse réellement — MEMENTO pourrait l'éclairer sur le coût réel d'une simulation (nombre de tours × poids moyen par tour mesuré par MEMENTO), en sens unique, exactement le même principe déjà établi entre Smart Conso API et l'Article 8 (informe, ne tranche jamais). Rien construit ce soir : idée capturée pour ne pas la perdre, MEMENTO pas encore terminé au moment de la demande | terminée — fidèle |

| 258 | 2026-09-21T13:10Z | Méthode de travail / Feuille de route | Séquence actée entre la fin des tâches en cours et CASSANDRA-RH/la refonte graphique | important | Demande explicite de l'utilisateur, avec accord demandé explicitement (« Ok pour toi ? tu notes ca dans le plan ? ») : « entre cassandra et la refonte : ronde periodique, simulation, corrections eventuelles (cassandra, KPI, etc.), version de reference (Ines) et ensuite go avec au demarrage recrutement de Cassnadra ». Accord donné : la séquence a du sens (donne à CASSANDRA-RH une base vérifiée et à jour avant son recrutement, plutôt que de construire par-dessus un état non revalidé) et cohère avec l'instruction déjà actée « on finit toutes les tâches ouvertes avant cassandra puis la refonte ». **Ordre acté, à suivre une fois les 7 tâches de ce soir closes** : 1/ Ronde périodique CIRCLE-TASKS ; 2/ une simulation Article 18 complète ; 3/ corrections éventuelles trouvées (CASSANDRA n'existant pas encore à ce stade, seuls KPI/autres correctifs réels s'appliquent — la mention "cassandra" dans la demande visait la famille KPI que CASSANDRA reprendra plus tard, jamais un recrutement anticipé) ; 4/ une édition de référence INES-official ; 5/ SEULEMENT ENSUITE, démarrage du recrutement CASSANDRA-RH (round 2 de calibrage, tâche #134). Rien construit ce soir, décision de séquencement uniquement | terminée — fidèle |

| 259 | 2026-09-21T13:20Z | Conception / Nouvel outil (CASSANDRA-RH) | Deux idées de conception notées pour le round 2 de calibrage de CASSANDRA-RH : stagiaire pré-processeur + rapport de licenciements | normal | Deux demandes explicites de l'utilisateur, en cours de discussion, non construites (CASSANDRA-RH n'a pas encore démarré, cf. décision #258 juste au-dessus). (1) « il faudra un vrai systeme de recrutement à la disposition de cassandra : un autre agent qui lui mache le travail ? genre un stagiaire ? avec un systeme qui gere le tout ? » — piste donnée par l'agent : reprendre la répartition déjà établie ailleurs dans ce projet entre travail mécanique gratuit (LE-COORDINATEUR : agrège ce que d'autres outils ont déjà calculé) et jugement réel coûteux (THE-FINAL-JUDGE : ~37 000 tokens fixes par appel, Article 22/SMART-CONSO-TOKEN) — un « stagiaire » scripté et gratuit qui prépare/formate les candidats, une vraie étape de jugement CASSANDRA réservée à ce qui a vraiment besoin d'un jugement, plutôt qu'un second appel Agent complet à chaque passage de recrutement. (2) « aussi voir dans le rapport de cassandra les membres à licencier par ex » — un rapport RH de CASSANDRA (déjà acté en #251) devrait aussi signaler les membres sous-performants/inutilisés, en s'appuyant vraisemblablement sur `toolsNeverUsed()`/le taux de trouvaille de `scripts/tool-usage.mjs` déjà existant, plutôt qu'un second calcul. Les deux restent des pistes de conception, à retrancher/valider explicitement au round 2 de calibrage (tâche #134), jamais implémentées ce soir | terminée — fidèle |

| 260 | 2026-09-21T13:40Z | Nouvel outil / Agent complet | MEMENTO construit et clos (tâche #169) | important | Nouvel Agent complet (blueprint `docs/memento-blueprint.md`, instanciation `docs/referentiel/memento.md`, registre `docs/memento/`) — le seul outil du réseau ciblant les Personnages (Lia/Noé) plutôt qu'un membre de l'équipe. Précédé de deux investigations Article 19 (agents Explore séparés) : stockage de `Life` déjà rigoureusement plafonné (aucune reconstruction nécessaire) ; patron de persistance de `lib/gemini-keys.ts::episodes` identifié comme le patron exact à réutiliser pour le rôle (b). Deux rôles confirmés par calibrage explicite (AskUserQuestion, toutes réponses "recommandées" + les 3 vérifications de cohérence sélectionnées) : (a) `scripts/memento.mjs::checkMemoryCoherence()` — cohérence mécanique (ordre chronologique, remise à zéro suspecte, régression de gravité de worstMoment), jamais un second appel Gemini ; (b) `lib/memento-weight.ts::recordContextWeightSample()` — observation pure du poids réel envoyé à Gemini par tour, ajoutée juste après la construction du payload réel dans `lib/lia.ts::think()`, jamais utilisée pour modifier ce qui est envoyé (frontière Article 0/8). Persisté après coup dans `.memento-history.json` (local, gitignored) par `scripts/kpi-report.mjs::reportMementoWeight()`/`averageContextWeightByActor()`. Exclusion documentée de CIRCLE-TASKS (rôle a n'a de sens que sur une partie réelle, rôle b déjà rapporté dans la famille KPI). Entrée Doc-Report REGISTRIES (famille "Personnages (mémoire narrative)") et "Pack Mémoire" dans PRESTATIONS — vérifié explicitement avec l'utilisateur que Pack Mémoire cible bien les Personnages, jamais les membres de l'équipe (le risque de confusion inverse déjà rencontré une fois, #245). `lib/reference.ts` Version 189, `check-house.mjs` vert, `tsc --noEmit` sans nouvelle erreur. Clôt les 7 tâches de la soirée (#165 à #169 + les deux queued #256/#257) — prochaine étape : la séquence actée en #258 (Ronde → simulation → corrections → édition INES → CASSANDRA-RH) | terminée — fidèle |

| 261 | 2026-09-21T14:10Z | Charte / Outillage de travail | Correction structurelle : les Personnages (Lia/Noé) sortent entièrement de l'organigramme de travail | important | Demande explicite et insistante de l'utilisateur, après avoir remarqué un motif récurrent : « ca fait plusieurs fois qu'il y a un debut de chevauchement : il faut absolument trouver une solution. si tu veux, on sort les persos de l'équipe, ils sont juste des perso dans la simulation, aucune existence dans l'équipe » puis « marque bien la separation partout, dans les regles de travail, etc. fiabilise, integre ca aux process existants ». Root-cause identifiée avant correction (Article 19) : la correction de #245 avait ajouté « Personnages » comme une 5e catégorie DE L'ORGANIGRAMME lui-même (à côté de Direction/Équipe noyau/Membre/VIP) — un patch réactif qui gardait Lia/Noé dans le même tableau que l'équipe, permettant à la même confusion de revenir (constaté une 2e fois en évaluant si le menu PRESTATIONS de MEMENTO leur était réservé). Root-cause plus profonde encore : la règle n'a jamais vécu que dans une narration éparse (docs/suivi) et un commentaire de code, jamais comme une règle canonique d'un document de travail vivant — exactement le manquement que docs/regles-de-travail.md décrit déjà pour un autre cas (§7ter, « une règle transversale ne compte que si elle est câblée dans chaque checklist concrète »). **Corrections appliquées, en suivant ce même principe (câblage dans chaque checklist concernée, jamais une seule mention isolée)** : (1) `scripts/lib-shell.mjs::PERSONNAGES`/`assertNotAPersonnage()` reformulé d'une "catégorie interne" en une liste d'EXCLUSION au bord du domaine équipe (commentaire + message d'erreur réécrits). (2) Nouvelle règle canonique ajoutée dans `docs/regles-de-travail.md` §7ter (« Personnages (Lia, Noé) : hors de l'équipe, jamais une catégorie de l'organigramme »), câblée dans la checklist d'intégration d'un nouvel Agent juste au-dessus. (3) Correction explicite de la note fondatrice de CASSANDRA-RH (2026-09-20T09:40Z, qui prévoyait de « noter les mascottes Lia/Noé ») : elle pourra un jour agréger les verdicts narratifs déjà produits ailleurs (MEMENTO/EL-PROFESSOR/check-spirit.mjs), jamais les noter/scorer comme des membres — à appliquer explicitement au round de calibrage de la tâche #134. (4) Renommage en cascade : famille Doc-Report de MEMENTO ("Personnages (mémoire narrative)" → "Hors équipe (mémoire narrative des Personnages)"), CLAUDE.md et `docs/referentiel/memento.md` reformulés dans le même sens. (5) Pack Mémoire (PRESTATIONS) confirmé légitime après vérification explicite : l'éligibilité du catalogue a toujours été « déclenche un outil membre de l'équipe », jamais « cible un membre » — aucune régression nécessaire. `lib/reference.ts` Version 190. Ne modifie aucun comportement en jeu, uniquement l'organisation et la documentation du réseau d'outils | terminée — fidèle |

| 262 | 2026-09-21T14:20Z | Conception / Nouvel outil (CASSANDRA-RH) | Deux nouveaux rôles notés : alerte KPI proactive + vision croisée code/membres, plus un audit HARMONIA du système KPI planifié | normal | Demande explicite de l'utilisateur, non construite (CASSANDRA-RH n'a pas encore démarré, cf. #258) : « cassandra suit les KPI et te remonte des alertes si des KPI sont trop faibles ou si des membres n'atteignent pas leurs objectifs. Cassandra a une vison intelligente du rapport entre la performance du code/du site et les resultats des membres. tout le systeme de kpi est bien interconnecté. On passera ce systeme au peigne fin avec Harmonia pour voir si on en as oublié des connexions. » Rejoint et enrichit la décision #251 (CASSANDRA reprend toute la partie KPI) et l'idée déjà en file du 2026-09-20T09:47Z (score cible par membre) : (1) rôle d'ALERTE proactive sur KPI trop faibles ou objectifs de membre manqués, jamais une simple lecture passive du tableau de bord ; (2) une lecture croisée explicite performance code/site ↔ résultats des membres, jamais deux familles de chiffres lues séparément. Action future distincte notée : un vrai passage HARMONIA dédié au système KPI lui-même (vérifier qu'aucune connexion entre familles/outils n'est oubliée) — à planifier une fois CASSANDRA-RH construite, rejoint la tâche #145 déjà ouverte (audit périodique du tableau de bord/KPI) sans la remplacer. Rien construit ce soir, capturé pour le round de calibrage #134 | terminée — fidèle |

| 263 | 2026-09-21T14:25Z | Méthode de travail / Ronde périodique | Ronde CIRCLE-TASKS lancée (sélection recommandée) — 1er pas de la séquence actée en #258 | normal | Demande explicite : « lance la ronde periodique ». Garde-fou en 2 temps respecté (AskUserQuestion, « lancer avec les paramètres recommandés » choisi). Items recommandés exécutés : digest THE-KING (0 tension, philosophie modifiée il y a 0j) ; rapport KPI (`kpi-report.mjs` — robustesse du code 100%, alerte réelle notée : couverture du tableau de bord à 33%, normal en l'absence de serveur/simulation en cours, attendu résolu par l'étape 2 de #258) ; signal ALWAYS-NEW-CODE (zone la plus négligée toujours "Fatigue", inchangé) ; signal CLEAN-DIRTY-OLD (1 jour) ; vérification câblage HTML (tous câblés) ; poids CLAUDE.md (25170 tokens, "élevé", aucune aside narrative datée restante — tri désormais entièrement manuel) ; relecture carnets correctifs/points fragiles (tous les correctifs encore à 0/2, rien à retirer, en attente de la simulation à venir) ; tâche ouverte la plus ancienne signalée ; scan Smart Conso API (aucun schéma coûteux, aucune salve non confirmée) ; scan SMART-CONSO-TOKEN portée Global sur CLAUDE.md/regles-de-travail.md/philosophie-et-politique.md (~68882 tokens combinés, hausse apparente vs dernier scan expliquée par un changement de PORTÉE — dernier scan "zoomé" sur CLAUDE.md seul, jamais une vraie dégradation) ; signal édition INES-official (aucune encore produite — normal, réservée à l'étape 4 de #258) ; garde-fou profil-utilisateur (7 fiches, aucun lien mort) ; synthèse réseau LE-COORDINATEUR complète (`runNetworkCheck()` — tout vert sauf le signal ARGUS déjà connu, 6 champs life.ts potentiellement jamais lus, suivi dans docs/argus/) ; catalogue LE-COORDINATEUR régénéré (Pack Mémoire intégré, nouvelle version réelle livrée en HTML à l'utilisateur). Items exclus par la sélection recommandée (jamais lancés) : relecture exhaustive du référentiel, photo de la dream team, THE-SCREENER (pas de session active), THE-FINAL-JUDGE, THE-DEEP-READER. Nouvelle fiche de profil utilisateur écrite (`docs/profil-utilisateur/observations/2026-09-21-1410.md`) | terminée — fidèle |

| 264 | 2026-09-21T14:40Z | Charte / Outillage de travail | Nouvelle règle canonique : « Moteur du jeu vs Outillage de travail », deux natures de code jamais confondues | important | Question directe de l'utilisateur juste après la clôture de #261 : « MEMENTO n'est pas un membre de l'équipe je pense ? est-ce que regles-du-temps est un membre de l'équipe ? est-ce que regles de travail est un membre de l'équipe ? MEMENTO est similaire à ces scripts, on aurait pu l'appeler regles dela memeoire non ? » puis, après ma réponse identifiant la vraie root-cause : « il faut peut être créer 2 catégories de scripts ? [...] a toi de choisir la meilleure formule ». Root-cause : "MEMENTO" désignait sous un même nom deux artefacts de nature différente — `scripts/memento.mjs` (rôle a, une vraie vérification mécanique, structurellement un Membre de l'équipe comme `check-argus.mjs`) et `lib/memento-weight.ts` (rôle b, un point d'observation câblé dans `lib/lia.ts`) — le second n'a jamais été un outil de travail, il vit dans le MOTEUR DU JEU lui-même (terme déjà présent dans CLAUDE.md, « le moteur applicatif vit dans lib/ »), exactement la même catégorie que `lib/gemini-keys.ts`. **Choix retenu (2 catégories de code, jamais 3 — Personnages reste un axe séparé car ce n'est pas du code)** : Outillage de travail (`scripts/*.mjs`) = Membre de l'équipe (badge/blueprint/PRESTATIONS, inchangé) ; Moteur du jeu (`lib/*.ts`, `app/*`, `components/*`) = jamais un membre, jamais un badge, c'est le produit lui-même — vocabulaire réutilisé de CLAUDE.md plutôt qu'inventé. Nouvelle section canonique ajoutée dans `docs/regles-de-travail.md` §7ter, juste après la règle Personnages. Nouveau garde-fou mécanique `scripts/doc-report.mjs::findEngineCodeInRegistries()` : aucune entrée `REGISTRIES` ne doit jamais pointer son `scriptPath` vers `lib/`/`app/`/`components/` — vérifié vert contre l'état réel du dépôt (PRESTATIONS et CIRCLE_ITEMS ne référencent leurs outils que par nom, jamais un chemin, donc rien à vérifier mécaniquement de ce côté). `docs/referentiel/memento.md`, CLAUDE.md et le commentaire de `lib/memento-weight.ts` reformulés : MEMENTO n'est plus présenté comme « un outil » mais comme une initiative à deux facettes. Oubli corrigé au passage : le récapitulatif de fin de Ronde (#263) généré mais jamais livré — envoyé à l'utilisateur après qu'il l'a signalé. `lib/reference.ts` Version 191, `check-house.mjs` vert, `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 265 | 2026-09-21T15:00Z | Méthode de travail / Feuille de route | Séquencement revu : tâches ouvertes → CASSANDRA-RH → simulation (remplace l'ordre de #258) + 2 vrais manquements trouvés dans le traitement des sorties d'outils | important | Demande explicite : « et suite aux circle recap : pas d'actions entreprises ? c'est pas dans le process ? et avant de passer à la simulation, on finit les taches ouvertes, puis cassandra, puis simulation ». Vérification honnête contre la règle réelle (docs/regles-de-travail.md, « Recevoir une sortie d'outil n'est pas la même chose que la traiter ») : deux signaux de la Ronde #263 n'ont jamais reçu de traitement explicite (agir ou reporter avec une raison écrite) — (1) ALWAYS-NEW-CODE n'a JAMAIS tourné une seule fois depuis sa création (docs/always-new-code/index.md entièrement vide, vérifié en direct) — "Fatigue" n'est pas "la zone la plus négligée", c'est juste la première d'une liste où AUCUNE zone n'a jamais été examinée, signalé à chaque Ronde sans jamais être traité ni reporté avec raison ; (2) le poids de CLAUDE.md (25170 tokens, "élevé") a épuisé tous ses candidats mécaniques (passe #162) — la suite exige un tri manuel jamais programmé ni reporté explicitement. Le signal KPI (couverture 33%) est différent : sa raison de non-traitement (résolu structurellement par la prochaine simulation) était déjà suffisamment écrite. **Nouveau séquencement acté, remplace l'ordre de #258 (Ronde→simulation→corrections→INES→CASSANDRA)** : Ronde (#263, close) → **tâches ouvertes** (6, périmètre confirmé par question de calibrage : #137 auditer/fiabiliser Smart Conso API/SMART-CONSO-TOKEN, #138 accompagnement temps réel SMART-CONSO-TOKEN, #140 référentiel générique/spécifique Claude Code, #145 audit périodique tableau de bord/KPI, + les 2 trouvées ce soir : #170 premier vrai passage ALWAYS-NEW-CODE, #171 passe manuelle d'allègement CLAUDE.md) → **CASSANDRA-RH** (round de calibrage #134) → **simulation** Article 18. INES-official (édition de référence) et les corrections post-simulation restent dans la feuille de route mais leur position exacte n'est plus fixée par #258 — à recaler explicitement le moment venu | terminée — fidèle |

| 266 | 2026-09-21T15:10Z | Charte / Outillage de travail | Tâche #137 close après vérification : déjà fiabilisée en continu, rien à construire | normal | Reprise de #137 (« auditer et fiabiliser l'exploitation de Smart Conso API/SMART-CONSO-TOKEN partout où la charte l'exige »), déjà substantiellement travaillée le 2026-09-21T02:55Z (#235, retrouvé avant de refaire le travail, Article 19). Vérification live des trois garde-fous existants contre l'état réel du dépôt : `checkToolConnections()` (EXPECTED_CONNECTIONS — CLAUDE.md + 4 docs/referentiel/*.md de tools coûteux), `findJudgeSpawnsWithoutConsultation()` (THE-FINAL-JUDGE + THE-DEEP-READER) et `findUnconfirmedBursts()` (Smart Conso API) retournent tous `[]` — aucune régression malgré tous les outils ajoutés depuis #235 (THE-KING, INES-official, Doc-Report, MEMENTO, aucun n'appelant Gemini ni ne déclenchant d'agent séparé, donc aucune nouvelle entrée requise). Trouvaille structurelle : ces trois garde-fous ne sont PAS de simples vérifications ponctuelles — ils sont déjà exercés contre les VRAIS fichiers du dépôt à CHAQUE commit, bloquant le pre-commit hook (`scripts/check-house.mjs`, lignes ~3121 et ~4192-4199 pour `checkToolConnections()`, la synthèse `runNetworkCheck()` à 10 lignes pour les deux autres) — une fiabilisation permanente et auto-vérifiante, pas un audit ponctuel qui périmerait. Aucune liste `EXPECTED_CONNECTIONS`/costly manquante trouvée (seuls THE-FINAL-JUDGE/THE-DEEP-READER portent `costly:true` dans tout le dépôt, tous deux déjà couverts). Décision : ne rien construire de nouveau ici serait la mauvaise réponse à une question mal posée — inventer un correctif sur un système déjà fiable violerait le garde-fou ALWAYS-NEW-CODE (« vérifier d'abord que ce n'est pas déjà une décision assumée »). Tâche #137 fermée, rien construit ce soir | terminée — fidèle |

| 267 | 2026-09-21T15:25Z | Charte / Outillage de travail | Tâche #145 close : audit du tableau de bord/KPI, 2 écarts doc/code trouvés et corrigés + rapport de la Ronde archivé/indexé | important | Reprise de #145 (« vérifier que toutes les familles de KPI existent et sont bien à jour, vérifier le tableau de bord »). Vérification réelle plutôt qu'une confiance de mémoire : (1) `docs/referentiel/tableau-de-bord.md` affirmait encore "5 familles" dans la section couverture, faux depuis l'ajout de Smart Conso comme 6e famille le 2026-09-20 (le code, lui, utilise bien 6 depuis cette date) — corrigé avec note explicative. (2) La section "KPI — Mémoire des personnages (MEMENTO, rôle b)" (`reportMementoWeight()`), déjà réellement produite par chaque rapport depuis la construction de MEMENTO ce soir, n'était documentée nulle part — ajoutée, explicitement notée observationnelle et hors des 6 familles (même précédent qu'ARGUS/HARMONIA). (3) Écart de process trouvé en creusant `docs/referentiel/kpi-index.md` : le rapport KPI lancé pendant la Ronde de ce soir (#263) avait ajouté sa ligne au CSV automatiquement mais n'avait jamais été archivé (`docs/referentiel/kpi-rapports/manuel-2026-09-20T20-51.txt`, créé maintenant) ni indexé (ligne ajoutée à `kpi-index.md` avec comparaison honnête au run précédent full_sim16) — les deux étapes manuelles de la procédure, oubliées sur le moment. Vérifié sain par ailleurs : `KPI_HISTORY_COLUMNS` toujours synchronisé avec l'en-tête réel du CSV. `lib/reference.ts` Version 192, `check-house.mjs` vert (aucun code changé, uniquement de la documentation) | terminée — fidèle |

| 268 | 2026-09-21T15:55Z | Charte / Outillage de travail | Tâche #172 : MEMENTO renommé en memory-audit (rôle a, confirmé Membre de l'équipe) + memento weight (rôle b), ombrelle retirée, code des 2 rôles séparé | important | Nouvelle clarification directe de l'utilisateur après une première tentative erronée de l'agent (le même soir) de retirer MEMENTO de l'équipe : « memento audite la capacité des persos sur la memoire [...] donc oui, il fait bien partie de l'equipe aux cotés de el professor [...] on l'appellera memory-audit [...] on pourrait le laisser en 'memento weight' [...] ces 2 scripts ne doivent plus etre reunis dans le meme script ». Les 2 modifications prématurées (`scripts/le-coordinateur.mjs`, `scripts/doc-report.mjs`) annulées par `git checkout` dès réception du message de pause de l'utilisateur, avant la clarification. 4 questions de calibrage posées (surnom vs renommage de fichier → surnom, fichier technique inchangé ; séparation du code des 2 rôles → oui maintenant ; devenir du nom "MEMENTO" → disparaît complètement). Investigation confirmée : `scripts/memento.mjs` mélangeait bien le code des deux rôles (les 3 vérifications de cohérence ET les fonctions de persistance/agrégation du poids mémoire), contrairement à l'hypothèse initiale d'une séparation déjà propre — extrait dans un nouveau fichier `scripts/memento-weight.mjs`. Renommage effectué partout : `scripts/doc-report.mjs` (famille passée de "Hors équipe" à "Simulation & qualité narrative", aux côtés d'EL-PROFESSOR), `scripts/le-coordinateur.mjs` ("Pack Memory-Audit"), `scripts/kpi-report.mjs`/`scripts/check-house.mjs` (imports scindés selon le rôle), `scripts/circle-tasks.mjs`/`scripts/lib-shell.mjs` (renvois), `lib/memento-weight.ts`/`lib/lia.ts` (commentaires), `docs/regles-de-travail.md` §7ter et la règle "Moteur du jeu vs Outillage de travail". `docs/referentiel/memento.md` scindé en `memory-audit.md` + `memento-weight.md` ; `docs/memento-blueprint.md`/`docs/memento/` renommés en `docs/memory-audit-blueprint.md`/`docs/memory-audit/` (`git mv`) — choix assumé de ne PAS créer un second blueprint générique pour memento weight (jugé trop mince, une poignée de fonctions d'observation), documenté directement dans sa fiche d'instanciation. Historique conservé tel quel partout ailleurs (CLAUDE.md Versions 189-191, ce fichier lignes #264-267, kpi-index.md, archives KPI) — seuls les documents décrivant l'état ACTUEL du projet ont été corrigés (Article 13). `lib/reference.ts` Version 193, `check-house.mjs` vert (146 passed), `tsc --noEmit` sans nouvelle erreur (l'unique erreur restante, `vite.config.ts:60`, vérifiée pré-existante avant tout changement de ce soir via `git stash`). | terminée — fidèle |

| 269 | 2026-09-21T16:05Z | Conception / Nouvel outil (route-booster) | Chantier route.ts recalibré en 2 scripts liés : route-booster (opération de découpage) + route-find-booster (navigation post-découpage) | normal | Message reçu en parallèle du travail MEMENTO (#172) : « le script route-find-booster est la solution de navigation, cette solution appartient au modele mis à jour avec decoupage. le script route-booster est destiné a assurer l'operation de decoupage des maintenant ou si elle vient à etre à nouveau necessaire ». Système imaginé et expliqué à l'utilisateur : route-booster détecte mécaniquement les points de coupe candidats dans une fonction géante (bannières de commentaires, branches `if (body.mode ===`) + un indice de risque (fermetures/état partagé), sans jamais appliquer le découpage lui-même — l'agent extrait manuellement, une fonction à la fois, avec tests à chaque étape ; reste dans l'équipe pour re-signaler un futur fichier qui regonfle. route-find-booster n'a de sens qu'une fois le découpage fait : carte sémantique + recherche par concept sur les fonctions nommées, avec tag optionnel vers les 8 thèmes HARMONIA (réglerait au passage l'ambiguïté trouvée par la tâche #170 : route.ts pointait vers 3 zones ALWAYS-NEW-CODE à la fois). Deux questions de calibrage tranchées : construire les 2 scripts proprement avant de découper (plutôt que découper directement puis documenter après) ; statut "sans blueprint", comme LE-COORDINATEUR (pas de fiche générique/instanciation/registre séparés). Rien codé ce soir — remplace/précise la tâche #173 (« découper route.ts »), dont la description a été mise à jour en conséquence | terminée — fidèle |

| 270 | 2026-09-21T16:35Z | Conception / Nouvel outil (route-booster) | route-booster.mjs + route-find-booster.mjs construits, testés et vérifiés live contre route.ts — tâche #173 avancée (2 scripts prêts, découpage réel restant) | normal | Suite directe de #269. Les 2 scripts construits exactement comme conçus : `findCutPoints()`/`analyzeClosureRisk()`/`proposeDecomposition()` (route-booster.mjs) et `extractFunctionIndex()`/`tagHarmoniaThemes()`/`searchByConcept()`/`buildIndex()` (route-find-booster.mjs). Premier essai live contre le vrai route.ts : 0 candidat détecté — bug de calibrage trouvé et corrigé (la contrainte d'indentation ≤4 espaces supposait à tort une faible profondeur, alors que toute la fonction POST est indentée par un bloc `try` englobant réel) ; une fois retirée, 24 branches `if (x.y === "...")` réelles détectées avec un vrai indice de risque calculé pour chacune. 3 questions reçues en parallèle traitées : (1) booster check-house.mjs (4833 lignes) — possible mais avec route-find-booster généralisé à un motif de bloc `{...}` plutôt que route-booster (déjà bien découpé en petits blocs, pas de fonction géante) ; signalé, non construit (tâche implicite, pas encore numérotée séparément). (2) l'équipe suivi conso récupère-t-elle déjà memento weight ? Réponse honnête : non, pas encore construit — nouvelle tâche #174. (3) confirmé : un futur document `docs/referentiel/regles-de-la-memoire.md` (3e axe transversal, sur le modèle de regles-du-temps.md/regles-de-l-espace.md) comblerait le trou de nommage laissé par la disparition de l'ombrelle MEMENTO — nouvelle tâche #175, jamais construite ce soir. Documentés dans `docs/regles-de-travail.md` (statut "sans blueprint" + 2 lignes dans la carte des outils §7ter). `lib/reference.ts` Version 194, `check-house.mjs` vert (148 passed), `tsc --noEmit` sans nouvelle erreur. Reste à faire pour clore #173 : le vrai découpage de route.ts lui-même (pas fait ce soir, chantier distinct à part entière) | terminée — fidèle |

| 271 | 2026-09-21T17:20Z | Conception / Nouvel outil (find-booster) | find-booster promu Membre de l'équipe complet — 2 nouveaux motifs (renommage "route-" + généralisation blocs, puis tableau titré), recommendFindBooster(), badge/blueprint/instanciation/registre | important | Trois questions successives de l'utilisateur, chacune traitée avant de continuer : (1) « renomme route-find-booster en find-booster, plus logique, puisqu'il n'est pas restreint au fichier route.ts » — renommé (fichier inclus, jamais commité avant ce soir). (2) « assure toi que find booster est bien construit pour aider les 2 fichiers, autant l'un que l'autre » — bug réel trouvé en vérifiant : find-booster ne trouvait qu'1 fonction sur check-house.mjs (91 vrais blocs de test réels, jamais des fonctions nommées) — `extractBlockIndex()` ajouté, `buildIndex()` combine désormais les deux motifs. (3) « est-ce que find-booster ne devrait il pas aussi t'aider pour le fichier regles de travail » puis « ainsi que pour le fichier references.ts » — 2 motifs de plus : `extractHeadingIndex()` (titres Markdown, docs/regles-de-travail.md : 1896 lignes, 40 titres réels) et `extractTitledArrayIndex()` (tableau `{title,text}`, lib/reference.ts) — cette dernière question a révélé un vrai principe : lib/reference.ts ne fait que 132 lignes mais chaque entrée est un pavé de texte sur une seule ligne, donc le nombre de lignes seul est un mauvais indicateur. `recommendFindBooster()` ajouté en réponse directe (« est-ce que find-booster pourrait détecter quand un fichier est trop lourd [...] ou c'est toi fait cette analyse systématiquement ? ») : réutilise `estimateTokens()` de smart-conso-token.mjs (jamais une seconde formule), vérifié live à recommander lib/reference.ts (poids réel élevé, peu de lignes) et à écarter lib/house.ts (peu de lignes, poids réel faible) — la preuve vivante que le critère de poids réel réussit là où le nombre de lignes aurait échoué. Question « pourrait-il devenir ton compagnon du quotidien ? si oui, blueprint + bienvenue dans l'équipe » — 2 questions de calibrage tranchées (route-booster reste sans blueprint, seul find-booster promu ; paquet complet = standard déjà établi type ARGUS/HARMONIA) puis construit : `docs/find-booster-blueprint.md`, `docs/referentiel/find-booster.md` (incluant une obligation écrite d'usage réel avant toute lecture intégrale d'un gros fichier, même limite honnête que SMART-CONSO-TOKEN, renforcée par le rappel automatique post-commit du menu PRESTATIONS et par tool-usage.mjs), `docs/find-booster/index.md`, entrée Doc-Report REGISTRIES ("Outillage de navigation"), exclusion CIRCLE-TASKS documentée, statut "Agent" 🎖️ dans la carte des outils §7ter, 2 entrées PRESTATIONS ("Pack Découpage" pour route-booster, "Pack Boussole" pour find-booster — garde-fou de fraîcheur du catalogue déclenché et corrigé au passage). Question « synergies avec Doc-Report ? d'autres outils concernés ? » — 3 pistes identifiées et notées dans la fiche d'instanciation (Doc-Report : colonne "bénéficierait de find-booster", ALWAYS-NEW-CODE : carte du fichier avant le zoom, SMART-CONSO-TOKEN : import direct déjà câblé), aucune construite ce soir. Usage réel déjà journalisé (`recordToolUsage`) pour route-booster et find-booster. `lib/reference.ts` Version 195, `check-house.mjs` vert (148 passed, tests des 4 motifs + recommendFindBooster), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 272 | 2026-09-21T17:40Z | Charte / Outillage de travail | Écart de process trouvé et corrigé : le premier passage réel ALWAYS-NEW-CODE (tâche #170) n'avait jamais reçu sa ligne de suivi, rédigée seulement maintenant, a posteriori | important | Demande explicite de l'utilisateur : « un etat des taches à faire doit etre fait en conclusion de circle [...] fais un etat des lieux complets de staches. utilise l'outil » — `check-tasks-details.mjs projet_entier arborescence` lancé et lu en entier (205 tâches). En reconstituant l'état réel, deux écarts trouvés : (1) la ligne #165 restée "ouverte" malgré sa clôture réelle en #267 (corrigée juste au-dessus) ; (2) **celui-ci** : le premier vrai passage ALWAYS-NEW-CODE (survol des 8 zones, zoom profond sur "Fatigue"/`lib/simulation.ts`, livré par fichier joint plus tôt dans cette même session, avant la reprise du chantier MEMENTO) n'avait jamais reçu de ligne dans ce fichier — violation directe de la règle "chaque tâche substantielle DOIT être documentée dans docs/suivi/ dans le MÊME commit que le travail qu'elle décrit". Corrigé en la documentant ici, tardivement mais explicitement signalé comme tel (jamais silencieusement comme si c'était fait à temps). Rappel du contenu réel de ce passage : survol mécanique des 8 zones (signal de churn git réel via un script jetable), zone "Fatigue" retenue (seule zone à la fois assez volumineuse et au mapping non ambigu), SMART-CONSO-TOKEN consulté avant le zoom (verdict `investissement_reconnu`), lecture complète de `lib/simulation.ts` (219 lignes), 2 trouvailles CONFIRMÉES (les fonctions de couple `sharedActivityBonus`/`mutualAttraction` dupliquent le domaine de `lib/relationship.ts`, déjà existant pour ce sujet), 1 trouvaille PROBABLE (`tvPrograms`, contenu narratif, mieux placé dans `lib/story.ts`), 1 point À SURVEILLER (le vocabulaire intentions/pièces n'a pas de foyer naturel existant), 1 faux-positif écarté après vérification du garde-fou trottoirGranted (le calcul du visage semblait hors-sujet mais `lib/face-render.ts` documente déjà une séparation calcul/rendu voulue). **Statut réel, jamais appliqué** : aucun changement de code proposé n'a été appliqué — la question posée à l'utilisateur (« veux-tu que je fasse le déplacement des deux fonctions confirmées... et/ou celui de tvPrograms... ou préfères-tu attendre un futur passage ») est restée sans réponse, le fil de conversation étant parti vers MEMENTO puis route-booster/find-booster. Tâche #170 (interne) reste donc légitimement ouverte, en attente de cette décision — jamais fermée à tort | terminée — fidèle (le retard de documentation est corrigé ; la décision de fond, elle, reste en attente, cf. tâche interne #170) |

| 273 | 2026-09-21T18:10Z | Correctif de code / ALWAYS-NEW-CODE | Tâche #170 close : les 2 déplacements confirmés appliqués (`sharedActivityBonus`/`mutualAttraction` → `lib/relationship.ts`, `tvPrograms` → `lib/story.ts`) | important | Suite de #272 : décision restée en attente reçue (« Oui, applique les 2 déplacements »), puis « continue avec les tâches en cours ». Exécuté : les deux fonctions retirées de `lib/simulation.ts`, ajoutées à `lib/relationship.ts` (déjà le foyer d'`attractionAfterTurn`/`isInLove`/`sleepRoom`) avec un commentaire renvoyant à la tâche #170 ; `tvPrograms` déplacée dans `lib/story.ts` de la même façon. Sites d'import mis à jour (`lib/turn.ts`, `lib/world.ts`, `app/api/lia/route.ts`). Deux écarts réels trouvés et corrigés en vérifiant TOUS les usages (Article 3, jamais un déplacement sans grep exhaustif sur les 3 noms dans tout le dépôt, pas seulement `lib/`) : `scripts/check-house.mjs` importait encore `sharedActivityBonus` depuis l'ancien module transpilé `test-simulation.mjs` (corrigé, réimporté séparément depuis `test-relationship.mjs`) ; `app/page.tsx` importait encore `tvPrograms` depuis `@/lib/simulation` (corrigé, réimporté depuis `@/lib/story`) — ce second écart n'avait pas été vu par la recherche initiale de la tâche #170 (limitée à `lib/*.ts`), retrouvé seulement en élargissant le grep à tout le dépôt. Le point "à surveiller" (vocabulaire intents/rooms) et le faux-positif visage restent inchangés, comme conclu en #272. `npx tsc --noEmit` revérifié (seule erreur restante : `vite.config.ts:60`, pré-existante, non liée à ce chantier) ; `node scripts/check-house.mjs` vert (148 tests, dont ceux qui exercent `sharedActivityBonus` via son nouvel import). `lib/reference.ts` Version 196. Tâche #170 (interne) désormais fermée. | terminée — fidèle |

| 274 | 2026-09-21T18:45Z | Conception / Nouvel outil (CLONE-HUNTER) | CLONE-HUNTER construit, testé, câblé dans tout le réseau d'outils et vérifié sans écart par checkAgentOnboarding() | important | Question directe de l'utilisateur : « est-ce qu'on a deja un outil qui traque les redondances, repetition, duplicatas, dans le code ? » — vérifié (Article 19) en lisant les fonctions exportées d'ARGUS/HARMONIA/AXA-CHECK/CLEAN-DIRTY-OLD une par une (pas seulement en cherchant le mot "duplication") : aucune ne fait ce métier, vrai trou confirmé. Trois questions de calibrage posées et tranchées avant construction : périmètre (tout le dépôt) ; statut (Membre complet dès la construction, plutôt que l'étape "sans blueprint" habituelle) ; nom (CLONE-HUNTER, choisi par l'utilisateur parmi des propositions). `scripts/clone-hunter.mjs` : `findDuplicateBlocks()` (diff de blocs, étend la comparaison ligne par ligne plutôt qu'un fenêtrage à taille fixe) + `clusterDuplicates()` (union-find, un bloc dupliqué à N endroits ne produit jamais N alertes redondantes). Premier essai live : 27 clusters, plus de 20 dans `components/ui/*` — vérifié (git log, motifs Radix/"use client") qu'il s'agit du kit shadcn vendu tel quel (duplication assumée par design), exclu du scan par défaut ; les 7 clusters restants dans `scripts/` sont tous réels (le plus significatif : `loadJson()` dupliqué entre `smart-conso-api.mjs` et `smart-conso-token.mjs`). Demande explicite complémentaire reçue en cours de construction : « tu integres clone hunter à la ronde periodique, en lancement auto comme les autres de la suite verif » — `clone-hunter-run` ajouté à CIRCLE_ITEMS (thème "Passages réels (smoke run)", un vrai lancement à chaque passage comme profil-utilisateur-guard/network-check-run, jamais un simple signal de fraîcheur ; CIRCLE_ITEMS 20→21, deux compteurs figés dans check-house.mjs corrigés en conséquence). Demande explicite finale reçue en cours de câblage : « dans le package d'entree il y a aussi : s'assurer que le nouvel outil est bien cablé avec les autres membres » — `checkAgentOnboarding()` (déjà existant, jamais utilisé jusqu'ici pour auto-vérifier un câblage en cours de construction plutôt qu'après coup) lancé live contre clone-hunter, a trouvé 4 écarts réels (absent de la table §7ter, absent de PRESTATIONS, absent des deux endroits de CLAUDE.md) — les 4 corrigés puis reconfirmés à zéro écart, badge "🎖️ Membre certifié" obtenu. Câblage complet : blueprint (`docs/clone-hunter-blueprint.md`), instanciation (`docs/referentiel/clone-hunter.md`), registre (`docs/clone-hunter/`), Doc-Report REGISTRIES (famille "Qualité du code"), PRESTATIONS ("Pack Chasse aux clones"), §7ter (statut Agent 🎖️), CLAUDE.md (section blueprint + bullet référentiel technique), AXA-CHECK `AGENT_SCRIPT_FILES` (couverture de test), usage réel journalisé (`tool-usage.mjs`). Tâche interne #177 ouverte (jamais calibrée ce soir) pour une v2 sémantique, suite à la demande de l'utilisateur de la garder en to-do. `lib/reference.ts` Version 197, `check-house.mjs` vert (149 tests), `tsc --noEmit` sans nouvelle erreur (seule erreur restante : `vite.config.ts:60`, pré-existante). | terminée — fidèle |

| 275 | 2026-09-21T19:05Z | Charte / Outillage de travail | Cérémonie de certification construite (bloc visuel dédié, une seule fois par outil) + note CASSANDRA-RH sur les blueprints | important | Suite directe de la certification de clone-hunter (#274) : demande explicite « quand tu affiches "membre certifié" tu ne donnes pas l'état des infos du badge ni l'icône badge [...] le moment de l'intégration doit être bien repérable [...] imagine un système autour de ce moment ». Vérifié d'abord que la doctrine existante (docs/regles-de-travail.md, §"Le badge", 2026-09-20) répond déjà à « qui délivre le go » : LE-COORDINATEUR aujourd'hui via checkAgentOnboarding(), CASSANDRA-RH en consultation/affichage une fois construite — rien à trancher là. Ce qui manquait : rendre le moment VISIBLE. 3 questions de calibrage posées et tranchées (toutes recommandées) : bloc visuellement à part, jamais mêlé au reste ; déclenché SEULEMENT la première fois qu'un outil devient certifié (jamais à chaque mention ultérieure) ; une fonction dédiée plutôt qu'une habitude d'écriture. Construit dans scripts/le-coordinateur.mjs : `formatBadgeCeremonyAnnouncement()` (encadre le `message` déjà produit par checkAgentOnboarding(), repris verbatim), `announceBadgeCeremony()` (ne déclenche que si `complet` et jamais vu avant), détection mécanique via un nouveau journal local `.badge-ceremony-history.json` (gitignored, ajouté à Doc-Report LOCAL_JOURNALS) plutôt que la seule mémoire de session. `loadBadgeCeremonyHistory()` réutilise `loadJson()` de tool-usage.mjs (désormais exportée) au lieu d'écrire une 4e copie — CLONE-HUNTER avait trouvé cette exacte duplication (3 occurrences) le soir même, jamais rouvrir un cas déjà signalé (Article 3). Bloc généré rétroactivement et vérifié pour clone-hunter (déjà certifié cette nuit sans que ce bloc n'existe encore) — livré à l'utilisateur dans la conversation. Note complémentaire traitée dans le même message : le recrutement futur de CASSANDRA-RH devra chercher aussi des blueprints, pas seulement des scripts/skills — consignée dans docs/regles-de-travail.md (règle "Personnages hors de l'équipe"), à reprendre au round de calibrage de la tâche #134, rien construit ce soir sur ce point. `lib/reference.ts` Version 198, `check-house.mjs` vert (150 tests), `tsc --noEmit` sans nouvelle erreur. | terminée — fidèle |

| 276 | 2026-09-21T20:15Z | Correctif de code / route.ts | 6 branches de mode extraites de route.ts (identify/unlock_garden/mark_dossier_seen/spin_bonus/reset/skip_to_revelation), zéro régression, cœur "chat" laissé intact par décision explicite | important | Demande explicite de l'utilisateur : « le decoupage de route.ts doit etre fait dans les regles de l'art : pas le droit à l'ereur : c'est ce que cet outil doit garantir ». Trois tours de calibrage successifs avant tout code : (1) route-booster montrait 24 candidats mais les scores de risque du mode "chat" (42 à 84) ont révélé une vraie limite — option "repenser la structure d'abord" choisie ; (2) lecture réelle du cœur "chat" (Article 19) a montré ~1000 lignes de `const` séquentielles à 300+ références croisées, jamais isolables sans un vrai parseur AST (que ce réseau d'outils n'a jamais, par choix délibéré) — option "se limiter aux branches sûres, laisser le cœur intact" choisie, jamais "tenter quand même" ; (3) en relisant CES MÊMES branches réputées sûres, découverte que route-booster avait sous-compté : 6 branches sont réellement self-contained (identify/unlock_garden/mark_dossier_seen/spin_bonus/reset/skip_to_revelation), pas seulement 4 — ses "sortant" pour spin_bonus/reset/skip_to_revelation étaient des faux positifs de son heuristique texte (confond une variable RÉUTILISÉE PAR NOM d'un bloc à l'autre avec une vraie fuite, ne comprend pas l'imbrication des accolades). Extraites une par une (`handleIdentify`, `handleUnlockGarden`, `handleMarkDossierSeen`, `handleSpinBonus` — la plus grosse, ~230 lignes incluant toute la logique de la roulette des bonus —, `handleReset`, `handleSkipToRevelation`), suite de tests COMPLÈTE (`node scripts/check-house.mjs`, 150 tests + `npx tsc --noEmit`) relancée après CHAQUE extraction individuelle, jamais un lot groupé — 150/150 à chaque étape, aucune régression. `POST()` passe de ~1665 à ~1327 lignes. Le cœur "chat" (~1000 lignes restantes) reste explicitement intact, reconnu comme une vraie limite actée avec l'utilisateur, pas un report tacite — sa restructuration attend un outil dédié (tâche #180, ouverte le même soir suite à une demande explicite complémentaire de l'utilisateur : « je ne veux pas lacher : mettons sur la to do de creer un outil dedié pour permettre à find booster d'etre efficace meme sur route.ts ou un fichier complexe » — jamais construit ce soir, jamais perdre la vocation première de find-booster sur les fichiers déjà structurés). `lib/reference.ts` Version 199, `check-house.mjs` vert (150 tests), `tsc --noEmit` sans nouvelle erreur (seule erreur restante : `vite.config.ts:60`, pré-existante). | terminée — fidèle |

| 277 | 2026-09-21T20:30Z | Correctif de code / memento weight | Tâche #174 close : `scripts/kpi-report.mjs::reportMementoWeight()` affiche désormais la tendance multi-sessions, pas seulement la session en cours | normal | Suite directe de #270 (question posée à l'époque, restée en file) : « est-ce que l'équipe suivi conso récupère bien les données de memento weight ? ». Vérification réelle avant de coder (Article 19) : `scripts/memento-weight.mjs::persistContextWeightSamples()` écrivait bien `.memento-history.json` à chaque rapport KPI, mais aucune fonction ne le relisait jamais — un vrai journal en écriture seule, `averageContextWeightByActor()` ne portant que sur l'échantillon de la session en cours. Vérifié contre la frontière Article 8 avant d'agir : ce chantier reste strictement observationnel (mesure de poids réel envoyé à Gemini), jamais une connexion vers Smart Conso API/SMART-CONSO-TOKEN (territoire écrit comme exclu des deux côtés, cf. `docs/referentiel/memento-weight.md`). Corrigé en exportant `loadHistory()` (déjà écrite en privé, jamais exposée) plutôt que d'écrire un second mécanisme de lecture — `reportMementoWeight()` affiche désormais côte à côte la moyenne de la session en cours ET la moyenne accumulée sur l'historique multi-sessions (plafonné à 500 échantillons), sans jamais fabriquer un verdict de hausse/baisse à partir de ce seul chiffre (la lecture humaine reste seule juge d'une vraie dérive, même principe que Smart Conso API vis-à-vis de l'Article 8). Nouveau test ajouté dans `scripts/check-house.mjs` (bloc memento-weight existant, temp dir isolé) : vérifie que `loadHistory()` relit exactement ce que `persistContextWeightSamples()` vient d'écrire, et retombe sur un historique honnêtement vide une fois le fichier réel restauré/supprimé. `docs/referentiel/memento-weight.md` et `docs/referentiel/tableau-de-bord.md` mis à jour en conséquence. `lib/reference.ts` Version 200, `check-house.mjs` vert (150 tests), `tsc --noEmit` sans nouvelle erreur (seule erreur restante : `vite.config.ts:60`, pré-existante) | terminée — fidèle |

| 278 | 2026-09-21T20:45Z | Charte / Documentation | Tâche #175 close : `docs/referentiel/regles-de-la-memoire.md` créé, 3e document transversal (sur le modèle de regles-du-temps.md/regles-de-l-espace.md) | important | Suite directe de #270 (trou de nommage identifié le même soir : « un futur document docs/referentiel/regles-de-la-memoire.md [...] comblerait le trou de nommage laissé par la disparition de l'ombrelle MEMENTO »). Investigation réelle du code avant rédaction (Article 19) : `lib/life.ts` (tous les champs persistés de `Life` et leur plafonnage dans `readLife()`), `lib/dialogue.ts` (les trois couches anti-répétition : `dialogue_fingerprints` session-only, `wordFrequency`/`recentEchoWords`, `themeFrequency`/`overusedThemes`), `lib/lia.ts` (la mémoire narrative méta : mémoire asymétrique, perception bornée, continuité de soi, incertitude chiffrée, `principes.md` 8.18), et vérification directe que `dialogue_fingerprints` est bien vidé à chaque `reset` (même liste de tables que `conversations`/`memories`/`agent_state`/`world_requests`, `app/api/lia/route.ts:383`) — confirmant la règle transversale centrale du document : aucune mémoire de personnage ne survit à un reset complet. Trois natures de mémoire distinguées explicitement (persistée dans `Life` / technique anti-répétition / narrative méta), jamais confondues, avec une checklist de cohérence mémorielle en fin de document sur le modèle des deux autres docs transversaux. Articule sans les confondre memory-audit (l'outil qui vérifie la cohérence de la mémoire persistée, section 7) et memento weight (qui mesure son poids, jamais son contenu, section 8) — les deux fiches d'instanciation (`memory-audit.md`, `memento-weight.md`) mises à jour avec un renvoi croisé vers ce nouveau document. Écart de doc/code réel trouvé et corrigé au passage (Article 13, vérification périodique) : CLAUDE.md citait encore "5 familles" pour le tableau de bord KPI dans sa liste de référentiels techniques, resté périmé depuis le passage à 6 familles le 2026-09-20 (déjà corrigé dans `docs/referentiel/tableau-de-bord.md` le 2026-09-21, tâche #267, mais jamais répercuté dans CLAUDE.md lui-même) — corrigé dans le même commit, avec l'entrée de liste pour le nouveau document ajoutée juste au-dessus. `lib/reference.ts` Version 201, `check-house.mjs` vert (150 tests, aucun nouveau nécessaire — travail purement documentaire), `tsc --noEmit` sans nouvelle erreur (seule erreur restante : `vite.config.ts:60`, pré-existante) | terminée — fidèle |

| 279 | 2026-09-21T20:50Z | Méthode de travail / Feuille de route | Tâche #171 (passe manuelle d'allègement CLAUDE.md) : reportée explicitement, pas abandonnée | normal | Reprise de #171 dans l'ordre de file convenu (E→F→H→B→CASSANDRA-RH→simulation→refonte). Vérification réelle avant d'agir (Article 19, jamais une passe à l'aveugle) : `findRedundantRulePairs()` (CLAUDE.MD.SPY) relancé contre le fichier réel, 0 paire redondante détectée — confirme ce que #181 avait déjà conclu le 2026-09-20 : le gisement mécanique (asides narratives datées, répétitions détectables) est réellement épuisé, toute réduction supplémentaire exigerait un tri manuel phrase par phrase, avec le risque réel d'affaiblir une règle par erreur que le garde-fou non négociable du fichier lui-même interdit ("en cas de doute, ne pas couper"). SMART-CONSO-TOKEN consulté avant cette vérification (`charter_size_tax`, verdict "ok", aucun seuil franchi). Question de calibrage posée à l'utilisateur (Article 16, décision qui touche directement à la charte) : reporter / faire le tri manuel maintenant avec relecture coupe par coupe / déplacer plutôt que couper. Réponse : reporter et enchaîner sur la tâche suivante (#138) — la tâche #171 reste ouverte dans le suivi interne, jamais fermée à tort (elle n'a reçu aucun travail réel ce soir, seulement une vérification qui confirme qu'elle reste légitimement en attente) | reportée — décision explicite de l'utilisateur, aucune régression |

| 280 | 2026-09-21T21:15Z | Nouvel outil / Enrichissement | Tâche #138 close : `detectTaskMomentum()` — SMART-CONSO-TOKEN accompagne désormais une tâche longue en temps réel, pas seulement avant chaque action isolée | important | Reprise de #138, explicitement laissée en file lors de l'audit #137 (« trop ouvert pour être implémenté sans calibrage »). Trois questions de calibrage posées et tranchées (Article 16) : (1) déclencheur — réponse « trouve un système pertinent qui combine plusieurs solutions » : trois axes indépendants combinés en OR (nombre d'actions coûteuses enchaînées ≥5, temps réel écoulé ≥45 min, poids cumulé estimé ≥50 000 tokens), chacun attrapant un motif de dérive différent qu'un seul axe manquerait ; (2) forme — « entre les 2 : informé de manière claire et précise » : un bloc distinct (`⏳ SMART-CONSO-TOKEN — chantier en cours`), purement informatif, jamais un point d'arrêt qui bloquerait la suite ; (3) répétition — une seule fois par tâche (recommandé). Implémenté dans `scripts/smart-conso-token.mjs` : `detectTaskMomentum()`/`formatTaskMomentumBlock()`/`TASK_MOMENTUM_THRESHOLDS`, une notion de "tâche" approximée honnêtement par une LANCÉE (`currentTaskRun`, actions consécutives sans écart de plus de 30 min) faute d'un vrai identifiant de tâche dans l'historique existant. Discipline "une seule fois" obtenue sans second fichier d'état : un marqueur interne (`long_task_signal`) réécrit dans le même historique via `recordAction()`, qui rejoint la lancée courante et se tait tant qu'aucun écart ne l'a fermée. `recordAction()` gagne un champ optionnel `tokensEstimes` (même patron que `reductionPct` déjà existant, jamais un second calcul — l'appelant fournit le chiffre déjà connu, ex. le ~37 000 documenté pour un spawn d'agent séparé). Câblé dans `main()` après une confirmation réelle (`--confirm`) seulement. Correctif trouvé et corrigé au passage (Article 3) : `main()` affichait le résumé de rythme à partir de l'historique chargé AVANT `recordAction()`, jamais rafraîchi avec l'action qui vient d'être confirmée — corrigé en réutilisant la valeur de retour de `recordAction()`. `docs/referentiel/smart-conso-token.md` mis à jour. `lib/reference.ts` Version 202, `check-house.mjs` vert (150 tests, 1 nouveau bloc couvrant le déclencheur combiné, la lancée, le marqueur et la discipline une-fois-par-tâche), `tsc --noEmit` sans nouvelle erreur (seule erreur restante : `vite.config.ts:60`, pré-existante). Clôt la file E→F→H→B : reste CASSANDRA-RH (#134) puis la simulation Article 18 | terminée — fidèle |

| 281 | 2026-09-21T21:35Z | Nouvel outil / Enrichissement | find-booster : optimisations réelles (tâche #182) + connexion Doc-Report construite | important | Demande explicite : « optimiser ce que find-booster sait déjà faire [...] un bond en avant [...] adaptabilité et gain de temps » puis « améliore aussi la connexion avec doc-report et les journaux [...] utile ? ». Déclenché par une question directe de l'utilisateur qui a révélé que find-booster n'était en réalité pas encore intégré aux habitudes réelles de recherche de l'agent ce soir (honnêtement reconnu, corrigé sur-le-champ, 3 usages réels journalisés via tool-usage.mjs). Trois optimisations trouvées en utilisant find-booster SUR LUI-MÊME (dogfooding, Article 19) : (1) bug auto-référentiel — `extractTitledArrayIndex()` matchait sa propre ligne de documentation citant l'exemple littéral du motif recherché (regex non ancrée, ne filtrait pas les lignes de commentaire) — corrigé en filtrant `//`, jamais en changeant le motif ; (2) `recommendFindBooster()` lisait le fichier deux fois — corrigé via un nouveau cœur interne `buildIndexFromSource()` réutilisé par `buildIndex()` et `recommendFindBooster()` ; (3) `buildIndex()` découpait la source en lignes 3 fois séparément pour les 3 extracteurs JS — découpé une seule fois désormais, chaque extracteur acceptant chaîne ou tableau sans changer la signature des appels existants. Ampleur du gain assumée honnêtement modeste (millisecondes, jamais perceptible) — le vrai bénéfice est la qualité du résultat, pas la vitesse. Connexion Doc-Report construite : `flagFindBoosterCandidates(registries, recommendImpl)` (scripts/doc-report.mjs) appelle `recommendFindBooster()` pour chaque `scriptPath` réel de REGISTRIES (dédupliqué), signale les scripts assez lourds pour mériter une recherche par concept — vérifié en direct : SMART-CONSO-TOKEN, KPI et LE-COORDINATEUR franchissent le seuil. Câblé dans le rapport CLI de Doc-Report. Connexion aux JOURNAUX locaux évaluée puis explicitement écartée (confirmé en lançant find-booster lui-même sur LOCAL_JOURNALS : zéro résultat, les journaux sont des données brutes `{path,owner,purpose}`, aucun des 4 motifs réels ne s'applique). `docs/referentiel/find-booster.md` mis à jour (nouvelle section optimisations + section connexion Doc-Report + section évaluée-et-écartée). `lib/reference.ts` Version 203, `check-house.mjs` vert (151 tests, 2 nouveaux blocs), `tsc --noEmit` sans nouvelle erreur (seule erreur restante : `vite.config.ts:60`, pré-existante) | terminée — fidèle |

| 282 | 2026-09-21T21:50Z | Méthode de travail / Outillage de navigation | Rappel find-booster sharpened dans le post-commit + réponse honnête sur le périmètre (conversation/sessions compressées) | important | Question directe de l'utilisateur : « comment tu t'assures que tu vas utiliser cet outil finder [...] comment tu sécurises ça sans pouvoir l'oublier ? ». Réponse honnête donnée : aucune garantie mécanique n'existe dans cette architecture (rien ne peut intercepter un Read/Grep avant qu'il n'ait lieu), même limite déjà documentée pour SMART-CONSO-TOKEN — le rappel générique du menu PRESTATIONS s'était révélé concrètement insuffisant (l'agent avait dû admettre ne pas avoir utilisé find-booster jusqu'à ce qu'on le lui demande). Renforcé (jamais remplacé) : `scripts/hooks/check-last-commit.mjs` appelle désormais `flagFindBoosterCandidates()` (Doc-Report, tâche #182) à chaque commit et nomme les scripts réels, maintenant, assez lourds pour mériter une recherche par concept — vérifié en direct (SMART-CONSO-TOKEN, KPI, LE-COORDINATEUR affichés). Répond dans la foulée à une question distincte de l'utilisateur sur le périmètre de find-booster (« peut-il chercher ailleurs que dans le code, comme dans la conversation, les sessions compressées ? ») : vérifié en direct plutôt que supposé — la conversation vive n'est jamais un fichier accessible (hors de portée par nature), et `docs/suivi/sessions/*.md` (le proxy le plus proche sur disque) ne produit que 2 entrées géantes et inutilisables via `extractHeadingIndex()` en le lançant réellement dessus (une table markdown de 281 lignes ne correspond à aucun des 4 motifs réels) — hors périmètre aujourd'hui, à raison. `docs/referentiel/find-booster.md` mis à jour. `lib/reference.ts` Version 204, `check-house.mjs` vert (151 tests, aucun nouveau nécessaire — check-last-commit.mjs reste un orchestrateur non testé directement, cohérent avec le reste du fichier ; sa fonction sous-jacente `flagFindBoosterCandidates()` est déjà testée), `tsc --noEmit` sans nouvelle erreur (seule erreur restante : `vite.config.ts:60`, pré-existante) | terminée — fidèle |

| 283 | 2026-09-21T22:05Z | Nouvel outil / Enrichissement | find-booster : 2e passe d'optimisation (recherche multi-mots-clés en OR) + réponse sur l'alimentation du catalogue LE-COORDINATEUR + vérification finale de câblage | important | Demande explicite : « refais une passe sur le code de find booster [...] 1/ performant d'un cran encore 2/ synergies + alimentation catalogue coordinateur » puis « fais une passe de la suite verif [...] pour tout checker ». Trouvaille réelle en relisant `main()` avec un œil neuf : plusieurs mots-clés CLI étaient joints en une seule phrase littérale (`keywordParts.join(" ")`), jamais trouvée même si chaque terme existe séparément — reproduit en direct contre le vrai `scripts/smart-conso-token.mjs` (0 résultat pour « recordAction assess » avant correction, 5 résultats après). Corrigé : nouvelle fonction `searchByConcepts()` (pluriel, combine les mots-clés en OR), `searchByConcept()` (singulier) inchangé pour la compatibilité, `entryHaystack()` factorisé entre les trois fonctions qui recalculaient séparément le même nom+description en minuscules (DRY, Article 3). Question « alimentation catalogue coordinateur » traitée par investigation (Article 19) plutôt que construction aveugle : vérifié que `recordCatalog()`/`buildCatalogDelivery()` n'archivent une nouvelle version QUE quand le contenu STATIQUE de PRESTATIONS change réellement (anti-doublon délibéré) — y injecter le signal LIVE de `flagFindBoosterCandidates()` (qui change à chaque modification de code) casserait cette discipline et ferait tourner le catalogue en boucle ; expliqué pourquoi le bon endroit reste le rappel post-commit déjà câblé (Version 204), jamais le catalogue historisé. Vérification finale demandée : `checkAgentOnboarding('find-booster')` relancé en direct avec les vrais fichiers du dépôt (docs/regles-de-travail.md, arborescence réelle de docs/) — `gaps: []`, badge "🎖️ Membre certifié" toujours détenu, aucune régression de câblage introduite par les 3 passes de ce soir. `docs/referentiel/find-booster.md` mis à jour (2 nouvelles sections). `lib/reference.ts` Version 205, `check-house.mjs` vert (151 tests, 3 nouvelles assertions dans le bloc existant), `tsc --noEmit` sans nouvelle erreur (seule erreur restante : `vite.config.ts:60`, pré-existante) | terminée — fidèle |

| 284 | 2026-09-21T22:20Z | Conception / Nouvel outil (CASSANDRA-RH) | Dossier de conception consolidé `docs/cassandra-rh-conception.md` — réorganisation des 16+ lignes de suivi éparpillées, aucune construction | important | Demande explicite : « fais une legere passe sur cassndra pour reaorganiser outes les notes et t'assurer que tu n'as rien oublié ». Relecture complète de toutes les lignes CASSANDRA-RH du suivi (2026-09-20T09:40Z originelle, #147, #171, #172, #179, #202, #203, #219, #222, #245, #251, #255, #258, #259, #261, #262, #265, #275) — jamais réécrites (le suivi garde son rôle chronologique intact), consolidées dans un nouveau document dédié structuré par thème (rôle/anti-doublon, missions, suivi RH dans le temps, organigramme corrigé, intégration/badge, statut cible, crochets déjà posés dans le code, 8 pistes non tranchées listées explicitement pour le futur round de calibrage #134). Rien construit — préparation uniquement, remplace le round de calibrage improvisé par une base complète et vérifiée | terminée — fidèle |

| 285 | 2026-09-21T22:35Z | Méthode de travail / Suivi | Vérification de `docs/cassandra-rh-conception.md` contre l'historique brut complet (0 appel API), 1 nuance retrouvée et explicitée | important | Demande explicite : « revois l'historique de conversation en profondeur (y compris sessions compactées si besoin) [...] vois si tu peux utiliser find booster ici, à bon escient ». Réponse honnête sur find-booster : hors périmètre pour cette tâche (déjà vérifié la session même — ni la conversation vive ni les sessions compressées ne sont des fichiers accessibles). Le bon outil (THE-DEEP-READER) coûte ~37 000 tokens fixes ; avant de le lancer, vérifié l'option gratuite suggérée par SMART-CONSO-TOKEN (`ARCHIVE_FIRST_REMINDER`) : grep + parsing Python du fichier JSONL brut de la session (`/root/.claude/projects/.../b96a7cb6-....jsonl`, 352 Mo, 0 appel API) pour extraire tous les VRAIS messages utilisateur mentionnant "cassandra" (21 trouvés, y compris dans la partie antérieure à la compaction dont je n'avais que le résumé). Chacun vérifié contre `docs/cassandra-rh-conception.md` (créé à la ligne #284 juste avant) : aucun écart substantiel, une seule nuance restée implicite (2026-09-20T13:32Z : « en quoi le passage par CASSANDRA et LE-COORDINATEUR garantit une vraie utilité, pas seulement une case cochée ») — explicitée comme 9e piste de calibrage. Note méthodologique honnête ajoutée au document : cette technique est un filtre mot-clé gratuit, jamais un remplacement de THE-DEEP-READER pour une évaluation de fidélité qualitative globale. THE-DEEP-READER jamais lancé — l'option gratuite a suffi pour cette question précise et cherchable | terminée — fidèle |

| 286 | 2026-09-21T23:05Z | Méthode de travail / Feuille de route | État des lieux des tâches réellement ouvertes (docs/suivi vs aide-mémoire de session) + calibrage de 4 idées en attente | important | Demande explicite : « reprends les taches ouvertes, pour tout finir avant cassandra, sauf les taches reportés, mais je dois revoir lesquelles ». Vérification factuelle via `check-tasks-details.mjs`/`loadAllTaskRows()` : sur 219 lignes du suivi, seules 6 ont un statut réellement ouvert, et 5 d'entre elles sont des morceaux de la conception CASSANDRA elle-même (design en cours sans numéro, #147, #171-suivi, #172-suivi, #179-suivi) — aucune tâche « hors CASSANDRA » n'est marquée ouverte dans la source durable. La 6e (#279) n'est qu'un rappel de report déjà acté (tâche interne #171, allègement CLAUDE.md), pas un travail en attente. Présenté à l'utilisateur avec les 4 idées encore seulement dans l'aide-mémoire de session interne (jamais discutées) : #92 (trottoir 3D), #177 (CLONE-HUNTER v2), #180 (outil dédié fichiers denses non structurés), #181 (compaction des journaux Doc-Report). Calibré point par point (Article 16, format une-idée-par-question) : **#92 reportée** (confirmé, reste dans la refonte visuelle globale après Cassandra) ; **#177 à traiter maintenant** (cf. ligne suivante) ; **#180 reportée**, avec réponse technique donnée à la question posée par l'utilisateur (extension de find-booster vs outil séparé) : outil séparé recommandé plutôt qu'une extension — find-booster repose sur 4 motifs structurels fiables et un 5e motif « flou » pour texte non structuré casserait cette fiabilité et fausserait `recommendFindBooster()` ; un futur "deep-hard-finder" réutiliserait plutôt la détection de points de coupure déjà existante dans `route-booster.mjs` (branches `if`, commentaires après ligne vide) plutôt que de repartir de zéro ; **#181 calibrée puis reportée** (l'utilisateur a demandé à préciser la mécanique exacte avant d'enregistrer, pas à construire tout de suite) : deux questions techniques tranchées — forme du résumé = chiffres agrégés (total/moyenne/nombre d'événements sur la période), jamais une phrase perdant le détail chiffré (les tableaux de bord qui lisent ces journaux doivent pouvoir recalculer une vraie statistique) ; seuil de déclenchement = 200 entrées accumulées par journal (même ordre de grandeur que la limite déjà utilisée pour l'historique des clés Gemini, `lib/gemini-keys.ts::episodes`), purement théorique aujourd'hui (journaux réels à 8-18 entrées). Question annexe de l'utilisateur traitée en aparté : le système d'objectifs/score cible par membre d'équipe (#147) n'est PAS déjà réalisé — c'est une extension RH proposée en plus du tableau de bord/KPI existant, encore non tranchée, qui appartient au round 2 de calibrage CASSANDRA (#134). Rien construit sur #181/#180/#92 ce soir — uniquement une décision de report/traitement enregistrée pour chacune | terminée — fidèle |

| 287 | 2026-09-21T23:20Z | Conception / Nouvel outil (objectifs vs résultats) | Nouvel outil validé : séparer le calcul objectifs/résultats de CASSANDRA-RH, conception à sauvegarder pour construction future | important | Demande explicite de l'utilisateur : « je pense qu'il faut separer le calcul des objectifs de Kpi de cassndra ; tout en conservant l'utilité qu'elle va en avoir [...] est-ce qu'un outil peut nous aider à concevoir ces objectifs maintenant et à les suivre : resultats vs objectifs ? un outil existant ? un outil à creer ? un petit script ? un sous-agent-script dans l'équipe de Cassandra ? ». Vérifié (Article 19) qu'aucun outil existant ne fait ce métier : plusieurs outils emploient « cible »/« target » mais toujours dans un autre sens (modèle Gemini cible, niveau CHECK-LEVEL-TARGET, seuil du badge) — aucun n'a la notion d'un objectif fixé PAR ENTITÉ, comparé dans le temps à un résultat mesuré. Recommandation donnée et validée par l'utilisateur (« je valide, à mettre dans la to do ») : un nouveau petit Membre de l'équipe autonome (même statut que CLONE-HUNTER/AXA-CHECK), JAMAIS un sous-agent interne à CASSANDRA (l'organigramme déjà acté de CASSANDRA est plat — Direction/Équipe noyau/Membre de l'équipe/VIP — aucun autre outil n'a de sous-agents, et CASSANDRA n'existe pas encore : la lier à son calendrier romprait justement l'objectif de séparation demandé) et JAMAIS une extension du tableau de bord/KPI existant (qui mesure déjà des résultats mais n'a aucune notion d'objectif à atteindre — greffer ça mélangerait deux rôles). Rôle strictement délimité à 3 points, actés : (1) un petit registre où un objectif chiffré est fixé pour une entité (l'utilisateur, un script, un outil) sur une période ; (2) il ne mesure JAMAIS lui-même un résultat — il lit ce que les outils existants savent déjà (taux de trouvaille de `tool-usage.mjs`, couverture d'AXA-CHECK, chiffres du tableau de bord/KPI), jamais un second calcul divergent ; (3) il calcule l'écart objectif/résultat et le statut (atteint/en dessous/dépassé) par entité et par période — rien de plus, aucun jugement RH ni proposition de formation (ça reste le rôle de CASSANDRA plus tard, qui CONSOMMERA cet outil exactement comme elle consommera ARGUS ou AXA-CHECK, principe déjà écrit dans `docs/cassandra-rh-conception.md`). Rien construit ce soir — conception validée et enregistrée pour une future tâche de construction (calibrage : entités concernées, fréquence de période, emplacement du registre — encore à faire avant tout code) | ouverte — conception validée, construction non commencée |

| 288 | 2026-09-21T23:25Z | Méthode de travail / Vérification | Vérification croisée outils : CIRCLE-TASKS / HYPER-SCAN-CHECKPOINT / LE-COORDINATEUR ne se chevauchent pas | normal | Question explicite de l'utilisateur : « j'ai aussi l'impression que circle et HYPER-CHECK [...] se chevauchent, avec en plus le coordinateur : est-ce le cas ? est-ce qu'il y a des ameliorations/optimisations/fusions à faire à ce niveau ? ». Vérifié dans le code réel (Article 19), pas de mémoire : `CIRCLE_EXCLUDED_REGISTRIES` (`scripts/circle-tasks.mjs`) exclut EXPLICITEMENT `hyper-scan-checkpoint` de la Ronde périodique avec sa propre raison écrite (« outil exceptionnel (Article 21), jamais coché par défaut ni régulier ») — décision assumée, pas un oubli. L'item `coordinateur-catalogue` de CIRCLE_ITEMS ne fait qu'appeler les fonctions de LE-COORDINATEUR (`recordCatalog()`/`buildCatalogDelivery()`) — aucune logique recopiée, un simple déclencheur périodique vers le vrai propriétaire (même schéma que `network-check-run` → `runNetworkCheck()`). Verdict : trois rôles distincts et non chevauchants — LE-COORDINATEUR (l'annuaire : que sait chaque outil), CIRCLE-TASKS (le calendrier : quand lancer les vérifications gratuites périodiques, y compris le rafraîchissement de LE-COORDINATEUR), HYPER-SCAN-CHECKPOINT (l'audit exceptionnel, jamais routinier par nature, Article 21). Aucune fusion/optimisation recommandée : fusionner casserait ces séparations délibérées. Garde-fou déjà existant qui maintient cette frontière en continu, pas seulement documentée une fois : `findRegistriesMissingFromCircle()` vérifie mécaniquement qu'aucun nouvel outil ne se retrouve ni dans la Ronde ni dans la liste d'exclusions justifiées | terminée — fidèle (vérification, aucune action de code nécessaire) |

| 289 | 2026-09-21T23:40Z | Conception / Nouvel outil (CLONE-HUNTER) | Construction de CLONE-HUNTER v2 (renommage bijectif cohérent) — tâche interne #177 close | important | Demande calibrée point par point plus tôt ce soir (ligne #286) : « #177 à traiter maintenant ». Toujours zéro nouvelle dépendance, zéro parseur AST, dans le même esprit que v1 : `tokenizeLine()` découpe une ligne en tokens ; `matchLineTokens()` exige que les tokens non-identifiants soient identiques ET qu'un SEUL renommage bijectif cohérent explique les identifiants — jamais juste "même forme de ligne" (trop bruyant seul, ex. `return x;` partagé partout) ; `extendNearDuplicateBlock()` propage ce mapping sur tout le bloc (même diff de blocs que v1) ; `findNearDuplicateBlocks()` filtre `hasRealRenaming()` pour ne jamais recompter un doublon littéral déjà vu par v1 (Article 3). Un mot-clé du langage ou une propriété réelle après un "." (`.length`) ne sont jamais renommables (`RESERVED_WORDS`, `isRenamableIdentifier()`). Dogfooding immédiat, payant dès le premier lancement live : v2 sur `clone-hunter.mjs` lui-même a révélé que v1 et v2 dupliquaient leur propre boucle de parcours de paires candidates — extrait `findBlocksFromIndex()` (index → paires → extension déléguée à une stratégie `extend` → filtre optionnel `accept`), désormais réutilisé par les deux, zéro trouvaille sur clone-hunter.mjs après correction. Vérifié live contre le vrai dépôt : 6 trouvailles réelles inédites (dont `collectCoverage()` dupliquée deux fois dans `scripts/axa-check.mjs`), même exclusion `components/ui/` héritée. Limite honnête documentée : `hasRealRenaming()` évalué sur le mapping complet avant troncature de recouvrement même fichier, cas marginal non corrigé (Article 5). `docs/referentiel/clone-hunter.md`, `docs/clone-hunter/index.md`, blueprint générique de CLAUDE.md, `lib/reference.ts` Version 208 mis à jour. `check-house.mjs` vert (152 tests, +1 bloc de tests v2), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 290 | 2026-09-21T23:50Z | Conception / Outillage de travail (registre canonique des outils) | Trou confirmé : HYPER-SCAN-CHECKPOINT n'appelle pas CLONE-HUNTER (oubli, jamais une exclusion assumée) ; nouvelle idée validée : formaliser un registre canonique unique des membres de l'équipe | important | Suite directe de la vérification CIRCLE-TASKS/HYPER-SCAN-CHECKPOINT/LE-COORDINATEUR (ligne #288) : l'utilisateur relance « dans ce cas, on devrait lui plugger tous les outils de la suite verif [...] cette liste de membres se fait remarquer à plein d'endroits dans le code, il serait bon de formaliser tout ça ». Vérifié dans le code (Article 19) : `scripts/hyper-scan-checkpoint.mjs` (version légère) lance déjà réellement en sous-processus `check-argus.mjs`/`check-harmonia.mjs`/`always-new-code.mjs`/`axa-check.mjs`/`clean-dirty-old.mjs`/`check-house.mjs` — mais PAS `clone-hunter.mjs`, construit après le dernier réglage de ce fichier et jamais rajouté à cette liste : un vrai oubli, aucune raison documentée d'exclusion (contrairement à l'exclusion assumée et écrite d'HYPER-SCAN-CHECKPOINT lui-même dans `CIRCLE_EXCLUDED_REGISTRIES`). Racine du problème identifiée : `checkAgentOnboarding()` (`le-coordinateur.mjs`) ne vérifie qu'UN outil à la fois contre le tableau Markdown humain de `docs/regles-de-travail.md` §7ter — aucune vraie liste unique, machine-lisible, n'existe ailleurs ; chaque script qui a besoin de connaître "tous les outils" (les appels `sh()` d'HYPER-SCAN-CHECKPOINT, `CIRCLE_ITEMS`/`CIRCLE_EXCLUDED_REGISTRIES`, `PRESTATIONS`, `REGISTRIES` de Doc-Report) maintient sa propre copie séparée. Des garde-fous croisent déjà DEUX de ces listes entre elles (`findToolsMissingFromMenu()`, `findRegistriesMissingFromCircle()`), mais aucun ne protège la liste d'agrégation d'HYPER-SCAN-CHECKPOINT — exactement le trou trouvé. Recommandation donnée : un seul registre-source (probablement `lib-shell.mjs`, déjà le point de partage neutre entre `circle-tasks.mjs` et `check-tasks-details.mjs`), dont les listes existantes deviennent des dérivés ou sont vérifiées contre lui, plutôt que des copies indépendantes. Rien construit ce soir — chantier réel touchant plusieurs fichiers (`lib-shell.mjs`, `circle-tasks.mjs`, `hyper-scan-checkpoint.mjs`, `doc-report.mjs`, `le-coordinateur.mjs`), à calibrer avant construction plutôt qu'improvisé. L'oubli de CLONE-HUNTER dans HYPER-SCAN-CHECKPOINT reste, lui, un correctif ponctuel simple (un appel `sh()` de plus) indépendant de ce chantier plus large — pas encore appliqué, en attente de décision de l'utilisateur | ouverte — 2 pistes validées, construction non commencée |

| 291 | 2026-09-22T00:15Z | Charte / Outillage de travail | Ronde CIRCLE-TASKS (22 commits sans passage) — sélection recommandée lancée en entier | normal | Suite de la feuille de route déjà actée (ligne #258) : « on finit toutes les tâches ouvertes avant cassandra puis la refonte » — plus aucune tâche hors CASSANDRA n'étant réellement ouverte, prochain pas confirmé par l'utilisateur (« Oui, lance la Ronde maintenant »). Sélection recommandée exécutée en entier (exclusions par défaut inchangées : référentiel exhaustif, dream-team-photo, THE-SCREENER conditionnel, THE-FINAL-JUDGE/THE-DEEP-READER coûteux) : rapport KPI (`kpi-report.mjs`, run `ronde-circle-tasks-22-commits` — couverture du tableau de bord 33%/2 sur 6, normal hors session de jeu/simulation, aucune alerte de code) ; scan Smart Conso API (aucun schéma coûteux, aucune salve non confirmée) ; garde-fou profil-utilisateur (`check-profil-utilisateur.mjs`, 8→9 fiches, aucun lien mort) ; nouvelle fiche de profil rédigée (2026-09-22-0005.md, 4 signaux nouveaux, 2 confirmés) ; synthèse réseau LE-COORDINATEUR (`runNetworkCheck()`, tous les indicateurs "ok") ; catalogue LE-COORDINATEUR régénéré (nouvelle version réelle archivée, PRESTATIONS avait changé depuis CLONE-HUNTER) ; carnets de correctifs/points-fragiles relus (aucune graduation possible, aucune nouvelle simulation depuis le dernier passage, toujours 0/2 partout) ; signaux THE-KING/ALWAYS-NEW-CODE/CLEAN-DIRTY-OLD/html-wiring/CLAUDE.md/suivi-tâche-la-plus-ancienne déjà calculés en direct par `buildCircleReport()` lors de l'impression du menu — aucune tension, aucune alerte. INES-official : aucune édition n'a jamais été produite, signalé mais volontairement NON construit ce soir (l'édition de référence est explicitement l'étape 4 de la feuille de route déjà actée, après la simulation — la produire maintenant frontrunnerait cet ordre déjà validé). Item CLONE-HUNTER déjà couvert par la construction de v2 plus tôt ce soir (lignes #289/#290), pas relancé une seconde fois. Petite trouvaille annexe, non corrigée (hors scope de cette Ronde) : le catalogue LE-COORDINATEUR nomme "Pack Boussole" deux prestations distinctes (check-tasks-details et find-booster) — collision de nom à corriger une prochaine fois, sans impact fonctionnel. Rien construit côté code ce soir — uniquement des vérifications et un signal | terminée — fidèle |

| 292 | 2026-09-22T00:45Z | Conception / Nouvel outil (organisation de l'agence) | Créé `docs/referentiel/organisation-agence.md`, le référentiel canonique de l'organigramme de l'outillage de travail | important | Demande explicite de l'utilisateur : « ajoute aussi la distinction déjà faite par ailleurs : les 2 cadres Cassandra et le coordinateur, les employés noyau = la suite verif = les admin [...] "les gardiens sacrés du code" c'est pas mal [...] consolidons toute cette partie, livre moi l'organisation complète, bien définie [...] ce doc sera mis dans le référentiel à l'usage de CASSANDRA ». Vérifié dans le code et les documents existants (Article 19) avant de proposer quoi que ce soit : l'organigramme (Direction/Équipe noyau/Membre/VIP) était déjà calibré en 4 rounds dans `docs/cassandra-rh-conception.md` §4, et la table maîtresse outil par outil dans `docs/regles-de-travail.md` §7ter — jamais réinventés, seulement consolidés et enrichis. Correction apportée à la question de l'utilisateur : le critère exact des "Gardiens sacrés" n'est pas "délivre un scan de qualité" (CLONE-HUNTER aussi en délivre un, et n'en fait pas partie) mais "tourne automatiquement à CHAQUE commit" (Article 20) — confirmé exactement les 4 membres déjà connus (ARGUS/HARMONIA/AXA-CHECK/CLEAN-DIRTY-OLD). 4 questions de calibrage posées et toutes validées côté recommandé : (1) les scripts Infrastructure (check-house.mjs, check-argus.mjs...) restent un 3e rang d'employés sans dossier individuel, pas exclus de l'agence ; (2) le code tiers vendu tel quel (components/ui/*) rejoint Personnages/Moteur du jeu comme 3e catégorie "jamais un employé" (déjà appliqué en pratique par CLONE-HUNTER, formalisé ici) ; (3) 6 suites de travail proposées pour les Membres ordinaires (Suivi-Conso, Simulation & Qualité narrative, Audit lourd, Dette & Structure du code, Référentiel & Vue d'ensemble, Smart Breaker/CHECK-LEVEL-TARGET à part) ; (4) gabarit de poste enrichi réservé aux 4 Gardiens sacrés (position dans le crochet post-commit, rôle dans le badge), gabarit standard partout ailleurs — jamais un gabarit par groupe, complexité non justifiée. `docs/cassandra-rh-conception.md` §4 réduit pour référencer le nouveau document plutôt que dupliquer l'organigramme (garde uniquement les décisions propres à la conception de CASSANDRA elle-même : contrôle croisé, agrégation dans le badge). CLAUDE.md (section référentiel technique) mis à jour. Document explicitement destiné à devenir le domaine d'entretien de CASSANDRA-RH une fois construite (round #134) — en attendant, mise à jour manuelle comme la table `regles-de-travail.md` §7ter. `lib/reference.ts` Version 209. `check-house.mjs` vert (152 tests, aucun changement de code), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 293 | 2026-09-22T01:00Z | Conception / Nouvel outil (organisation de l'agence) | Renommages point par point + nouvelle mission CASSANDRA-RH « Promotion de poste » | normal | Seconde salve de réponses de l'utilisateur, point par point, sur `docs/referentiel/organisation-agence.md` : renommages actés — « l'Agence de production de code » devient « l'Agence Codex » (en souvenir de Codex, l'IA qui a initialement produit le code du projet) ; « Suite Simulation & Qualité narrative » devient « Suite Audit Simulation » ; « Suite Référentiel & Vue d'ensemble » devient « La Cour du Roi » (logique : THE-KING y siège) ; « À part » devient « Les Agents Spéciaux ». Appliqué dans `organisation-agence.md` et CLAUDE.md partout où ces noms apparaissaient, avec au passage une petite référence croisée corrigée (« cf. §5.3 », qui n'existe pas, devient « point 3 ci-dessous »). Puis nouvelle demande explicite : « c'est cassandra qui peut aussi decider de l'evolution d'un petit script au passage script plus important [...] elle l'identifie, verifie si le script est bien eligible [...] puis propose son evolution, avec toujours notre confirmation » — nouvelle mission ajoutée à `docs/cassandra-rh-conception.md` §2, « Promotion de poste » : CASSANDRA identifie un script Infrastructure/Utilitaire nommé ayant grossi, vérifie l'ÉLIGIBILITÉ réelle (jamais la taille/ancienneté seule — réutilise le critère déjà établi du « domaine de jugement propre au projet », `docs/regles-de-travail.md` §7ter, pour ne pas laisser l'utilisateur croire qu'un simple grossissement suffirait), PROPOSE l'évolution vers Agent/Membre, exécution toujours soumise à confirmation explicite — même schéma que le recrutement en 3 temps déjà acté. Rien construit côté code (CASSANDRA-RH n'existe pas encore). `lib/reference.ts` Versions 210-211. `check-house.mjs` vert (152 tests, aucun changement de code), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 294 | 2026-09-22T01:45Z | Charte / Outillage de travail (CLONE-HUNTER, badge) | Promotion complète de CLONE-HUNTER en 5e Gardien sacré + refonte du badge (câblage réciproque, message dynamique, déclenchement automatique) | important | Suite directe de la ligne #293 : demande explicite « clone hunter doit rejoindre les gardiens sacrés [...] Cassandra devrait pouvoir nous aider [...] identifie le fonctionnement specifique des gardiens [...] lorsqu'on integre un nouveau gardein, veille à ce qu'il rentre bien dans tous ces fonctionnements ». Câblage technique terminé (post-commit hook et retrait de CIRCLE-TASKS déjà faits en amont) : `docs/referentiel/organisation-agence.md` §3 réécrit avec le critère double exact (scan de qualité ET tourne à chaque commit — jamais un seul des deux volets), CLONE-HUNTER déplacé de la Suite Dette & Structure vers les Gardiens (5 membres), nouveau « Répertoire des fonctionnements spécifiques partagés par tous les Gardiens » (7 points : câblage post-commit, retrait CIRCLE-TASKS, test check-house.mjs, badge/couverture, agrégation HYPER-SCAN-CHECKPOINT, validation croisée informelle, aucun coût API) ; gap réel corrigé au passage : HYPER-SCAN-CHECKPOINT n'appelait jamais `clone-hunter.mjs` (trouvé ligne #290, corrigé ce soir). `CLAUDE.md`, `docs/regles-de-travail.md` §7ter et `docs/referentiel/clone-hunter.md` mis à jour pour refléter le nouveau statut (plus de mention CIRCLE-TASKS). Puis demande explicite distincte : « je veux un message avec de meilleures infos [...] pour le message du badge [...] celle qui dit que l'agent-script est bien câblé avec tous les autres, et tous les autres sont bien câblés à lui, et ses prestations figurent bien au catalogue du coordinateur ». Réponse en 3 parties dans `scripts/le-coordinateur.mjs::checkAgentOnboarding()` : (1) catégorie affichée dans le badge (`AGENT_CATEGORIES`, nouvelle table dans `lib-shell.mjs`, miroir code de l'organigramme) ; (2) nouveau type de gap `reciprocalWiring` (tableau {label, ok} fourni par l'appelant) qui rend mécanique le vrai défaut trouvé deux fois ce soir (un Gardien câblé quelque part mais oublié ailleurs) ; (3) le `message` n'est plus un texte figé mais une énumération construite dynamiquement de ce qui a RÉELLEMENT été vérifié (table maîtresse, PRESTATIONS, instanciation, registre, blueprint, CLAUDE.md, suivi, Doc-Report, câblage réciproque). Puis troisième demande explicite : « tu crées un petit script pour gérer toute cette partie validation/intégration/badge/message [...] avec déclenchement auto [...] ce petit script existe deja ? » — vérifié (Article 19) : la logique existait déjà (`checkAgentOnboarding()`/`announceBadgeCeremony()`) mais n'était jamais appelée automatiquement. Décision actée avec l'utilisateur (2 questions de calibrage) : étendre LE-COORDINATEUR plutôt qu'un nouveau fichier séparé, notification terminal uniquement (jamais un nouveau rapport archivé). Nouvelle fonction `checkAllAgentBadges()` (balaie toute la table maîtresse, réutilise `checkAgentOnboarding()`/`announceBadgeCeremony()` sans les réimplémenter) câblée dans le VRAI hook post-commit (`scripts/hooks/check-last-commit.mjs`), en réutilisant les données déjà calculées à ce point (couverture AXA-CHECK par script via `collectScriptCoverage()`, comptes ARGUS/HARMONIA/CLEAN-DIRTY-OLD/CLONE-HUNTER déjà calculés, `buildRealOnboardingContext()` de check-tasks-details.mjs pour CLAUDE.md/table/docs/suivi) et calcule le câblage réciproque des Gardiens en relisant directement post-commit-hook/hyper-scan-checkpoint.mjs/circle-tasks.mjs. Testé en conditions réelles (exécution directe du hook) : 7 certifications réelles jamais annoncées auparavant se sont déclenchées correctement (THE-SCREENER, THE-FINAL-JUDGE, THE-DEEP-READER, SMART-CONSO-TOKEN, check-tasks-details, THE-KING, INES-official), confirmé idempotent au second passage (0 ré-annonce). `.badge-ceremony-history.json` confirmé déjà dans `.gitignore`. 6 nouveaux tests `check-house.mjs` (reciprocalWiring, catégorie dans le badge, message dynamique, sweep complet de `checkAllAgentBadges()`, idempotence, nouvelle certification détectée au bon moment). `check-house.mjs` vert (158 tests), `tsc --noEmit` sans nouvelle erreur (seule erreur préexistante `vite.config.ts` non liée) | terminée — fidèle |

| 295 | 2026-09-22T02:00Z | Conception / Nouvel outil (CASSANDRA-RH) | Deux nouvelles idées CASSANDRA-RH consignées + audit de complétude du document de conception | normal | Suite de la ligne #294 : demande explicite « c'est cassandra qui garde en commun avec le coordinateur l'histroique des badges attribués [...] c'est cassandra qui verifie [...] que chaque membre a recu son poste de travail adapté à sa classe [...] verifie que toutes les idees sur cassndra ont bien été consignées ». Deux ajouts à `docs/cassandra-rh-conception.md` : (1) §7, précision que `.badge-ceremony-history.json` (construit ligne #294) est une ressource DÉJÀ PARTAGÉE entre LE-COORDINATEUR (qui y écrit) et la future CASSANDRA-RH (qui la lira) — ni un doublon ni un trou, réponse directe à la question de l'utilisateur ; (2) §2, nouvelle mission « Vérifier le gabarit de poste adapté à la classe » — CASSANDRA s'assure que chaque Gardien a bien le gabarit enrichi (2 champs propres) et chaque Membre ordinaire le gabarit standard, jamais l'inverse. Audit de complétude effectué : relecture intégrale du document + grep ciblé de `docs/suivi/` sur les lignes datées du 2026-09-22 mentionnant CASSANDRA (#290-293) — toutes déjà reflétées dans le document, aucun écart trouvé au-delà des 2 idées ci-dessus et d'un renommage oublié (« Direction » → « Les Agents Cadre », 2 occurrences dans §4, corrigé au passage). Rien construit côté code (CASSANDRA-RH n'existe toujours pas) | terminée — fidèle |

| 296 | 2026-09-22T02:35Z | Charte / Outillage de travail (LE-RÉGISSEUR) | Simulation full_sim17 lancée et livrée + rendu HTML des transcripts câblé | important | Simulation complète lancée (Article 18) pendant la construction de CASSANDRA, sur demande explicite : révélation atteinte, phase 2 intégralement parcourue (cf. `docs/simulations/index.md` full_sim17). Archivage réel via LE-RÉGISSEUR, rapport KPI lancé avant tout redémarrage de serveur (tout vert, aucune alerte). Anomalie de script (pas de jeu) trouvée et documentée : mon propre script de simulation vérifiait la présence du dossier une seule fois, trop tôt — le dossier existait bel et bien plus tard dans le journal complet, extrait et livré après coup. Puis demande explicite : « le transcript ne m'a pas été livré en beau html [...] je souhaite que les transcripts soient livrés en html agréables à lire ». Fermé un trou déjà repéré par Doc-Report (« Simulations » marquée `delivery_html` mais jamais câblée) : `archiveSimulationFiles()` (`scripts/le-regisseur.mjs`) produit désormais un `.html` compagnon coloré par personnage pour le transcript ET le dossier, en réutilisant tel quel le type de bloc `dialogue` de `html-report.mjs` (déjà conçu "pour les simulations" le 2026-09-20, jamais câblé jusqu'ici) — le `.txt` de référence reste inchangé, seule la présentation à la remise change. Bug pré-existant trouvé et corrigé au passage (Article 3) : le test de `persistContextWeightSamples()` supposait `.memento-history.json` vide sans jamais le vider avant de tester — resté invisible tant que ce fichier local était petit, révélé par les 100+ échantillons réels écrits par full_sim17 lui-même. `docs/regles-de-travail.md` §7 mis à jour (le `.html` devient le format de livraison par défaut, jamais le `.txt` brut). 6 nouveaux tests `check-house.mjs`, `check-house.mjs` vert, `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 297 | 2026-09-22T03:10Z | Conception / Nouvel outil (CASSANDRA-RH, Catalogue, Stagiaire) | Round de calibrage complet sur le Catalogue LE-COORDINATEUR/CASSANDRA + le Stagiaire (10 questions) — décisions consignées | important | Suite directe de la ligne #296 (vérification Doc-Report en cours) : demande explicite « fais en sorte que le catalogue te soit typiquement utile à ça, avec la liste de tous les outils et les prestations combinées possibles, trouvées par le coordinateur [...] ça concerne toutes les familles d'outil de l'agence, tous les membres certifiés ». Doc-Report relancé en direct pour répondre d'abord à « tu es passé par doc-report ou pas » : confirmé, l'écart « Simulations — remise HTML » de la ligne #296 est bien fermé (seul écart restant : THE-DEEP-READER, préexistant, hors sujet). Puis 3 rounds de questions de calibrage (4+2+3=9, plus la question ouverte initiale) : le catalogue riche décrit par l'utilisateur (coût, fiches par outil, KPI par outil, combos figées en prestations pérennes) s'est révélé être LE RAPPORT PRINCIPAL déjà prévu pour CASSANDRA-RH (ses deux missions fondatrices KPI+recrutement), pas un document séparé — CASSANDRA l'écrit et le signe, LE-COORDINATEUR reste fournisseur de calculs bruts. Décisions actées, consignées dans `docs/cassandra-rh-conception.md` §8bis (Catalogue) et §8ter (Stagiaire) : fiche par outil complète (coût, famille, usage, couverture, fraîcheur, tout regroupé) ; nouveau score KPI composite par outil (fréquence d'usage + taux de trouvaille de `tool-usage.mjs` + fraîcheur CLEAN-DIRTY-OLD, zéro nouvelle mesure) ; combinaisons en chaînes de 3+ outils, pas seulement des paires ; critère de matching mécanique mixte (thème HARMONIA + source de données partagée + suite d'organigramme) ; promotion combo→prestation pérenne seulement sur preuve d'usage réel répété (`tool-usage.mjs`) ; réutilisation du dossier d'archivage déjà construit à la tâche #154 (`docs/le-coordinateur-catalogue/`), jamais un second dossier ; déclenchement automatique mais UNIQUEMENT lors des Rondes CIRCLE-TASKS, jamais à chaque commit ; CASSANDRA seule alerte l'utilisateur sur ce sujet. Décision non recommandée par l'agent mais actée par l'utilisateur : le Stagiaire (agent séparé pré-processeur, initialement repoussé au round précédent, §8 point 6) est construit dès maintenant — rôle strictement limité à la rédaction des fiches individuelles par outil (jamais les combinaisons ni le résumé, qui restent la voix propre de CASSANDRA), déclenché au cas par cas par CASSANDRA elle-même (jamais systématique à chaque Ronde), personnage fixe recopié à l'identique à chaque appel (même garde-fou anti-dérive que THE-FINAL-JUDGE/THE-DEEP-READER), consultation Smart Conso API/SMART-CONSO-TOKEN obligatoire avant chaque appel réel (Article 22). `docs/cassandra-rh-conception.md` §8 réorganisé : 8 des 9 points listés comme "non tranchés" depuis le round précédent (2026-09-21) étaient en réalité déjà résolus mais jamais reflétés dans le document — corrigé (Article 13), seul le point 9 (preuve concrète du bénéfice de l'intégration) reste ouvert. Deux fichiers HTML de full_sim17 (transcript + dossier) livrés en pièce jointe pendant ce round, clôturant la demande de la ligne #296. Rien construit côté code ce soir (CASSANDRA-RH n'existe toujours pas en tant qu'outil réel — `scripts/cassandra-rh.mjs` reste un brouillon non testé, non intégré, non commité) | terminée — fidèle (conception uniquement, prochaine étape : analyse de la simulation full_sim17 comme convenu à la ligne #296, puis construction réelle du noyau CASSANDRA-RH) |

| 298 | 2026-09-22T03:30Z | Charte / Refonte graphique (LE-RÉGISSEUR) | Couleurs réelles des personnages dans les rapports HTML + zoom 150% du transcript + note charte graphique partagée | normal | Deux retours directs de l'utilisateur sur le rendu HTML déjà livré : « je veux du rose pour lia, et du bleu pour noé, à l'image du site » puis « rajouter le zoom 150% à l'ouverture [...] pour le rapport simu transcript ». Vérifié dans le code réel (Article 19) avant de choisir une couleur : `app/globals.css` définit déjà `--lia: #f29bc3`/`--noe: #55dbe5`, les vraies couleurs du jeu — `scripts/html-report.mjs` utilisait par erreur ses deux couleurs génériques de thème (orange/bleu, `--accent`/`--accent2`, utilisées ailleurs pour les titres/tableaux) pour les répliques, sans aucun lien avec l'identité des personnages. Corrigé en réutilisant telles quelles `--lia`/`--noe` du jeu réel, jamais une troisième palette de rapport inventée. Note ajoutée à `docs/referentiel/regles-des-graphismes.md` (à la demande explicite de l'utilisateur, qui anticipe le besoin) : construire pendant la refonte graphique une vraie charte graphique unique déclinée dans le jeu, l'interface ET les rapports — confirmé que cet emplacement ("un endroit pour enregistrer ces notes") existait déjà, comme l'utilisateur le pensait. Puis zoom 150% ajouté strictement à `renderTranscriptHtml()` (dense, répliques courtes), jamais imposé au dossier ni au reste des rapports partageant `THEME_CSS` — l'utilisateur a explicitement demandé de ne pas renvoyer de nouveau fichier, seulement le code. Les deux fichiers full_sim17 déjà livrés régénérés localement (couleurs + zoom) mais non renvoyés, conformément à la demande. 2 nouvelles assertions dans `check-house.mjs` (présence du zoom sur le transcript, absence sur le dossier). `lib/reference.ts` Version 215. `check-house.mjs` vert (153 tests), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |

| 299 | 2026-09-22T04:00Z | Méthode de travail / Refonte graphique (THE-SCREENER) | Trouvaille : THE-SCREENER n'a jamais produit de rapport réel + nouvelle règle « fichier préliminaire dédié » pour les gros chantiers | important | Question directe de l'utilisateur : « après chaque simu, est-ce qu'un rapport est bien enregistré par the screener [...] ? ». Vérifié dans le registre réel (Article 19) : `docs/the-screener/index.md` affirme toujours « Aucun passage archivé pour l'instant », malgré full_sim10/11/16/17 exécutées depuis sa construction — le mécanisme de capture est testé en isolation mais n'a jamais été réellement déclenché pendant une simulation (fermeture auto des popups d'accueil jamais faite). Un vrai oubli process, consigné dans `docs/referentiel/points-fragiles.md`. Puis demande explicite d'un nouveau processus général : « lorsqu'une idée est dite en avance au sujet d'un chantier à venir, cette idée est automatiquement enregistrée sur un fichier préliminaire dédié [...] surtout si le chantier en question est un gros morceau du projet ». Nouvelle règle ajoutée à `docs/regles-de-travail.md` §10 : un registre chantier→fichier préliminaire (CASSANDRA-RH→`cassandra-rh-conception.md`, refonte graphique→`regles-des-graphismes.md`), le critère de ce qui compte comme « gros morceau », et une extension proposée (pas construite) de THE-DEEP-READER pour vérifier rétroactivement qu'aucune idée n'a été oubliée. Appliqué dans la foulée à l'idée elle-même : note « booster THE-SCREENER + le calibrer sur mes préférences » consignée dans `docs/referentiel/regles-des-graphismes.md`. 2 nouvelles tâches internes ouvertes (#185 extension THE-DEEP-READER, #186 câblage réel de THE-SCREENER). Rien construit côté code (documentation uniquement). `lib/reference.ts` Version 216. `check-house.mjs` vert (153 tests), `tsc --noEmit` sans nouvelle erreur | terminée — fidèle |


| 405 | 2026-09-22T05:00Z | Correctif de code / THE-KING + check-tasks-details | Retrofit historique daté de THE-KING (2/19 → 19/19 principes datés) + 2 faux positifs réels du garde-fou de fraîcheur des chantiers | important | Suite de la file de tâches ordonnée par l'utilisateur (« continues les taches en cours dans l'ordre », THE-KING venant juste après `lib/reference.ts`). **(1) THE-KING.** Constat mesuré avant de toucher au code (Article 19) : `extractPrincipleUnits()` trouve bien 19 principes dans `docs/philosophie-et-politique.md`, mais `extractPrincipleDate()` n'en datait que **2** — les dates n'apparaissent que dans un tag du type `[Synthèse, 2026-09-19]`, et les principes fondateurs `[Explicite]` n'en portent aucune. `buildEvolutionDigest()` ne racontait donc l'histoire que de 2 principes sur 19, et le document passait pour figé alors qu'il ne l'est pas — un outil dont le rôle central est justement de « conserver un historique de l'évolution du document » (demande d'origine, tâche #167). Dater les 17 autres à la main aurait produit exactement ce que l'Article 24 interdit : une liste recopiée qui se périme au prochain principe ajouté. Date manquante donc **dérivée de l'historique git réel** — `principleDateFromGit()` (`git log --diff-filter=AM --format=%ad --date=short -S<titre> -- <fichier>`), en prenant la DERNIÈRE ligne du log, c'est-à-dire le PREMIER commit qui a introduit le titre, jamais le dernier qui l'a touché (une reformulation n'est pas une naissance). `principleDate()` réunit les deux sources, la déclarée primant toujours, et expose une `provenance` (« déclarée »/« git ») — une date dérivée n'est jamais présentée comme déclarée, et quand aucune source ne sait, la date reste `undefined` plutôt qu'inventée. Résultat vérifié en direct : **19/19 datés** (2 déclarées + 17 dérivées), de `2026-09-18 — 1.1 L'authenticité prime sur l'agrément` à `2026-09-20 — 2.9 Un outil fiable allège une règle`. Second trou trouvé en finissant : `main()` n'affichait QUE la fraîcheur — le digest et les tensions n'existaient que pour l'appelant CIRCLE-TASKS, jamais pour quelqu'un qui lance l'outil à la main ; le digest, le décompte déclarée/dérivée, les tensions et un avertissement honnête d'inexactitude s'affichent désormais dans la sortie de `node scripts/the-king.mjs`. **(2) check-tasks-details.** Le test live de `checkChantierFileFreshness()` a cassé la construction (son rôle exact) et a révélé DEUX faux positifs réels, corrigés à la racine (Article 3) : (a) *une MENTION n'est jamais une APPARTENANCE* — la tâche #304, dont le Détail dit « continuer à enchaîner les tâches ouvertes sans s'arrêter (**sauf pour la refonte graphique**) », était comptée comme une tâche DU chantier refonte graphique, le sens exactement inverse ; le rattachement ne lit plus que Sujet et Sous-sujet, les deux champs de CLASSEMENT que l'agent choisit délibérément, jamais le Détail qui est un récit libre (mesuré avant de trancher : 36 vs 43 lignes pour CASSANDRA, 8 vs 5 pour la refonte — aucune vraie détection perdue, un chantier réellement traité étant toujours classé) ; (b) *un verdict qui contredisait sa propre phrase* — le message affichait « (1.0j) plus récente que … (1.0j) », soit dans la tolérance, tout en déclenchant l'alerte, parce que la comparaison tournait sur des fractions non arrondies (`1.03 > 0.004 + 1`) alors que le message arrondit ; cette fraction n'est que du décalage d'horloge entre l'horodatage narratif du suivi et l'horloge système de git, exactement ce que `TOLERANCE_DAYS` existe pour absorber — comparaison désormais en jours entiers, la seule granularité que la tolérance ET le message expriment tous les deux. Même racine que plusieurs corrections du même jour : **une absence de mesure, ou une mesure approchée, lue comme une mesure ferme**. Tests : 4 nouveaux cas THE-KING (avec `shImpl` injecté, jamais le vrai git) + 1 vérification live 19/19 + 4 nouveaux cas chantier (cas 5 mention-seule et son contrôle négatif, cas 6 décalage d'horloge et son contrôle négatif) ; `node scripts/check-house.mjs` vert (170 tests), `npx tsc --noEmit` vert (seule erreur restante : `vite.config.ts:60`, pré-existante, non liée). Docs mises à jour le jour même (Article 13) : `docs/referentiel/the-king.md` et `docs/referentiel/check-tasks-details.md`. | terminée — fidèle |

| 406 | 2026-09-22T05:40Z | Nouvel outil / Fiabilité des rapports (TOOL_RELIABILITY) | Avertissement « mes résultats peuvent être inexacts » généralisé aux 28 outils heuristiques, avec deux garde-fous mécaniques | important | Demande explicite de l'utilisateur : « ecotoken devrait indiquer en debut de rapport : "attention mes resultats peuvent etre inexactes" [...] on pourrait elargir cette regle à tous les outils concernés, sauf ceux qui sont fiables 100% (calculs mathematiques purs) ». Constat avant de coder (Article 19) : chaque outil disait DÉJÀ sa nuance (ARGUS « potentiellement jamais lu », CLONE-HUNTER « jamais une factorisation acquise », check-tasks-details « jamais une certitude d'oubli ») — mais chacun à sa façon, à un endroit différent, parfois pas du tout. Construit : `TOOL_RELIABILITY` (`scripts/lib-shell.mjs`), registre unique donnant à chacun des **34 outils de la table maîtresse** sa nature (`mecanique`/`heuristique`) ET sa raison propre — jamais un texte passe-partout ; `reliabilityNotice()`/`printReliabilityNotice()` rendent la phrase commune, et rendent `null` pour un outil mécanique comme pour un outil non classé (un outil inconnu ne doit jamais hériter d'un avertissement par défaut qui masquerait son absence du registre). Frontière posée exactement comme demandé : 6 outils restent muets parce que leur sortie est un fait exact (check-house, LE-COORDINATEUR, doc-HTML, le compteur d'usage, INES-official, objectifs-vs-resultats) ; 28 avertissent. Cas intéressant tranché : la couverture mesurée par AXA-CHECK est exacte, mais en conclure « robuste » ne l'est pas — donc heuristique. **Registre tenu à la main et assumé** (Article 24, cas explicitement prévu : aucune mécanique ne peut deviner si un calcul est exact) — ce qui est mécanique, ce sont ses deux garde-fous (`scripts/doc-report.mjs`) : `findToolsMissingReliability()` (aucun outil de la table maîtresse non classé — donc aucun nouvel outil ne peut naître sans avertissement, exactement l'angle mort que 6 Agents réels avaient connu avec `AGENT_SCRIPT_FILES`) et `findHeuristicToolsWithoutNotice()` (aucun outil heuristique muet), tous deux vérifiés en direct contre le vrai dépôt à chaque commit. Le second est vérifié **par slug**, jamais « la fonction apparaît quelque part » : CHARTER-SPY partage `smart-conso-token.mjs` avec son hôte et l'un aurait couvert l'autre — la même erreur de fond que le reste de la soirée (une présence approximative lue comme une conformité). Deux câblages acceptés : `printReliabilityNotice()` pour les outils à console, `reliabilityNotice()` en bloc d'en-tête pour ceux dont le rapport est un document construit (THE-FINAL-JUDGE, THE-DEEP-READER). **Deux écarts réels trouvés en câblant** : (1) memory-audit n'avait AUCUNE forme de rapport alors que Doc-Report le déclare produisant un rapport « texte » — `buildMemoryAuditReport()` lui donne la même forme qu'à ses pairs ; (2) mon propre script de câblage automatique a sauté l'import de `doc-report.mjs` parce que le fichier contenait déjà la chaîne `printReliabilityNotice` dans la regex de son propre garde-fou — trouvé par le test de fumée qui lance réellement chaque CLI, jamais par la seule lecture du code. L'avertissement manuscrit que THE-KING avait reçu plus tôt le même soir a été retiré au profit de la phrase partagée : une phrase écrite à la main en pied de page était exactement le défaut corrigé ici. Les 27 CLI relancés un par un après câblage, zéro erreur. `node scripts/check-house.mjs` vert (171 tests, dont 13 nouvelles assertions sur le registre et ses deux garde-fous), `npx tsc --noEmit` vert (seule erreur restante : `vite.config.ts:60`, pré-existante). Règle documentée le jour même (Article 13) dans `docs/regles-de-travail.md` §7, avec l'« emplacement générique d'en-tête » nommé comme place réutilisable pour une future phrase générique — première réponse à la demande de gabarit unifié des rapports, qui reste ouverte. | terminée — fidèle |

| 407 | 2026-09-22T06:10Z | Nouvel outil / Gabarit des rapports (report-template.mjs) | Gabarit unifié : un seul contrat, deux rendus (texte + HTML) + emplacement générique d'en-tête + classement de TOUS les émetteurs | important | Demande explicite : « doc-report nouvelle fonction : s'assurer que tous les reports ont le meme format, gabarit [...] graphique (txt ou html, mise en page, couleurs) mais aussi au niveau du contenu : message d'en tete, titres, facon de presnter, organisation, structure... Il y a une place dans chaque report pour pouvoir importer une phrase generique, comme le probleme qu'on a eu a devoir inserer la phrase "resultats non garantis" ». **Asymétrie réelle mesurée avant de coder (Article 19)** : les rapports HTML partageaient DÉJÀ un contrat unique depuis `html-report.mjs` (`title`/`subtitle`/`dateLabel`/`blocks`/`footer`) — c'est pour ça qu'ils se ressemblent tous ; les rapports TEXTE n'avaient AUCUN contrat, chaque outil improvisant son en-tête, son titre, sa date, son ordre. D'où l'épisode #198 : la phrase de fiabilité insérée à la main dans 28 fichiers faute d'emplacement prévu. Construit `scripts/report-template.mjs` : `REPORT_CONTRACT` (ce que tout rapport porte, avec obligatoire/facultatif mécaniquement lisible), `buildReportFrame()` (point de passage unique), `renderTextReport()` + `renderTextBlock()` (même vocabulaire de blocs que le HTML — note/code/list/table — sinon un outil devrait écrire son corps deux fois), et `genericSlots()` : **l'emplacement générique d'en-tête**, rempli depuis le registre partagé dès que l'outil donne son nom, affiché AVANT le titre (une nuance imprimée sous les constats se lit une fois le mal fait). `renderHtmlReport()` routé par le MÊME cadre, avec un paramètre `tool` facultatif pour ne casser aucun de ses ~10 appelants (vérifié : un appelant sans `tool` produit un HTML identique à l'octet près). **Deuxième volet, demande du même message** : « assure toi que doc-report est bien pluggé à tous les outils qui emettent un rapport ». Mesuré : 25 scripts écrivent un fichier, 16 seulement déclarés — 9 ni déclarés ni écartés. Mais en les regardant un par un, la plupart n'écrivent PAS un rapport : d'où `FILE_WRITER_NATURES`, trois natures jamais confondues (rapport = destiné à être lu, doit suivre le gabarit ; journal = mémoire machine relue par du code ; infrastructure = n'écrit rien qui lui appartienne), chacune avec sa raison écrite. Garde-fou `findUnclassifiedFileWriters()` (Article 24) : aucun script ne peut écrire un fichier sans être classé. **Il a attrapé ses propres auteurs deux fois le soir même** : `report-template.mjs` (classé infrastructure — il définit le gabarit sans en produire) puis **Doc-Report lui-même** (classé « rapport » : un gardien qui s'exempte laisse exactement le trou qu'il traque ailleurs). `toolsBoundByReportTemplate()` dérive les 29 tenus au gabarit de REGISTRIES + les « rapport », jamais une troisième liste. **Bug réel trouvé au premier essai de la fonction** : `renderTextReport()` reconnaissait un cadre déjà construit à son TITRE — or un objet brut d'appelant en a un aussi, si bien qu'il sautait silencieusement tout le gabarit ; corrigé à la racine (reconnu à `slots`, la seule chose que `buildReportFrame()` est seul à poser). Tests : 22 nouvelles assertions, dont l'ordre slot-avant-titre, la non-duplication en cas de double normalisation, l'intégrité des appelants HTML existants, et les 3 cas du garde-fou d'émetteurs — `node scripts/check-house.mjs` vert (172 tests), `npx tsc --noEmit` vert, 5 gros appelants HTML relancés. Documenté le jour même dans `docs/regles-de-travail.md` §7. Reste ouvert (tâche #201, pure-gold-unity) : vérifier que chaque outil tenu APPLIQUE réellement le gabarit, et non seulement qu'il le doit. | terminée — fidèle |

| 408 | 2026-09-22T06:40Z | Nouvel outil / CASSANDRA-RH (organigramme) | CASSANDRA reconstruit l'organigramme depuis les données réelles — tâches #171/#172/#179 fermées d'un coup | important | Première tâche du mode nocturne autonome, priorité explicite de l'utilisateur (« tu avances sur les outils en pause : cassandra en priorité »), calibrée juste avant son coucher (8 questions) : « CASSANDRA le reconstruit à chaque fois ». **Constat avant de coder (Article 19)** : `docs/referentiel/organisation-agence.md` décrit bien l'organisation, mais sa propre fiche reconnaît que « la tenue à jour de ce document reste manuelle » — et trois tâches de suivi le demandaient depuis des jours (#171 tenir la liste à jour, #172 liste des employés puis organigramme, #179 connaissance parfaite de chaque membre). **Preuve directe que la tenue manuelle ne tenait pas, trouvée en construisant** : TROIS membres certifiés (CIRCLE-TASKS, tool-brain, find-deep-booster) n'appartenaient à AUCUNE suite depuis leur certification, sans que personne ne l'ait jamais vu — ils forment désormais la **Suite Orientation** (calibrage utilisateur : ceux qui disent quoi faire ensuite et avec quoi, jamais ce qu'il faut en penser). Construit : `buildOrganigramme()`, `findMembersWithoutCategory()`, `renderOrganigrammeReport()` et `ORG_RANKS` (`scripts/cassandra-rh.mjs`). **Tout est DÉRIVÉ, rien n'est recopié** (Article 24) : les rangs viennent de la table maîtresse réelle et d'`AGENT_CATEGORIES`, les 6 Gardiens de `GARDIEN_DOMAINS` (leur source mécanique, jamais une seconde liste), les émetteurs de rapport de `FILE_WRITER_NATURES` — un organigramme qui se recalcule ne peut pas se périmer. `ORG_RANKS` centralise les libellés de rang en UN endroit, pour que le renommage calibré le même soir (Scribes 🥈 / Premium 🥇 / Platine noir ⬛, **mis de côté en attente de validation** car classé sensible « difficile à annuler ») reste une seule ligne à changer. **Deux défauts trouvés à la première lecture du rendu réel (Article 15)** : `Doc-Report`/`doc-report` apparaissaient en double et `check-spirit` figurait à deux rangs à la fois — même cause racine, un dédoublonnage par LIBELLÉ là où la table maîtresse écrit « Doc-Report »/« check-spirit.mjs » et le disque `doc-report.mjs`/`check-spirit`. Corrigé à la racine par `toolIdentitySlug()` (`le-coordinateur.mjs`), volontairement DISTINCT de `slugifyAgentName()` qui construit un chemin de documentation et ne doit jamais changer — deux questions différentes, jamais une troisième règle de nommage inventée. Câblé dans la Ronde (`organigramme-signal`, thème « Qualité du code » — pas « Qualité & fun », dont les 4 options étaient pleines, contrainte réelle attrapée par le test qui garde la limite d'affichage ; et parce qu'un membre sans suite est une dette d'organisation, pas un divertissement), rapport texte via le gabarit commun, archivé dans `docs/cassandra-rh/organigramme/`. `docs/referentiel/organisation-agence.md` reçoit un encadré disant qu'il n'est plus la source de vérité de la LISTE et ne garde que le POURQUOI. Résultat mesuré : 26 membres certifiés (2 Cadres, 6 Gardiens, 18 membres en 7 suites), 2 au socle, 7 émetteurs non certifiés, 0 sans suite. Tests : 16 nouvelles assertions (dont la somme des rangs, l'absence de doublon par identité, et un organigramme entièrement fabriqué pour ne dépendre d'aucun état du dépôt) — `check-house.mjs` vert (173 tests), `tsc` vert. | terminée — fidèle |

| 409 | 2026-09-22T07:10Z | Correctif de code / CASSANDRA-RH (angle mort d'effectif) | CASSANDRA ne voyait pas 4 de ses 26 membres — dont son propre co-directeur | important | **Trouvé en lançant la Ronde du mode nocturne** : son bilan complet annonçait « 22 membre(s) actif(s) — 1 Agent Cadre » pendant que l'organigramme construit une heure plus tôt (tâche #408) en comptait 26 et 2. Deux vues du même effectif qui se contredisaient. **Cause racine tracée (Article 19)** : `teamRoster()` filtrait `row.statut === "Agent"` seulement, une ligne écrite AVANT que la catégorie « Membre certifié (classique) » n'existe (2026-09-21) et jamais revisitée depuis. Conséquence réelle bien pire qu'un chiffre faux : les 4 classiques (LE-COORDINATEUR, CIRCLE-TASKS, find-deep-booster, tool-brain) étaient **totalement invisibles** à CASSANDRA — badge jamais supervisé, trou de couverture jamais détecté, jamais accueillis comme nouveaux visages. L'Agent RH ne voyait pas son propre co-directeur, à rebours exact de la tâche #179 (« connaissance parfaite de chaque membre de l'équipe »). **Décision prise seul, à confirmer au réveil** : un test existant (2026-09-21) affirmait explicitement le contraire (« only the real "Agent" rows must be counted [...] never a Membre certifié (classique) [...] never "membres" in the RH sense here ») — mais son commentaire ne justifiait nulle part cette exclusion (il portait sur un tout autre bug, celui des parenthèses dans les slugs), et elle contredit les mots explicites de l'utilisateur enregistrés LE MÊME JOUR dans `le-coordinateur.mjs` : « membre certifié couvre les deux catégories », « il reste néanmoins un vrai membre certifié badgé 🎖️, jamais un simple Utilitaire nommé ». J'ai tranché pour les mots de l'utilisateur et inversé l'assertion, en documentant les deux lectures dans le test pour que le choix reste réversible en une ligne. Correctif : `teamRoster()` filtre désormais sur `CERTIFIABLE_STATUTS` et porte `classique` par membre ; `computeBadgeResults()` dérive `ownKnowledge: !member.classique` au lieu de le figer à `true` — sans ça, élargir le roster aurait fabriqué 3 faux manques par classique (instanciation, registre, blueprint) qu'ils n'ont par définition pas à fournir. C'est exactement la distinction que `checkAllAgentBadges()` (LE-COORDINATEUR) appliquait déjà correctement : CASSANDRA s'aligne sur lui, jamais une seconde règle inventée. Vérifié : 26/26 badges complets, zéro faux manque, les deux vues concordent (26 certifiés, 2 Cadres). Un Utilitaire nommé reste bien exclu — il ne porte aucun badge. Tests : assertion inversée + 1 nouvelle sur le portage de `classique`, 4 assertions dépendantes réalignées (effectif, groupement par catégorie, signal léger) ; `check-house.mjs` vert (173 tests), `tsc` vert. | terminée — fidèle |

| 410 | 2026-09-22T07:40Z | Organisation / Smart Breaker | Smart Breaker certifié — l'anomalie était son statut, le vrai manque était son registre | normal | Calibré explicitement juste avant le coucher de l'utilisateur (« Anomalie — il doit être certifié »). **Le constat** : Smart Breaker n'était pas membre certifié pour une raison purement technique — son statut dans la table maîtresse était la chaîne `Agent (structure particulière : pas de dossier docs/ dédié, son « registre » est le fichier local .gemini-key-health.json, jamais committé)`, qui ne correspond à aucun statut certifiable. Un exclu du décompte sans que personne n'en ait jamais décidé ainsi. Mais le manque qu'elle décrivait était RÉEL (aucun registre committé) : corrigé pour de vrai plutôt que contourné. Créés : `docs/referentiel/smart-breaker.md` (l'instanciation — ce qu'il est, comment il est câblé, ses limites honnêtes ; son RÉCIT reste dans `smart-breaker-historique.md`, jamais dupliqué) et `docs/smart-breaker/index.md` (registre des ÉPISODES réels de blocage — jamais une copie de `.gemini-key-health.json`, qui contient l'état des clés et reste gitignoré ; deux épisodes réels déjà consignés, dont la leçon « le retryDelay de Google est trompeur pour un quota journalier »). Statut ramené à `Agent`. **Déviation de blueprint déclarée plutôt que renommage** : son blueprint s'appelle `docs/outil-resilience-api.md` parce qu'il a été écrit le 2026-09-18, AVANT que le surnom n'existe (baptisé le 2026-09-19) — le renommer casserait les renvois croisés. Nouvelle option `blueprintPath` dans `checkAgentOnboarding()`, exactement le même patron que `registryPathPrefix` déjà en place et pour la même raison (une déviation se DÉCLARE, jamais ne se devine), distincte de `cousinOf` qui dit « pas de blueprint propre, et c'est voulu ». Ligne du Catalogue de CLAUDE.md corrigée : elle portait encore « Outil de résilience API », le nom d'avant le surnom, avec une colonne Instanciation vide. **Les deux garde-fous de l'Article 24 se sont déclenchés à la seconde près**, preuve vivante qu'ils fonctionnent : `findScriptsMissingFromAgentFiles()` a exigé son entrée dans `AGENT_SCRIPT_FILES` (couverture AXA-CHECK) dès le changement de statut, puis `findRegistriesMissingFromCircle()` a exigé un item de Ronde ou une exclusion motivée pour le nouveau registre. Exclusion retenue et documentée : ce registre se remplit PAR ÉVÉNEMENT, jamais par calendrier — le sonder périodiquement coûterait de vrais appels API (Article 22) pour écrire « rien à signaler » presque à chaque fois. Effectif : 26 → **27 membres certifiés**, émetteurs non certifiés 7 → 6. `check-house.mjs` vert (173 tests), `tsc` vert. | terminée — fidèle |

| 411 | 2026-09-22T08:05Z | Correctif de code / SMART-CONSO-TOKEN ↔ ecotoken | Réponse à « est-ce que ecotoken partage son experience avec smart eco token ? » — le partage était à sens unique, l'expérience ne circulait pas du tout | normal | Question directe de l'utilisateur, mise en file puis traitée en mode nocturne. **Réponse honnête après vérification dans le code (Article 19)** : le partage existait bien, mais à SENS UNIQUE et volontairement — `ecotoken.mjs` importe 5 fonctions de `smart-conso-token.mjs` (`estimateTokens`, `measureClaudeMdWeight`, `buildClaudeMdRuleTable`, `listDatedNarrativeMarkers`, `SCOPE_LEVELS`), et l'inverse est refusé par conception, un commentaire existant l'expliquant déjà : « ecotoken importe déjà ce fichier, l'importer en retour créerait un cycle ». Le lien retour se faisait par une simple phrase de texte nommant ecotoken. **Ce qui manquait réellement n'était donc pas du code mais de l'EXPÉRIENCE** : le registre `docs/ecotoken/index.md` accumule 17 passages réels, dont ce que l'utilisateur a REFUSÉ et ce qui attend encore sa décision — et rien ne le lisait. Or lire un fichier n'est pas importer un module : aucun cycle, la frontière déjà tranchée tient intacte. C'est exactement la règle de travail §3ter (« toute expérience vécue doit alimenter un outil »). Construit : `ecotokenExperience()` (lit le registre, distingue « refusé » de « à trancher » — deux faits différents, et la colonne Décision est la seule écrite à la main) et `ecotokenExperienceNote()` (`null` quand il n'y a rien à dire, jamais une ligne creuse), câblées dans `scanDocumentWeight()` via un paramètre facultatif `ecotokenIndexText` — un appelant qui ne le fournit pas obtient un avis identique à l'octet près. **Fait utile immédiatement remonté** : 15 propositions d'ecotoken attendent encore une décision de l'utilisateur, et 1 cible a déjà été refusée (« 5 passages déjà mécanisé ») — à ne jamais reproposer en tête. Frontière inchangée : SMART-CONSO-TOKEN dit combien ça coûte et ce qui a déjà été tenté, ecotoken dit quoi faire ; aucun des deux ne recalcule ce que l'autre sait. Tests : 12 nouvelles assertions (comptage des seules lignes réellement datées, distinction refusé/en attente, registre vide honnête, avis inchangé sans registre). `check-house.mjs` vert (174 tests), `tsc` vert. | terminée — fidèle |

| 412 | 2026-09-22T08:25Z | Correctif de code / THE-SCREENER (tâche #186) | Premier déclenchement réel — et il annonçait « Capture réussie » sur une image où il n'y a rien à voir | important | Tâche #186, ouverte depuis le 2026-09-19 et explicitement autorisée par l'utilisateur avant son coucher (« Oui, branche-le — c'est de l'outillage »). Le serveur tournait avec `full_sim18` en cours : exactement la condition réelle qui manquait depuis la construction de l'outil. **Constat** : `docs/the-screener/index.md` affirmait « Aucun passage archivé pour l'instant » malgré full_sim10/11/16/17 — un vrai oubli process, jamais une décision. Premier lancement effectué, capture obtenue… et **inutilisable** : l'image ne montre QUE la popup « Comment vous appeler ? », toute la scène 3D floutée derrière. **Cause tracée (Article 19)** : `nicknameOpen = ready && !world.story?.observer && !nicknameDone` (`app/page.tsx:260`) — cette popup reste ouverte tant que la partie n'a pas d'observateur, donc pendant TOUTE la phase 1 d'une simulation. Le bon moment de capture est la phase 2, une fois l'observateur entré. **Le vrai défaut était dans l'outil, pas dans la page** : il annonçait « Capture réussie » sur cette image. Pour un outil dont le rôle est de NOTER un rendu graphique, c'est un faux succès qui aurait fait poser une note sur une image sans rien à noter — exactement la famille d'erreur corrigée partout ailleurs cette nuit (une absence de mesure lue comme une mesure). Corrigé : `captureOnce()` détecte une modale bloquante (`.nickname-overlay`) et rend `masque: true` avec la raison ET le bon moment de capture ; la sortie console dit « Capture prise mais MASQUÉE » et « aucune note graphique ne doit être posée » ; le rapport HTML place l'avertissement AVANT l'image, jamais sous elle (un lecteur doit le savoir avant de regarder). Registre enfin peuplé avec ce premier passage réel et sa leçon. Aucune mutation de l'état du jeu pendant la simulation en cours : la capture reste en lecture seule (l'alternative — remplir le pseudo — aurait posé un observateur au milieu d'une partie). Tests : 4 assertions (avertissement présent, placé avant l'image, absent sur une capture nette, échec réel distinct d'une capture masquée). `check-house.mjs` vert (175 tests). | terminée — fidèle |

| 413 | 2026-09-22T08:45Z | Correctif de code / CLONE-HUNTER | Le détecteur de duplication était aveugle aux lettres accentuées — donc à la moitié de ce dépôt | normal | Bug consigné dans `docs/referentiel/points-fragiles.md` le 2026-09-22 (« idée non tranchée : un mode simu pour CLONE-HUNTER ») mais jamais corrigé : `TOKEN_RE` ne reconnaissait que l'ASCII (`[A-Za-z_$][A-Za-z0-9_$]*`), si bien que « Noé » se scindait en DEUX jetons (« No » + « é ») là où « Lia » n'en faisait qu'un — cassant l'égalité de longueur que `matchLineTokens()` exige avant toute comparaison. Corrigé avec `\p{L}`/`\p{N}` et le drapeau `u`, jamais une liste d'accents recopiée à la main qui oublierait le prochain caractère (Article 24). **Portée réelle du bug, plus large que le cas d'origine** : il ne touchait pas que les prénoms des personnages — tout identifiant, toute chaîne et tout commentaire en français était mal découpé, c'est-à-dire une grande partie de ce dépôt. Vérifié après correctif : `tokenizeLine("Noé intrigue")` rend bien 2 jetons, même compte que `"Lia intrigue"`. **Ce qui n'a PAS été fait, et pourquoi** : `matchLineTokens()` n'apparie toujours pas ces deux phrases de prose — c'est correct, CLONE-HUNTER est un détecteur de CODE et `isRenamableIdentifier()` exige un contexte d'identifiant. Le « mode simu » (détecter la répétition dans un transcript) reste l'idée non tranchée qu'elle était, non construite : la corriger aurait demandé un vrai round de calibrage, et ce correctif-ci n'en était que le prérequis mécanique. Tests : 5 assertions (mot accentué en un jeton, même compte que son équivalent sans accent, identifiant français réel, ASCII inchangé à l'identique, chaîne entre guillemets gardée entière). `check-house.mjs` vert (176 tests). | terminée — fidèle |

| 414 | 2026-09-22T09:00Z | Correctif de code / THE-SCREENER (seconde passe) | Trois fenêtres modales éliminées une par une — la troisième résiste, code spéculatif retiré plutôt que laissé | normal | Suite directe de #412. La première correction supposait que seule la popup de pseudo bloquait ; une capture prise en phase 2, observateur bien posé, restait pourtant masquée. **Trois fenêtres partagent la classe `.nickname-overlay`** (`app/page.tsx`) : reprise de partie, pseudo, avertissement légal. Éliminées dans l'ordre, chacune vérifiée : (1) le pseudo dépend de l'état du jeu — résolu en capturant en phase 2 ; (2) l'avertissement légal et le message d'accueil dépendent de la MÉMOIRE DU NAVIGATEUR (`maison-disclaimer-accepted`, `maison-welcome-seen`), pas du jeu — pré-posés via `addInitScript()`, ce qui reproduit exactement un visiteur déjà venu une fois et ne touche rien côté serveur (l'alternative, remplir le pseudo, poserait un observateur au milieu d'une partie) ; (3) reste « Reprendre la maison ? ». Le diagnostic NOMME désormais la fenêtre trouvée au lieu de lister les trois possibles — la première version disait « pseudo, avertissement ou reprise » et envoyait chercher la mauvaise. **Tentative retirée, assumée** : cliquer « Conserver l'histoire » (bouton purement local, aucun reset) n'a pas suffi en conditions réelles — la fenêtre restait détectée juste après. Code retiré plutôt que laissé en place : un outil qui prétend avoir dégagé la vue sans l'avoir fait est pire qu'un outil qui dit honnêtement « masqué ». Piste notée pour l'utilisateur : la bonne réponse est probablement côté APPLICATION (un paramètre d'URL d'observation sans fenêtre modale), ce qui relève de la refonte graphique — sa borne, donc arrêt volontaire ici. `check-house.mjs` vert (176 tests). | terminée — fidèle |
| 415 | 2026-09-22T09:40Z | Analyse de simulation / Correctif de code / Charte | full_sim18 analysée (Article 18) : deux causes racines de SCRIPT trouvées, la conclusion de la tâche #143 établie comme fausse, trois garde-fous mécaniques construits, l'écho systématique Lia/Noé mis de côté pour validation | critique | Plan de nuit autonome, étape 1.2 (« tu fais cette analyse et applique les ajustements ou correctifs, sauf si validation tu mets de côté »). **Trouvaille principale** : `dossierHumanTurns` était à 20 (seuil : 3), 2 tirages de roulette enregistrés, les deux personnages au salon sur 69/69 blocs de phase 2 — toutes les conditions documentées réunies — et pourtant `dossierAsked: {}` : aucun des trois pièges jamais posé, tout le second acte absent. Cause tracée en lisant les modes réels du journal (`payload.mode`) plutôt qu'en supposant : la phase 2 était faite de **20 tours `chat` et de rien d'autre**, alors que `dossierGateEligible` (`app/api/lia/route.ts:671`) exige un tour `interact`/`autonomous` tandis que `dossierHumanTurns` (ligne 598) ne monte, lui, que sur un tour `chat` — le script remplissait le compteur sans jamais offrir le tour où le piège pouvait être posé. **Ceci corrige explicitement la conclusion de la tâche #143** (2026-09-20), qui attribuait le même symptôme sur full_sim16 à un budget de 25 tours trop court : hypothèse jamais vérifiée contre la contrainte de mode, et fausse — même famille d'erreur que le reste de cette session (une absence de mesure lue comme une mesure). Vérifié que le jeu réel n'a PAS ce trou : `app/page.tsx:245` relance un tour `autonomous` toutes les 21 s en parallèle du chat, donc un vrai visiteur atteint bien le dossier retourné — aucune ligne du jeu touchée (Article 19 + périmètre sensible). **Seconde cause racine, trouvée en traçant un nom inventé** : Noé appelle l'observateur « Caspeer », nom que personne n'a donné ; `story.observer` était vide car le script n'envoie jamais de tour `identify` avant ses `chat`, contrairement à `app/page.tsx:268`. Un seul manque qui explique DEUX symptômes cherchés séparément — le nom inventé (Article 12) et la fenêtre de pseudo qui masque toute capture THE-SCREENER depuis des jours (tâche #186, « la troisième fenêtre qui résiste » du commit 75ad7fb de cette nuit). **Troisième bug, trouvé en archivant** : `summarize-simulation-log.mjs` ne savait plus lire la forme de journal que le script produit aujourd'hui (plate : `{payload,status,round,evidence,decisions}` au lieu d'imbriquée `response.story`) et affichait sereinement « Round final : ? / 0 événement » sur un journal de 76 requêtes montant au round 64 — encore la même famille. **Correctifs appliqués** (outillage seulement, jamais le jeu) : `detectJournalShape()`/`summarizeFlatActions()`/`shapeBlindSpots()`/`modeOf()` (les deux formes lues, la forme nommée en tête du résumé, ce que chaque forme ne PEUT pas donner déclaré, un zéro jamais silencieux), `checkPhase2Autonomy()`/`formatPhase2Autonomy()` et `checkObserverIdentified()`/`formatObserverIdentified()` — de vrais garde-fous mécaniques sur le journal réel (Article 24), jamais une promesse écrite dans un coin, puisque le script de simulation lui-même est réécrit à chaque fois et jamais committé. Les deux règles aussi écrites noir sur blanc dans `docs/regles-de-travail.md` étape 1. Résultat : full_sim18 passe de « 0 événement » à 38 événements extraits, et son résumé archivé porte désormais les deux avertissements en tête. **Mis de côté pour validation, aucun correctif appliqué** : la violation massive de l'Article 11 relevée par EL-PROFESSOR (note 42/100, `docs/el-professor/full_sim18.md`) — Lia et Noé répondent à CHAQUE message de l'observateur par deux répliques qui disent la même chose, avec des squelettes de phrase et des verbes partagés (« je préfère **crever** les yeux ouverts que de **gober** tes illusions » / « je préfère **crever** l'abcès que de **gober** tes fables »). Toucher au registre des personnages relève du périmètre sensible défini par l'utilisateur, donc jamais appliqué sans son accord, malgré l'évidence du défaut. Deux anomalies mineures également notées sans correction : « On est touchées » pour un couple dont Noé est masculin (famille #109) et les jauges internes énoncées en clair (« Mon incertitude est à 20 % »). Archivage LE-RÉGISSEUR fait, ligne de jugement ajoutée à `docs/simulations/index.md`, note et rapport détaillé ajoutés à `docs/el-professor/`. `check-house.mjs` 176/176, `tsc --noEmit` propre (hors l'erreur préexistante de `vite.config.ts`) | terminée — fidèle |
| 416 | 2026-09-22T10:10Z | Ronde finale / Correctif de code | Ronde finale de la nuit (étape 7 du plan) : le compteur d'usage des outils disait « jamais sollicité » là où il ne mesurait rien du tout | important | Étape 7 du plan de nuit autonome (« tu lances une ronde finale »). Items gratuits exécutés : KPI (Robustesse du code 100 %, Smart Breaker performance 100 % / améliorations 88 %), Doc-Report, CASSANDRA-RH, check-tasks-details, tool-brain, THE-KING (philosophie fraîche, aucune tension détectée), check-house 176/176, tsc propre. **Vrai constat remonté par la Ronde, et corrigé** : Doc-Report et tool-brain annonçaient 10 outils « jamais sollicités », dont plusieurs que je venais littéralement de lancer. Deux causes distinctes, longtemps confondues sous la même étiquette : (1) `doc-report.mjs` et `ecotoken.mjs` ont bien un CLI mais n'appelaient jamais `recordCliUsage()` — Doc-Report se comptait donc lui-même comme jamais sollicité tout en reprochant cette absence aux autres ; enregistrement ajouté aux deux. (2) `the-final-judge.mjs` et `memento.mjs` (memory-audit) n'ont AUCUN CLI — ce sont des bibliothèques appelées autrement, donc le compteur ne pourrait rien voir même s'ils tournaient dix fois par jour ; les accuser de désusage était une absence de mesure lue comme une mesure, exactement le motif récurrent de cette session. Nouveau `scriptRecordsItsUsage()` (lecture du vrai fichier, jamais une liste tenue à la main — Article 24) : l'étiquette distingue désormais « jamais sollicité » (vrai signal, INES-official par exemple, qui lui enregistre bien) de « aucun point d'enregistrement dans son script — ce n'est PAS un constat de désusage ». 4 nouveaux tests. À noter pour plus tard, non corrigé : « Tableau de bord / KPI — remise HTML — dernier rapport : jamais committé » reste vrai, à trancher avec l'utilisateur | terminée — fidèle |
| 417 | 2026-09-22T11:20Z | Nouvel outil / Nouvel Agent / Charte | Deux outils de process construits (god-of-all-process, process.simulation.guardian) + carte d'identité en tête de chaque rapport + process autonome formalisé | critique | Quatre demandes du matin, traitées point par point après quatre fenêtres de calibrage (Article 16). **(a) Carte d'identité des rapports** (`report-template.mjs`) : la demande était « la version de claude utilisée au moment de la conception du rapport, avec aussi en plus les infos generiques ». Occupe l'emplacement générique déjà prévu, jamais réécrite à la main. Cinq lignes choisies par l'utilisateur : version de Claude, date/heure UTC, état exact du code (commit + branche + si des travaux n'étaient pas enregistrés), contexte de production (Ronde / nuit / demande), fraîcheur de l'outil. **Le modèle ne se devine pas** : un script ne peut pas savoir quel modèle fait tourner l'agent, donc `recordAgentSession()` le dépose dans `.agent-session.json` (jamais committé, déclaré dans LOCAL_JOURNALS) et, en son absence, le rapport écrit « non renseignée » — jamais un nom deviné ni le dernier connu. Corollaire ajouté par l'utilisateur : veiller à ce que ce cas n'arrive jamais est le travail du gardien de process, d'où `checkAgentSessionDeclared()`. **(b) god-of-all-process** : calibré comme « le tool-brain des process » — à la question « qu'est-ce qui gouverne ça ? », il répond quel process s'applique, où il est écrit, qui le surveille, et quelles étapes ont laissé leur trace. Suit l'avancement en le DÉDUISANT des traces disque, jamais via un compteur qui se désynchroniserait. Trois états jamais confondus (tracée / non tracée / sans trace vérifiable). **Bug réel trouvé sur lui-même à son tout premier lancement** : trois de ses propres sondes pointaient vers des chemins inexistants et annonçaient des étapes « manquantes » qui avaient parfaitement eu lieu — corrigé, et `findBrokenProbes()` ajouté pour qu'une sonde cassée soit désormais rapportée comme un défaut de l'outil, jamais comme un reproche au travail surveillé. Garde-fous Article 24 : gardien réel, document réel, tensions déclarées citant des process existants. Autorité : signale, ne corrige jamais. **(c) process.simulation.guardian** (`scripts/process-simulation-guardian.mjs`, nom donné par l'utilisateur, aligné partout après une incohérence de nommage que j'avais moi-même créée) : référent de discipline d'exécution, consulté AVANT le lancement — parce qu'une simulation coûte une heure de quota et que les deux défauts de full_sim18 étaient visibles dans le scénario. Porte 5 leçons, chacune payée par une simulation entière, et vérifie les 9 moments narratifs + la consultation Smart Conso API. Bloque par défaut, passage en force possible avec raison écrite archivée. **Bug réel trouvé par son propre test** : ses contrôles renvoyaient `plan.X === true`, donc toujours un booléen — « pas renseigné » devenait « non ». La même erreur que partout ailleurs, commise dans le code même censé la combattre ; corrigé en lisant la valeur brute (`champ` déclaré, plus de fonction de test par leçon). **Et à son premier lancement réel, il a trouvé une vraie étape sautée cette nuit** : la ligne KPI de full_sim18 n'avait jamais été écrite — comblée dans le même commit. **(d) `docs/mode-auto-process-guardian.md`** : le mode nocturne formalisé, avec la définition gravée de « sensible » (personnages + visiteur + irréversible), la règle « préparer tout sauf l'application » sur une branche séparée quand on bute dessus, le rendu (fichier texte + 5 lignes), et quoi faire du temps restant. **Correction de ma propre imprécision de cette nuit** : j'avais écrit « le vrai visiteur reçoit un tour autonome toutes les 21 s » comme un absolu — vérifié dans `app/page.tsx`, c'est le comportement par défaut mais il s'interrompt en pause, à la reprise d'une partie sauvegardée, ou après une erreur réseau ; corrigé dans les deux endroits qui l'affirmaient. Six garde-fous existants ont refusé le travail en cours de route (journal local non déclaré, outil heuristique sans avertissement, manque au catalogue LE-COORDINATEUR, manque à AGENT_SCRIPT_FILES, classification de fiabilité absente, assertions de slots périmées) — tous traités à la racine, aucun contourné. `check-house.mjs` 177/177, `tsc --noEmit` propre | terminée — fidèle |
| 418 | 2026-09-22T11:45Z | Correctif de code / Personnages (SENSIBLE — validé) | Écho systématique Lia/Noé corrigé à la racine + deux règles de langue, après validation explicite de l'utilisateur | critique | Le vrai sujet de full_sim18 (note EL-PROFESSOR 42/100), mis de côté cette nuit parce qu'il touche le registre des personnages — périmètre sensible. Quatre choix validés en fenêtre de calibrage ce matin avant toute ligne de code. **Investigation d'abord (Article 19)** : le second personnage VOIT bien la réplique du premier (`heard` passé au second appel `think()`, `app/api/lia/route.ts:975`), et la seule consigne anti-écho qu'il recevait était « sans répéter sa question ». Étroite pour un problème qui porte sur le FOND. Le registre anti-répétition existant (`dialogueProgress`, `overusedThemes`, `echoWords`, le test auto-appliqué sur « on tourne en rond ») est riche mais travaille à l'échelle de la SESSION et sur les MOTS : rien ne gardait la paire du MÊME TOUR, qui est exactement là où la collision se produit. **Correctif 1 — exigence d'angle différent** : la consigne étroite est remplacée (pas complétée — la laisser continuerait à suggérer qu'éviter la question suffit) par une exigence de fond que le modèle s'applique à lui-même (corollaire de l'Article 17, jamais une liste de formulations interdites) : ce que ma réplique apporte que la sienne n'apportait pas — objection, autre aspect, détail non relevé, pique qui vise autre chose, ou s'adresser à LUI plutôt qu'à l'humain — plus l'interdiction explicite de recopier la charpente de sa phrase (même amorce, même charnière, mêmes verbes), qui est précisément la forme qu'avait prise l'écho (« je préfère **crever** les yeux ouverts que de **gober** tes illusions » / « je préfère **crever** l'abcès que de **gober** tes fables »). **Correctif 2 — accord du duo** (`lib/lia.ts`) : règle de grammaire générale plutôt qu'un cas corrigé — dès qu'un personnage dit « on »/« nous » du duo, masculin pluriel, le féminin ne revenant que pour soi seule. Corrige « on est touchées » (famille #109). **Correctif 3 — jauges internes jamais en chiffres** : un personnage ressent un doute, il n'en connaît pas le pourcentage ; dire la direction et l'intensité, jamais la valeur. Les chiffres qui décrivent le MONDE (âge, heure, date sur un document) restent dicibles — borner la règle évite de coûter à l'enquête ses détails concrets. Corrige « Mon incertitude est à 20 % » dit par les DEUX au même tour, qui aggravait l'écho. **Reste à faire, non fait dans ce commit** : le quatrième choix validé (« pas toujours les deux qui répondent » — laisser Lia user de son silence méprisant, déjà prévu par la charte mais jamais observé puisqu'elle est obligée de parler à chaque tour) est structurel et sera traité séparément, avec le soin que demande `route.ts` (criticité 9). 6 nouveaux tests, dont 3 lus sur le vrai fichier de consignes et 3 sur la charge réellement envoyée au modèle. `check-house.mjs` 177/177, `tsc --noEmit` propre | terminée — fidèle |
| 419 | 2026-09-22T12:10Z | Charte / Nouvel outil (process maître) | Article 25 (vérifier régulièrement son propre travail) + god-of-all-process se surveille désormais lui-même + renommage mode-auto-process-guardian | critique | Trois demandes de la file du matin. **(a) Article 25 ajouté à la charte** (demande textuelle : « verifie régulièrement ton travail : à inscrire dans la charte [...] en utilisant si besoin les outils »). Distingué explicitement de ses voisins pour ne pas créer de doublon (Article 14 = vigilance AU MOMENT d'agir ; Article 20 = les Gardiens à chaque itération ; Article 21 = vérification exceptionnelle sur demande ; Article 25 = retour à froid vers ce qu'on a soi-même déjà considéré comme terminé). Justifié par quatre écarts réels trouvés le matin même en relisant la nuit : une affirmation absolue qui ne l'était pas, une ligne d'index KPI jamais écrite sur une tâche close, trois sondes d'un outil neuf pointant nulle part, et la confusion « absence lue comme mesure » commise dans le code écrit pour l'empêcher. Aucun n'aurait été trouvé de mémoire — tous l'ont été en relançant un outil réel contre l'état réel. Corollaire écrit : un outil qui n'a jamais tourné pour de vrai n'est pas vérifié, c'est une intention. **(b) LE PROCESS MAÎTRE** — question de l'utilisateur : « the god of process a-t-il son propre process, et verifie-t-il son propre process ? (process maitre, tres important) ». La réponse était NON, et c'était le seul point aveugle du dispositif : l'outil qui reproche aux autres de ne pas avoir de gardien n'en avait aucun lui-même. Corrigé par un 4e process `meta` (« Tenue du dispositif de process lui-même »), gardé par god-of-all-process en personne, plus `selfCheck()` qui rend cette auto-surveillance réellement exécutable plutôt que déclarée : il tombe au rouge si le process maître disparaît, s'il est confié à un autre gardien, ou si un process perd son gardien/document/sonde/tension. 4 tests, dont deux qui vérifient la détection en retirant réellement le process maître et en le détournant. **(c) Renommage** : `docs/process-autonome.md` → `docs/mode-auto-process-guardian.md` (nom corrigé en cours de route par l'utilisateur, la première version disait « process-mode-autonome-ia »), références mises à jour partout, nom du process aligné dans le registre. `check-house.mjs` 177/177, `tsc --noEmit` propre | terminée — fidèle |
| 420 | 2026-09-22T12:40Z | Nouvel outil / Migration de masse | pure-gold-unity construit ET chantier d'unification terminé : 11/29 → 29/29 rapports au gabarit partagé | critique | Deux demandes de la file : « ou en est de l'outil d'unification, il est pret ? » (réponse honnête : NON, seule l'obligation existait) et « signaler et tout migrer maintenant, en verifiant bien le travail et en fiabilisant ». **(a) `scripts/pure-gold-unity.mjs`** : mesure quels outils APPLIQUENT réellement le gabarit, pas seulement lesquels le doivent — la distinction « devoir » / « faire » qui est le motif récurrent de tout ce paysage. Première mesure réelle : 11/29 (38 %), l'obligation existait depuis le matin et n'avait rien changé. Classe chaque outil non conforme par difficulté de conversion (légère/moyenne/lourde) d'après des indices réels dans son code, et ne migre JAMAIS tout seul. **(b) Rendu la migration possible** : `printReportHeader()` ajouté à `report-template.mjs` — beaucoup d'outils impriment au fil de l'eau plutôt que de construire un objet ; leur demander de tout restructurer, c'était réécrire 18 sorties avec le risque d'en abîmer une. L'en-tête partagé (avertissement + carte d'identité) passe par `buildReportFrame()`, donc aucun second gabarit ; le corps reste la voix de chaque outil. **(c) Les 18 migrés**, 12 par un passage mécanique et 6 à la main. **Erreur réelle commise et rattrapée** : le passage mécanique a visé, dans ecotoken, le titre d'une SOUS-COMMANDE écrit en gabarit de chaîne — le `${c.messagesParSession}` se serait affiché littéralement. Le script tournait sans erreur ; seule la RELECTURE de sa sortie l'a montré. C'est exactement l'Article 25 écrit une heure plus tôt : un script qui démarre n'est pas un script correct. Corrigé à la main, les deux points d'entrée d'ecotoken revérifiés un par un. **(d) memory-audit, le dernier cas** : `pure-gold-unity` le voyait « non conforme sans aucun indice de rédaction manuelle » et invitait à vérifier à la main plutôt qu'à trancher — il avait raison de ne pas trancher : c'est une bibliothèque sans un seul `console.log`, dont l'appelant imprime. La bonne conversion n'était pas un en-tête imprimé mais le rendu partagé appliqué au texte produit. **(e) Garde-fou élargi, jamais affaibli** : `findHeuristicToolsWithoutNotice()` cherchait littéralement `printReliabilityNotice(slug)` ; les outils migrés ne l'appellent plus, c'est le gabarit qui pose l'avertissement. Les quatre portes d'entrée du gabarit sont désormais acceptées, mais toujours AVEC le slug exact — sans quoi un outil pourrait poser l'en-tête d'un voisin et passer pour couvert. Résultat vérifié : 29/29 (100 %), chaque outil migré relancé pour de vrai. `check-house.mjs` 177/177, `tsc --noEmit` propre | terminée — fidèle |
| 421 | 2026-09-22T13:05Z | Nouvel outil / Charte / Ronde | Rapport de conformité des process centralisé par god (coupable nommé), scan des scripts sans process, Article 26, précision d'évolutivité à l'Article 24 | critique | Suite de la file du matin. **(a) `buildProcessComplianceReport()`** : LE rapport de process de la Ronde, et le seul — architecture fixée par l'utilisateur (« c'est god of process qui produit le rapport uniquement [...] les rapports de process secondaires ne produisent pas de rapport directement livrés à la ronde : ce serait trop »). Les gardiens secondaires gardent leur verdict, god le RELAIE en disant lequel l'a produit, jamais ne le recalcule. Le responsable de chaque étape sautée est NOMMÉ (« il designe le coupable ») : l'agent, un outil, ou personne — cette troisième catégorie étant réservée aux étapes qu'aucun mécanisme ne peut vérifier, jamais un fourre-tout pour une vraie faute. **(b) `findScriptsDeservingProcess()`** : répond à « god of process devrait etre l'outil qui scanne si un script qui le merite n'a pas de process ». Critère calibré : mérite un process un outil dont l'usage suit une suite d'étapes qu'on peut sauter, jamais un outil qu'on lance d'une commande. Deux indices réels minimum (sous-commandes multiples, artefact durable écrit, passage en force prévu) — un seul est trop courant. 12 scripts remontés au premier passage. **(c) Item de Ronde `god-of-all-process-conformite`** : id aligné sur la convention `<slug-outil>-<suffixe>` après que le garde-fou de couverture des registres l'a refusé — il apparie le registre à l'id de l'item, pas au dossier déclaré, ce que j'ignorais. **(d) Article 26** ajouté à la charte : les process se respectent, god en est LE référent (avant d'agir / à chaque Ronde / il signale ce qui manque), et l'agent est tenu de lire son rapport et d'en tenir compte — « il signale, il ne corrige jamais : la décision reste humaine, mais l'ignorer en silence n'en est pas une ». **(e) Précision d'évolutivité à l'Article 24**, demandée en cours de route : un nouvel outil doit hériter de tout ce que l'équipe sait faire. **Écart réel mesuré et nommé plutôt que masqué** : ajouter les deux outils du jour a demandé SIX inscriptions manuelles, chacune signalée par son garde-fou seulement APRÈS échec — les garde-fous détectent l'oubli au lieu de l'éviter, l'agent reste le mécanisme d'intégration. Chantier ouvert, jamais une case cochée. 7 nouveaux tests. `check-house.mjs` 177/177, `tsc` propre | terminée — fidèle |
| 422 | 2026-09-22T13:15Z | Charte / Correction d'une erreur | Précision d'évolutivité (Article 24) réellement ajoutée après un échec silencieux, + complément « mieux vaut trop de questions » (Article 16) | important | **Correction d'une erreur réelle de ma part, signalée à l'utilisateur dans la foulée.** Le commit précédent annonçait dans son message et dans sa ligne de suivi que la précision d'évolutivité avait été ajoutée à l'Article 24 — l'insertion avait en réalité échoué (ancre de texte non trouvée, le script a levé une AssertionError) mais le commit est parti quand même, puisque le reste du travail était valide. Le commit affirmait donc quelque chose de faux. Précision réellement ajoutée cette fois, avec son contenu complet : l'évolutivité ne s'arrête pas à « aucune liste recopiée sans garde-fou », elle exige qu'un outil qui rejoint l'équipe hérite de tout ce que l'équipe sait déjà faire ; et l'état réel mesuré est écrit noir sur blanc plutôt que masqué — six inscriptions manuelles ont été nécessaires pour les deux outils du jour, chacune signalée par son garde-fou seulement APRÈS échec, donc les garde-fous détectent l'oubli au lieu de l'éviter. Chantier ouvert, jamais une case cochée. **Second ajout** : complément à l'Article 16 (« mieux vaut trop de questions que pas assez ») — le minimum de trois est un PLANCHER, jamais une cible ; cinq décisions réelles appellent cinq questions. Borné explicitement pour ne pas rouvrir la porte aux questions creuses déjà interdites : décomposer un vrai sujet, jamais gonfler le nombre. `check-house.mjs` 177/177 | terminée — fidèle |
| 423 | 2026-09-22T13:30Z | Nouvel outil / Chantier terminé | Les deux notes en tête de chaque rapport (santé de l'outil + gravité du contenu) et le chantier « fixer des objectifs » enfin terminé | critique | Deux points de la file, liés. **(a) LES DEUX NOTES** — demande : « Les outils ont tous une note à chaque rapport », calibrée en DEUX notes séparées. Elles répondent à deux questions sans rapport et les additionner les rendrait illisibles : la SANTÉ dit comment va l'outil (sollicité pour de vrai ? un objectif lui est-il fixé ?), la GRAVITÉ dit ce que vaut ce rapport-ci et vient de l'outil lui-même — non fournie, elle est dite non fournie, jamais devinée : supposer « rien à signaler » sur un rapport qui annonce peut-être un incendie serait le pire défaut possible. Les deux passent par la carte d'identité partagée, donc atteignent les 29 rapports sans qu'aucun outil n'ait à y penser (exigence d'évolutivité de l'Article 24). **Bug réel attrapé avant livraison** : ma première version cherchait le champ `tool`/`slug` du journal d'usage alors que le vrai nom est `toolSlug` — chaque outil se serait affiché « jamais sollicité » dans son propre en-tête, une fausse mesure diffusée d'un coup sur 29 rapports. Trouvé en LISANT un vrai événement du journal plutôt qu'en supposant depuis le nom de la fonction qui l'écrit. **(b) CHANTIER OBJECTIFS TERMINÉ** : l'outil `objectifs-vs-resultats` était construit depuis deux jours mais son registre ne contenait qu'UN objectif — l'outil était fini, la pratique n'avait jamais démarré. 15 lignes ajoutées, chacune raisonnée et non copiée d'un gabarit. **Décision de conception importante prise en écrivant** : une ABSENCE D'OBJECTIF ASSUMÉE devient un état à part entière (colonne `—` + raison écrite). Sans elle, un Gardien qui tourne automatiquement à chaque commit aurait été poussé à recevoir une cible de fréquence qui ne mesurerait que le nombre de commits, et la note de santé aurait encouragé à inventer des objectifs creux pour passer au vert — exactement le travers que le badge évite déjà. La note lit donc trois états, jamais deux : chiffré / absent mais décidé / vraiment absent. Vérifié en direct : argus 2/2 (absence assumée), tool-brain 2/2 (objectif atteint), check-level-target 1/2 (vrai manque, toujours signalé). **Trouvaille annexe** : deux slugs coexistaient pour le même outil (`simulation-process-guardian` et `process-simulation-guardian`), résidu du renommage du matin — l'objectif est posé sur le slug actuel. 11 nouveaux tests. `check-house.mjs` 177/177, `tsc` propre | terminée — fidèle |
| 424 | 2026-09-22T13:45Z | Nouvel outil / CASSANDRA-RH | CASSANDRA devient gardienne des objectifs/KPI : 16 membres branchés à rien trouvés et comblés, couverture 29/29 | important | Demande de la file : « Cassandra est la gardienne des objectifs et KPI : c'est elle qui verifie que tout est bien pluggé sur les KPI/objectifs et qui l'indique dans son rapport ». **`objectivesCoverage()`/`objectivesCoverageLines()`** : elle CONSTATE le branchement, elle ne recalcule jamais une note — `objectifs-vs-resultats` compare un objectif à un résultat, `kpi-report` mesure les familles, elle vérifie que chacun a bien quelqu'un pour le mesurer. C'est précisément le trou qu'aucun des deux ne peut voir : chacun ne connaît que ce qui lui est déjà déclaré, donc un membre branché à RIEN est invisible pour les deux. La liste des membres vient du roster réel, jamais d'une énumération dans le code (Article 24) : un outil qui rejoint l'équipe apparaît dans la couverture le jour même. **Résultat du premier passage : 16 membres sur 29 branchés à rien.** Les 16 comblés dans la foulée, chacun avec une décision raisonnée et écrite — 11 objectifs chiffrés (Smart Conso API et SMART-CONSO-TOKEN mesurent le respect d'une obligation de la charte, pas une performance ; find-booster mesure si je cherche par concept au lieu de lire des fichiers entiers ; CASSANDRA elle-même en a un, par cohérence minimale) et 5 absences assumées (HYPER-SCAN-CHECKPOINT et THE-DEEP-READER : une cible de fréquence pousserait à dépenser pour un chiffre ; Smart Breaker : lui fixer une cible reviendrait à souhaiter des pannes ; check-level-target : appelé PAR d'autres outils, son usage se lit dans le leur ; find-deep-booster : hors chantier de découpage, zéro est la bonne valeur). Couverture finale vérifiée en direct : **29/29**. **Un test rendu obsolète par le succès** : une assertion constatait que check-level-target était branché à rien ; le trou étant comblé, elle est reportée sur un slug synthétique pour que son intention survive au fait qu'aucun outil réel ne soit plus dans ce cas — sinon la garantie disparaîtrait au moment précis où elle devient un acquis à protéger. 5 nouveaux tests. `check-house.mjs` 177/177, `tsc` propre | terminée — fidèle |
| 425 | 2026-09-22T16:50Z | Nouvel outil / angel-of-ia-process | Construction de l'Agent, et DEUX passages ratés du même outil avant qu'il cesse d'accuser à tort | critique | Agent demandé par l'utilisateur (« un outil dédié à la verification des regles de travail »), nommé par lui. Arbitrage : outil SÉPARÉ de god-of-all-process — god surveille le déroulé d'ACTIVITÉS, angel la CONDUITE de ceux qui travaillent (agent ET utilisateur, notés pareil, « que ce soit pour moi ou pour toi »). Son apport propre, que rien d'autre ne fait : croiser `.tool-usage-history.json` avec `git log` pour savoir si les outils à consulter AVANT d'agir l'ont vraiment été avant. **Deux passages ratés, consignés parce qu'ils illustrent le défaut récurrent du projet mieux qu'aucun autre exemple.** 1er passage : six accusations, six fausses — les « consultations tardives » étaient moi relançant ces outils pour vérifier leur migration vers le gabarit unifié ; correctif, une origine `verification` ajoutée à USAGE_ORIGINS et écartée par angel. 2e passage : les six tenaient toujours, portées par des `cli_direct` antérieurs au correctif — le premier ne réparait que l'avenir. La vraie cause était écrite noir sur blanc dans la définition même de cette origine (« le script sait qu'il a été lancé, jamais POURQUOI ») : elle ne prouve AUCUNE consultation, elle prouve un lancement. La lire comme une consultation, c'était présenter une mesure ADJACENTE comme la mesure visée — commis dans l'outil même écrit pour surveiller la discipline. Correctif de fond, désormais sa règle : seules les origines de JUGEMENT (`spontane`/`demande`, celles que l'agent déclare sciemment) fondent un manquement nommé ; un `cli_direct` hors d'ordre devient un signal non concluant, « montré, jamais imputé ». Résultat sur les mêmes 10 commits : 0 manquement, 6 signaux. Garde-fou Article 24 : `findOriginesInconnues()` — si une origine nommée disparaît du compteur, angel renvoie `mesurable:false` plutôt que de compter zéro en silence (un faux vert serait pire que l'erreur surveillée). Badge complet obtenu, au prix de 7 enregistrements manuels (lib-shell ×2, doc-report, table maîtresse §7ter, PRESTATIONS, CLAUDE.md, axa-check) + exclusion de Ronde MOTIVÉE (god centralise, décision explicite de l'utilisateur) + ligne d'objectif chiffré — chacun signalé par un garde-fou APRÈS échec, jamais prévenu : la limite d'évolutivité déjà consignée reste entière. 8 assertions neuves, 177 tests au vert. | terminée |
