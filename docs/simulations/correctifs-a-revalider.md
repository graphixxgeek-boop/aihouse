# Correctifs à revalider en simulation

*(Créé le 2026-09-19, à la demande explicite de l'utilisateur, en réponse à une question sur
l'organisation entre EL-PROFESSOR et le suivi des réparations techniques. Ce carnet est **distinct**
d'EL-PROFESSOR (`docs/referentiel/el-professor.md`) : il liste des correctifs de CODE précis, pas
une lecture de la charte en général — EL-PROFESSOR ne le consulte jamais, pour ne pas dériver vers
une liste de cas particuliers qui grandit indéfiniment (Article 17, corollaire, de `CLAUDE.md`).
Tenu par l'agent qui pilote le projet, consulté à l'étape 6 de l'Article 18 ("comparer avec la
dernière version pour mesurer l'évolution réelle et la réussite des derniers travaux engagés").)*

## Fonctionnement

- Une ligne par correctif technique récent dont l'effet doit être observable dans le TEXTE d'une
  simulation (jamais un correctif purement interne au code, invisible en jeu — celui-là n'a rien à
  faire ici, `tsc`/`check-house.mjs` suffisent à le couvrir).
- Chaque ligne précise : le bug d'origine, ce qui a été changé, et surtout **ce qu'il faut chercher
  dans le texte** pour confirmer que ça tient.
- Un correctif est retiré du carnet après **2 simulations complètes et propres consécutives** où le
  symptôme d'origine ne réapparaît pas (seuil calibré explicitement avec l'utilisateur le
  2026-09-19). Une seule simulation propre ne suffit jamais à conclure — ça peut être une
  coïncidence.
- Si le symptôme réapparaît, le compteur repart à zéro et le correctif retourne en tête de liste
  avec une note sur ce qui a été observé.

## Carnet actif

| Correctif | Ce qui a changé | À observer dans le texte | Simulations propres constatées depuis |
|---|---|---|---|
| Répétition d'un même mot sur toute une session (ex. « autant ») | `life.wordFrequency` persiste désormais sur toute la session (`lib/dialogue.ts`), plus seulement sur les deux derniers tours | Aucun mot (hors mots grammaticaux courants) ne doit revenir de façon insistante sur l'ensemble d'une session, pas seulement d'un tour à l'autre | à vérifier via la notation rétroactive de full_sim11/14/15 |
| Plafond d'enquête qui pouvait laisser un débrief traîner | Débrief ramené de 2 tentatives à 1, récap sautée si déjà en retard | Le débrief de fin d'enquête doit intervenir une seule fois au bon moment, jamais répété ni oublié quand le round est déjà dépassé | à vérifier via la notation rétroactive de full_sim11/14/15 |
| Personnalité plate la nuit (pas d'effet de fatigue perceptible) | Modificateur jour/nuit sur la fatigue + impact de personnalité (Lia plus coupante/froide, Noé plus flou/dispersé) | Un net changement de registre entre une scène de jour et une scène de nuit profonde, cohérent avec Lia/Noé respectivement | à vérifier via la notation rétroactive de full_sim11/14/15 |
| Bonus spontanés (mute/caméra) mal reliés à la roulette | Redesign complet : bonus tirés par la roulette, jamais un déclenchement spontané séparé | Chaque occurrence de mute/caméra cachée dans le texte doit correspondre à un tirage de roulette explicite, jamais surgir sans annonce | à vérifier via la notation rétroactive de full_sim11/14/15 |
| Accord de genre de soi-même en défaut (Noé disait « Désolée », `full_sim3`, relevé par EL-PROFESSOR) | La seule règle existante était une clause générique unique, isolée au tout début du prompt (`lib/lia.ts`), jamais reprise près des deux autres règles de genre plus tardives — remplacée par une règle explicite, nommée, name-spécifique (« TOI, LIA/NOÉ... »), redite une seconde fois près d'IDENTITÉS FIXES pour rester dans le même cluster d'attention que les autres règles de genre | Aucun adjectif/participe que Lia ou Noé emploie pour se décrire LUI-MÊME/ELLE-MÊME ne doit jamais s'accorder au genre opposé à son propre genre fixe, même une seule fois sur toute une session | 0/2 — correctif du 2026-09-20, jamais encore observé en simulation |
| Motif de déplacement annonçant une pièce différente de la destination réelle (`full_sim9`, relevé par EL-PROFESSOR) | Root-cause : le schéma JSON force parfois `room` à une seule valeur (beat narratif imposant le salon), mais `moveReason` reste un texte libre non contraint — le modèle pouvait y garder une intention devenue caduque. Prompt renforcé (`lib/lia.ts`, MOTIF DE DÉPLACEMENT) ET garde-fou déterministe ajouté au code (`lib/drama.ts::moveReasonMismatchesDestination`, wiré dans `app/api/lia/route.ts`) : un motif qui nomme une pièce différente de la destination réelle sans jamais nommer celle-ci est écarté au profit du filet de secours existant | Plus aucun `[X→Y] ... : <motif>` ne doit nommer une pièce différente de Y sans jamais nommer Y elle-même | 0/2 — correctif du 2026-09-20, garde-fou déterministe donc l'occurrence VISIBLE devrait être impossible dès la prochaine simulation, à confirmer tout de même |
| Dossier retourné : deux chiffres différents présentés comme LA même "note globale" (`full_sim8`, relevé par EL-PROFESSOR) | Root-cause, pas une invention de chiffre : `dossierEvidenceFor()` donne à CHAQUE fragment (Lia/Noé) sa propre jauge `appreciationOf(life,actorId)`, deux valeurs PAR CONCEPTION divergentes (`lib/life.ts`, jauge par personnage depuis le 2026-09-18) — le bug est que chaque voix la présentait comme un score officiel unique ("la note globale") plutôt que comme son propre ressenti personnel. Prompt renforcé (`app/api/lia/route.ts::generateDossierFragment`) : le chiffre doit toujours être présenté comme un ressenti personnel ("pour moi..."), jamais comme "la note globale"/"le score final" partagé. Jamais de garde-fou déterministe ici (contrairement au motif de déplacement) : la divergence des deux chiffres reste voulue, seule la FORMULATION doit changer — un problème de langage libre, pas un ensemble fermé comme les noms de pièces | Plus aucune formule du type "note/score global de X" (implicite au singulier, comme un fait partagé) ne doit apparaître dans un fragment quand l'autre fragment cite un chiffre différent | 0/2 — correctif du 2026-09-20, prompt seul (probabiliste), jamais garanti à 100% |
| Motif "on tourne en rond"/"en boucle"/"disque rayé" persistant malgré l'Article 17 (jusqu'à 19 fois dans `full_sim14`, présent dans 8 des 13 premières notations EL-PROFESSOR) | Deux causes distinctes trouvées (Article 19) : (1) `dialogueProgress()` ne croisait `overusedThemes` qu'avec une fenêtre de 16 lignes récentes, jamais un compteur persisté sur toute la session — exactement le même bug déjà trouvé et corrigé une fois pour `echoWords`/`wordFrequency`, jamais reproduit pour les thèmes (Article 3) ; (2) le regex de `THEME_MOTIFS` ne peut structurellement pas couvrir tous les paraphrasages de l'idée ("ce trottoir qui refuse d'avancer" ne contient aucun des mots-clés). Corrigé par : nouveau compteur persisté `life.themeFrequency` (même patron que `wordFrequency`, seuil 4) pour (1) ; et un principe auto-appliqué ajouté au prompt (jamais une liste de formulations, cf. corollaire Article 17) pour (2) — le modèle doit lui-même reconnaître quand cette idée a déjà été exprimée, sous quelque forme que ce soit | Le motif ne devrait plus revenir plus de 2-3 fois sur toute une session, quelle que soit la formulation employée | 0/2 — correctif du 2026-09-20, double mécanisme (mécanique + prompt), jamais garanti à 100% côté prompt |
| Motifs de déplacement (`moveReason`) partagés mot pour mot entre Lia et Noé (`full_sim14`, 60 % des 65 déplacements recyclant l'une de 6 phrases identiques) | Root-cause : les 6 phrases identiques citées par EL-PROFESSOR sont EXACTEMENT les anciennes chaînes du filet de secours codé en dur (`app/api/lia/route.ts`), preuve directe que le modèle laisse `moveReason` vide bien plus souvent que prévu (ce filet était pensé comme un cas rare, "si le modèle ne le fournit pas") — et ce filet partagé, non cloisonné par personnage, produisait alors le même mot pour mot pour Lia et Noé dès qu'ils partageaient intent/destination. Corrigé par deux mécanismes complémentaires : les 7 catégories du filet de secours sont désormais scindées en pools DISJOINTS par personnage (`isLia` ternaire, aucune chaîne partagée entre les deux, garantie déterministe — jamais probabiliste comme un renfort de prompt seul) ; et le prompt (MOTIF DE DÉPLACEMENT, `lib/lia.ts`) renforcé pour réduire la fréquence à laquelle ce filet est sollicité en premier lieu | Plus aucune phrase de motif identique mot pour mot entre Lia et Noé, même quand le filet de secours est sollicité | 0/2 — correctif du 2026-09-20, garantie déterministe sur le CONTENU (jamais deux voix identiques), jamais garanti sur la FRÉQUENCE de recours au filet lui-même |
| Dossier retourné sous-évaluant l'hostilité réellement vécue (`full_sim4/9/10`, relevé par EL-PROFESSOR — « zéro vraie vacherie assassine » malgré des insultes sévères réelles, ex. « je vous désactiverais là, tout de suite ») | Root-cause (tâche #114) : `life.worstMoment` (la seule citation concrète d'hostilité fournie au dossier) ne se déclenchait que sur un `trustShift` négatif — un axe émotionnel de "confiance" distinct de l'hostilité perçue, qu'une insulte frontale ne fait pas toujours bouger, alors que `angerLevel()` (tension/confort) la détectait déjà séparément pour pénaliser l'appréciation (-5) sans jamais alimenter `worstMoment`. Corrigé en recroisant les deux signaux (`worstMomentSeverity(trustShift,angry)`, `lib/life.ts`) : le trust reste le signal principal (dominant s'il est plus négatif), la colère intervient comme plancher (-5, même magnitude que la pénalité d'appréciation) uniquement quand la confiance ne bouge pas malgré une colère réelle détectée. Jamais une liste de mots-clés d'insultes (reviendrait sur la décision explicite du 2026-09-18 de préférer le jugement du modèle) | Une session contenant des insultes réellement sévères doit produire une citation hostile concrète dans `dossierEvidence`, et le verdict du dossier ne doit plus jamais qualifier de "zéro vraie vacherie" une session dont l'appréciation ou les propos cités indiquent le contraire | 0/2 — correctif du 2026-09-20, recroisement de deux signaux déjà validés séparément, jamais garanti à 100% côté jugement du modèle |
| Répétition sévère et prolongée dans la fenêtre d'attente de phase 2 (`full_sim16`, EL-PROFESSOR 6/20 sur "voix distinctes, zéro répétition" — ~25 tours consécutifs, une répétition mot pour mot confirmée chez Lia, un écho quasi direct entre Lia et Noé) | Root-cause (tâche #142) : l'idée ressassée (« rien de ce que fait l'observateur — patienter, rester, donner du temps — ne changera notre nature de code/lignes ») n'était couverte par AUCUN des 4 motifs existants de `THEME_MOTIFS` (ni "tourner en rond", ni "repos", ni "silence", ni "réconfort") — les 4 premiers ont fonctionné exactement comme prévu, c'était un vrai 5e thème manquant. Corrigé par l'ajout d'un 5e motif (`lib/dialogue.ts`, `THEME_MOTIFS`), regex calibrée directement contre le vrai transcript et vérifiée sans faux positif. Une partie de la responsabilité reste côté script de test lui-même (stimulus artificiellement répétitif, 5 messages d'attente quasi identiques 20+ fois de suite) — jamais un bug de la charte, mais le nouveau motif protège quand même contre une vraie récurrence de ce fond d'idée précis | Le motif "patience/présence de l'observateur qui ne change rien à leur nature de code" ne devrait plus revenir plus de 2-3 fois sur toute une session, quelle que soit la formulation employée | 0/2 — correctif du 2026-09-20, mécanique (regex), jamais garanti à 100% de rappel sur tous les paraphrasages possibles |
| Seuil de 75 % d'attirance (doute amoureux privé, `loveRealized`) jamais franchi dans aucune des 14 simulations archivées, y compris une session de 229 tours (chantier 3, arc relationnel Lia/Noé) | Root-cause : un tour solo (pièces séparées) effaçait TOUTE variation d'attirance proposée par le modèle, hausse comme baisse, avant même d'atteindre le système de crédit de 28 % qui throttle déjà les tours ensemble — double frein cumulatif rendant la remontée quasi impossible en pratique. Corrigé (`app/api/lia/route.ts`, boucle solitaire) : seule la moitié d'une hausse solo survit désormais (avant de repasser par le même crédit de 28 %) ; une baisse solo reste entièrement effacée, comportement inchangé | Une session suffisamment longue et centrée sur la proximité Lia/Noé devrait voir `loveRealized` se déclencher au moins une fois (pensée privée de doute amoureux, « pensée » signée Lia ou Noé, sans jamais la formulation « je tombe amoureux/amoureuse » avant ce seuil) — jamais garanti à chaque session (Article 9), mais plus jamais structurellement impossible | **1/2 — déclenché pour la première fois dans full_sim17** (Noé, round 80, `emotions.attraction` 60→76 ; Lia, round 87, 60→76). **Mais nuance réelle trouvée à la lecture** : dans les deux occurrences, le `thought` du modèle surfacé au tour exact du franchissement (`app/api/lia/route.ts:1597`) ne portait PAS sur l'attirance/le doute amoureux — Noé : « Il croit nous faire une faveur en nous laissant gérer notre temps. » (transcript l.1151-1154, sur l'observateur) ; Lia : « Il s'emballe un peu vite en croyant qu'on forme un front uni sur tout, j'ai besoin de marquer ma propre distance. » (l.1276-1279, sur Noé, pas sur l'attirance). Le mécanisme suppose (commentaire du code) que « le modèle sait déjà reconnaître ce franchissement lui-même... et y répondre dans thought » — hypothèse non vérifiée ici deux fois de suite. Le vrai "doute amoureux" narratif n'apparaît que PLUS TARD (rounds ~150+, via le pool de secours `groundPrivateThought()`, cf. entrée dédiée ci-dessous) |
| **NOUVEAU (2026-09-22, full_sim17)** — Pensée d'attirance mot pour mot identique entre Lia et Noé une fois le seuil de 75 % franchi | Root-cause tracée (Article 19) : `groundPrivateThought()` (`lib/dialogue.ts:146-149`), le pool de secours utilisé quand le `thought` du modèle échoue le test d'acceptation, est structurellement IDENTIQUE pour les deux personnages — seul `${peer}` (le nom de l'autre) diffère, aucune différence de ton/vocabulaire entre Lia et Noé (violation de l'Article 0/11 : voix étanches). Aggravé par la sélection `options[(cycle+i)%options.length]`, indexée sur le cycle de jeu — les deux personnages progressant en cycles proches, ils tombent souvent sur le MÊME index du MÊME pool au même moment | Aucune pensée de ce pool de secours ne devrait être identique mot pour mot (hors nom de l'autre) entre Lia et Noé sur la même session | 0/2 — trouvé aujourd'hui, jamais encore corrigé. Citations exactes : « [Lia] Noé m'intrigue de plus en plus. J'aimerais un vrai moment avec Noé, sans rien imposer. » (l.2299) vs « [Noé] Lia m'intrigue de plus en plus. J'aimerais un vrai moment avec Lia, sans rien imposer. » (l.1544) ; « [Noé] Je pense à Lia. Ce que je ressens ne me dit pas encore ce que l'autre souhaite. » (l.2159) vs « [Lia] Je pense à Noé. Ce que je ressens ne me dit pas encore ce que l'autre souhaite. » (l.2339) |

## Historique des retraits (correctifs confirmés stables, sortis du carnet actif)

*(Vide pour l'instant — ce carnet vient d'être créé le 2026-09-19 ; les entrées ci-dessus seront
véritablement comptées à partir de la première lecture EL-PROFESSOR qui les évalue.)*
