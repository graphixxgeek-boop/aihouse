# SMART-CONSO-TOKEN — index des décisions et évolutions

*(Cf. `docs/referentiel/smart-conso-token.md` pour les règles complètes. Ce fichier résume, par
décision durable, ce qui a changé et pourquoi — pas chaque consultation individuelle, celles-là
vivent dans `.smart-conso-token-history.json`, local et non versionné.)*

| Date | Décision | Notes |
|---|---|---|
| 2026-09-20 | Création de l'outil + registre initial de schémas coûteux (recherche web réelle) + premier seuil dur proposé (`agent_subagent_spawn` : 3 appels confirmés / 2h) | Créé à la demande explicite de l'utilisateur, pendant de Smart Conso API pour les tokens de l'agent. Connaissance déclarée valable pour "claude" (recherche du 2026-09-20) — à revalider explicitement si le modèle/la plateforme change (`checkKnowledgeFreshness()`). Seuil dur **encore en attente de validation explicite**, laissé "proposé" à la demande de l'utilisateur. |
| 2026-09-20 | Premier scan réel (`scanDocumentWeight`) : `CLAUDE.md` fait 1407 lignes / ~28 792 tokens estimés | Très largement au-delà du repère "progressive disclosure" (300 lignes limite, 100 idéal). Constat factuel seulement, **aucune proposition de réduction faite ni appliquée** — un changement de ce fichier reste soumis à la même prudence extrême que tout ce qui touche la charte (Article 14). À reprendre lors d'une vraie session de tri dédiée, jamais en aparté d'un autre chantier. |
| 2026-09-20 | Premier scan `Global` complet (71 documents, `scanScope`) : ~231 561 tokens au total, 6 documents au-delà du repère (`CLAUDE.md`, `principes.md`, `regles-de-travail.md`, `parametres.md`, `regles-du-temps.md`, `philosophie-et-politique.md`) — rapport archivé dans `docs/smart-conso-token/scans/scan-2026-09-20-02-08.md` | **Limite trouvée en utilisant l'outil pour la première fois** : le scan ne distingue pas encore un document TOUJOURS CHARGÉ (seul `CLAUDE.md` l'est réellement, relu à chaque message) d'un document lu À LA DEMANDE (les 5 autres, dont la taille est normale pour de la documentation de référence, pas un problème urgent). Deux tâches ajoutées à la to-do avec l'accord explicite de l'utilisateur : restructurer `CLAUDE.md` (chantier sensible, session dédiée) et améliorer le scan pour faire cette distinction avant le prochain passage. |
| 2026-09-20 | Scan affiné (v2) : distinction toujours-chargé/lu-à-la-demande ajoutée (`alwaysLoaded`), résultats reclassés en ACTION REQUISE (1 seul document : `CLAUDE.md`) vs INFORMATIF (5 documents) ; ajout de `countDatedNarrativeMarkers()` (29 asides narratives datées repérées mécaniquement dans CLAUDE.md, ~1708 tokens, point de départ sûr pour une future restructuration) ; ajout de `findJudgeSpawnsWithoutConsultation()` (audit rétroactif RÉEL en comparant l'historique local au registre archivé de THE-FINAL-JUDGE, sa propre preuve externe qu'un agent séparé a bien été appelé) ; ajout de `AUTOMATION_TOKEN_NUANCE` (l'automatisation mécanique économise réellement des tokens, l'automatisation par agent séparé n'en économise jamais, ajoute toujours un coût fixe) — rapport archivé dans `docs/smart-conso-token/scans/scan-2026-09-20-02-19-v2.md` | Demandé explicitement par l'utilisateur après avoir jugé le premier scan pas assez actionnable. Premier audit rétroactif réel : le passage THE-FINAL-JUDGE du 2026-09-20 (avant la création de SMART-CONSO-TOKEN) n'a logiquement aucune consultation confirmée associée — constat honnête, pas une anomalie. |
| 2026-09-20 | Livrable concret ajouté : `listDatedNarrativeMarkers()` localise chaque aside (ligne + extrait), archivé en une vraie liste de travail prête à l'emploi dans `docs/smart-conso-token/scans/claude-md-narrative-candidates.md` (29 candidats, ~1708 tokens) | Demandé explicitement : « il délivre un vrai résultat [...] il sait comment économiser ». Le gain réel n'est pas le scan lui-même (gratuit, mécanique) mais le temps/tokens qu'évitera de dépenser la future session de restructuration (tâche #122) en partant d'une analyse déjà faite plutôt que de zéro — jamais une application automatique, toujours une matière prête pour une vraie décision. |
| 2026-09-20 | Distinction investissement/consommation-sans-retour ajoutée : `classifyConsumption()` (buildsReusableTool/preventsFutureDebugging → investissement ; isDuplicateOfRecent/scope disproportionné → sans_retour), câblée dans `assess()` (nouveau verdict `investissement_reconnu`, jamais un contournement du seuil dur) et `computeInvestmentRatio()` (bilan sur l'historique classifié, affiché dans le crochet post-commit à côté du rythme) | Demandé explicitement : « il peut y avoir des "investissements" en token [...] il ne faut pas qu'il décourage un investissement sain, qu'il vienne de moi, toi ou les outils [...] mesure precise de la pertinence du besoin ». Un seuil dur reste toujours non négociable (Article 22) même face à un investissement reconnu — la classification informe, jamais ne tranche à la place de la règle déjà posée. 115/115 tests, tsc propre. |
| 2026-09-20 | Auto-diagnostic sécurisé ajouté : `recordAction()` conserve destinataire + verdict, `recordOutcome()` attache un résultat réellement observé (jamais deviné), `diagnoseAdviceAccuracy()` repère un seuil dur probablement non respecté et tout résultat qui contredit/confirme un verdict passé — jamais un ajustement automatique | Tension réelle signalée avant d'implémenter (Article 14) : la demande littérale de l'utilisateur (« il se corrige de façon autonome ») romprait la règle déjà établie plusieurs fois. Calibrée en version sécurisée après 3 questions explicites : jamais d'auto-ajustement, portée agent+outils seulement (jamais l'utilisateur, aucune trace mécanique fiable de ses décisions), affiché aux moments déjà existants (crochet post-commit). 116/116 tests, tsc propre. |
| 2026-09-21 | Scan Global relancé (item `smart-conso-token-scan` de CIRCLE-TASKS, Ronde AUTO) : 130 documents (71 le 2026-09-20), ~464 861 tokens au total — rapport archivé dans `docs/smart-conso-token/scans/scan-2026-09-21-circle.md` | Toujours 1 seul document en ACTION REQUISE : `CLAUDE.md`, 29 606 tokens (+814 depuis le 2026-09-20, ~+2,8 %, cohérent avec les 3 versions ajoutées ce soir). 9 documents INFORMATIF, dont `docs/regles-de-travail.md` désormais à 52 228 tokens — à garder à l'œil lors d'une future passe d'allègement, aucune urgence constatée ce soir. |
| 2026-09-22 | Scan Global relancé (item `smart-conso-token-scan` de CIRCLE-TASKS, Ronde AUTO exécutée par Opus 5) : **197 documents** (130 le 2026-09-21, +52 %), **~712 107 tokens** au total (+53 % en un jour) — rapport archivé dans `docs/smart-conso-token/scans/scan-2026-09-22-circle-auto.md` | Toujours 1 seul document en ACTION REQUISE : `CLAUDE.md`, ~29 606 tokens, **strictement inchangé depuis le 2026-09-21** (les ajouts documentaires du jour sont tous allés dans `docs/`, jamais dans la charte — exactement le comportement voulu). Trouvaille réelle de ce passage : `docs/suivi/sessions/session_0151JrVYzJ2bdCShaXhFAjLo.md` pèse ~109 295 tokens pour 469 lignes et est devenu le document le plus lourd du dépôt, près du double de `regles-de-travail.md` — le système de suivi n'a aucun plafond ni rotation par session (signal, jamais une décision prise seule). Friction de méthode relevée : aucune sous-commande CLI n'existe pour lancer un scan de portée, l'`execute` de l'item suppose pourtant qu'on « relance » un scan. |
