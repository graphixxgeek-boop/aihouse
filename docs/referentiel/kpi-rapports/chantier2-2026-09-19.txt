Tableau de bord — rapport KPI complet (cf. docs/referentiel/tableau-de-bord.md pour les règles).

**🔧 Performance globale du Smart Breaker : 98%**
**📈 Améliorations réalisées sur l’outil : 88%** (7/8 capacités prévues, implémentées et testées)
→ Action : Validation qualité intégrale de GEMINI_FALLBACK_MODELS avant activation en production

== Robustesse du code : tsc --noEmit ==
OK — 0 erreur.

== Robustesse du code : check-house.mjs ==
OK — 80/80 bloc(s) de test passés, suite verte.

== Robustesse du code : points fragiles ouverts ==
8 point(s) fragile(s) ouvert(s) — détail dans docs/referentiel/points-fragiles.md.

== Robustesse du code : taille et densité ==
lib/lia.ts : 172 lignes (prompt géant, à surveiller — pas un défaut en soi).
lib/dialogue.ts : 242 lignes.
lib/ : 24 fichiers.

== KPI global — Robustesse du code ==
100% (moyenne : propreté tsc 100%, suite de tests 100%). Points fragiles et taille de code restent affichés en compteurs bruts ci-dessus (pas de plafond naturel pour un %).
→ Lecture : code sain, rien à faire ici pour l’instant.

== Performance runtime — Smart Breaker (détail) ==
Tours réels (appels Gemini nécessaires) : 299.
Clé principale déjà indisponible au départ du tour : 199 fois sur 299 (67%).
Tentatives individuelles (clé × modèle) : 304 — 299 réussies, 3 bloquées par quota (429), 2 erreurs transitoires (503), 0 clés invalides (401/403).

== Qualité de sortie ==
Pas de tour enregistré cette session — rien à mesurer (normal si le serveur vient de démarrer ou si aucune simulation n’a encore tourné).

== Cohérence logique ==
Pas de tour enregistré cette session — rien à mesurer.

== Rejouabilité (signal partiel — diversité récente des bonus) ==
KPI global Rejouabilité (partiel) : 89% des 9 bonus possibles vus parmi les 12 derniers tirages (8/9).
Limite honnête : bonusLog ne garde que les 12 derniers tirages (app/api/lia/route.ts) — ce chiffre reflète la diversité RÉCENTE, pas garantie sur toute la session si plus de 12 tirages ont eu lieu. Un historique inter-sessions réel (tours jusqu’à révélation, nombre de disputes, variété des ouvertures) reste à construire (chantier 2, cf. docs/referentiel/tableau-de-bord.md).
→ Lecture : bonne diversité récente — rien à faire ici pour l’instant.

== KPI global — Performance runtime ==
98% (identique au KPI en tête de rapport — cette famille EST le Smart Breaker aujourd’hui).

== KPI général — Couverture du tableau de bord lui-même (à surveiller en priorité) ==
60% des 5 familles ont produit une vraie mesure cette exécution (3/5).
→ Action : une ou plusieurs familles n’ont renvoyé aucune mesure — vérifier si c’est normal (serveur non lancé, aucun tour joué cette session) ou si un branchement du tableau de bord lui-même est cassé, avant de faire confiance à la synthèse ci-dessous.

== Synthèse ==
🚨 ALERTE TABLEAU DE BORD — couverture du tableau de bord à 60% (3/5 familles mesurées).

== SYNTHÈSE COMPACTE (à relayer telle quelle dans la conversation) ==
Run : chantier2-2026-09-19
| Famille | KPI global |
|---|---|
| 🔧 Smart Breaker — performance | 98% |
| 📈 Smart Breaker — améliorations | 88% |
| Robustesse du code | 100% |
| Qualité de sortie | N/A |
| Cohérence logique | N/A |
| Rejouabilité (partiel) | 89% |
| Couverture du tableau de bord | 60% |

🚨 Points d'attention : couverture du tableau de bord à 60% (3/5 familles mesurées).

Historique complet (toutes les exécutions, tous les chiffres) : docs/referentiel/kpi-historique.csv — à envoyer en pièce jointe, jamais collé dans la conversation.
