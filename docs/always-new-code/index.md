# ALWAYS-NEW-CODE — index des passages

*(Cf. `docs/referentiel/always-new-code.md` pour les règles complètes. Ce fichier archive chaque
passage réel (zoom profond sur une zone) et sert de mémoire de couverture pour la rotation
intelligente — un thème absent de ce tableau est traité comme jamais examiné.)*

| Date | Zone examinée | Trouvailles confirmées | Rapport | Notes |
|---|---|---|---|---|
| 2026-09-21 | Fatigue | 2 | `docs/always-new-code/2026-09-21-fatigue.md` | Premier vrai passage de l'outil (rotation, seule zone jamais examinée) — seuil 68 dupliqué sans source commune (`lib/simulation.ts`/`lib/lia.ts`), pas de prédicat `isDawn()` malgré le patron déjà établi par `isNight()`/`isMidnight()` (`lib/daynight.ts`). Aucun changement appliqué — questions de calibrage préparées pour le réveil de l'utilisateur (Article 23 : jamais une application automatique). |
