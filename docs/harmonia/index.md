# HARMONIA — index des balayages et frictions

*(Cf. `docs/referentiel/harmonia.md` pour les règles complètes et la carte des dépendances par
thème. Ce fichier résume, par exécution, ce qui a été vérifié et ce qui en a été fait.)*

| Date | Liens vérifiés | Frictions confirmées | Notes |
|---|---|---|---|
| 2026-09-19 | 5 (durée jour/nuit, décalage minuit, malus nuit blanche, seuil respect sincère) | 0 | Premier balayage réel d'HARMONIA, lancé le jour de sa création. Deux erreurs trouvées et corrigées DANS LE SCRIPT lui-même en le construisant (un motif de code trop strict qui ne matchait pas la vraie syntaxe, un motif de documentation qui ne suivait pas le saut de ligne réel du texte) — jamais la documentation ou le code modifiés pour faire disparaître un faux signal, cohérent avec le principe "toujours revérifier le code, jamais une hypothèse". |
| 2026-09-22 | Passage à raisonnement (thème "relation Lia/Noé", demande explicite de l'utilisateur) : relationship.ts, life.ts, dialogue.ts, world.ts, turn.ts, lia.ts (construction du prompt) | 0 | Consulté SMART-CONSO-TOKEN avant lancement (verdict avertissement_souple, négociable — ciblé sur les fichiers du thème plutôt qu'un vrai `full_repo_scope`). Vérifié : `relationship.ts` reste des fonctions pures (les deux personnages passés explicitement en paramètres, aucun état global caché) ; `life.ts` sépare correctement les champs par acteur (`{1:...,2:...}`) SAUF `wordFrequency`/`themeFrequency`, volontairement PARTAGÉS — vérifié que c'est voulu (Article 11 interdit à un personnage de faire écho aux mots de l'autre, un registre unique est donc la bonne conception, pas un bug) ; `dialogue.ts::groundPrivateThought()` a déjà ses pools de pensées séparés Lia/Noé (tâche #187, corrigée avant ce passage) ; `world.ts::readWorld()` lit l'état partagé une seule fois par tour et le distribue de façon cohérente aux deux agents ; `turn.ts` référence systématiquement l'autre agent par un vrai lookup (`agents.find(a=>a.id!==...)`), jamais une supposition ; `lia.ts` porte une garde explicite écrite dans le prompt lui-même (« Les pensées de l'autre et ses souvenirs privés ne sont jamais accessibles à ton personnage, même si le contexte technique les contient ») — une seconde couche de protection en plus de la séparation architecturale des deux appels Gemini (Article 8), jamais la seule ligne de défense. Aucune friction trouvée dans ce passage ciblé — les points de vigilance historiques de ce thème (pools partagés, cf. tâches #187/#188) étaient déjà corrigés avant ce soir. Limite honnête : passage ciblé sur les fichiers du thème, pas un audit ligne à ligne de app/api/lia/route.ts (l'orchestrateur complet) ni de simulation.ts — à envisager si un doute réel survient sur ces deux fichiers spécifiquement. |
